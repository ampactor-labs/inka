# LOOP-PROGRESS — first-light drive (autonomous /loop)

> Durable per-iteration log. Updated EVERY loop iteration regardless of how the
> loop fires (ScheduleWakeup auto-fire or manual `/loop` re-paste). The current
> cursor + the prompt's ITERATION LOG mirror the latest entry here.
>
> **Goal:** first-light = the FIXED POINT `diff(m3.wat, m4.wat)` EMPTY AND
> correctness (7/7 micros + repro). Oracle = the artifact (`bash tools/state.sh`),
> never the census. Method: audit before symptom; probe the artifact; ultimate
> form in the WHEEL, seed minimal-correct.

| iter | changed | measured | next |
|---|---|---|---|
| 1 | fix 2: subst_ty/instantiate chase-to-root (both layers) | trap marched canonicalize→filter (progress) | fix 1 |
| 2 | fix 1: register_effect_ops full var set (WHEEL) | seed mirror paused (was over-engineering the disposable seed) | seed minimal-correct |
| 3 | **DISSOLVED handler_state_inits_registry** → live HandlerKind read (commit `7448ad4`) | filter-in-arm trap CLEARED; trap marched to `lower_one_arm_decl→lower_expr`; micros 7/7, seed compiles wheel, drift clean, census 204→201 | probe the lower_expr unreachable |
| 4 | probe only (no commit) | hypothesised the §4③ nested-handler evidence gap (lower_expr garbage node inside map(lower_one_arm_decl)). eprint-instrument INVIABLE (adding one perform shifts the ev-row → seed exit-127). | iter 5: STATIC objdump probe. |
| 5 | probe only (no commit) — **iter-4 hypothesis DISPROVEN** | **REAL ROOT = field-offset mis-resolution (NOT evidence).** objdump of m2 `lower_one_arm_decl`: arm's fields resolve to `args@0, body@8, op_name@20` (idx 0,2,5) — arm's type is mis-unified with a ≥6-field record. Correct `{args,body,op_name}` (sorted) = `0,4,8`. So `lower_expr(arm.body)` reads offset 8 (a phantom-shifted slot, NOT body) → non-node → `else→unreachable`. `resolve_field_offset` (lower.mn:1278) computes `4*idx` over arm's *resolved* sorted field list; the type is wrong, not the formula. This is field-offset-under-polymorphism — the ORIGINAL session's diagnosed root. | iter 6: find WHERE arm's type picks up the 3 phantom fields (idx 1,3,4 between args/body and body/op_name). Probe arm's resolved type at lower_one_arm_decl (the field names) — static or via lookup_ty on arm's handle, NOT eprint. Likely the map-lambda's `arm` param type generalizes/mis-unifies. Fix the inference so arm = exactly {args,body,op_name} → body@4. Connects to the type-param-effects offset-flow (carry the concrete type through the generalized helper). |

| 6 | probe only (no commit) | **6-field type IDENTIFIED.** arm's type = `{args, arms, body, ename, name, op_name(, slot)}` (offsets args@0, body@8/idx2, op_name@20/idx5 match exactly). The extra fields (arms, ename, name, slot) are the **group** record `{ename, arms}` (lower.mn:696,704) + the **placement** record `{ename, name, slot}` (lower.mn:682-684) from `build_handler_arm_names`/`place_arm_in_groups`. So the handler-arm record type CROSS-UNIFIES with the group/placement records in that machinery — a let-polymorphism / instantiation failure (the "row-polymorphic field access in a generalized helper", original-session diagnosis). resolve_field_offset (lower.mn:1278) is CORRECT (4*idx, fallback 0); the bloated TYPE is the bug. Separately found an ARITY BUG: `infer_one_handler_arm` (3 params: arm, result_h, payload_h — line 2165) is called with 2 args at line 2154 (via infer_handler_arms, the inline-handle path, line 753) but 3 args at line 2817 (register_handler, decl path). The 2-arg call passes garbage payload_h into unify_op_payload — corrupts the effect PAYLOAD type (not arm's record fields), so likely not THIS trap, but a real bug to fix. | iter 7: PIN the exact cross-unification. The handler arm `{args,body,op_name}` and the placement `{ename,name,slot}` are map's input (a) and output (b) in build_handler_arm_names — if map's `a` and `b` unify (mono-instantiation / failed let-generalization), the types collapse. READ map/fold defs (lib/prelude.mn) + how the seed instantiates their scheme (fresh a,b per call?) + the generalize/instantiate path (infer.mn — connects to fix1/fix2 chase-to-root). Likely fix: proper let-polymorphism so map/fold instantiate fresh element vars (the handler-arm type stays {args,body,op_name}). Hypothesis-test option: pin lower_one_arm_decl's arm or the arms element type and check if the trap clears (confirms type-is-the-issue) — but prefer the structural inference fix over annotation. Also fix the arity bug (infer_handler_arms needs a payload_h — mint one like register_handler line 2766). |

| 7 | fix applied then REVERTED (no commit) | **let-poly asymmetry found + ruled out for THIS trap.** subst_ty's TRecordOpen arm (infer.mn:2452) drops the open tail `v` that free_in_ty COLLECTS (infer.mn:2316) — a real let-poly leak (seed documents it deferred, "Hβ-infer §12", scheme.wat tag 110). Fixed BOTH layers (freshen the tail, mirror subst_row's EfOpen). Rebuilt seed, ran oracle: arm.body STILL reads offset 8 (objdump unchanged: 0,8,20) → the fix did NOT change arm's type. So the bloat is a UNIFICATION binding arm's open tail to {arms,ename,name,slot} in a SINGLE inference (pre-generalize), NOT instantiate-sharing. Reverted both (instantiate is the unpatchability danger-zone; don't keep a disproven change). | iter 8: PIN the cross-unification (decisive — approaching the no-march threshold). arm (handler {args,body,op_name,...v}) unifies with placement {ename,name,slot} + group {ename,arms} in build_handler_arm_names (lower.mn:680-704). PROBE via `report` (already in inference's row → won't shift ev-slots like eprint did) at the record-unification site (unify TRecord/TRecordOpen, infer.mn:1384-1397) to catch arm's tail gaining fields. OR test: brand the placement/group records nominal (TName) so they CAN'T structurally unify — if arm.body→4, the structural unify was the cause. Likely root: map's INPUT (a) and OUTPUT (b) unify at `map((arm)=>placement, arms)` (a/b should be distinct) — check map's scheme instantiation at that call. If unfixable in 1-2 iters, write the blocker + STOP. |

| 8 (ultracode) | **FIELD-OFFSET TRAP FIXED.** root = use-before-generalization, leaking via the UNQUANTIFIED effect ROW (carries combinators' residual effects + parameterized payload TYPES). Diagnosis workflow (5 tracers, high-confidence) → pre_register registers fns as Forall([]) (monotype, shared vars); the seed quantified params+ret but EXCLUDED the row (its own deferred "Hβ.infer.row-normalize" follow-up). FIXES (all stacked, none reverted — see CLAUDE.md "proven fix stays"): (a) subst_ty TRecordOpen freshens its open tail [wheel+seed]; (b) pre_register quantifies free_in_ty(fn_ty) incl the row [wheel] / params+ret+row [seed walk_stmt.wat] — $build_inst_mapping already mints graph_fresh_row for row handles so the old cross-category concern is stale. VERIFIED: m2 no longer traps at lower_expr; runs through lowering; micros 7/7; census 201 (unchanged); seed builds. The reorder-lib-first experiment was DISPROVEN (census→531) and reverted. | iter 9: the trap MARCHED from lower → EMIT. m2 now compiles the wheel into a ~150GB m3 (O(N²) wat-emission: growing repeated docstrings, 74K²≈150GB). EMIT is UNCHANGED by the inference fixes → pre-existing, unmasked. Find the O(N²) accumulation in the wat_to_string/wat_collect buffer (str_concat_all "materializes ONCE" claim — verify it's not re-concatenating per emit) + the docstring (Documented/DS) emit path. Add a state.sh m3/m4 size guard so the runaway is capped, not 150GB. |

| 9 (ultracode) | EMIT corruption diagnosed (not yet fixed) + O(N^2) inventory. | Workflow wh7r6r56g (5 agents): the 150GB m3 is a SINGLE wild-byte_len dump — a wat_emit(s) gets a value that is a GRAPH POINTER, not a String (proven by decoding the dump as graph-node records; project_emit_state's projections are corrupt — top_fn_names even lost "main"). DISPROVED the data-section hypothesis: a guard on emit_string_data (cap byte_len>100000 → ZWILD marker) found ZERO wild entries, 150GB persisted → the corrupt wat_emit is in FUNCTION emit, not the string table. This is the THIRD appearance of the field-offset-under-polymorphism class (handler arms [fixed af3375a] → emit). ROOT: use-before-generalization — a forward-ref combinator (src using lib's map/fold) gets a pre_register scheme WITHOUT its body's a≠b relationship (that only exists post-generalize), so it stays under-constrained and cross-unifies. af3375a (row-quant) is a PARTIAL patch. O(N^2) inventory (Morgan directive): string_offset_lookup (wasm.mn:206, also a Carried-Truth re-derivation), string/state/wasi dedup linear-scans (wasm.mn:350,364), zip_with/enumerate slice-chains (prelude.mn:184), emit_static_top_closures name-scan (wasm.mn:672). | iter 10: (1) CONFIRM the emit corruption is the field-offset class (one targeted check — is a forward-ref map/fold value's type bloated, or is it an af3375a side-effect?). (2) Then the ULTIMATE root fix: dependency-ordered (SCC / Damas-Milner letrec) generalization — generalize each def fully before its dependents; kills the whole class. (3) Convert the O(N^2) inventory to O(1)/O(log N) (sorted-set, content-addressed offset map, buffer-counter). (4) Add a state.sh m3/m4 size cap so the runaway flags, not 150GB. Stack correct fixes; oracle = diff(m3,m4) empty + micros + repro, NOT census. |

| 10 | state.sh size cap (commit 2189fee); ordering hypothesis DISPROVEN | Confirmed wat_to_string handler STATE offsets are CORRECT (buf@8,count@12) — refutes the handler-state hypothesis. emit_string_data also clean (iter 9 guard). So the corrupt wat_emit(s) is in the FN-RECORD collectors (collect_fn_emit_records / extract_top_fn_name / project_emit_state) — a value read at a wrong offset = a graph pointer (agent: "top_fn_names lost main", "offset-0 read returned a graph pointer"). TESTED the use-before-generalization/ordering fix: BOTH lib-first (census 531) and foundational-lib-first (census 557, trap moved EARLIER to node_to_pexpr's map = indirect-call type mismatch) REGRESS → ordering is NOT the root (if it were, lib-first would help). Reverted. Added state.sh m3/m4 500MB cap (head -c + PIPESTATUS) → runaway flags, not 150GB. | iter 11: PIN the specific field-offset read. (1) run state.sh once (src-first, now capped — safe) to rebuild the correct post-fix m2. (2) objdump .build/state_m2.wasm at extract_top_fn_name (wasm.mn:603, reads LLet's name) + collect_fn_emit_records_expr (wasm.mn:~936, reads LMakeClosure/LMakeContinuation/LDeclareFn fields) — find which ADT-variant/record field is read at a wrong offset. The LowExpr ADT variant payloads (LLet(Int,String,LowExpr), LMakeClosure(...)) — is a variant field mis-resolved? This is the field-offset class on ADT VARIANTS (af3375a fixed records/TRecordOpen; variants may have their own instance). (3) Fix structurally (the value's type must resolve so its field offset is right). Then the O(N^2)→O(1) inventory. Oracle = the artifact; stack correct fixes. |

## Standing notes
- **PLAN.md §7 is stale** (still names the ev-slot seam as THE blocker) — refresh
  at a consolidation point to: registry dissolved (live HandlerKind); cursor =
  the hoeffect/nested-evidence root surfacing at `lower_handler_arms_as_decls`'
  `map(lower_one_arm_decl)`.
- **The current trap is FIELD-OFFSET, not evidence** (iter 5 corrected iter 4):
  `arm.body` mis-resolves to offset 8 in `lower_one_arm_decl` because arm's type
  is mis-unified (6+ fields, body at idx 2 not 1). The probe disproving the
  evidence hypothesis IS the discipline working — never crown a hypothesis before
  the artifact confirms it.
- **Hypotheses are claims until the artifact confirms.** eprint is inviable here
  (perturbs the ev-row); use objdump on `.build/state_m2.wasm` (the committed-
  wheel m2). The trap marches deeper per fix = progress.
- **Census baseline** bumped 190→201 (`tools/verify-baseline.txt`) for the
  type-param substrate; census is a shadow — never bump to force a commit.
