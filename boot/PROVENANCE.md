# boot/mentl.wasm — the pinned fixpoint compiler

This binary IS Mentl: the wheel compiled by itself to the byte-identical
fixed point (first light, 2026-07-10, commit 87c0152, tag `first-light`).
It replaces the hand-WAT seed as the bootstrap: the default build loop is

    boot/mentl.wasm  compiles  src/**+lib/**  →  m2   (wheel-emitted)
    m2               compiles  src/**+lib/**  →  m3
    assert m2 == m3                                    (the fixpoint ratchet)

Because boot is itself wheel-emitted, m2 == m3 IS the fixed point — the
seed-era rule "NOT m2 == m3" (PLAN §6) compared a SEED-emitted m2 against a
wheel-emitted m3 and retired with the seed.

Provenance, self-confirmed at pin time:

- source: THE ANALYSIS-CORE ORDER LAW + doc's env completion (2026-07-23).
  The infer-context-bracket audit's yield, landed as what the artifact
  admits: the core's install order is SETTLED and written at ONE home
  (pipeline.mn's spine block — ledgers innermost, lookup_ty before env
  before graph before mutate_sink, diagnostics outer to graph because
  graph_bind's occurs-check reports FROM the arm), and doc_run gains the
  env_handler its core lacked (its env ops resolved through the dispatch
  boundary's instance while its graph was chain-local — the
  accident-invariant class, now a contract). The consolidation itself —
  a bracket fn over a body thunk, all 14 chains rewritten, wheel 1,568
  lines smaller, census 0 — was REFUTED by the artifact before commit:
  a closure's evidence snapshot predates the bracket's installs, so
  evidence-dispatched core ops (executable_gate's verify_debt; the at
  verb's ambiguous cursor ops) fault the ev-scan into the sentinel while
  singleton-tier ops pass. Reverted whole; the measurement, the DEP
  (evidence crossing fn frames vs the M3 lexical fence), and the build
  order are banked on Hβ.cli.infer-context-bracket (named-residue index).
- boot/mentl.wasm  sha256 4b10e457ff1ce7ad25a1f36348fdc9c4a05629e100025fe871afeef9c48f027b
  (CLEAN m2 == m3 at 559,460 lines, census 0, comment-refs 0,
  frontier 212/0, battery 113/113)
- generated by: CLEAN fixpoint; self-confirms below (prior pin follows)

- source: THE WHEEL SERVES ITS OWN IDE — `mentl space` (2026-07-23).
  ide/serve.mn absorbed whole as src/main.mn's space arms: the mime
  projection, the one-read request (bytes_buf + str_of_buf), the
  request-line path parse, the route ("/" and directory paths to
  index.html; ".." refuses), the isolation-pair respond (COOP/COEP —
  the shared-memory compiler's requirement, on every response), and
  the tail accept loop (constant stack for the server's life).
  serve.mn + serve.sh + serve.wasm are DELETED — the scaffold's own
  written dissolution ("dissolves at the verb") executed. The
  listener is the install shim's seam: WASI p1 has no bind/listen,
  so the host resource rides the same shim boundary as run's exec
  seam (--dir "$MENTL_HOME::." maps the repo at guest "." — the verb
  serves from any directory; -S tcplisten=127.0.0.1:7378, port
  override MENTL_SPACE_PORT); without a listener the verb refuses
  and TEACHES the seam. Two frontier legs, both seen RED on the
  pre-verb boot (unrecognized command): the no-listener
  refusal-teach, and the live serve asserting 200 + the
  cross-origin-isolation pair on ide/index.html.
- boot/mentl.wasm  sha256 61c10ab8986cdba63e7713c94b5b4fd856a3b7eccfe2c96843dc6ab3f2e30d7d
  (CLEAN m2 == m3 at 559,432 lines, census 0, comment-refs 0,
  frontier 212/0, battery 113/113)
- generated by: CLEAN fixpoint; self-confirms below (prior pin follows)

- source: GRADIENT_QUEUE DELETED WHOLE (2026-07-23). The
  built-exposed-zero-callers block (the GradientQueue effect, the
  gradient_queue_default handler, top_n_by_score / sort_by_score_desc /
  insert_descending / reason_of_located — 107 lines) is gone; the
  named-residue peer Hβ.cursor.gradient-queue-activate-or-delete
  resolves as DELETE. The CONCEPT (idle cores ask the cursor "what
  next?"; the argmax IS the priority queue) stays band E's
  work-stealing-via-gradient design — the dead larval body taxed the
  read without serving it.
- boot/mentl.wasm  sha256 56f01996e24cfe7486818b968866eb4d07a6ad573107b1fe7cb55f6e01823c3d
  (CLEAN m2 == m3 at 558,531 lines, census 0, comment-refs 0,
  frontier 210/0, battery 113/113)
- generated by: CLEAN fixpoint; self-confirms below (prior pin follows)

- source: THE COMMENT WEAVE AT THREE ALTITUDES (2026-07-23). SYNTAX's
  "never dropped" is now true for the measured gap: a block-INTERIOR
  comment attaches to the finest FOLLOWING node (the next statement,
  the final expr, or the block's own unit node — attachment by HANDLE,
  so the comment-before-final-expr case never wraps the final as an
  ExprStmt), and a TRAILING same-line comment attaches BACKWARD to the
  node whose line it shares (same-line decided by token geometry — no
  TNewline between item and comment; span comparison failed because a
  braceless let-chain leaves the position past consumed newlines, the
  str_contains doc attaching to str_compare until the token test).
  The address surface renders the attached prose's first line as the
  Lede facet (the one comment_first_line projection — a duplicate lede
  helper died to E_DuplicateFnName's own refusal). Interior comments
  thereby joined the backtick gate, and the referent set grew its
  SCOPE face: each top-level decl contributes (extent, comment_locals)
  — extents from consecutive stmt starts, since decl spans are
  head-only — so an interior comment's backticks resolve against the
  enclosing fn's params and binders; the eight residual phantoms were
  the narration class (other-scope and historical names) and
  unbackticked per the contract. The affine ledger's
  destructure-arm/sibling-move pairing forced the block loop
  destructure-free (helpers own the collects — the pattern is banked
  in the loop's own comment).
- boot/mentl.wasm  sha256 aa6338e90b9ffc585ebf8dd84fb2b50061fcb9a3b8e98d3c0cc131a479436815
  (CLEAN m2 == m3 at 558,532 lines, census 0, comment-refs 0,
  frontier 210/0, battery 113/113)
- generated by: CLEAN fixpoint; self-confirms below (prior pin follows)

- source: THE BLOB DECLARES CALLEE-FIRST + THE SUBSTRATE FACE TABLE +
  THE N-CURSOR FIELD (2026-07-23). The canonical wheel input is now
  lib-before-src (wt_wheel lib src; march.sh matches): lib declares the
  vocabulary src consumes, so every src->lib reference reads the FINAL
  scheme instead of the loose pre-registered snapshot — measured, the
  src-first blob left 492 wheel fns with fully-bare published schemes
  (every param and the result a raw var; the order-conditional class at
  its true size), and the flip halves it to 256 (the residual is
  intra-src forward references, the named order peer). The sharper
  order re-judged the wheel: 57 diagnostics trued in one sweep — 33
  declared rows widened to their bodies' truth (mostly + Alloc), the
  escaping family's two op declarations now speak the string name-sets
  the family always carried, expr_child_handles reads {name, init}
  records by their init edge (MakeRecordExpr AND ResumeExpr passed the
  record itself to node_handle), render_propose_arm's guarded unwrap
  became the match it always was (Pure holds), and the zero-caller
  cursor_session_batch + CursorSessionResult deleted whole. WITH the
  flip: seq_op_sig — ONE face table both substrate paths derive from
  (infer_seq_op forces each position against it; a HOLED/under-applied
  seq-op call builds its product-with-holes from it, never from the raw
  body's env scheme whose `l + 8` types l Int — the 14 wheel
  `|> list_set(??, …)` pipes rewrote saturated and the class is gated),
  and the N-cursor FIELD lands: `mentl <file>:0` projects the whole
  absence field — holes ranked first (each with its Propose fan, ties
  teaching), the annotation gradient after, every position through the
  same cursor_at_handle + render_at path the single address uses.
- boot/mentl.wasm  sha256 6807a214cb300d88f7d9416ff751821e112c9f0a850955c0cba8c6387b08e944
  (TRANSITION m3 == m4 at 557,233 lines, census 0 at every generation,
  comment-refs 0, frontier 209/0, battery 113/113)
- generated by: the prior pin's wheel compiling this tree (m3 == m4
  self-confirmed by tools/march.sh; prior pin follows)

- source: THE INSTANCE CROSSES THE FN BOUNDARY (2026-07-23). The
  fold-family dig's root, landed whole: subst_row's EtVar arm returned
  the EMPTY row when the var was SOLVED to a closed row — the bound
  row's whole present set (every effect instance a callee's scheme
  proved) discarded at instantiation, so no Iterate/user-effect
  instance ever crossed a fn boundary and map's element died at the
  caller. The merge arm reads the binding live (names freshened
  through the same mapping the params use, under the solved tail) —
  and the TRIO LAW completes it: free_in_row and occurs_in_row now
  descend a BOUND tail exactly as subst_row traverses it (collection
  without descent left the inner payload vars unquantified → shared
  raw across every call site → map's install bound the shared answer
  var to [b] and each later HOF decl collided against it, the
  cross-decl Bool-vs-List wave the install probe pinned). With them:
  publish_with_instances (a declared-bare name keeps the body's
  parameterized instance), the install-frame gate (fragments of ONE
  instance join by position only inside a tee body's frame — a
  caller's independent callee instances stay independent), the
  effects.mn bare→parameterized upgrade at the name-set seams,
  prelude's callee-first iterate order, the compare-leaf f64 stash
  pair (the bare (f64.lt) select never assembled), and the spec walk
  chasing body-bound vars. The two-pass walk built en route was
  REVERTED by measurement (its second pass tips the 4GB image at the
  m3 emit — the seq-op-row precedent; order-independent verdicts stay
  the named peer). The fix exposed one canonized silent-wrong:
  mn-mapelem banked exit 1 — offset-0 alpha where .beta=2 was meant —
  and one under-declared fixture row (stats fsum + Alloc); both trued.
  Gates: mn-forward-wide-instantiation (RED on the prior pin) +
  bisect-r 42 + the fixed micro.
- boot/mentl.wasm  sha256 cad3ca5372e9962b65b12f8becc108bfbbf10bf3ce134d3e8b72dc82f64b6631
  (TRANSITION m3 == m4 at 553,900 lines, census 0 at every generation,
  comment-refs 0, frontier 205/0, battery 113/113)
- generated by: the prior pin's wheel compiling this tree (m3 == m4
  self-confirmed by tools/march.sh; prior pin follows)

- source: THE TUPLE PATTERN READS THE BRACKET (2026-07-23). The last
  lower-time layout bake in the pattern path dies: LPTuple carries
  (elem_ty, sub_pat) — the LIVE element vars — and (offset, width)
  project at EMIT through the bracket-aware channel (pat_elem_repr
  reads lookup_ty, the same channel the binop dispatch and the
  construction stores read; pat_tuple_off is the identical align_for +
  repr_width prefix-sum). A var-typed tuple destructure is itself a
  worthiness witness (spec_oph_wide_pair's TTuple arm — a var element
  landing WIDE at the key; pointer instantiations keep word slots, no
  twin minted). Seen RED twice on the prior pin: the worthy twin's WAT
  did not assemble ($x.f64 undefined local — the in-flight N-cursor
  code's (Float, Handle) ranked tuples were the first trigger), and
  the non-worthy floor read an f64's high word at baked offset 4 as
  the next element — an invalid exit status, zero diagnostics
  (probe-second). Gate: mn-generic-wide-tuple-pattern (twin assembly +
  floor-face value + mixed-order (Int, Float, Int) alignment, exit 42).
- boot/mentl.wasm  sha256 808524325f7ab799c479331bde7a404ea29f3c9a3078d4ce5086d75220d4e412
  (m2 == m3 at 550,965 lines, census 0, comment-refs 0, frontier 202/0,
  battery 113/113)
- generated by: CLEAN fixpoint; self-confirms below (prior pin follows)

- source: THE FAN PROJECTS (2026-07-23, the /loop's queue-0 landing). A
  tie at a `??` now LISTS the proven survivor space — every candidate
  rendered with its Reason, then the collapsing move — never a bare
  count (propose-fan-demo: `mentl bit.mn:8:30` projects both Bit
  survivors 0 and 1 with their seed Reasons; seen RED as the count-only
  line on the prior pin). Both candidate-verify loops gain the FORK
  PAIR: graph checkpoint + heap region (mark/reset with the virginity
  contract) per candidate — memory AND graph restored at each fork
  boundary, O(1) each (one trail mark + one bump word); the medium
  itself demanded verify_each_enriched's row widen to the body's Alloc
  truth (census 0 -> 1 -> 0). enumerate_inhabitants' multi-shot
  resume-per-proven arm carries the same pair; its first wheel-internal
  consumer is the N-cursor pass (the queue's next landing).
- boot/mentl.wasm  sha256 08640f17d5d8a1030681a3863a5085c97137df0bfc656565d27497ea03b4ba72
  (m2 == m3 at 550,078 lines, census 0, comment-refs 0, frontier 199/0,
  battery 113/113)
- generated by: CLEAN fixpoint; self-confirms below (prior pin follows)

- source: THE MANIFEST TELLS THE TRUTH (2026-07-23). Every src module's
  import list resolves its whole vocabulary — the medium's own
  E_MissingVariable diagnostics were the worklist (probe each module at
  `mentl src/<m>.mn:1:1`, fix, re-probe to zero): nine primary fn moves
  to DAG-honest homes (emit_runtime_contract, ls_find_str_local(+loop),
  main_param_count, seq_elem_face, handler_stateful, variant_specs_of
  (+filter/+from), ctor_payload_tys, ctor_result_is, param_tys_of →
  lower; node_handle, parse_span_of → graph; why_expand → query;
  query_flow_label (+predicate/+row siblings) → types), plain imports
  across fourteen modules (lower += infer alone cleared seven names;
  the voice family's odd misses were oracle effect ops), and
  main_param_count rewritten on lower's own decl walk — whose first
  flat form MISSED the LLet-closure named-fn shape and dropped _start's
  argv arg; the march's transition arbitration caught the unassemblable
  m3 before any pin moved, and the fix matches exactly the shape set
  reach_decl_name enumerates. The comment-truth sweep rode along
  (T2–T7: the deleted-seed mirror claims, the six-vs-eight field count,
  dead item-11.B pointers healed to the live peer handle, the stale
  voice.mn:9 example, era-name narration dropped for mechanism prose)
  plus SYNTAX's two corrected claims (the retired third projection; the
  comment-attachment gap measured and named with its build).
- boot/mentl.wasm  sha256 cab557e806c2794f9cce818bb5519ec82fc190b26a268959bc04b7a7f70a2431
  (m2 == m3 at 549,887 lines, census 0, comment-refs 0, frontier 198/0,
  battery 113/113)
- generated by: CLEAN fixpoint; self-confirms below (prior pin follows)

- source: THE DIRECT-CALL CASH-OUT (2026-07-23). A call whose callee
  inference proved a top-level FnScheme emits `call $name` /
  `return_call $name` with NATIVE argument widths — no spill cells, no
  table hop, no wf$ wrapper, true tails including wide results. The root
  it cures, dug from the signal-crucible's stack exhaustion: the
  word-face wrapper broke tail calls (a body's return_call lands in the
  wrapper, whose body-call was plain — one leaked frame per iteration;
  4096 samples deep tipped the stack, the atan ladder innocent on top).
  Arg widths follow the callee's DECLARED signature (word floor for
  generics, native f64 for declared Floats); a twin's widths read the
  SAME spec_site_pairs/spec_subst_pairs projection that minted the
  redirect. The wheel SHRANK ~20k lines / −5.3% wasm bytes — every named
  call dropped its indirect ceremony. Word-result wrappers tail-collapse
  as the belt. signal-crucible transplanted and GREEN (exit 42,
  oracle-cross-validated); frontier 198/0; battery 113/113; census 0.
- boot/mentl.wasm  sha256 f8abad90edb6f26ffdf14cf745e83db283d5b88005a4b008f04b91b6e44565d8
  (m2 == m3 at 549,924 lines, census 0, comment-refs 0)
- generated by: TRANSITION then CLEAN (the node's emit crossed one
  generation at the 37,350-line diff; the twin-width fix confirmed the
  fixed point); self-confirms below (prior pin follows)

- source: THE SOCKET SPEAKS AT THE ADDRESS SURFACE (2026-07-23). render_at
  surfaces the CursorView's Propose facet (`mentl hole.mn:9:37` → `Query:
  ?? : Positive · Propose: 1` — the one proven survivor through the same
  projection the edit accept path applies; ties teach, never first-wins),
  carried by the resolver's column law: file:L:C picks the TIGHTEST
  containing node (the code caught up to its own comment), identical spans
  pick the LATEST mint (the more derived node — the NHole over its id
  cell). The sharper wheel caught the session's own first ty_of_kind
  NHole arm as ill-sorted (NodeKind vs NodeBody, census 0 → 1) — deleted;
  the census-as-ratchet keeping the builder honest within the hour.
  Frontier 193/0 (+ cursor-address-propose, seen RED pre-arm).
- boot/mentl.wasm  sha256 17d1c3be1ef9bb21793549cb706f764969397f594310d98c02ceb8356a82f4b4
  (m2 == m3 at 567,046 lines, census 0, comment-refs 0)
- generated by: CLEAN fixpoint; self-confirms below (prior pin follows)

- source: THE NULL-SINGLETON CLASS CLOSES AT EVERY ALTITUDE (2026-07-23).
  The cross-compile trap's root — effect_census_collector running as a
  null-state singleton in emit's walk extent, its accumulator at absolute
  address 12 — is closed three ways: project_emit_state installs every
  visitor family its walk fires; singleton_perform_block guards STATEFUL
  singleton op calls with the SingletonUninstalled refusal (the compile
  gate's conjunction at the runtime altitude — stateless stays unguarded
  by the same licence, byte-identical to the pre-guard form); and the
  battery ships REGION-BRACKETED (heap_mark/heap_reset per micro, 113/113,
  ~192MB peak — the arena's first real workload). The guard was seen RED
  live: its first firing exposed the test verb's own missing handler
  bracket (battery_run's fs ops as null-state singletons), fixed in the
  same landing; its second firing exposed the edit chain's missing
  lookup_ty_graph (null page read as the graph in `??` authoring
  sessions) and triggered the seven-chain census — five chains gained
  their missing analysis installs (edit, pipeline_check, check_source,
  at, repl, doc). Fixture mn-singleton-preinstall-call (expect 134)
  holds the class. Five ladder runs, one landing: TRANSITION (guard
  bytes) → CLEAN (test-verb bracket) → TRANSITION (stateful-only
  refinement, wheel 95 lines smaller) → CLEAN (edit-chain install) →
  CLEAN (the census sweep).
- boot/mentl.wasm  sha256 cadb105b68b64a00e496e1611363c94f3ed239794bf724f5a517817c5ffd302c
  (m2 == m3 at 566,812 lines, census 0, comment-refs 0)
- generated by: CLEAN fixpoint (the census sweep crossed in one
  generation from the prior transition pins); self-confirms below
  (prior pin follows)

- source: THE RESET RESTORES VIRGINITY (2026-07-23). heap_reset zeroes the
  region before the rewind (the dedicated $heap_reset_impl preamble fn);
  zero-reads stay true under reuse instead of holding by monotonicity's
  accident. The twelve-kill forensic corpus and the loaded address-pair
  probe live in the PLAN named-residue index; CLAUDE.md gains the
  alive-law section and the forensic laws. Board whole, frontier 192/0.
- boot/mentl.wasm  sha256 229fda2f71a5972f6614ce8b1dde5da529126bc294d25674c05cde3816d0ea20
  (re-assembled from the SAME byte-identical fixpoint wat by the canonical
  wt_asm — the hand ladder had dropped --debug-names; the march's container
  carries the name section, readable backtraces; the wat is unchanged)
- generated by: TRANSITION (the 2-line preamble/call-form crossed one
  generation from the singleton pin fc2a9520…); re-pin from m3 per the
  transition rule; self-confirms below (prior pin follows)

Prior pin's provenance:

- source: THE SINGLETON TIER TELLS THE TRUTH (2026-07-22). LDirectPerform —
  the singleton perform as its own node, every reader delegating through
  singleton_perform_block (byte drift impossible; the ladder confirmed
  CLEAN) — and the gate's stateful-and-never-installed refusal closes the
  zero-state-global silent-wrong. Frontier 192/0, census 0.
- boot/mentl.wasm  sha256 fc2a952039580e390f02f5ecc0735828025e64f7f3574424a918920d77e0db3e
- generated by: the singleton-arc wheel compiled by the crucible pin
  (fe68767f…) held CLEAN m2 == m3 == m4; re-pin via cp of that m2;
  self-confirms below (prior pin follows)

Prior pin's provenance:

- source: THE CRUCIBLE TIER (2026-07-22). DSP / ML / adaptive-LMS
  real-workload gates with python cross-oracles, RED-by-perturbation; the
  build surfaced and fixed the float `<~` miscompile (feedback slots
  hardcoded i32 — repr now read live). Frontier 191/0, census 0.
- boot/mentl.wasm  sha256 fe68767f39431eed1deff292945aba5cbe946f3bc28d70bba11709d06bbc0b49
- generated by: the merged tree held CLEAN m2 == m3 == m4 under the
  staging-root pin (ccd9381d…); re-pin via cp of that m2; self-confirms
  below (prior pin follows)

Prior pin's provenance:

- source: THE STAGING CLOBBER'S ROOT (2026-07-22). The interpolation fold
  stamped one handle on every interior concat; emit's per-handle staging
  local folded all segments into one cell (nine re-sets in the diagnostics
  arm, reads against stale values). Interior concats now mint their own
  handles; the outermost keeps the node's. TRANSITION m3 == m4; board whole.
- boot/mentl.wasm  sha256 ccd9381d5564b33f0aaea7fdc19ea7b0a532e986c6fad85467d6edd5e722c4a8
- generated by: the fixed wheel's m3 (the emit change crossed one
  generation from the gate pin d56abbb6…); re-pin from m3 per the
  transition rule; self-confirms below (prior pin follows)

Prior pin's provenance:

- source: THE EVIDENCE HOLE REFUSES AND TEACHES (2026-07-22, the gate arc).
  E_EffectUnhandled born armed: main-row effect with a floor demand and no
  install refuses with the graph-derived proposal (HandlerKind arms through
  EffectOpScheme). The criterion reads the ARTIFACT via the emit's own
  single-walk seventh projection (EmitEffectCensus) over the post-reach
  tree — the lower-side ledger it replaced was the Carried-Truth violation
  Morgan called. Two inference roots felled (the var-tail mask into the
  graph at diff_row; multi-effect installs subtract the full arm set); the
  Show/Hash effect shells dissolved into seq-op table faces; the
  effectful-arg staging clobber witnessed thrice and sequenced around; the
  pure row is the wheel's own first top-level let; fn env entries carry
  Located decl spans. Frontier 181/0, census 0, crefs 0.
- boot/mentl.wasm  sha256 d56abbb6b6f38971b5d45b219554170ec36af6ae3cd8d68a8ad469ced13723dc
- generated by: the gate wheel compiled by the pure-row pin (3cecf9e9…)
  held CLEAN m2 == m3 == m4; re-pin via cp of that m2; self-confirms below
  (prior pin follows)

Prior pin's provenance:

- source: THE BACKTICK CONTRACT AT ZERO (2026-07-22, same day as the triple).
  All 52 unresolved comment references swept — dead names repointed to live
  successors, narration unbackticked per the contract (a backtick is a
  reference into the one namespace); the checker's leftover CLH per-comment
  eprint deleted (1,536 stderr lines per compile). comment_refs_max: 0 —
  the prose boundary is now a hard gate.
- boot/mentl.wasm  sha256 10639d69c22fe792e42e4942b36842468a7103b2c98a9926f3abee906e8f2c00
- generated by: the swept wheel compiled by the triple pin (09380a33…) held
  the clean m2 == m3 == m4 ladder; re-pin via cp of that m2; self-confirms
  below (prior pin follows)

Prior pin's provenance:

- source: THE ROW IS A TRIPLE — the six-form EffRow tree dissolves into ONE
  canonical record (2026-07-22, the representation-law update). EfRow(present,
  absent, tail), EffTail = EtClosed | EtVar | EtAll; reading = present ∪
  (tail ∖ absent); canonical at birth (ef_make), zero read-time
  normalization. Kills the fat-row root (the old normalize_inter open×neg
  arm dropped the negation from an unresolved tail, so installs typed before
  their body's row grounded never subtracted the handled effect — dead
  evidence entries, the undiagnosed 0x100000000 class). One-writer rulings:
  subsumption reads via resolve_row (no binds); path compression is
  unify_row's alone. The free-tail law re-derived on the triple (a
  still-free tail is vacuous at the gate — the 646-error wave of m2's first
  self-judgment, cleared). The first top-level let the medium ever compiled
  exposed emit_init_lets as the one emit_expr caller without the local
  `~> emit_memory_bump` bracket (fixed to emit_one_fn_to_string's shape;
  frontier mn-top-level-let RED exit-134 on the prior pin, 42 here).
  Hygiene: 12 six-form remnants + 23 zero-reference fns deleted; zero
  six-form mentions remain in code or prose. Net −372 lines. Board: census
  0 at every generation; feedback-iir 30; handled-Ping 42; top-level-let 42.
- boot/mentl.wasm  sha256 09380a336df9a1f36bd219cec331d21ae7bd1afec089ccc35a423cd2f5390651
- generated by: the triple wheel compiled by the prior pin (658f3988…) held
  the clean m2 == m3 == m4 ladder (size-guarded, byte-identical); re-pin via
  cp of that m2; the re-pinned boot self-confirms below (prior pin follows)

Prior pin's provenance:

- source: THE TEST VERB + THE REGION SUBSTRATE — CLI absorption stage 2
  (2026-07-22). `mentl test <dir>`: a DIRECTORY target IS the battery
  (fs_list_dir_impl — the fd_readdir transport joins io.mn; a file target
  stays the single check). Each micro declares its own oracle in its first
  line (`// expect: N` — the expectation is graph content ON the artifact,
  never a side-table row); each compiles IN-PROCESS under a fresh handler
  chain (verdict = holes + refusals, the check verb's own licence), its WAT
  written under .build/test/ beside a streamed manifest line
  (MICRO stem path want). Execution stays the shim's seam — WASI owns no
  process-spawn (the process_exec precedent). One process now does what a
  wasmtime boot per micro did. The REGION substrate landed with it: Alloc
  gains heap_mark()/heap_reset (emit = the bump global read/written — the
  §5.O arena's first grain), gated RED-first (mn-heap-region: a reclaimed
  region's next alloc returns the SAME address; exit 134 on the pre-arm
  pin f60110f4, 42 here). The battery's own region is the honest residue:
  bracketing each compile with mark/reset traps compile #2 in its own
  infer while EVERY input is probe-verified intact (libs len+head+tail,
  the names, the reset address), and WITHOUT the reset 100 compiles run
  straight to the 4GB ceiling — 12 short of the battery. The no-reset form
  ships; verify.sh keeps the per-process loop as the gate; the peer
  Hβ.runtime.cross-compile-durable-state carries the probe corpus (PLAN
  §7 named-residue index). Board: census 0, frontier 168/0 (+heap-region),
  proof-exactness 9/9, crown 5/5, micros green.
- boot/mentl.wasm  sha256 658f39883a03ac31cfbdd74c81ebe8a991a0e2377628e402744c8ad32d0d3b64
- generated by: an intermediate transition mid-arc (the substrate-arm emit
  change crossed one generation; re-pin from its m3), then the battery
  close-form held the clean m2 == m3 under it; re-pin via cp of that m2;
  the re-pinned boot self-confirms the clean m2 == m3 (prior pin follows)

Prior pin's provenance:

- source: THE LSHOW/LHASH BUILD — show/hash dispatch moves to emit; four
  latent breaks close (2026-07-22). to_string/hash arrive at emit as
  STRUCTURAL nodes (LShow/LHash) whose type dispatch runs under the active
  specialization bracket — the LAST pair of type-directed lower-time bakes
  (the D3 map's inventory), so a worthy twin's to_string renders the VALUE
  (the name bake rendered the ADDRESS) and its hash agrees with its eq; the
  show/hash operand is a worthiness witness, so a render-only generic still
  twins. Every emit-side walk delegates through show_node_of/hash_node_of
  (the one dispatch home, synthesizing the exact call shapes lower baked —
  concrete scalar sites byte-identical); the reach projections mirror the
  same dispatch (show_reach_names/hash_reach_names) so the prune keeps what
  emit mints. FOUR pre-existing breaks closed, each probe-pinned: (1) the
  aggregate call is a DIRECT (__state, arg) call to $show_<sig>/$hash_<sig>
  (§5.U "called directly like $str_eq") — the old closure-convention call
  referenced a global no module ever emitted (undefined-global assembly
  failure in every minimal module); (2) the leaf's interior renderers
  survive reach (int_to_str was pruned — undefined-fn assembly failure);
  (3) the decor literals actually register — the call-site fold_closures
  .show field read resolved a WRONG EMPTY SLOT (the trecordopen-wrong-field
  class LIVE: register_show_literals received n=0 while generation read n=1
  from the same record; fixed by passing the whole closed-annotated record);
  (4) hash(x) gains its DECLARATION (the exact to_string mirror — the name
  never resolved before, E_MissingVariable at every user call; the entire
  hash surface was unreachable). Twin fold collection runs per-demand under
  its bracket, so a twin's substituted aggregate sigs generate beside the
  floor's. Gates RED-first on the prior pin: mn-generic-show (run 1, the
  address render), mn-aggregate-show (assembly failure), mn-aggregate-hash
  (E_MissingVariable). Board: census 0, frontier 165/0, proof-exactness
  9/9, crown 5/5, micros green.
- boot/mentl.wasm  sha256 f60110f4e6e303e126be92df9166988d95265afbca1ba4506235a5d66b7c8b58
- generated by: boot -> m2 -> m3 (the emit change crossing one generation,
  8352-line diff; the new wheel is SMALLER — the name-bake machinery
  deleted) -> m4 (m3 == m4, TRANSITION); re-pin via cp of the m3 artifact;
  the re-pinned boot self-confirms the clean m2 == m3 (prior pin follows)

Prior pin's provenance:


- source: THE EXACT-SUBSTITUTION SWAP — monomorphization residue face 3 closes
  (2026-07-22). The specialization bracket now carries the site's EXACT
  instantiation pairs (root handle -> concrete type) instead of one wide type:
  a free leaf answers its OWN root's binding, a free var whose root is not in
  the pairs stays the honest floor TVar, and the one-wide-type demand guard —
  the free-leaf rule's requirement — is DELETED. Sound because the subscript
  fracture fix (prior pin) made a generic body's element classes one
  union-find root with the scheme's quantified vars; the interim free-leaf
  rule and its guard were scaffolding around the fracture and retired with
  it. Mixed instantiations twin correctly: sqboth(a, b) = (a*a, b*b) at
  (Float, Int) emits $sqboth$sp20 squaring the f64 natively and the i32
  natively in one body (gate mn-generic-multitype, RED run 1 — address
  arithmetic — on the prior pin, 42 here). Board: census 0, frontier 156/0,
  clean m2 == m3.
- boot/mentl.wasm  sha256 4a114123c8816b066940ce7cd5921521c658b0c407d33d8c832d9466f282b863
- generated by: boot -> m2 == m3 (clean fixed point — the wheel demands no
  twins, so the swap is wheel-neutral yet tool-changing; re-pin from the
  m2cache artifact, self-confirming) (prior pin follows)

Prior pin's provenance:


- source: THE SUBSCRIPT FRACTURE FIX — Force = UNIFY, never overwrite, at the
  xs[i] sugar arm (2026-07-22, infer.mn IndexExpr). The arm bound the receiver
  with graph_bind — a cell OVERWRITE that DISCARDED the receiver's proven
  TList(elem) and orphaned a fresh element class per subscript — while its own
  sibling seq_force states the law in its comment ("graph_bind would DISCARD
  the arg's proven type"). Probe-pinned by an adversarial worktree agent:
  merge's condition subscripts rooted at 13777 while its param element rooted
  at 13793 — the union-find FRACTURE that forced the monomorphization
  free-leaf rule; with the fix the same probe shows ONE class, so the exact
  root-keyed substitution (and the multi-type-generic unlock) becomes
  possible. Both forces now unify (receiver against TList(elem), index against
  TInt), with the §4① String seam preserved: a proven-TString receiver is
  SATISFIED (a String IS a sequence) and s[i] binds TByte — sharper than the
  old clobber-to-TList. The refuted hypothesis is part of the record:
  instantiate-shares-never-clones was NOT the fracture root (instantiate heals
  through argument unification, polymorphic recursion included) — the label
  died to the artifact, as the pipeline demands. Board: census 0, frontier
  153/0, proof-exactness 9/9, crown 5/5, micros green.
- boot/mentl.wasm  sha256 da45bcdded23d097c7bef96dc733eff328c80f4089cff5567fc83669989ade23
- generated by: boot -> m2 (the fix in m2's code) -> m3 (the sharper classes
  applied to the wheel's own subscripts) -> m4 (m3 == m4, TRANSITION); re-pin
  via cp of the m3 artifact; the re-pinned boot self-confirms the clean
  m2 == m3 fixed point (prior pin follows)

Prior pin's provenance:


- source: NESTED-LAMBDA TWINS — the monomorphization residue's first face
  closes (2026-07-22). A lambda born inside a generic body inherits the
  parent's vars, so it twins under the same instantiation: a worthy demand
  contributes every record nested in its body (collect_fn_emit_records over
  the parent, mangled with the parent's enc), and the closure-mint arm
  redirects the record's table index to the nested twin through the bracket's
  active enc (spec_closure_name — the LGlobal redirect's closure-mint face,
  gated on the registry; captures, self-binding, and local naming keep the
  original name, so the record local and self-recursion are untouched). The
  bracket state widened from spec_wty to (spec_wty, spec_enc) — spec_ctx_set/
  clear/spec_enc_active on the ONE LookupTy handler. Landing it surfaced and
  fixed a width-consistency break: a param used ONLY inside a nested lambda
  had no param-decl width source (find_local_handle_expr stopped at the
  closure boundary) while the capture-store read the live handle — the twin
  declared (param $k i32) and read $k.f64, an assembly failure. Captures and
  evs are PARENT-frame expressions; the walk now reads them (bodies stay
  scoped out). Gate: tests/frontier/mn-generic-nested-lambda.mn, RED (run 1)
  on the prior pin, 42 here. Board: census 0, frontier 153/0, m2 == m3.
- boot/mentl.wasm  sha256 a919906dd9d7acb76f9f4666ca57772b6c98ba6a07ae897ea8bcaa506e36e5d2
- generated by: boot -> m2 == m3 (clean fixed point; the wheel itself demands
  no twins, so the emit change is wheel-neutral yet tool-changing — re-pin
  from the m2cache artifact; the re-pinned boot self-confirms) (prior pin follows)

Prior pin's provenance:


- source: NAMED-GENERIC MONOMORPHIZATION — the §5.U scalar half lands
  (2026-07-22). A named generic fn used to compile ONCE at the RI32 word floor;
  reached at a Float instantiation its body compared/added the word-protocol
  REFERENCES of the f64 values (bump addresses ascend, so prelude sort returned
  its input in allocation order and reduce(xs, min) picked by address — values
  intact, order garbage, zero diagnostics). Now emit runs a DEMAND ANALYSIS:
  every reference site's instantiation is a PROJECTION off the live union-find
  (the scheme's type walked against the site's resolved type — zero new
  storage, zero infer changes), a site whose quantified vars land on ONE
  distinct wide type becomes a candidate, and a candidate is WORTHY when its
  body performs arith/compare/eq on a free-floored operand (the address-
  comparison witness) or an interior site redirects to a worthy twin (the
  transitivity fixpoint — sort twins because merge is worthy, no recursion
  special-case). A worthy twin is the SAME fn record emitted once more under
  the SPECIALIZATION STATE: lookup_ty_graph (the one LookupTy handler) carries
  spec_wty, set/cleared around the twin's emission, and while set a free var
  answers the wide instantiation — the free-leaf rule, forced by the artifact
  (a generic body's unresolved classes FRACTURE: merge's param element and its
  comparison operands root at different handles, so no root-keyed mapping can
  be complete; one-wide-type demands make every free unambiguous). STATE-swap,
  never a second handler: a second LookupTy handler demoted every lookup_ty
  perform out of the singleton direct-dispatch tier — measured as 16 frontier
  reds (ev-scan faults at infer sites that never threaded evidence). Twins ride
  every name surface appended (table via their $wf$ word faces when wide, idx
  globals, static closure records); the LGlobal/LFnRef arms redirect through
  spec_target_name gated on the seeded spec_registry (SpecTwins, the
  StringTable pattern), with a spec_twins_exist fast path keeping twin-free
  modules at one op per reference. Two more roots fixed en route, each
  probe-pinned: the concat floor at wide_all (an open element through a
  filter-with-handler chain — the declared [String] Intent Boundary), and the
  order-conditional row class (wasm.mn sorts before types.mn/lower.mn, so
  inferred rows disagreed between Tier-2 callees and bare callers — every spec
  fn now DECLARES its row, the build_reach_index precedent). Gates RED-first:
  tests/frontier/mn-generic-float-{accumulator,comparator}.mn run 1 (the
  silent-wrong) on the pre-spec boot, 42 here; Int instantiations stay at the
  byte-identical floor. Board whole: census 0, frontier 150/0, proof-exactness
  9/9, crown 5/5, micros green, m2 == m3.
- boot/mentl.wasm  sha256 92fceff07558b20136fab3134366a78d19c3b7212ff2d5b97bdda7016bde94a4
- generated by: the handler-swap candidate pinned mid-arc went through
  boot -> m2 -> m3 == m4 (TRANSITION), then the state-swap correction reached
  the CLEAN m2 == m3 fixed point; re-pin via cp of the m2cache artifact; the
  re-pinned boot self-confirms m2 == m3 (prior pin follows)

Prior pin's provenance:


- source: EFFECT-POLYMORPHIC STORED FUNCTIONS + the mentl verb table (2026-07-21).
  A first-class CLOSURE carrying its own effect row can now be stored in an ADT
  field and called — the medium carries functions-with-their-rows through data,
  the same capability handlers rest on. The root was a hole in the inferencer:
  quantify_ctor_ty passed a function-typed ctor field's effect ROW through
  unquantified, so its open tail was a single free var that finalize closed to
  Pure — every effectful stored closure then "violated Pure." The fix
  (quantify_ctor_row) re-mints the field's open tail as a genuine row variable
  (parse_type_ty had minted it as a TYPE handle, which free_in_row never
  collects and instantiate cannot freshen) and quantifies it into the ctor
  scheme; result_ty filters row handles out by node-kind so the type stays
  non-parametric, and instantiate's is_row_handle -> mint_row freshens the row
  per use, so a table of heterogeneous-row closures unifies by the row algebra.
  This UNBLOCKED the mentl CLI overhaul: the verb set had three drifting homes
  (the hand-written verb_catalog help, the `if mode == "..."` parse chain, the
  dispatch match); now ONE VerbSpec table — each row carrying the verb's name,
  arg hint, BUILDER CLOSURE, and description — is projected by both the parser
  (find_verb -> the closure builds the typed Invocation, or a per-verb
  ParseError that names the missing argument) and the help catalog, so they
  cannot drift. Witnessed RED->GREEN on the wheel's own census: the verb table
  drove it from 7 E_PurityViolated (unfixed boot -> m2) to 0 (fixed m2 -> m3),
  then m3 == m4 byte-identical. Board whole: census 0, frontier 131/0
  (+stored-fn-effect-poly capability test), proof-exactness 9/9, crown 5/5,
  micros green, phantom 286. The re-pinned boot self-confirms the CLEAN m2 == m3
  fixed point.
- boot/mentl.wasm  sha256 1167ddfecdcd2ab763e9a586a86888fc0e8df5bdbb1a8afd40f5e96f39328f36
- generated by: boot -> m2 (7 verb-table diagnostics, functional core + the fix
  in m2's code) -> m3 (clean, the fix applied) -> m4 (m3 == m4); re-pin via cp
  of the m3 artifact; the re-pinned boot self-confirms the CLEAN m2 == m3 fixed
  point (prior pin follows)

Prior pin's provenance:

- source: THE WIDE-ELEMENT [Float] CASH-OUT — §5.U's stride carrier reaches its
  first wide element (2026-07-21). A [Float] is now a first-class packed
  sequence, end to end: the literal is born stride-8 (make_list_sc, the
  literal allocator's second face paired with make_list), the generic layer
  crosses every wide value BY REFERENCE (the WORD PROTOCOL — "a handle IS a
  word": emit_wide_ref spills an f64 to a fresh cell and leaves its address,
  emit_wide_deref cashes it back, and every call_indirect speaks the WORD-ARITY
  face), and the concrete sites cash the width. This KILLS the float-evidence-ft
  class: a wide-signatured fn — named (`scale`) or a capturing lambda — reaches
  the fn table through a `$wf$<name>` word-face wrapper (deref args, direct-call
  the native body at full speed, spill the wide result), so a polymorphic
  call site never needs to know the callee's emission. structural == compares
  VALUES through list_eq_f64 (never the references); the reach walk contributes
  the f64 leaf family (list_eq_f64/list_compare_f64/list_hash_f64/float_to_str)
  at the exact ==/ordering/show/hash sites emit mints them — the visitor-walk
  projection the runtime-contract names, so a float-free module drags in
  nothing. One real emit bug fixed en route: emit_wide_ref used $state_tmp as
  scratch, clobbering the closure record a capturing lambda holds live there —
  a dedicated $wide_cell local closes it. TRANSITION m3 == m4 (the 6056-line
  m2/m3 diff is the emit change — the $wf$ wrappers, the $wide_cell local, the
  reachable f64 family — crossing one generation), then the re-pinned boot
  holds the CLEAN fixed point m2 == m3. Board whole: census 0, frontier 128/0
  (+4 wide-element fixtures: list/map/hof/show, RED-first under the
  pre-wide-element boot where f64.const into an i32 slot did not even
  ASSEMBLE), proof-exactness 9/9, crown 5/5, micros green, phantom 286.
- boot/mentl.wasm  sha256 683d66cbe44992046abdcac0c01d902b07adafe53173d6308d1d8912d7ca056c
- generated by: boot -> m2 -> m3 (TRANSITION m3 == m4), re-pin via cp of the m3
  artifact; the re-pinned boot then self-confirms the CLEAN m2 == m3 fixed
  point (prior pin follows)

Prior pin's provenance:

- source: THE STRING=[BYTE] MERGE — §4① lands whole (2026-07-21). String IS a
  stride-1 byte list: TString unifies with [byte] and binds a generic element
  to TByte (the merge lives in unify_types; TString stays nullary, no clone),
  strings carry the unified [count][tag_word][bytes@+8] record (literals via
  the emit's 8-byte header; every builder via bytes_buf; io/net/json/lsp/
  persist/driver/main off the +4 layout), byte_at IS list_index, ++ IS the
  O(1) concat node, str_slice IS the slice node, and the sign-bit view shape
  is deleted (view_base -> str_payload, the stride-normalizing
  materialization boundary). TByte~TInt is check-level value-equality
  (same_ground, never a binding, forced by datum-last inference order); the
  slot is guarded by the carrier on reads, store_strided's 0..255 range trap
  on writes, and construction-follows-result-type. list_to_flat split to two
  altitudes (typed [a]->[a] table entry over flat_raw) — healing the entire
  13-entry libs-isolation shadow at its one root. Landed via the two-stage
  DISPOSABLE ladder: boot compiled W1 (old layout + new compiler) -> m2; m2
  compiled the real tree -> m3 == m4 == m5 byte-identical (m3 == m4 directly:
  the migration is behavior-preserving for the compiler's own computation).
  Census 0; frontier 116/0 including the NINE-fixture behavioral battery the
  fixpoint oracle cannot see (map/fold/filter/eq/concat/slice/interp/parse
  over real strings + the range trap at exit 134), RED-first under the
  pre-merge boot (15 refusals). W1 discarded — zero permanent compat.
- boot/mentl.wasm  sha256 dbcaca3e4be07108adc4ad31f38569b57946d1eb24a6f9efb771a0a787bfdbeb
- generated by: the two-stage ladder (boot -> m2[W1] -> m3 -> m4 -> m5, fixpoint
  m4 == m5) + re-pin via cp of the m4 artifact (prior pin follows)

Prior pin's provenance:

- source: STEP 0 of the String=[byte] value-ontology unification (2026-07-20,
  Hβ.value.ontology-derivation-complete). Adds TByte — the packed sequence
  ELEMENT-slot leaf (a byte VALUE is a word, repr RI32, arithmetic is Int; its
  SLOT stride in a packed sequence is 1) — as the 15th Ty constructor, with a
  new stride_of projection (1 for TByte, repr_width otherwise) and a distinct
  fold-sig "b" so [byte] never shares a generated helper with [Int]. H6 forced
  every arm: 12 exhaustive Ty matches gained a TByte case (occurs_in,
  same_ground, subst_ty, free_in_ty, chase_probe_tag, show_type, ty_handle_of,
  extract_row, query_flow_label, the synth enumerator, the to_string dispatch).
  Dead vocabulary — nothing produces TByte yet — so the emit is unchanged for
  every real program: CLEAN FIXED POINT m2 == m3 (byte-exact), census 0, micros
  72/72. The RI64/RF32 gradient-arm precedent (a trigger, not a new scalar). The
  value/slot split is the novel move the eventual String = TList(TByte) merge
  rides — Rust's u8 conflates them; Mentl keeps byte-value = Int-word, byte-slot
  = stride-1. Next in the arc: the element-stride-dispatched flat leaf, then the
  per-decl arena (§5.O, the OOM keystone), then the merge.
- source: the §4① string-layer typing + the expect_same chase-first root fix
  (2026-07-20, Hβ.infer.expect-same-chases-bound-var). A Float POSITIONAL
  constructor field filled from an unannotated param left the param an
  unresolved var — the scalar clobbered the arg reference's NBound(TVar(binder))
  binding in expect_same (the lone unify arm that bound without chasing),
  orphaning the binder — so `g` floored to i32 and the f64 call site trapped
  `indirect call type mismatch` (the ctor-arg face of Hβ.emit.float-evidence-ft;
  it also blocked the LSP serve on json's parse_number Float). The fix is one
  line — expect_same chases the var live like every other arm. It unmasked the
  runtime's pervasive handle-word pun (§4① — a handle IS a word), so the
  foundational fix TYPED the string layer: byte_len/byte_at/str_slice/str_concat/
  the float builders are seq-ops (typed calls over self-consistent raw bodies,
  the list_index pattern), str_of_buf is the one construction boundary (a raw
  [len][bytes] buffer IS a String), raw byte access in the parse/comment
  functions became byte_at/byte_len, and state slots dedup by handle identity
  (i32.eq) not str_eq. Full-wheel census 0; the LSP json float blocker cleared
  (serve reaches the LSP layer, hover-response the next rung). TRANSITION
  m3 == m4 (594-line m2/m3 diff = the emit change crossing one generation);
  re-pinned from m3. Board whole at pin: census 0, frontier 89/0,
  proof-exactness 9/9, crown 5/5, micros 72/72, instrument reads clean-passes.
- source: the mount-independent weave rank (2026-07-19). The instrument
  gate went RED on the ultracode pin: `check <clean>` from an installed
  context exited 1. driver_module_order_key ranked src-before-lib by a
  position-0 prefix test (`str_slice(p, 0, 4) == "src/"`), so under the
  /mentl-home mount every resolved path (`/mentl-home/src/...`) ranked as
  lib — a mount-DEPENDENT weave order that re-fired the order-conditional
  verdict class (two E_TypeMismatch at prelude consumers) the canon
  exists to sidestep. The rank now reads the src/ SEGMENT
  (`str_contains`), invariant under any mount prefix. Blob path
  untouched; clean fixed point m2 == m3; boot re-pinned because the fix
  is wheel-neutral yet tool-changing (the check/compile verbs run from
  boot). Board whole at pin: census 0, frontier 86/0, proof-exactness
  9/9, crown 5/5, instrument reads clean-passes.
- boot/mentl.wasm  sha256 628b509f54cee3516e865b6b43ea60bf8545414fc0942d23777f64eb50aabfdc
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/march/m3.wat boot/mentl.wasm` (prior pin follows)

Prior pin's provenance:

- source: THE ULTRACODE BATCH — COLUMN 2 CLOSES (2026-07-19). Four
  isolated worktree builders (a 4-reader recon fleet first, then 4
  opus builders), each transplanted onto main by hand and marched
  serially, every merge gated whole. (1) R3 — the decidable arithmetic
  Verify fragment: node_const_at folds +,-,*,/,% over ground Int/Float
  operands with zero minting; nested `self` resolves through the
  PWithSelf BINDER threaded into every fold (subst_self is the
  one-line wrapper; the shallow operand swap deleted); div/mod-by-zero
  refuses to fold. `self + 1 <= 10` discharges with zero debt, SYNTAX's
  own Even discharges, and a proven-false construction REFUSES under
  the armed class (frontier 84/0 at that pin). (2) The generative
  self-test loop: the medium proposes through the edit transport,
  compiles, runs, classifies, reduces a failing case to minimal form
  (842B -> 131B), banks crucibles — four real bugs on run one,
  including the sharpest demonstration in the repo: `0`, the only
  inhabitant of `Int where self == 0`, is a PROVEN fill that still
  divides by zero — proof is a monotone filter over admissible values,
  not a guarantee about the program around them, now executable. Its
  regression detector caught s03's case GRADUATING against current
  boot (the bucket sweep had fixed it). (3) The LSP transport runs the
  frontend: didOpen drives driver_check into the session-level graph,
  serve_run carries the analysis handlers, and `query "type double"`
  projects a real function type off the live graph; the end-to-end
  serve session documents its pinned pre-existing blocker (json
  parse_number — the FOURTH independent hit on
  Hβ.emit.float-evidence-ft this session). (4) The CFC pipeline's
  first real pass: cos/sqrt/atan2 into math.mn, the comodulogram in
  lib/dsp/cfc.mn, and the synthetic PAC gate finds the planted (6,40)
  coupling at exit 42 — MVL peak/median 5.93 matching a numpy oracle
  (5.92); read_recording is the real-file transport. Frontier 86/0 at
  this pin; census 0 throughout; every march held m2 == m3.
- boot/mentl.wasm  sha256 162875bb87c227587457dd11da48b73349821ba06ac681062d95ee52613fcd89
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/march/m3.wat boot/mentl.wasm` (prior pin follows)

Prior pin's provenance:

- source: THE OCCURS CHECK SEES BOUND STRUCTURE — THE EIGHTH ARMING
  (2026-07-18). `fn selfapply(f) = f(f)` TRAPPED the compiler: chase_deep
  and subst_ty spun to stack exhaustion with ZERO occurs reports, because
  the occurs leaf compared handles while the cycle lived in BOUND
  structure — bind a := TFun([b], r) first (b free, passes every
  comparison), close b := TVar(a) after; no handle compare sees it, only
  descending a's binding does. The leaf now answers the real question —
  "does handle occur in the type GRAPH reachable from here": raw handle,
  then root (union-find), then the root's BINDING recursed (NBound and
  NRecordRowBound; TRecordOpen's rest symmetric). Termination is the
  guarded-write invariant itself: every bind runs the check, the graph
  stays acyclic, the recursion bottoms out — and chase_deep carries a
  depth-200 tripwire as the belt (a firing names the shape by
  constructor tag; show_type on a cycle would spin, measured). The
  probe arc: the fixture's tag probe named the cycle (TVar(3)/TVar(19))
  and PROBE-OCCURS proved the guards never fired — the diagnosis was
  mechanical. Wheel compile time unchanged (~11s). E_OccursCheck ARMS
  (the eighth): the fixture that found the trap now gates it — reports
  the infinite type and refuses (exit 1, zero WAT; frontier 73/0).
  m2 == m3 clean fixed point; census 0; micros 72/0.
- boot/mentl.wasm  sha256 220203939deaf611468b4fe442699ec617d0bfd9b5f4353ba74e0275aeeb456f
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/march/m3.wat boot/mentl.wasm` (prior pin follows)

Prior pin's provenance:

- source: THE SEVENTH ARMING (2026-07-18, the column-2 closeout's first
  half). E_MissingVariable ARMS: wheel census 0, and the user-path
  licence MEASURED — a no-import stdlib program (map / fold / len / any)
  resolves via the DAG's prelude seed, compiles exit 0, runs 20; the
  stdin contract is self-contained input, where a miss is a real break.
  Pre-arming, the refusal fixture compiled a missing-name executable to
  2,795 bytes at exit 0 (the diagnostic on stderr) — the silent-wrong
  the arming deletes; post-arming: exit 1, zero WAT (frontier 72/0,
  run_refusal). E_OccursCheck does NOT arm with it: its fixture found a
  live trap — `fn selfapply(f) = f(f)` spins chase_deep to stack
  exhaustion BEFORE any report (the occurs guards at graph_bind and
  unify never see the cycle this shape builds), banked as
  Hβ.infer.selfapply-cyclic-ty-spin with the fixture bannered. The
  three occurs var-leaves now ROOT-CHASE (TVar / TRecordOpen's rest /
  EfOpen's tail — unify IS union-find; a raw handle compare passed the
  indirect cycle a ~ (b -> r) with b's root already a), sound hygiene
  the march holds. And Hβ.cli.process-exec-wire is CLOSED BY
  MEASUREMENT: run_run already routes process_exec ~> process_no_exec,
  which refuses exit 1 and teaches the shim — PLAN's entry was stale.
  m2 == m3 clean fixed point; census 0.
- boot/mentl.wasm  sha256 b19a768fe708952d18c66f81f13b83089d29470e98b39fedd471d8d7e842657c
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/march/m3.wat boot/mentl.wasm` (prior pin follows)

Prior pin's provenance:

- source: THE UNIFIED EACH (2026-07-18, the eight interrogations over the
  fold's traversals). FIVE walks of the lowered tree were ONE walk five
  times: four byte-identical ~60-arm collectors (eq / compare / hash /
  show — each re-walking the whole tree for one leaf's type closure, the
  eq/cmp copies silently DROPPING their trigger's right subtree) + four
  byte-identical 13-line dedup walkers + register_show_literals
  re-collecting the show closure for the data section. Now: ONE collector
  carries the four closures as one record (a trigger arm contributes to
  its slot; recursion is uniform over every child, the dropped-subtree
  class closed by construction); ONE dedup walk emits each distinct
  signature's helper, the leaf selected by the FoldOp ADT; emit_module
  computes the record ONCE and both readers read it.
  cmp_agg_operand_contrib deleted as a byte-copy of eq_operand_contrib.
  25 fns deleted; net −318 lines in the emit. The landing's own first
  compile hit Hβ.lower.trecordopen-wrong-field live (an unannotated acc
  inferred a partial OPEN record and the field offsets floored at
  runtime) — the closed-record Intent-Boundary pins on every acc param
  are load-bearing and the comment names why. TRANSITION m3 == m4;
  census 0 (warnings 738 -> 732); frontier 71/0, proof-exactness 9/9,
  crown 5/5, micros-through-m2 72/0.
- boot/mentl.wasm  sha256 7853c3f295ebdcfa50bf4ae646c747505cded8f108312cfa1e0c3c63d4404186
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/march/m3.wat boot/mentl.wasm` (prior pin follows)

Prior pin's provenance:

- source: THE MEDIUM BUILDS ITSELF WITH ITS OWN MODULE RESOLUTION
  (2026-07-18, the manifest arc — Hβ.driver.wheel-imports-are-the-
  manifest CLOSED). `mentl compile main` emits the whole wheel (497,934
  WAT lines, ZERO diagnostics), it assembles, and the DAG-built wheel
  compiles and runs programs. The roots, each found by interrogation:
  (1) `import own` lexed TImport TOwn and ident_at's fabrication floor
  walked an empty module path — import segments are TRANSPORT names, so
  the keyword-spelled words {own, ref} are legal segments
  (import_segment_at, defined ABOVE its caller: the forward-ref
  quantified-return floor turned the first placement into a loud ++
  trap). (2) The prelude is PRE-IMPORTED — the DAG seeds it where it
  resolves; a bare scratch context compiles bare programs. (3) The weave
  order is CANONICAL, never the discovery order: leaves-first exposed
  three real latent mismatches alphabetical masked (prelude sum/chunk/
  trim meeting TIGHT runtime schemes where the canon met loose
  pre-registered ones — Hβ.infer.order-independent-verdicts names the
  class); the DAG discovers the SET, the sort fixes the WEAVE. (4) The
  edit session reads ONE weave (driver_entry_with_ranges — the address
  transport's route) and OPENS AT THE ENTRY (a Located caret at the
  entry's weave line; the argmax's module-aware decay then ranks the
  entry's own `??` above the closure's recovery holes). (5) NModule had
  readers and ZERO writers — the driver's fold now mints the module
  structure into the graph (same_module's decay was flattening every
  span to the "" module). (6) check rides the same weave (the
  per-module walk's missing-vocabulary noise retires until the
  per-module env overlay lands). Named live bug DODGED not fixed:
  patch_set_target's state write clobbers the tail of the last-allocated
  list (the ranges read is hoisted above it; peer
  Hβ.patch.set-target-state-clobber, two-line repro in the session).
  Frontier 71/0, census 0, fixed point, proof-exactness 9/9, crown 5/5.
- boot/mentl.wasm  sha256 95a1b3ddb4aa87b9924f45ffc755f408fc8e0cd6c89f529eb559574277dd40ed
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/march/m3.wat boot/mentl.wasm` (prior pin follows)

Prior pin's provenance:

- source: ▶▶▶ CENSUS ZERO (2026-07-18). The medium's verdict on its own
  source: NO ERRORS — 2,266 three days ago, 73 this morning, 0 at this
  pin. The last two representation changes: (1) NRecordRowBound — the
  record-row residual (a `...R` rest's remaining fields) gets its OWN
  node kind with a type-side occurs guard (occurs_in_record_fields);
  NRowBound's payload is EffRow ONLY, occurs_in_row is total over the
  six row forms with no cross-sort catch-all, mk_record_row_residual's
  Ty-wrap DELETED, ten mirror arms across the NodeKind matches. (2)
  slot_present — the Option-of-handle niche's READ half: the arg-slot
  presence test goes through a substrate-table-typed `a -> Bool` whose
  raw body compares the word, so the test never pins a slot's element
  type onto Int (next_free_slot + collect_slot_nodes read it; the full
  niche repr — construction and match projection — stays named as
  Hβ.emit.option-niche-repr). m2 == m3 clean fixed point. Frontier
  71/0, proof-exactness 9/9, crown 5/5, micros-through-m2 72/0, the
  census ratchet holds at ZERO — a rising census is now a refusal to
  merge, and every future class arming has its wheel-side precondition
  met (the user-path condition still gates name-dependent classes on
  the manifest arc, per the panel's licence correction).
- boot/mentl.wasm  sha256 bf00e685b1d122dbe3fa4c88a8c1540e461d3b24dcdcb1e6add6c35e5d036991
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/march/m3.wat boot/mentl.wasm` (prior pin follows)

Prior pin's provenance:

- source: EVERY APPLICATION STAGES ITS BOUNDARY (2026-07-18). Census
  6 -> 2; E_UnresolvedType EXTINCT. The seq-op fast path typed its call
  and returned — skipping the stage_continuation_boundary every other
  application performs ("EVERY application result is a possible
  suspension boundary", the fn's own law). Lower's k2 crossing machinery
  then found NoBoundary at exactly four wheel sites (a seq-op call on
  the k2 spine: alt_same_name_set, rows_equal, splice_at, k2_is_park) —
  identified by a one-build probe hijacking the reports' epoch field as
  a site marker (all four fired at continuation_boundary_at's NoBoundary
  arm). The fix is the law applied: the fast path stages the same
  boundary. TRANSITION m3 == m4 (the staged boundaries shift emit one
  generation); the new wheel reproduces itself. Remaining census: the
  two named representation changes (Hβ.graph.rowbound-ty-residual-tagged,
  Hβ.emit.option-niche-repr's arg-slot instance).
- boot/mentl.wasm  sha256 4fdeba773fd8ac8adbafaf2754f6ce7dd5fc0a853628ffe8e7d851bf320c1e4b
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/march/m3.wat boot/mentl.wasm` (prior pin follows)

Prior pin's provenance:

- source: THE DIAGNOSTIC LEARNS ITS ADDRESS (2026-07-18). Census 7 -> 6.
  The escaping family speaks canonical NAME-SETS end to end — the
  `-> Option(EffRow)` op declaration was the family's one row-speaker
  (compute_escaping_rows builds [(String, [EffName])]; ev derivation
  consumes names), and its instantiation at escaping_row's match was the
  List-vs-EffRow error. And E_UnresolvedType stops reporting at 0:0:
  all eight lower report sites read the node's own weave span
  (parse_span_of) instead of span_zero — the four census entries became
  LOCATABLE and immediately named their root: every one is a generic
  fn's && short-circuit whose boundary-weave thunk row never resolves
  (alt_same_name_set, rows_equal, splice_at, k2_is_park — one root,
  named Hβ.lower.boundary-weave-generic-thunk-row). m2 == m3 clean
  fixed point.
- boot/mentl.wasm  sha256 f96428611ff80817a8f908b6023a989a32d00f7398f44473673a43bd384121dc
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/march/m3.wat boot/mentl.wasm` (prior pin follows)

Prior pin's provenance:

- source: THE BOUNDARY MADE NAMEABLE (2026-07-18). Census 30 -> 7 in one
  landing, three families. (1) The representation layer's two altitudes
  got two NAMES: alloc_list / slice_raw are the RAW constructors
  (word-level, callable from lists.mn's own walks); make_list / slice
  stay the table-typed consumer names — a raw body calling a table-typed
  name forced [a] onto its word locals (the whole 10-error lists
  cluster; flat_fill_slice_range's list_index became the unchecked read
  its construction proves). int_to_str joined the table's String face
  (a [len][bytes] builder whose consumers read String). (2) The lathe
  caught up to SYNTAX's `-> !`: parse_type_atom gained the TBang arm and
  quantify_ctor_ty mints a fresh bottom var per occurrence — abort()
  finally types as never-returns, and the voice tree_list stub + search
  backtrack mismatches dissolved. (3) Decl-vs-artifact lies fixed where
  the artifact was right: verify_candidate takes Candidate;
  drain_string_literals returns pairs; HandlerDeclStmt / LHandleWith
  carry their real element records; CursorSessionResult constructs its
  record; the LPLit emit arm matches LowValue's LString (the
  tag-coincident LitValue spelling was the duplicate-ADT merge class
  live); seq_force admits TString under a TList force (§4①: len on a
  String is canonical). TRANSITION m3 == m4; shadow re-blessed 14 -> 3.
- boot/mentl.wasm  sha256 1502fb0acb175e3eed669b912e5b8a8174b224c80dfe4fdb89a74a786f6915bf
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/march/m3.wat boot/mentl.wasm` (prior pin follows)

Prior pin's provenance:

- source: A BUCKET IS A LIST THAT STARTS EMPTY (2026-07-18). The four
  str_hash/handle indexes (env, imap's smap, own's region index, wasm's
  string-offset index) zero-filled their slot buffers and pushed every
  reader through `if x == 0 { [] } else { x }` — a word test no type
  admits (eleven List-vs-Int census errors, the D root's bucket half).
  The absence was never Option-of-list; an empty bucket IS an empty
  bucket. list_filled(n, []) mints each index with one shared empty
  (buckets are only ever REPLACED, so the sharing is sound); the guards
  and env_bucket_at DELETE into direct list_index reads. And lower's
  eff_names_of null guard (`name == 0`, three Int-vs-EffName) came out
  with its class: the dissolved sorted insert was the null's home, the
  buffer-counter set builds slice to exact count, and the battery +
  self-compile arbitrated the removal green. Census 44 -> 30.
  m2 == m3 clean fixed point; runtime-shadow fingerprint re-blessed
  (lists.mn spans shifted under list_filled).
- boot/mentl.wasm  sha256 213a89ebdf63ac86f9666bd6dd79ad4a54aace66a56daac634fb72ed6c77869e
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/march/m3.wat boot/mentl.wasm` (prior pin 7455cd9d… follows)

Prior pin's provenance:

- source: THE KIND IS THE NAMESPACE (2026-07-18). `effect Sample` and
  `type Sample = Float where …` are both canonical (SYNTAX'S own §4③ and
  §refinement examples share the name) — but the env is one entry stack
  and the effect decl registers in phase 2, always newer, so every
  annotation naming the type resolved to the EFFECT's entry and baked a
  bare nominal TName that never unified with its base (10 Sample-vs-Float
  + 5 kin in lib/dsp). The read now carries its expectation:
  env_lookup_type walks the same str_hash bucket for the newest LIVE
  type-bearing entry (alias-schemed Forall or RecordSchemeKind) —
  quantify_ctor_ty, every annotation surface's one resolver, reads it.
  Effect-position readers keep the plain lookup. Census 59 -> 44.
  m2 == m3 clean fixed point; boot re-pinned (wheel-neutral yet
  tool-changing — the filtered read must live in the compiling wheel).
- boot/mentl.wasm  sha256 7455cd9da117bf4734b4b3d0124f1ccaafee4c6396f71179b13d1c1713790180
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/march/m3.wat boot/mentl.wasm` (prior pin b4d8011f… follows)

Prior pin's provenance:

- source: THE SEQ-OP HOLE GUARD + THE RECURRENCE PRIOR (2026-07-18).
  Two inference roots. (1) `list_set(??, i, v)` and kin took the seq-op
  fast path SATURATED — the hole counted as an argument, so the partial
  application the Stage Law promises never minted (E_TypeMismatch at
  every piped substrate stage). The fast path now guards on
  hole-freedom + full arity; a holed call falls through to the
  partial-application machinery like any other callee. (2) The
  `(prev) => body <~ spec` recurrence form typed the lambda ITSELF as
  the feedback result — prev never unified with the prior output, and
  the RHS was never checked as a state element. PFeedback now applies
  the single-param lambda to its own result (the prior IS the prior
  output, read live) and unifies the RHS against FeedbackSpec. Census
  73 -> 59. TRANSITION m3 == m4; the new wheel reproduces itself.
- boot/mentl.wasm  sha256 b4d8011f92d5faf294a0b13a57a7e8eb47164eb59628fabb1ba27dbe17ec0d09
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/march/m3.wat boot/mentl.wasm` (prior pin 8e248607… follows)

Prior pin's provenance:

- source: THE RECORD-CTOR ARROW VIEW (2026-07-18). `X({...})` — the
  single-variant nominal record's ctor in the one application syntax —
  met the env's RESULT binding at the call's chase and mismatched at 29
  sites (the tail's biggest root). The arrow is a VIEW minted at the
  read (record_ctor_arrow_view — the identical instantiation infer_pat's
  RecordSchemeKind arm performs; one view, two readers) and the ordinary
  saturated machinery does the rest. Census 101 -> 73. TRANSITION
  m3 == m4; the new wheel reproduces itself.
- boot/mentl.wasm  sha256 8e248607ed693e415f406ecf549d1b765d57344affa102cd4690c3b51501a90c
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/march/m3.wat boot/mentl.wasm` (prior pin bfc576f2… follows)

Prior pin's provenance:

- source: FIVE MORE CLASSES EXTINCT (2026-07-18). E_EffectMismatch and
  E_PurityViolated to ZERO (the widen loop's fixpoint — nine
  rebuild-and-re-judge iterations; negation-carrying declarations
  matched on positive parts, ! entries preserved). E_ResumeOutsideArm:
  the synth candidate fan moved INTO its arm (backtrack's try_each
  shape — the census was right that resume is arm-bound).
  E_ConstructorArity: QRFlowLabel's second field bound.
  E_PatternInexhaustive: the counter was PAlt-BLIND — both walks
  (collect_arm_tags, arms_have_wildcard) now flatten alternation
  branches, and voice's two H6-exhaustive matches were exhaustive all
  along. register_one_variant's refutable let-destructure became the
  match it always was; unlock_capability gained AWrapHandler ->
  CSandbox; dead lib/runtime/buffer.mn DELETED (zero consumers).
  Census 185 -> 101: E_TypeMismatch 97 + E_UnresolvedType 4 are ALL
  that remain. m2 == m3 clean fixed points throughout.
- boot/mentl.wasm  sha256 bfc576f2c997d98458dc6b45c473073b54ed2fd059a1a96e5b55cabb9f599ac2
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/march/m3.wat boot/mentl.wasm` (prior pin 6e7c10b2… follows)

Prior pin's provenance:

- source: THE ADDRESS IS THE SURFACE (2026-07-18). `mentl voice.mn:9`
  answers — the README §9 promise made a working transport. VAt parses
  at the argv boundary; driver_entry_with_ranges carries the module-
  range map out of the concatenation fold it always computed; the
  three-case line rule (widest-starting-on-line, tightest-covering,
  nearest) resolves over the span index every node writes at birth;
  cursor_at_handle projects the same eight-arm CursorView mentl edit
  reads; the terse facet-silent render reads the source slice, env
  schemes for decls, literal bodies for free cells, and the Why walk
  through the range map. propose_at is the ABSENCE facet structurally
  (authored_hole — the same read the transport gates patches on).
  The session's exact-reason-span resolver SURVIVES, documented as the
  live-generation crutch (measured: find_tightest over mint spans
  resolved a stale generation's hole and 13 edit fixtures went red) —
  it dissolves with Hβ.cursor.session-weave-epoch-scope. Named peers:
  Hβ.emit.float-evidence-ft (an f64-arg candidate ctor through an
  all-i32 $ft — first fired by the first float-position enumeration
  ever taken), why-flow-naming + refinement-provenance (the README
  Why lines' remaining substrate). Census 213 -> 185 (the widen
  cascade's second round + the transport's own rows). Frontier 71/0.
- boot/mentl.wasm  sha256 6e7c10b252e04757062516fbc04a28bd417c51f745d994a5c2b5fb7e9dcb633f
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/march/m3.wat boot/mentl.wasm` (prior pin 01ccaa3a… follows)

Prior pin's provenance:

- source: THE EFFECT-TRUTH SWEEP, ROUND 1 (2026-07-18). The census-spec
  fleet (29 per-file readers) produced exact edit specs for 192 sites;
  148 applied: declared rows widened to the body's truth, aspirational
  `with Pure` dropped for the honest Memory/Alloc the bodies perform,
  missing match arms written with correct payload arities, the
  LStateSlotStore patterns bound their fourth field. Census 301 -> 213
  (EffectMismatch 95 -> 70, PurityViolated 56 -> 24, PatternInexhaustive
  25 -> 7, ConstructorArity 4 -> 1). m2 == m3 clean fixed point.
- boot/mentl.wasm  sha256 01ccaa3af891863b55cbbc480ddc00dc4a8f08368188ac99aa0d23ead5addcce
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/march/m3.wat boot/mentl.wasm` (prior pin 57a2113e… follows)

Prior pin's provenance:

- source: TWO CHECKER ROOTS (2026-07-18). The medium's own diagnostics
  were the bugs. E_IfMissingElse EXTINCT (31 -> 0): the unit test matched
  TName("Unit"), a spelling the inferencer never produces — real unit is
  TUnit — so every genuinely-unit then was judged non-unit (probe:
  then_ty=() at all 25 sites), plus 6 unresolved op-results the unify
  below resolves. E_FeedbackNoContext EXTINCT (11 -> 0): the check read
  the COMPILER'S runtime handler stack while inferring the COMPILED
  program's structure — a category error firing on every real `<~`,
  SYNTAX's canonical lowpass included; the compile-time class question is
  the named peer Hβ.effects.iterate-class-declaration. Census 343 -> 301.
  m2 == m3 clean fixed point (report-only changes).
- boot/mentl.wasm  sha256 57a2113e9ce840cf54d61e2e750130b66862b672ed381480960f859b31f1cd72
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/march/m3.wat boot/mentl.wasm` (prior pin 3112cec5… follows)

Prior pin's provenance:

- source: THE INDEX LAW IS REAL (2026-07-18). SYNTAX §Indexing's "traps
  on out-of-range" was prose — list_index tag-0 raw-loaded. The checked
  entry landed with its structural prerequisite: `&&`/`||` SHORT-CIRCUIT
  (lower's BKBool arm — boolean verbs are control, not arithmetic). The
  eager i32.and had run every guarded read: set_insert, cache_filter_loop,
  register_one_variant (nullary ctors probed index 0 of empty lists on
  every `type X = A | B`) — three latent OOBs surfaced in the trap's
  first hour. Then the trap swept the DRIVERLESS-CHAIN class: check,
  check_source, edit, and repl chains all ran infer's region/consume ops
  with no analysis handlers installed — zero state records reading the
  zero-filled sentinel page as empty ledgers, "working" on garbage since
  the chains were born. All four now install affine_ledger +
  region_tracker. The dead narrowing-elision write deleted (never fired;
  discharge-gated write is the named peer). micro oob-traps=134 is the
  law's executable form. Census 348 -> 343. TRANSITION m3 == m4 for the
  lowering, then clean fixed points through the chain fixes.
- boot/mentl.wasm  sha256 3112cec50a6a779c9e8f67c8707b6d8d2eb50d52de5ab42c10529af95d95b814
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/march/m3.wat boot/mentl.wasm` (prior pin 01d77f31… follows)

Prior pin's provenance:

- source: THE VALUE CLASSES ARM (2026-07-18, §11 col 2). diag_refuses
  gains ERefinementRejected (a decidable-FALSE claim emitted 2,513 bytes
  at exit 0 while §11 said the class was locked) and EOwnershipViolation
  (its one false channel — the unresolved-callee move default — died with
  graph_bind_hole; the detector survived adversarial falsification).
  node_const folds negated literals, so the canonical Sample range
  (`-1.0 <= self`) DECIDES — and the sharper compiler immediately caught
  the wheel fabricating tag -1 into TagId's 0..255 refinement
  (lower_pat's unresolved-ctor arm). That fabrication is DELETED into
  LPUnresolvedCon: match test honestly false, sub-binders still declared,
  the dead arm assembles. mn-refine-reject (dormant, asserted the
  pre-arm exit-0) superseded by frontier refusal fixtures; the
  handler-forward-ref regression 2522f0dc lacked is added. Census 348;
  frontier 69/0; m2 == m3 clean fixed point.
- boot/mentl.wasm  sha256 01d77f31cd05f3071fbe34e16e157c9a5a8ee7d4a197f84b42df118176e3639b
- its .wat (m2 == m3 asserted) sha256 ee42b8dd531004ec9991ad2321dcc8ec2ce2565ba8739bbb3fd89bf99ca214e9
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/march/m3.wat boot/mentl.wasm` (m2 == m3 byte-identical;
  prior pin d6dd8ed9… follows)

Prior pin's provenance:

- source: ONE MISS, ONE DIAGNOSTIC + THE HANDLER-NAMESPACE REFUSALS
  (2026-07-18, §11 col 2). The env-miss path bound TVar(self), tripping
  graph_bind's own occurs check (a spurious 0:0 E_OccursCheck per missing
  name) and reading downstream as an unconstrained var (the ownership
  move-default cascade). It now binds NErrorHole via graph_bind_hole; an
  unresolved VarRef callee borrows its direct args. Two new classes born
  ARMED (decl-site, no resolution dependency): E_HandlerStateShadowsOp —
  a state field naming an op of its handled effect compiled clean and
  returned the WRONG value; the medium's own m3 leg caught the second
  wheel violation (`caret`) the same hour the check landed — and
  E_DuplicateFnName. Voice state fields renamed turns/caret_now; the
  dead duplicate FeedbackSpec deleted. Census 352 -> 348; the last
  forward-ref roots felled. E_MissingVariable/E_OccursCheck at wheel-zero
  but NOT armed (the panel's licence correction: user paths still fire
  them on unresolved stdlib vocabulary until the manifest lands).
  m2 == m3 clean fixed point; re-pinned so the refusals + the hole-bind
  are live in the CLI. Frontier gains run_refusal + two fixtures.
- boot/mentl.wasm  sha256 d6dd8ed968e66bfd48cc34aa1a43866353bc068a993afe654f8a6300071d33b4
- its .wat (m2 == m3 asserted) sha256 c81ebde12c7fcb5168651bb53cec56159f3fc8e8c2ffa69260b54c33958053fd
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/march/m3.wat boot/mentl.wasm` (m2 == m3 byte-identical;
  prior pin 45244e15… follows)

Prior pin's provenance:

- source: THE SMT HANDLER TELLS THE TRUTH (2026-07-18, §11 col 2 + ARC 0).
  verify_smt declared only `witnesses` while its arms update and read
  `debt` — the unknown-debt ledger never existed as state, so both arm
  references were unbound names (2 E_MissingVariable + 2 paired
  E_OccursCheck). `debt = []` added. Same commit: the solver's polarity
  fixed to validity-as-UNSAT-of-the-negation (SAT of an open predicate
  admitted any satisfiable predicate as discharged; SmtSat now returns a
  COUNTEREXAMPLE alongside the rejection — predicate_decide already
  short-circuits ground predicates, so the solver only ever sees open
  ones); node_to_predicate gained its missing BNe arm (`self != 0` fell
  to the opaque PBoolNode and accrued debt instead of deciding); and
  show_pred_operand renders UnaryOp/BinOp operands recursively (the
  `<expr>` fabrication hid the exact bound the diagnostic teaches).
  Census 356 -> 352. m2 == m3 CLEAN FIXED POINT (content change, not
  compiler-behavior change); re-pinned so the renderer/predicate/debt
  fixes are live in the CLI. THE BOARD IS WHOLE.
- boot/mentl.wasm  sha256 45244e15579736fcdc6d90bbbb2cd92040765bffbb9113c47d2032cc777e012f
- its .wat (m2 == m3 asserted) sha256 e0e1ba1664fe7131ec66bcc82e577345c41b607307a2bae8f867aa2b01e72cf6
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/march/m3.wat boot/mentl.wasm` (m2 == m3 byte-identical;
  prior pin 8bf740a4… follows)

Prior pin's provenance:

- source: A HANDLER'S IDENTITY IS A PARSED FACT (2026-07-18,
  pre_register_handler_sig, §11 col 2). `pre_register_stmt` registered
  FnStmt, TypeDefStmt, EffectDeclStmt — but not HandlerDeclStmt. Handlers
  entered the env only at the main walk's `register_handler`, so every
  `~> handler_name` before the handler's declaration in source order
  (115 sites across the concatenated wheel) floored E_MissingVariable.
  The handler's identity — its effect (derivable from the arms' op names),
  its instance type, its config, its residual row handle, and its
  HandlerKind — reads only parsed structure and the effect env (already
  registered by EffectDeclStmt). Moving the identity registration to
  pre_register makes handler names resolve order-independently.
  The main walk's register_handler reads the pre-registered r_handle from
  the env so forward references share the same residual row handle.
  Census 578 -> 356 (-222): E_MissingVariable 115 -> 4, E_OccursCheck
  115 -> 4. Clean m3 == m4 TRANSITION. THE BOARD IS WHOLE.
- boot/mentl.wasm  sha256 8bf740a442c8bf00e83f1bfae3c8c06d4afcc61e0a61711b222a101c04ed13b7
- its .wat (m3 == m4 asserted) sha256 7a532ca3ad0f5d3f894e6dac126e808e90cd39b54ac1fdb5dd504585f5701369
- generated by: `bash tools/march.sh` + re-pin via
  `cp .build/march/m3.wasm boot/mentl.wasm` (m3 == m4 byte-identical;
  prior pin 361ed16c… follows)

Prior pin's provenance:

- source: A BORROWING PARAMETER BORROWS (2026-07-17, move-vs-borrow part 3,
  §11 col 2). Call inference inferred every argument before reading the
  callee's parameter product, so an owned value passed to `observe(ref value)`
  was consumed exactly like one passed to `take(own value)`. The call now reads
  the canonical TParam product after labeled/default argument resolution: a
  direct variable passed to authored or inferred Ref enters the existing borrow
  scope; Own parameters and nested computations keep normal move inference.
  Runtime `len` and `list_index` declare their actual read-only access, and
  update_file_text_loop carries list_set's returned owner. Census 582 -> 578;
  E_OwnershipViolation 4 -> 0. Soundness control: `take(value) + take(value)`
  remains one ownership violation. RED-first gates:
  mn-own-call-arg-borrow, mn-own-forward-ref-seq, and mn-own-call-arg-move.
  Clean m2 == m3 fixed point. THE BOARD IS WHOLE.
- boot/mentl.wasm  sha256 361ed16cdc4d10fcefcbfe89d385028560ee7a37700c46c171b9756e0dfb71db
- its .wat (m2 == m3 asserted) sha256 6756ca73d9c132e30e46862ad478564ea0e7071ed3e648dda8e3531d0845dfc9
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/m2cache/m2.wat boot/mentl.wasm` (m2 == m3 byte-identical;
  prior pin 1e06cdaa… follows)

Prior pin's provenance:

- source: A FIELD READ IS A BORROW (2026-07-17, move-vs-borrow part 2, §11
  col 2). Reading `.field` borrows the receiver — it never moves the record —
  but infer_field_access_record consumed the receiver at every access, so
  `Buffer{data: push(buf.data, x), count: buf.count + 1}` floored
  E_OwnershipViolation on `buf`. The receiver is now bracketed in a borrow
  scope when it is a value chain (a VarRef or nested field access); a call
  receiver (`f(x).field`) keeps its normal inference so `x`'s move into `f`
  still consumes. buffer.mn's buf.data/buf.count cleared, census 583 -> 582,
  soundness intact, a clean m2 == m3 TRANSITION. Residue (4): the
  call-arg-borrow (`len(nodes)`, `list_set(overlay_bufs)`) — an `own` value
  passed to a BORROWING parameter — the last move-vs-borrow piece, reading the
  callee's param ownership at each argument. THE BOARD IS WHOLE.
- boot/mentl.wasm  sha256 1e06cdaa47e0d861a5826c931566b699df0c9bb7aa603cc8ed007f74cf188898
- its .wat (m2 == m3 asserted) sha256 97844f832c9abd2577abaa15690a205a8cd6d0133e6e1290362659bae04e8385
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/m2cache/m2.wat boot/mentl.wasm` (m2 == m3 byte-identical;
  prior pin fbcf14f2… follows)

Prior pin's provenance:

- source: A READ IS A BORROW (2026-07-17, move-vs-borrow part 1, §11 col 2).
  An `if` condition and a `match` scrutinee are READ to decide control and
  never moved, so an `own` value used inside them (`if set_contains(buf, x)`)
  is a borrow, not a consume — but the ledger consumed at every use. A
  borrow_depth counter on affine_ledger, bracketed by borrow_enter /
  borrow_exit around the condition and scrutinee, drops consumes while inside.
  Cleared 32 of the 37 remaining "consumed twice" (census 615 -> 583) with
  soundness intact: a real double-move `(take(buf), take(buf))` to two `own`
  params is still caught. Carried-Truth: the graph knows a condition is a read;
  the ledger treated it as a move. Emit grew (the counter + two ops), a clean
  m2 == m3 TRANSITION. Guarded by the extended
  tests/frontier/mn-own-alternative-branches.mn (a condition-borrow case, 4
  false consumes on the old boot, clean run to 33 on this one). Residue (5):
  field-read (`buf.data`) + sequential call-arg-borrow — the move-vs-borrow
  tail needing a field-receiver borrow and callee-param ownership at call args.
  THE BOARD IS WHOLE.
- boot/mentl.wasm  sha256 fbcf14f28fb5a09c24b57793e3e43b484ff0f6a8a38012e3c1bd4bd95f1c1357
- its .wat (m2 == m3 asserted) sha256 92138a14e9285d2e871601953dc77224a952422569b74d47874d263de6bed3ec
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/m2cache/m2.wat boot/mentl.wasm` (m2 == m3 byte-identical;
  prior pin 6d693a87… follows)

Prior pin's provenance:

- source: OWNERSHIP STOPS FIGHTING SAFE CODE (2026-07-17, the affine model,
  §11 col 2). 115 of the 152 `E_OwnershipViolation` "consumed twice" errors
  were false positives — the medium was stricter than Rust on provably-safe
  code, the opposite of §4⑤'s Hylo-quiet thesis. Two masked bugs: (1) `if` /
  `match` arms were never bracketed as branches, so an `own` value read in
  both arms of `if i<0 {slice(buf)} else {list_set(buf)}` counted as two
  consumes — but the arms are ALTERNATIVES (exactly one runs). A `BranchMode`
  ADT (BParallel | BAlternative) now rides the branch frame; branch_exit
  reports a cross-arm collision only for BParallel (`><` / `<|`, all arms run),
  unions without collision for BAlternative. (2) affine_ledger is installed
  ONCE at the pipeline, not per body, so a name consumed in one function stayed
  in `used` and collided in the next naming the same `own` param — `buf` / `acc`
  are everywhere, the dominant shape. consume_enter_fn / consume_exit_fn now
  bracket each infer_fn body (a fresh consume scope, restored on exit,
  re-entrant for nested fn decls). Carried-Truth: the graph already knows a
  branch is an alternative and a body is a scope; the ledger re-derived neither.
  Census 727 -> 615; the `><` / `<|` collision path is unchanged (crown + the
  micro battery green). Emit grew (the ADT + two ops), a clean m2 == m3
  TRANSITION. RED-first gate: tests/frontier/mn-own-alternative-branches.mn
  (2 false "consumed twice" on the old boot, clean run to 30 on this one).
  Residue named (37 left): the move-vs-borrow-by-callee case — an `own` value
  read in an `if` condition or a field is a BORROW, not a move. THE BOARD IS WHOLE.
- boot/mentl.wasm  sha256 6d693a8739f6d8c7bb0ad29b52ab862982b7b33d62769041d2953a55515e4363
- its .wat (m2 == m3 asserted) sha256 769c995f7cc19e79c586c101e965b5e6ee163896e0001e5a2bc557dcd0186a49
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/m2cache/m2.wat boot/mentl.wasm` (m2 == m3 byte-identical;
  prior pin 5e9ec2d6… follows)

Prior pin's provenance:

- source: THE ALIAS EDGE IS READ, NOT RE-DERIVED (2026-07-17,
  `Hβ.infer.alias-preserving-unify`, §11 col 2). 362 census errors —
  `Span vs ValidSpan` / `Int vs ValidOffset`, both directions — were ONE
  root, the census's single biggest. `pre_register_decls` ran a single
  source-order pass, so a fn signature quantified before its refined alias
  was declared (parser.mn's `span: ValidSpan` precedes types.mn, which sorts
  last in the concatenated wheel) called `quantify_ctor_ty` against an env
  that did not yet hold the alias edge — and baked a bare nominal
  `TName("ValidSpan")` into both the param handle and the fn scheme. The main
  walk then tried to refine that bare name to its live
  `TAlias(ValidSpan, TRefined(Span, _))` and `unify(bare-TName, Span)` floored
  at the leaf. Fix: register the alias edges in a phase BEFORE any fn
  signature (`pre_register_alias`), so the resolver reads the LIVE edge.
  Carried-Truth exactly — the graph drew the alias; the resolver re-derived a
  name. The banked "a two-pass is a no-op" (2026-07-17, one session prior) was
  measuring BOOT's census (old inference) rather than the fixed compiler's;
  the fixed compiler reports 0 for the class, 1233 -> 874 total. Emit is
  byte-identical (an error hole and a resolved TAlias both lower at word
  width, same codegen path), so m2 == m3 held as a clean fixed point and boot
  was re-pinned only because the compiler binary changed (wheel-neutral yet
  tool-changing). Guarded RED-first:
  tests/frontier/mn-refined-alias-forward-ref.mn (a refined-alias param used
  before its type is declared) fails `Pos vs Int` on the old boot and runs to
  42 on this one. THE BOARD IS WHOLE.
- boot/mentl.wasm  sha256 5e9ec2d66327bf475130557e7976999602b30d333d2ca1a54072adf04533f62f
- its .wat (m2 == m3 asserted) sha256 e74354e7a3398a557e40a1446ba471fb8423624b00a91be406dc3e82a16d6b68
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/m2cache/m2.wat boot/mentl.wasm` (m2 == m3 byte-identical;
  prior pin ab34a853… follows)

Prior pin's provenance:

- source: THE SURFACE PARSES ITS OWN CANONICAL FORM (2026-07-17,
  `Hβ.parser.refined-alias-nonatomic-base`, §11 col 2). SYNTAX §«Type
  aliases»' own example `type NonEmpty = [a] where len(self) > 0` did not
  parse: parse_type_decl probed only `p2 + 1` for `where`, so a multi-token
  base (`[Int]` is three tokens) put `where` out of reach — P_UnexpectedToken
  on `where` at the `]`, then E_MissingVariable on the orphaned len/self. The
  fix parses the whole base type FIRST, then branches on what follows
  (`where` / `|`+`-`& / a ctor payload / nothing) — a `[base]` / `Option(k)`
  base now refines. The wheel uses no such form, so m2 == m3 stayed
  byte-identical and the march read a clean fixpoint; the parser fn's own
  bytes changed, though, so boot was stale until this re-pin (the case where
  a change is wheel-neutral yet tool-changing). Tooling improved WITH the
  medium in the same change: tests/frontier/mn-refined-alias-nonatomic.mn +
  its run_program registration guard the form (frontier 47 → 50). Verified in
  a pty: old boot failed the form, this boot exits 0. THE BOARD IS WHOLE.
- boot/mentl.wasm  sha256 ab34a85301fdfe4ec02c4c41b76ca67aa86dd1975248ab26c82bf6c806000afb
- its .wat (m2 == m3 asserted) sha256 377dc38e7c18ce16a24b298da99b73e874d29b598068dd92c9975f1bbf99d976
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/m2cache/m2.wat boot/mentl.wasm` (m2 == m3 byte-identical;
  prior pin e2babb24… follows)

Prior pin's provenance:

- source: BARE MENTL PROJECTS WHERE YOU ARE (2026-07-16, the welcome
  arc; two rungs). Bare `mentl` in a terminal hung on a stdin read; the
  fork lands on fd 0's filetype (fd_fdstat_get: character device = a
  human → welcome_run; pipe/file = the compile-stdin contract, the
  fixpoint path untouched). The welcome is a PROJECTION, not a manpage:
  fs_list_dir_names (fd_readdir on the cwd preopen, the dirent wire
  parse) names the directory's .mn sources and offers each one's next
  keystroke; the verb catalog is ONE string with two surfaces (welcome →
  stdout exit 0; the error path → stderr). `mentl help`/-h/--help reach
  the same projection. Rung 1 landed the ops with the fork deferred
  (reachability pruned the caller-less read — clean fixed point; boot
  cannot lower an import it predates, and stdin_is_tty executes ON the
  march path); rung 2 restored the fork through the new boot — fixed
  point again. Measured in a real pty: the directory projection saw
  song.mn and spoke first. THE BOARD IS WHOLE at the bless.
- boot/mentl.wasm  sha256 e2babb24b3912c0843ec5681f762d8ef34fd7c528803db39eefbb29f9f6b5c90
- its .wat (m2 == m3 asserted) sha256 caa80b0c3d619080da2a1887de285a321d334a233ed8c4005751872577f89cd6
- generated by: `bash tools/march.sh` (the ratchet loop; prior pin
  26bfe90a… — the anywhere-CLI wheel, whose entry follows; rung 1's
  intermediate pin is in git history)

Prior pin's provenance:

- source: MENTL RUNS FROM ANYWHERE — the install story is a pointer, the
  imports are the manifest (2026-07-16, ninth pin of the day; §11 column 1
  executed). Four landings in one arc: (1) the resolver chain
  (driver_module_path): direct existing-path → src/ → lib/ →
  /mentl-home/{src,lib} — the well-known guest path the install shim maps
  the repo to (an address, not an env var); (2) fs_at — the preopen table
  IS the mount table: absolute paths resolve by LONGEST guest-name prefix
  (the wasi-libc rule; fd_prestat_dir_name joins the import set), so the
  mapping is reachable — the old fs_dir_fd opened everything against the
  FIRST preopen; (3) prelude.mn declares its runtime imports (memory /
  strings / lists / threading) — the imports ARE the manifest, so a user
  program's one `import prelude` pulls the whole runtime through the dep
  walk; (4) driver_compile_entry — the compile verb reads the entry as the
  one-namespace CONCATENATION of its dep DAG (deps first, post-order — the
  wheel's own proven build form), so every dep's decls lower and emit
  (driver_check_entry lowered only the entry: $len undefined, the gap the
  first temp-dir run exposed). tools/install.sh writes ~/.local/bin/mentl,
  a POINTER to this boot — every re-pin is instantly the global CLI (the
  pin IS the release). MEASURED from a fresh temp dir: `mentl check` /
  `mentl compile` / `mentl < file` work; `mentl run t.mn` (import prelude,
  len([1,2,3])+39) → exit 42 through the shim's compile→assemble→execute
  half (WASI has no exec — the wheel's run verb names that seam);
  `mentl run hole.mn` REFUSES (exit 1, zero bytes, E_UnresolvedHole) —
  the refusal law survives the whole pipeline. Method notes re-paid: the
  stale-m3.wasm cp trap (assemble from the wat, NEVER cp on a clean fixed
  point) and one eprint-Heisenberg null. Shadow fingerprint re-pinned
  (6273b71e… — prelude's new imports shift the shadow); IDE shims gain the
  fd_prestat_dir_name stub. THE BOARD IS WHOLE at the bless.
- boot/mentl.wasm  sha256 26bfe90af945aab6274bc64d735f5c52d9a9988c31617cad8772f8bc216ac300
- its .wat (m2 == m3 asserted) sha256 49631004b8905571e352e44fa4a0b9ef8ccbf36698a78b8d31816452d66e0bf1
- generated by: `bash tools/march.sh` (the ratchet loop; prior pin
  94e449dc… — the live-regions wheel, first line of the ledger below;
  two intermediate transition pins of this arc — f0572443…, the
  fd_prestat_dir_name import wheel, and 84868a0b…, the /mentl-home
  resolver wheel — are in git history)

Prior pins, one line each, newest first (full mechanics in git history and
the PLAN §7 landing ledger; each was self-confirmed by the same ratchet at
its pin time; a line without a pin landed byte-identical under the pin
above it):

- regions are live: fn body = region, return = transfer (Hylo-quiet) · pin 94e449dc…
- the region tag speaks truth — the board is whole · pin 4b7f998f…
- the executable gate + the refinement-ledger truth · pin 701c7024…
- partial application as a value + the NFree-report deletion · pin cf479f9d…
- the capability `??` workflow + the let-statement bind · pin c323a40f…
- the positive `??` workflow green end to end · pin 7b188a31…
- the felt route lives — mentl edit reaches its projection window · pin 9c8b23ba…
- inference-owned executable boundaries + the pattern-constraint law · pin 7bd9e3e7…
- carry `<|` branch application identity from inference through emit · pin 289b2886…
- the fanout application-edge wheel · pin f711573d…
- Cluster A — the smap indexed-map consolidation · pin 2dc47d2f…
- value-proof (R2) — refinements discharge against the live value + the O(1) interner · pin 06847e5c…
- heal the crown dedup/membership inconsistency · pin 358faa34…
- the crown positive-gate root (by-name membership, 598→146) · pin 8a5d8ff7…
- unify the analysis verbs · pin 45ee71af…
- connect the analysis verbs · pin c4bdba19…
- the argv/CLI-dispatch wheel · pin bea3692b…
- the O(1) reachability index (perf iteration 6) · pin 349a3302…
- M4 the Abandon discipline · pin 67e44c9c…
- M3 the cut (the classifier flip) · pin ac204467…
- M1.1 the world-tag homomorphism · pin 70d184a0…
- the image-layout pin · pin d24af35a…
- FIRST LIGHT · pin b3314001… (commit 87c0152, tag `first-light`)

The hand-WAT seed (`bootstrap/`) was DELETED (7401c4b "Fly, my pretty <3",
2026-07-10, the day after first light); `--from-seed` is gone with it. The
cold-bootstrap recipe — and the diverse-second-seed ingredient for
trusting-trust (PLAN band J, `Hβ.closure.diverse-double-compilation`) — lives
at tag `first-light`, git archaeology out of the run path (PLAN §6).
