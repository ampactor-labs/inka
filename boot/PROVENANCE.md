# boot/mentl.wasm — the pinned fixpoint compiler

This binary IS Mentl: the wheel compiled by itself to the byte-identical
fixed point (first light, 2026-07-10, commit 87c0152, tag `first-light`).
It replaces the hand-WAT seed as the bootstrap: the default build loop is

    boot/mentl.wasm  compiles  src/**+lib/**  →  m2   (wheel-emitted)
    m2               compiles  src/**+lib/**  →  m3
    assert m2 == m3                                    (the fixpoint ratchet)

Because boot is itself wheel-emitted, m2 == m3 IS the fixed point — the
seed-era rule "NOT m2 == m3" (PLAN §6) compared a SEED-emitted m2 against a
wheel-emitted m3 and retired with the seed.

Provenance, self-confirmed at pin time:

- source: THE FIRST FAMILY MIGRATES (2026-07-30; the §7 ledger entry of
  the same name — iteration-is-topology's first executed migration).
  Pipeline's cold render/set walks: six index-threaded loop fns
  (owner_names_of, absent_from_loop, name_in_list_loop,
  severance_unlocks_loop, caps_not_in, cap_in, ref_span_lines's spine)
  DELETE whole into map/filter/any/fold — the tier's count on the file
  18 → 11, the wheel 55 lines smaller. The migration's own first red
  taught the recipe's load-bearing pin: under the Stage Law the datum
  infers LAST, so a datum-last lambda's bare `==` word-floors to
  pointer-eq while its element is still free — EffName membership went
  always-false and Alloc was offered severable on allocating rows (the
  severance-honest frontier leg caught it in one run); one typed
  operand (target: EffName) restores structural dispatch, and the
  recipe now states it: a datum-last lambda doing ==/arith on the
  element states the element's type. The law named in all three docs
  (CLAUDE Anchor 6, SYNTAX's token-note, PLAN's ledger + peer).
  Frontier 329/0; micros 121/0; proof-exactness 9/9; crown 5/5;
  census 0.
- boot/mentl.wasm  sha256 feccc8a9ad277f6f15cbeacbcc1859b77d6b1d9cae93bbb03373d7c757b955fa
  (CLEAN m2 == m3; re-pinned from m2 per march.sh)
- generated by: CLEAN (prior pin follows)

---

- source: ITERATION IS TOPOLOGY (2026-07-30; the §7 ledger entry of the
  same name — Morgan's interrogation of recursion itself, made a
  measurement). The audit verb gains the iteration-shape tier
  (recursion_shape_of, oracle.mn): a self-call threading an incremented
  index over the fn's own parameter is the imperative loop in
  recursion's costume — name-keyed re-entry where SYNTAX's declared
  model (|> stages, <~ cycles, Iterate handlers, derived folds) serves;
  the read is structural over the one total child projection, and a
  derived-fold-shaped self-call (recursing on children, not counters)
  never fires. Its first wheel-wide census: 390 convictions — the
  migration queue, measured by the medium itself (the name-based grep
  had guessed 78). The tier's own first form convicted itself
  (args_thread_index was index-threaded recursion) and was rewritten in
  the vocabulary it preaches. Two-face frontier leg registered
  (fire on the costume, silence on the vocabulary); frontier 329/0;
  micros 121/0; proof-exactness 9/9; crown 5/5; census 0.
- boot/mentl.wasm  sha256 d949fa7aff8c257951a341299e8dd8bab6011bc9e9a703b0e0adc1ecc34993cb
  (CLEAN m2 == m3; re-pinned from m2 per march.sh)
- generated by: CLEAN (prior pin follows)

---

- source: THE CYCLE DISCIPLINE (2026-07-30; the §7 ledger entry of the
  same name — the schemes-are-edges arc's first rung). A Tarjan cycle
  judges under monomorphic recursion by default: at group entry an
  unsig'd member's env view re-registers with its TYPE cells shared
  (Forall over only the row-sort quantifiers of the same pre-registered
  skeleton), so an intra-cycle forward use TEACHES the callee's actual
  param/ret cells instead of discarding its demand into a fresh copy —
  union-find propagation as the cycle's constraint channel. Row handles
  stay quantified (the full-mono probe measured five declared-row
  mismatches — application-site row unifies contaminating callee rows;
  the directional-edge law's class). A fully-annotated member keeps its
  quantified pre-registration: the signature price for polymorphic
  recursion. A group-exit sweep re-generalizes every member over the
  group's resolved cells. Census 0 at every generation; the bound-hit
  mover (parse_effect_list_from) is measured ROUNDS-resident —
  untouchable from the trial side; the rounds' deletion is the cure and
  this discipline is its prerequisite. Micros 121/0; frontier 328/0;
  proof-exactness 9/9; crown 5/5.
- boot/mentl.wasm  sha256 4e8eb504e3ce3a56c2ebc1dfd8ceb56b121db103c2def20a5cd5d25b9476cde0
  (TRANSITION m3 == m4; re-pinned from m3 per march.sh)
- generated by: TRANSITION (prior pin follows)

---

- source: THE WIDTH IS AN INPUT (2026-07-30; the §7 ledger entry of
  the same name). SYNTAX's representation pin is real: `type Coeff =
  Float repr f64` parses (parse_repr_pin after any type-decl base) and
  the bare-width atom (`k: f64`, `f32`, `i64`, `v128`) mints the same
  TReprPin(Repr, Ty) — the fifteenth Ty constructor, the gradient's
  authored INPUT. Identity is the base's everywhere (unify peels the
  pin exactly as TAlias's RN.2 shape — a var binds the PINNED type so
  repr_of chases to the width; fold_strip strips it so every dispatch
  sees the base); repr_of's own arm is the ONE width reader. The
  exhaustiveness census named all sixteen walk sites in one march
  (occurs_in, fp_ty, same_ground, the chase/subst families, free_in_ty,
  ty_handle_of, extract_row, enumerate_typed, fold_sig, show_type,
  query_flow_label, render_type_tokens, ty_lo) — each gained its
  transparent arm; the formatter renders bare atoms back bare, alias
  pins with their suffix. Fixture runs 42 (RED on the prior boot:
  `repr`/`f64` refused as unknown names); frontier 328/0.
- boot/mentl.wasm  sha256 f09e54a343af2e96751aacd6296d7af472ee4c4dc3f0e21c8b4ff63046fdb72f
  (CLEAN m2 == m3; re-pinned from m2 per march.sh)
- generated by: CLEAN (prior pin follows)

---

- source: THE WHOLE VALUE AND ITS PIECES (2026-07-30; the §7 ledger
  entry of the same name). SYNTAX's as-pattern (`name @ pat`) is real
  end to end: the parse peeks TAt after a lowercase pattern ident;
  infer binds the name's cell to the scrutinee's own (one unify edge —
  the binder IS the matched value's type) then walks the inner
  against the same handle; LPAs rides the lowered tree with the inner
  keeping the scrutinee's type; emit's predicate is the inner's alone
  (the binder never affects matching), the bind stores the whole
  value at the pattern's path then the inner's binders, the local at
  the heap-word floor the pattern binders share. Fixture runs 47
  (`e @ Click(x)` feeding both altitudes to one arm); frontier 327/0.
- boot/mentl.wasm  sha256 010fc3179edf0422daf5e371d8ffe24abc627b473fd468e6dbceb6a8fa6ab762
  (CLEAN m2 == m3; re-pinned from m2 per march.sh)
- generated by: CLEAN (prior pin follows)

---

- source: THE REST COMES TO RECORDS (2026-07-30; the §7 ledger entry
  of the same name). SYNTAX's documented `{name, ...rest}` record
  pattern is real end to end: the parse mirrors the list rest's
  at_ellipsis arm; infer binds the rest to a record carrying the SAME
  residual row the open pattern constrains (resolution flows by
  construction); lower_pat_typed resolves the receiver's full sorted
  field set (structural TRecord, or nominal through
  nominal_record_fields — the judge convicted the first TName
  three-field guess, arity 2 + the env channel is the truth) into
  residual (field, src_index) specs — and fixes the NAMED fields'
  offsets to their true full-set indices as the rider (the
  pattern-subset index bake was the wrong-slot class at the pattern
  layer); emit builds the residual record (alloc + slot copies, the
  declared rest local doubling as the accumulator) and the rest's own
  field access reads the residual layout. An unresolved receiver
  floors loudly (resolved-or-loud). Fixture runs 30; frontier 326/0.
- boot/mentl.wasm  sha256 7932c192625cb6391c6a4fbb4b61423e997156f1691d4e3ca3ce7ca7ee48b6fe
  (CLEAN m2 == m3; re-pinned from m2 per march.sh)
- generated by: CLEAN (prior pin follows)

---

- source: A CAPABILITY UNLOCKS ONCE (2026-07-30; the §7 ledger's
  localization entry carries the arc's context). The audit's
  severance line concatenated per-effect capability lists undeduped —
  IO and Network both unlock CSandbox, so every fn severing both
  rendered "Sandbox (proven no network access)" twice (measured on
  the day's own probe fixture). severance_unlocks dedups by
  structural == over the nullary capability ADT; the line reads each
  unlock once.
- boot/mentl.wasm  sha256 ec1d4664449e6a6c04e668eb7a334c1973f80ee2ec13dd927b379a2248428be7
  (CLEAN m2 == m3; re-pinned from m2 per march.sh)
- generated by: CLEAN (prior pin follows)

---

- source: THE DIAGNOSTIC SPEAKS THE USER'S LINE (2026-07-30; the §7
  ledger entry of the same name). Hβ.diag.file-local-span-render
  RESOLVES at the register: scope_localize rebuilds diag_line's one
  `at <span>` tail in the USER'S coordinates whenever the span falls
  inside ScopeAt's own range — a pure string/ctor read in the root
  arm (the span needs no graph world; the register's range IS the
  subtraction), never a per-line seam walk (the crc-class cost the
  debt renderer measured stays out by design). Measured with the
  quiet-discovery landing beneath it: a line-2 type error renders
  ONCE at 2:19 where the DAG path had printed it twice and at weave
  5730. ScopeAll (the census channels) stays weave-native by
  construction.
- boot/mentl.wasm  sha256 4aa1f090e50b73f896e210a9db137b6165de16026caf658df2d7e961031b538a
  (CLEAN m2 == m3; re-pinned from m2 per march.sh)
- generated by: CLEAN (prior pin follows)

---

- source: THE DISCOVERY PARSE GOES QUIET (2026-07-30; the §7 ledger
  entry of the same name). driver_extract_imports' throwaway bracket
  gains ~> diag_quiet — the trial's own absorption policy at the
  discovery walk: a structure read is not the reporting pass, its
  file-local spans are unplaceable, and the weave parse re-reports
  every diagnostic placeably — so the long-standing doubling (every
  parse diagnostic printed twice, file-local + weave) dies at its
  source; the fn's declared row drops the Diagnostic it no longer
  carries. Measured: the unterminated-string narration prints ONCE.
  The surviving coordinate is the weave span, which raises the
  standing Hβ.diag.file-local-span-render residue's priority — the
  seam render exists (span_render_local) but a per-line seam walk at
  census scale is the crc-class cost, so the render-side fix stays
  the named design (the register's own range as the subtraction).
- boot/mentl.wasm  sha256 0b3d9348d5710c344b222a63f644bd034194f5f4195ff3b62197781a8fb85c8d
  (CLEAN m2 == m3; re-pinned from m2 per march.sh)
- generated by: CLEAN (prior pin follows)

---

- source: THE STRING NAMES ITS OWN LINE (2026-07-30; the §7 ledger
  entry of the same name). Hβ.lexer.string-newline-refusal resolves:
  a raw newline in a SINGLE-line string literal reports
  P_UnclosedConstruct at the string's own line with the multi-line
  `"""` teaching, terminates the literal, and leaves the newline
  unconsumed so the rest of the file lexes true — where the swallow
  had run to the next quote or EOF and surfaced as a brace complaint
  lines away (measured: 6:1 for a line-2 error). The triple scanner
  is its own chunk walk, untouched; the wheel's own compile carries
  ZERO of the narration (the fmt string-atomicity law had kept
  content newlines out already), so the march is CLEAN at census 0.
  mn-string-newline banks the refuse contract in the battery.
- boot/mentl.wasm  sha256 ec3bc868c9e8f9b77a102fb1cbc0106151d0172699061ad0e35e08f30ba464d2
  (CLEAN m2 == m3; re-pinned from m2 per march.sh)
- generated by: CLEAN (prior pin follows)

---

- source: THE QUIET FN FITS UNDER THE CAP (2026-07-30; the §7 ledger
  entry of the same name). The call edge's fn-arg row meet goes
  DIRECTIONAL where the param's declared row is a concrete cap
  (closed or the !-stance EtAll): non-row components meet
  symmetrically, the TOP rows meet by row_subsumes(arg, param), and
  the masked position's wholesale slot takes a fresh var so the
  symmetric equality never re-runs — a Pure fn now admits where a
  `with Tick` fn is expected (RED through every prior pin, runs 7),
  while a VAR-tailed param row keeps the symmetric FLOW whole (the
  first form masked those too and the wheel's own census convicted
  297 sites in one march — the effect-polymorphic channel is a flow,
  never a cap). The noisy-into-narrow refusals stand (hof-row-gate +
  the Tick-vs-Pure face); frontier 324/0.
- boot/mentl.wasm  sha256 cd43c23c75b92bfa82454eb4f0062107632bfefd616bab29544afd4c1376e264
  (TRANSITION m3 == m4; re-pinned from m3 per march.sh)
- generated by: TRANSITION (prior pin follows)

---

- source: THE REFERENCE ENTERS ONCE AND SPEAKS LOCALLY (2026-07-30;
  the §7 ledger's seam-render entry carries the arc's context). The
  refs facet's collector dedups by SPAN — a reference's identity is
  its source location, and the converged rounds' re-minted
  generations had rendered one reference four times (measured) — and
  the seam projection moves to its one home (types.mn's
  module_seams / span_render_local, beside show_span), read by both
  the verify debt lines and the refs render: `refs of bump` answers
  `1 reference(s) at tests/frontier/mn-verify-interval:33` where
  four weave-coordinate copies stood.
- boot/mentl.wasm  sha256 a62b1299dbaff69c07f21a38b83b102b5fa5c09a1632da4f3338e5c75dba0b6c
  (CLEAN m2 == m3; re-pinned from m2 per march.sh)
- generated by: CLEAN (prior pin follows)

---

- source: THE DEBT SPEAKS THE DEVELOPER'S COORDINATES (2026-07-30;
  the §7 ledger's let-annotation entry carries the arc's context).
  The pending ledger's spans render as `path:local_line` resolved
  through the graph's own NModule seams — the driver minted one per
  woven file at discovery, and the renderer reads them live (the
  same seam truth the comment re-homing pass reads), so the weave
  coordinate that was hand-mapped FOUR times in one session now
  projects itself. A seam-free blob weave (stdin, the march) renders
  the raw span unchanged — the census's own coordinates, by
  construction.
- boot/mentl.wasm  sha256 852c34dc6b342248b0376155a2d12e40b7888095839692ad11e09d5109284a0e
  (CLEAN m2 == m3; re-pinned from m2 per march.sh)
- generated by: CLEAN (prior pin follows)

---

- source: THE DEBT NAMES ITS PRODUCER (2026-07-30; the §7 ledger's
  let-annotation entry carries the arc's context). resolve_cursor_target
  gains its authored `current_handle: Handle` and `-> Handle`: the
  Caret ctor's consumer-edge obligation discharges through the
  adopted return alias, and the producer's own return pends at its
  decl — the 1:1 placement trade the annotation queue predicted,
  kept because the ledger now attributes the missing proof to the fn
  that mints handles, never the handler that consumes them. Wheel
  debt holds 11 with the placement trued.
- boot/mentl.wasm  sha256 44877c73d2d0f1b21a9dfafe671cf5284725c21209b0fdc712a34b5f686efae3
  (CLEAN m2 == m3; re-pinned from m2 per march.sh)
- generated by: CLEAN (prior pin follows)

---

- source: THE LET ANNOTATION BECOMES A CONSTRAINT (2026-07-30; the §7
  ledger entry of the same name). apply_let_annotation only VERIFIED
  the refinement — the value cell never met the annotation, so
  `let x: Int = "hi"` compiled CLEAN (measured) and an annotated
  alias could never reach a consumer edge. The let now runs
  constraint-first (the flow licence's uncontaminated read), then the
  base unify (the mismatch refuses — the mn-let-ann-pins refuse
  contract), then the representative rebind (the decl pin's law at
  the let: a concrete-bound value cell cannot adopt through the
  unify, so the refined form is re-bound as the class
  representative). The chain's first wheel yield: lex's
  `let n: ValidOffset = byte_len(source)` discharges its own
  obligation through the interval fragment's len leg and the arg
  edge echo-stops through the adopted alias — wheel debt 12 → 11.
- boot/mentl.wasm  sha256 301945785419a94f586f80633a0d762437fc510b389e5227642f54f7a360f9ed
  (CLEAN m2 == m3; re-pinned from m2 per march.sh)
- generated by: CLEAN (prior pin follows)

---

- source: THE AUTHORED RETURN RIDES AS A VALUE (2026-07-30; the §7
  ledger entry of the same name). An authored `-> RetTy` now rides the
  pre-registered TFun as the resolved Ty VALUE instead of the bound
  cell: a value inside the record cannot be class-contaminated, so the
  interval fragment's callee read (callee_ret_lo) sees the callee's
  own annotation verbatim at every call site — the wrap/base fixture
  faces prove the cross-fn discharge, while an unannotated tail callee
  still refuses (ty_lo has no TVar arm by design: a chased ret var
  reaches the obligation's own class through tail-call merging — the
  measured g-launder shape). The self-call IH (seek's honest pend)
  stays the named remainder with its exact discriminator
  (callee-class == DECL-class, needing graph_root_of) banked in the
  residue index. The cell still binds for forward-ref grounding and
  quantification; infer_fn's own unify against the pre-scheme keeps
  the semantics.
- boot/mentl.wasm  sha256 5e34f7103bdd45a577646893ea2b52fd09edef9f254e6c7c254e97862e8f4b45
  (TRANSITION m3 == m4; re-pinned from m3 per march.sh)
- generated by: TRANSITION (prior pin follows)

---

- source: THE INTERVAL FRAGMENT AND THE FLOW LICENCE (2026-07-30; the
  §7 ledger entry of the same name). Verify grows its first inference
  leg past the ground fold: a two-face lower-bound read over node
  structure — literals, if/match/block joins, the len/byte_len floor,
  Add propagation, and type reads only across class boundaries (a
  BinOp's operands, a callee's published return) because the
  transparent positions share the annotation's own union-find class
  and a type read there proves the annotation from itself. The dig's
  soundness kill: the typed-identity echo-stop laundered computation
  results — `v - 1` through a `0 <= self` return ran to -1 at runtime
  with zero obligations — so value_flows_class licences the echo to
  value-flow nodes only; computations raise, the interval proves the
  provable, the rest pends visibly. Rides with the warm-persist
  capacity guard: image_pack's transient doubling crosses the
  allocator's signed-2GB boundary (measured: 807MB packed, 1017MB
  trapped), and a best-effort cache must never kill the compile — the
  driver skips the persist loudly past capacity.
- boot/mentl.wasm  sha256 a71ebbcb7bf981a7a935753be5d2643c66333280fe547b19cc1d8e10a6e3cd0f
  (CLEAN m2 == m3; re-pinned from m2 per march.sh)
- generated by: CLEAN (prior pin follows)

---

- source: THE COMMENT COMES HOME AT THE SEAM (2026-07-30; the §7
  ledger entry of the same name). The parser attaches prose over ONE
  seamless weave — correct single-file semantics, blind to module
  seams by construction — so a lib file's tail comment attached
  forward to the NEXT module's first decl and the entry's own fns
  rendered kernel prose as their Lede. The seam truth lives where the
  seams do: the driver's NModule fold now collects (line_start,
  nlines, handle) triples and rehome_seam_comments walks the parse
  span log, re-homing any comment whose own span's module differs
  from its attached node's module onto its own module's NModule cell.
  The dig surfaced the column's missing read AND the attach's merge
  trap: graph_comment_span_at projects the span the attach always
  stored (the read had discarded it — Carried-Truth at the op), and
  graph_clear_comment is the take-side (attach MERGES behind existing
  text per the blank-separated-blocks rule, so an empty attach
  prepends a paragraph instead of clearing). Resolves
  Hβ.parser.comment-attach-module-boundary at the driver layer.
- boot/mentl.wasm  sha256 997684f136f0962ca8c012a38e82f8c78df286209bc2be31088608bd4476446c
  (CLEAN m2 == m3; re-pinned from m2 per march.sh)
- generated by: CLEAN (prior pin follows)

---

- source: BOUND BEATS GHOST AT THE ADDRESS (2026-07-30; the §7 ledger
  entry of the same name). The head-anchored decl-span defect's
  address face closes: at file:L:C the tightest-containing rule let a
  never-judged parse cell over a decl's NAME beat the decl itself,
  and the projection rendered the ghost's free var (`width( :
  t76439@e54117 / Why: placeholder` at 1:4 — RED-banked live through
  the prior pin). address_better_a gains one ordering axiom above the
  area rule, column mode only: a BOUND cell beats a ghost
  (address_bound — the graph's own "was anything proven here" read);
  a `??` node is typed, hence bound, so the hole's reach is
  untouched. The healed face projects the decl's full eight aspects
  with the Why at its own line. Frontier gains the decl-name address
  leg (322 legs); the perimeter hook's lead-strip learns cd-prefixed
  compounds.
- boot/mentl.wasm  sha256 4f477b1f119b679e1587fc453cf54cfda4deb88bc8a06520baef3cce438bd02e
  (CLEAN m2 == m3; re-pinned from m2 per march.sh)
- generated by: CLEAN (prior pin follows)

---

- source: THE FLIP IS THE INSTRUMENT'S — three probes convict the
  print-carry, and the judgment stands converged (2026-07-30; the §7
  ledger entry of the same name). The bound-hit channel gains two
  graduated probes: probe_entry_hist (the mover's env entry index +
  row-tail letter per generation — its first read exposed
  env_snapshot's latest-per-name dedup, n=1 by construction) and the
  same-round re-read of row_print at the flip. Their verdict, run
  against the deterministic bound-forcer: the mover's entry is the
  SAME index with the SAME closed scheme in every round, the fresh
  read is closed every time, and every flip block reads A=open /
  B=closed — impossible under a faithful carry. The "movement" that
  keeps the cone hot and the bound firing is an artifact in the
  convergence loop's own prev/cur print bookkeeping resurrecting the
  trial's open render; the JUDGMENT has been converged since the
  cycle signatures landed (which is why emission stayed stable
  through every probe). The carry bug is the next opening single.
- boot/mentl.wasm  sha256 dd8a70f11ee01f827851d7a6e7d4d268fe06c7d0b2efd03adb83870a2e6dede7
  (CLEAN m2 == m3; re-pinned from m2 per march.sh)
- generated by: CLEAN (prior pin follows)

---

- source: THE DECLARED ROW PINS UNDER LAG (2026-07-30; the §7 ledger
  entry of the same name). The declared-effs gate's unbound arm was a
  silent hole: when mutual recursion left the body row unresolved at
  decl exit, `_ => ()` skipped enforcement entirely and generalize
  published an OPEN-tailed final — a declared row unenforced whenever
  round parity out-raced resolution (the cached flip render convicted
  it: parse_effect_list_from's published tail flipped open ↔ closed
  WITH its authored row). The arm now BINDS the declaration — the
  contract stands regardless of parity; teaching beyond it meets the
  bound row and mismatches loudly on the next unify. The marginal
  run-variant flip survives with all decl-exit channels closed (the
  A-phase's carrier is a skipped-round env read surfacing the trial's
  prereg entry — the banked next probe prints the entry's position
  and generation at the flip); emission stable, the fixpoint holds.
- boot/mentl.wasm  sha256 870d9fc920094f87e93eab173ffdbb9079020d9a705031fd08991e80f524fc03
  (TRANSITION m3 == m4; re-pinned from m3 per march.sh)
- generated by: TRANSITION; the m2/m3 diff was the pinning arm
  crossing one generation (prior pin follows)

---

- source: THE DEMAND LAW'S DUAL FACE — never demand a proof the body
  doesn't use (2026-07-30; the §7 ledger entry of the same name).
  span_area's ValidSpan param was ceremony: the area fold is total
  arithmetic over the four fields and the proof was never read, yet
  the demand converted every bare-span caller into debt (seven
  obligations at the address tie-break + find_tightest). Widened to
  bare Span: whole-weave debt 21 → 14, the day's Arc 3 arc
  138 → 14 (90%) — and the remaining fourteen are ALL genuine
  (span_join / span_zero / the module-mint construction pends, the
  0 <= self producers, TagId ×2, Sample ×1; zero ceremony left in
  the span class).
- boot/mentl.wasm  sha256 fb265daaef86d23f00ad3db1f57e422d1d6de72909d84cb545212057e63da820
  (CLEAN m2 == m3; re-pinned from m2 per march.sh)
- generated by: CLEAN (prior pin follows)

---

- source: THE LEDGER SPEAKS WHOLE AND THE CTOR FIELD CARRIES THE PROOF
  (2026-07-30; the §7 ledger entry of the same name). report_verify_debt
  graduates from a count to the complete projection — one line per
  obligation (span, classify_predicate class, predicate) — and its
  first firing classified the remaining 58 in one read: 47 span_valid
  boundaries, 31 of them infer-side consumers of node-destructured
  spans. The producer at THAT scale is the constructor field itself:
  Node = N(NodeBody, ValidSpan, Int) — every destructured node span
  reads ValidSpan by type, the obligation moves to N-construction,
  and the parser's mints discharge by identity through the already-
  annotated span_at/span_join. Whole-weave debt 58 → 21 this pin;
  138 → 21 across the day's Arc 3 arc — 85% of the wheel's proof
  debt discharged by construction-site typing, zero engine changes.
  The residue is exact: 10 span_valid at unfed construction sites
  (the lexer's counter-built mints — the Pure predicate-fn unfold's
  fixtures), 8 bare 0 <= self, 2 TagId, 1 Sample.
- boot/mentl.wasm  sha256 862f66fdb822f2eb5665ae74cf8b762bf44c560edae8c61380d51876c71f891b
  (CLEAN m2 == m3; re-pinned from m2 per march.sh)
- generated by: CLEAN (prior pin follows)

---

- source: THE PRODUCERS CARRY THE PROOF AND THE VERBS READ ONE WEAVE —
  Arc 3's opening landing (2026-07-30; the §7 ledger entry of the same
  name). Three span producers gain `-> ValidSpan` returns (span_join,
  span_zero, span_at — the construction-site law types.mn's own comment
  always promised): the whole-weave verify debt falls 138 → 58, ~80
  consumer boundaries collapsing by typed identity into two honest
  producer obligations. Landing it armed a gate that could never fail:
  the frontier's session leg went red as a TRUE positive — the cold
  audit's "pending: 2" was JUNK DEBT from the analysis verbs' own
  SECOND discovery path (driver_check_entry: per-module solo checks
  judged without their layer's vocabulary, the per-module-env-overlay
  residue, flooding E_MissingVariable on True/len/list_index from any
  out-of-tree directory with stderr discarded) while the RESIDENT
  session read the weave and told the truth. audit/teach/query now
  derive through THE ONE DISCOVERY HOME (driver_entry_with_ranges +
  driver_module_ast — the same weave the check verb, the address, and
  the session read); the leg is green from both sides.
- boot/mentl.wasm  sha256 d14fa41ebbf30d6e6bd7a59e9179982d184628308f8eaf84860dab2463db989a
  (CLEAN m2 == m3; re-pinned from m2 per march.sh)
- generated by: CLEAN (prior pin follows)

---

- source: THE PASS INVARIANTS STOP RE-DERIVING — classify-once and
  cone-proportional prints (2026-07-30; the §7 ledger entry of the
  same name). classify_fixpoint runs ONCE in the trial (its grades
  read arm structure, which no pass changes; the value is name-keyed
  and parse-independent) and the summaries VALUE threads to every
  round and the final — twelve whole-tree re-derivations of an
  invariant deleted. round_prints goes cone-proportional
  (round_prints_masked): a masked-out stmt's final is a VALUE (the
  value-boundary law — published schemes leak no live vars), so its
  fingerprint is invariant and carrying it replaces re-rendering it;
  the m3 == m4 oracle backstops the law. Both emission-neutral
  (CLEAN); the field read stays in its ~60-69s band because the
  alternating SCC families keep the cone large and the per-round
  re-parse dominates — the collapse waits on the generality join
  (the residue index carries its lgg re-specification).
- boot/mentl.wasm  sha256 43f33c0a7764826c840e7cd0b6306cbdf5eee627e0bdeeeeb07b77e16ad99c60
  (CLEAN m2 == m3; re-pinned from m2 per march.sh)
- generated by: CLEAN (prior pin follows)

---

- source: BINDING GROUPS FOR ORDER, MYCROFT FOR CYCLES — the Tarjan
  substrate lands and the classic mono-group form is REFUTED by the
  wheel itself (2026-07-30; the §7 ledger entry of the same name).
  scc_groups (Tarjan over the frees DAG, 7-tuple state, groups popped
  callee-first by construction) orders the trial's walk; each cycle's
  members judge adjacent in source order. The classic binding-group
  MONO semantics (members share unquantified live cells, generalize at
  group exit — GHC's form) was built WHOLE and convicted by the
  judgment: 29 E_TypeMismatch (Int vs Float, List(Byte) vs Int) where
  a cycle member is used at two instantiations by co-members — the
  rounds are a MYCROFT iteration (polymorphic recursion by fixpoint)
  and the wheel genuinely uses that power inside its cycles, so the
  weaker classic form cannot serve; the counted kill redirects the
  rung to per-SCC LOCAL Mycroft iteration (Salsa's own cycle-recovery
  shape), fresh nodes per re-judgment via branch-cursor instantiation.
  En route the medium taught its own surface twice: nominal record
  spread (P{...a}) is unparsed (anonymous {...a} is the documented
  form), and spread-update inference fragments a threaded record
  (the tuple state is the proven carrier).
- boot/mentl.wasm  sha256 5db9b4c3efc8216e26572b76b08937cdd9a6d28559ebb14991cae8bb7d22a1c3
  (TRANSITION m3 == m4; re-pinned from m3 per march.sh)
- generated by: TRANSITION; the m2/m3 diff was the group-ordered trial
  crossing one generation (prior pin follows)

---

- source: THE JUDGMENT CONVERGES CALLEE-FIRST — the rounds' resolution
  front dies at three roots (2026-07-30; the §7 ledger entry of the
  same name). The flip instrument (movers_diff + the Why-engine
  tail probe, graduated into the bound-hit report) measured the
  "oscillation" as a monotone open→closed FRONT climbing one
  caller-layer per round; the roots: ty_fingerprint rendered a row's
  name SET in storage order (the render now imposes ascending-handle
  order, the equality witness's own contract — the row layer stays
  deliberately orderless per its own law); stmt_layers_ast counted
  only BACKWARD edges (backward_depth deleted into a memoized descent
  over the whole frees DAG, cycles flattened by the on-stack guard);
  and the trial judged in source order only to be re-derived by round
  1 (the trial now walks its own layers — frees and decl names are
  parse truth, computed once, serving trial + cone + final). Movers
  at the bound: 16 → 1 (the unify/parser SCC chain — an SCC's closure
  still crawls its internal diameter one round per link; the
  SCC-local fixpoint is the named next rung, its true form the
  branch-cursor fan re-judging a cycle to a local fixpoint). Two
  honest widens (row_to_with_clause / neg_names_to_str + GraphRead —
  the converged judge resolving deeper than the bound-cut attractor
  ever did). TRANSITION m3 == m4; census 0 at every generation.
- boot/mentl.wasm  sha256 78b1736b6210d3fad5b918081fae93ca213ff58cdd325354214eb7340337bfac
  (TRANSITION m3 == m4; re-pinned from m3 per march.sh)
- generated by: TRANSITION; the m2/m3 diff was the converged judgment
  crossing one generation (prior pin follows)

---

- source: THE FIXTURE STATES ITS OWN CONTRACT — `// expect: refuse
  E_Class` joins `// expect: N`, and the battery judges the JUDGMENT
  (2026-07-29; the §7 ledger entry of the same name). The Expect ADT
  (ExpectRun | ExpectRefuse) reads the first line; battery_compile
  carries the collector (mcp_diag_collector — every line still reaches
  stderr) and returns the banked diagnostics beside the gate verdict;
  a refuse fixture passes iff the named class reports as an error —
  armed classes also withhold the artifact, unarmed ones surface and
  emit, exactly the medium's own sound-incomplete law, never stricter.
  This is the frontier bash's dissolution channel opened: a refusal
  leg becomes a fixture the medium reads itself (first two live:
  mn-refuse-forward-order / mn-refuse-hole, REFUSE-passing through
  `mentl test tests/micros` at 117 run-contracts + 2 refuse-contracts).
  With it: the mentl-first perimeter extends to the INVOCATION family
  (hand-assembled `source wt-env && wt_run …` refused toward the
  installed verbs — ⟳(2)'s 2026-07-26 catch, re-caught and made
  unsayable), and PLAN.md is declared THE SINGLE SOURCE OF TRUTH
  (Morgan 2026-07-29): the finish-line plan files verified integrated
  (§11 carries their arcs, definition-of-done, and risk tripwires) and
  retired to archaeology.
- boot/mentl.wasm  sha256 2fb58e9a8c2cc1f430433d40ebeb9378683dcff99c9dd5fdfea044118d2b40d6
  (CLEAN m2 == m3)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

- source: THE SESSION IS THE TRANSPORT — `mentl session` holds the
  resident graph behind the shim's tcplisten seam and answers the read
  verbs over a one-line wire speaking the CLI's OWN grammar (2026-07-29;
  the §7 ledger entry of the same name). The shim tab-joins argv; the
  session parses it with parse_cli_args — one grammar, two transports —
  and serves at/query/audit/teach through the SAME projections the cold
  verbs run (resident audit measured BYTE-EQUAL to cold); anything it
  does not serve answers the MISS sentinel and the shim falls back to
  the cold exec. The shim itself was reduced to a pure transport: WHICH
  verbs go resident is session_answer's match in the medium, the one
  policy home (its first form duplicated the routing in a bash case —
  the two-homes drift, caught by Morgan's own unease at the shim's
  growth). With it: the address path's span log flattens ONCE before
  the resolution walk (the iterate-flattens-once law, fourth kill — the
  resident at cost a measured 131s PER CALL walking the converged
  session's multi-generation snoc log; 0s after), and the repo's own
  hook set gains the mentl-first gate (.claude/hooks/
  pre-bash-mentl-first.sh — a grep against .mn source is REFUSED with
  the verb that answers better; prose could not enforce ⟳(2), so the
  wrong move is now unsayable, the larval mentl-audit family).
- boot/mentl.wasm  sha256 e09626cb2f1fb4e00fca1e2e02cad0407993be6ab98b53555602ec6000f75ab8
  (CLEAN m2 == m3)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

- source: THE ONE JUDGE — every judgment site runs the converged walk;
  the single-pass infer_program is DELETED (2026-07-29; the §7 ledger
  entry of the same name). The daily verbs (check/audit/teach/at/field/
  session/repl/battery/warm-cone) judged through the order-dependent
  single pass, so an intra-module forward reference read a loose
  pre-registration and bound silently — measured: a [tuple]-returning
  fn declared after its caller flowed into a [String] parameter with
  ZERO diagnostics (the audit_walk incident's minimal form). All nine
  call sites now run infer_program_converged; the fixture
  mn-check-forward-order REFUSES through this pin's check verb and was
  silent through the prior one. With it: the owner sweep's landable
  half (the free-vars family → parser.mn as parse truth; the
  convergence cluster + frontend + diag_branch → infer.mn beside the
  walks it drives; the spec trio + lookup_ty_graph + effect LookupTy →
  graph.mn), the effects prepass phase (pre_register_effect — a
  handler sig never again reads an incomplete effect env, any source
  order), the trial returning its stmts (the arm-position re-parse
  that double-reported every check's parse narration is deleted —
  Carried-Truth), THE OP-VOCABULARY HOME LAW (the intermediate pin
  9d346047 moved effect EnvRead/EnvWrite/BranchEnv/Intern beside their
  handlers and the frontier's warm legs refused with E_MissingVariable
  on the DAG — an effect declares in the LOWEST module every performer
  imports, types.mn for these; effect-beside-handler holds only where
  the handler's module is that floor, as graph.mn is for LookupTy),
  THE STRUCTURED DIAG BANK (diag_branch banks (diag, line); the
  join RE-PERFORMS diag_report per fact into the root chain, so every
  Diagnostic forwarder — tighten_collector, the mcp bank — sees a
  branch report as a live one; diag_absorb and both its arms DELETE —
  the old fold reached only the one handler carrying its arm, so on
  the converged daily path every branch-fired T_OverDeclared bypassed
  the tighten bank, measured as `mentl tighten` authoring nothing),
  and THE SNAPSHOT PROJECTS RESOLUTION (env_snapshot answers each
  name's LATEST live entry — the converged passes append one entry per
  generation per name, and the raw-buffer snapshot fed the ??-fan the
  same fn three times, refusing its own duplicate "tie"; the dedup
  probes the env's O(1) bucket index — an entry survives iff it IS its
  name's latest position — after the first seen-list form cost ~20
  minutes per compile leg at wheel scale and was superseded within the
  session, pins 4d50895d/afff6ade carrying the intermediates). Census 0.
- boot/mentl.wasm  sha256 223452c142b7398f9d7816a4a462d33ea4494445c912f1fc78b8d655e755a8d3
  (CLEAN m2 == m3; the session's intermediate pins — 9d346047 the
  one-judge TRANSITION, 4d50895d effects-home + structured bank,
  afff6ade the O(n²) dedup — each superseded when the frontier or the
  march clock convicted them)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

- source: THE AUTHORED ROW SPEAKS INSTANCES — eff_name_label closes its
  own banked follow-up (2026-07-29; the §7 ledger entry of the same
  name). The label that lands IN SOURCE (the tighten patch, the
  with-clause invitation) stripped every parameterized instance to its
  bare name — a proven Sample(44100) authored as Sample, a semantically
  WIDENED row that dropped the instance the proof carried. Literal-arg
  instances now render in place (SYNTAX's own canonical spelling); a
  type or node arg keeps the bare name — the sound wider-or-equal
  declaration, never an unparseable Cast(GNode) pasted into a
  signature (the render-vs-authorable seam named one pin earlier,
  closed at its authoring face). Frontier 318/0; census 0.
- boot/mentl.wasm  sha256 04ba90a3a5d5e55e22089348c1c8553a75d05e679f0413de9e746aa1ac64a9d3
  (CLEAN m2 == m3)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

- source: THE TUPLE INDEXES — SYNTAX §Indexing's documented form judges
  (2026-07-29; the §7 ledger entry of the same name). The index sugar
  forced EVERY receiver to List (the census's own conviction at
  audit_walk one pin earlier), while lower always carried the tuple
  dispatch (its LFieldLoad prefix-sum) — the judge's half now runs: a
  receiver whose SHALLOW root binding (graph_chase + fold_strip — the
  deep fold tripped its own depth belt on the wheel's unconverged
  chains, measured at the m3 leg) is a tuple, indexed by a literal,
  types as that position's element; an out-of-range literal reports
  EConstructorArity at the judgment (H6 at the product); a
  runtime-variable index or a free receiver takes the generic force
  and its honest mismatch. The open POSITIONAL demand (TRecordOpen's
  tuple sibling, for generic-body receivers) is the named remainder of
  Hβ.infer.index-expr-dispatch. En route: the judge's Cast(GNode) row
  render exceeds the parser's declared-row grammar (bare Cast is the
  authored spelling) — the render-vs-authorable seam, named. Fixture
  mn-tuple-index (both receiver shapes + a Float element) runs 42,
  RED on the prior boot. Frontier 318/0; census 0.
- boot/mentl.wasm  sha256 33a60e05f3c786054a342a589765dc850745a797da4783a19394ea6bd4619549
  (CLEAN m2 == m3)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

- source: THE AUDIT READS THE BANKS — pending proofs and tightenings
  speak per fn, and the session's bare path joins the bracket
  (2026-07-29; the §7 ledger entry of the same name). audit_project
  reads the two banks the judgment already holds — verify_debt()
  attributed by decl EXTENT (this decl's span start to the next's,
  parse_span_of the O(1) spans-column read) and tightenables() by the
  banked fn name — so the per-fn surface stays current with the
  frontier (two projections, one truth): `noisy : IO` now carries
  "tighten: with IO — body proves Pure (mentl tighten authors the
  patch)". The row leak the gate refused en route was REAL:
  mcp_run's no-project path ran mcp_loop BARE, and the tool arms'
  static rows (audit's new Verify + Tighten; query's ask) flowed to
  main — Query had survived only through its stateless default while
  verify_ledger, stateful, refused. One bracket now holds both paths
  (the conditional moved inside; the installs cost nothing on the
  empty path). En route the probe caught audit_walk piped into
  str_concat_all without render_audit — report tuples eaten as
  strings, a flat_fill trap the fixture surfaced before any bless.
  analyze_fns' chain gains tighten_collector (the forwarder, inside
  infer_context's thunk). Frontier 315/0; census 0.
- boot/mentl.wasm  sha256 c453d7a065d4e4cfb1d696d48309316f7c0e262427652e3730ab10fa5b292264
  (CLEAN m2 == m3)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

- source: THE OPEN HOLE ASKS — an undetermined `??` renders the
  collapsing question, never the raw var (2026-07-29; the §7 ledger
  entry of the same name). The field probe measured `?? : t10417@e19089`
  at `n + ??` — substrate vocabulary at the exact moment §1's law says
  the question beats the guess (the fan cannot enumerate an open type,
  so the missing constraint IS the answer). node_query_line gains the
  NHole arm: a still-free chased type renders "_ — still free: annotate
  the enclosing function's param or result and the fan arrives with the
  type"; a typed hole renders exactly as before. Frontier 315/0;
  census 0.
- boot/mentl.wasm  sha256 6933410121ba8c8c72482496f053caa876cb1037496c74b3d11a6dd737859b8b
  (CLEAN m2 == m3)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

- source: THE FIELD REACHES THE WHEEL — the spans spine column lands and
  the whole-problem-space read becomes daily at wheel scale (2026-07-29;
  the §7 ledger entry of the same name carries the record). `mentl
  src/main.mn:0` went OOM-trap -> timeout -> 8.5s/exit-0 across three
  measured shapes of ONE read: parse_span_of rode the generic find,
  whose iterate FLATTENS the whole (span, handle) log per call (the
  field calls it per position — alloc's wraparound trap, the backtrace's
  own frames); the allocation-free scan then measured O(n^2) over the
  snoc-spined log (a 600s timeout at the same scale); the resolution is
  the representation — a seventh spans column on the paged spine,
  dual-written at the ONE writer (graph_index_span keeps the ordered log
  for containment scans; graph_span_of is the by-handle O(1) read, the
  §5.O layer-2 form the frontier landing had named). WITH IT: canon.mn
  was imported by NOBODY — ty_string reachable only in the blob, so
  every DAG-path judgment (check/at/field) starved for it since the
  file's birth (E_MissingVariable at the daily surface; the blob-fed
  march structurally blind — the manifest law's exact class); parser.mn
  and infer.mn declare the import. The field over the wheel's own main
  now answers: 0 holes, 6 pending proofs, 2 tightenings, 115 gradient
  positions, each pending rendered at its span. Frontier 315/0;
  census 0.
- boot/mentl.wasm  sha256 ffa2734a510e5b5c7af1fd33b2037a828c44754e6d4a4728b472c5812fe02e49
  (CLEAN m2 == m3)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

- source: THE AUDIT'S VOICE TELLS THE TRUTH — the verb-shape tier lands
  and three voice roots fall with it (2026-07-29; the §7 ledger entry
  THE VERB-SHAPE TIER carries the record). SYNTAX ⌖'s law (`|>` is
  never optional on a chain) is now a read the medium makes about any
  source: pipe_shape_of walks a fn body's statement spine and a
  let-bound name consumed exactly once by exactly the following
  statement is a pipe stage written as a binding — runs of two or more
  report; a twice-used name (`<|` territory) and a single step (the
  law's own exception) stay silent. The dig convicted the absent-
  annotation alias (all three LetStmt mints reuse the value node;
  stmt_child_handles double-visited every unannotated let's value
  subtree — counting walks corrupted, boolean walks blind to it), then
  the tier's first runs convicted show_list rendering EVERY list in
  the voice reversed (map's declared (f, xs) rendered (xs, f)) and
  severance offering "proven zero allocation" on allocating rows
  (row_names read one row link; the reached set now reads the chased
  row). driver_check_entry refuses an unreadable entry loudly
  (EMissingModule) where it parsed emptiness — every analysis verb
  answered zero fns at exit 0 on a path outside the mounts. Frontier
  315/0 (audit-pipe-shape + audit-severance-honest RED on the prior
  boot); census 0.
- boot/mentl.wasm  sha256 0513aca1b92e8e7a25de004825db3ec706bc36cd2b6f5e89692a90692eeeaca7
  (CLEAN m2 == m3)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

- source: THE PROBLEM IS THE SOLUTION — every absence becomes a ranked
  frontier position (2026-07-29; the §7 ledger entry of the same name
  carries the record). The field's count line speaks four tiers now:
  holes, PENDING PROOF OBLIGATIONS (the verify ledger's live debt,
  each rendered at its span with the predicate and its Reason),
  TIGHTENINGS (every T_OverDeclared the judgment banked, each carrying
  its declared row, its proven row, and the standing patch invitation
  — the tighten_collector moved to pipeline.mn as the one home the CLI
  chains and the session bracket both install), then the gradient
  tier. The living resolution proven end to end: an edit that makes a
  row honest drops its tightening from the next frontier — the
  generation boundary clears both banks (tighten_reset + the new
  verify_reset, performed by session_current before re-derivation),
  and the enumerators dedup by span START with latest-mint-wins
  (address_better_a's tie rule as the enumerator's law; full-span
  equality missed a reshaped decl's generation pair — measured). The
  splice floor caught once more (the tightening's fn name as a pointer
  numeral — the named-mint String pin, spec_mangle's law). Measured
  live: pre-edit 1 hole / 1 pending / 1 tightening / 3 gradient;
  post-honest-edit 1 / 1 / 0 / 3 with exactly one re-derivation.
  Frontier 313/0 (the problem-space coproc leg RED on the pre-rung
  boot: a two-tier count line, debt and tightenings invisible, the
  second generation doubling every position).
- boot/mentl.wasm  sha256 8981b63cafde810578f273b639f4f14acaf476767a24701f6ee095efb734acb4
  (CLEAN m2 == m3)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

- source: THE FRONTIER TELLS THE TRUTH — the ranked absence field
  becomes a faithful session read, and the discovery parse gets its
  throwaway instance (2026-07-29; the §7 ledger entry of the same
  name carries the record). Three roots, each measured: (1) the caret
  channel read the CHASE — caret_span_of_handle followed the binding
  chain, so a hole unified with a call's result rendered at the
  call's site with the call's source as its Query; even the unchased
  reason rebinds at inference, so the read moved to the SPAN INDEX
  written at birth (parse_span_of — the only never-rebound channel,
  the same one address resolution trusts). (2) The gradient
  enumerator asked teach_gradient about every cell in range(0, next)
  — virgin cells included — and junk suggestions entered the field
  under garbage coordinates (prelude prose rendered as the entry's
  line 1); the kind gate scopes it to real fn decls, the C1c-era
  virgin-walk residue closed. (3) The birth-span read then EXPOSED
  the discovery generation: driver_extract_imports full-parsed every
  module into the LIVE graph, and wherever file-local coordinates
  coincide with the weave range (every 1-module world) the duplicate
  AST's holes entered the enumerators — each authored ?? counted
  twice, once judged and once as a free ghost, and the ??-authoring
  edit workflows focused the ghost (fifteen frontier legs RED at
  once). The discovery parse now runs in a THROWAWAY graph instance
  (the C1c branch-cursor config as isolation — the old chased read
  had hidden the class by accident; the accident is now a contract).
  The session gains the `frontier` tool: the entry's whole ranked
  field — holes first with their proven-survivor fans and
  tie-teaching, then the decl-scoped gradient tier — the oracle's
  frontier, the gradient's argmax uncollapsed, over the living graph.
  Frontier 312/0 (the frontier-read leg RED-measured on the pre-rung
  boot: wrong address, wrong Query slice, 7 ghost positions).
- boot/mentl.wasm  sha256 29acd4c66c63855c1ab964885f9105668a56dfe4026ae14d500a09f6a560b82c
  (CLEAN m2 == m3)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

- source: THE SEQUENCE-OF-STRUCT LEAVES AND THE FOLD BOUNDARY —
  structural ==/hash/ordering become true over lists of products,
  sums, nested lists, and computed strings, and the fold family
  resolves its types ONCE at entry (2026-07-29; the §7 ledger entry
  of the same name carries the record). The class, measured three
  ways on the prior boot: [(1,2,3)] == [(1,2,3)] answered FALSE
  (the sequence arms floored every structural element to $list_eq's
  per-element word compare — pointer identity as equality, at both
  the top-level and field altitudes); top-level hash of ANY list was
  an undefined-$hash_l<sig> assembly break (emit_hash's unconditional
  agg routing demanded a leaf the collector never contributed); and
  list-of-struct ordering compared pointers. The fix: three generated
  sequence walkers ($eq/$compare/$hash_l<sig> — the show family's
  recursive listbody shape; elements through $list_index,
  representation-total; the element leaf selected by face), the
  collector self-contributing structural-face lists, and THE FOLD
  BOUNDARY: chase_deep at the family's five entries (the two binop
  dispatches, show_hash_ty, the three operand contribs), so sigs,
  dedup, generation, and dispatch read ONE resolution by invariant —
  the paired-types program had measured the collision (two
  TList(TVar) sites with different bindings shared one raw-sig walker
  and trapped in list_eq_loop), and a 45-site sig-wrapper sweep was
  built and REVERTED for the boundary form (fix the origin, not N
  readers). hash_node_of's float-list route joins the field twin
  (list_hash_f64); manifest_same dissolved back into == (the
  workaround's written destiny — the session's manifest compare is
  the walkers' first wheel-internal consumer, proven by the living
  leg). TRANSITION m3 == m4 (the 476-line m2/m3 diff is the walkers
  crossing one generation); frontier 311/0 with the two new leaf
  legs; census 0 after one 20-fn honest widen round (chase_deep's
  row riding the entries).
- boot/mentl.wasm  sha256 b214afba53167e879a9311d501272026a5f7c0ce901e816290e47b179cba5878
  (TRANSITION: m3 == m4; re-pinned from m3)
- generated by: TRANSITION; re-pinned from m3 per march.sh (prior pin follows)

- source: THE SESSION GRAPH GOES LIVING — the resident graph tracks
  the tree, and the staleness check is a pure read (2026-07-29; the
  §7 ledger entry of the same name carries the record). Before every
  message the loop compares the tree's manifest — driver_manifest, a
  projection off the banked range map's own paths: (path hash, byte
  length, content hash) word triples, no discovery, no parse, no
  graph write — and a moved tree re-derives INTO the resident world
  (never a restore: the swap-crossing law's third constraint), the
  new generation shadowing by the env's latest edge, the message's
  region deliberately unreclaimed (moved ⇒ no reset — the fresh
  generation was minted after the mark; the fork-spine law). The
  living check crosses into the loop AS A VALUE (session_current on
  the resident path, the Pure identity on the no-project path), so
  the loop stays row-polymorphic and the gate's own refusal of the
  first form (derivation effects at the bare root) resolved
  structurally. Measured: one re-derivation per edit; post-edit reads
  answer the new truth (query resolves the new fn, audit lists it,
  the at reaches a line that did not exist before the edit); the
  pinned-boot RED answered the startup snapshot on every face. Two
  wheel finds en route, each probe-convicted: the first check form
  re-ran collect_dag per message and its discovery-parse spine growth
  died in a resettable region (spine_comment_at's list_index trap);
  and structural == over a sequence of PRODUCTS floors to $list_eq's
  per-element word compare — pointer identity as equality
  (Hβ.emit.seq-struct-eq-leaf, fixture
  tests/frontier/mn-list-tuple-eq.mn; manifest_same is the class's
  first named workaround). Frontier 305/0 with the living leg.
- boot/mentl.wasm  sha256 3973fd21f58ebf33facad09317f512dd3c0d88dfc066c997346a35cc161ce415
  (CLEAN m2 == m3)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

- source: AUDIT AND TEACH JOIN THE SESSION — the analysis verbs become
  resident reads, and the verb pair collapses to one composed
  projection (2026-07-29; the §7 ledger entry of the same name carries
  the record). analyze_fns's (project, render) pair fused into ONE
  `projection(ast) -> String` per verb — audit_project / teach_project,
  the pure homes the CLI verb and the session tool BOTH call (the
  address_project law at the analysis verbs: CLI derives an ast then
  projects; the session projects the living ast it already holds).
  mcp_run keeps the ast driver_entry_with_ranges returns (formerly
  discarded as _ast) and scopes it ONCE through driver_module_ast —
  the entry's top-level nodes cut by its weave range — so the session's
  audit/teach answer exactly the CLI's entry-parsed scope, never the
  weave's prelude/lib flood (the gate's tripwire asserts the ABSENCE
  of a prelude fn in the audit face). Measured live: the session's
  audit answers the CLI's byte-same four lines (double/main rows +
  severances), teach speaks the gradient's next annotation, and query
  still answers afterward in the same conversation. The session
  surface is propose + query + at + audit + teach. Frontier 304/0.
- boot/mentl.wasm  sha256 1c2e53fa2212aa2ecca811ca37c73dace9fe9a792e5ed7732acb59f728566361
  (CLEAN m2 == m3)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

- source: THE ADDRESS JOINS THE SESSION — the eight-aspect projection
  becomes a resident read, and the voice's transport becomes an install
  (2026-07-29; the §7 ledger entry of the same name carries the
  record). The address render family (render_at, render_field, the
  field tier, the Why hops) spoke print_string DIRECTLY — the
  transport baked into the narration, the exact violation io.mn's
  Console effect exists to prevent ("WHERE print lands is the
  installed handler's decision") — and now performs Console's print:
  the CLI's at_run installs stdout_console; the mcp session's new `at`
  tool runs the SAME extracted core (address_project, the one address
  home) under a collecting console (console_bank) and returns the
  banked lines as the tool text. The transport crossed the import
  boundary AS A VALUE: session_at_read builds in main.mn where the
  renders live and passes INTO mcp_run as a stored fn carrying its row
  (the effect-poly ctor landing's capability, exercised). Measured
  live: at {line:3, col:4} answers the identical projection the CLI
  serves at main.mn:3:4, beside query and propose in one resident
  conversation. Frontier 304/0 (the session leg asserts the at face;
  every CLI at leg green through the Console swap).
- boot/mentl.wasm  sha256 4d1bf583dd6191bf875e281161a4523b15eea78037ed99628a5e85fbe2488df7
  (CLEAN m2 == m3)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

- source: THE SESSION GOES RESIDENT — the mcp serve loop derives the
  project graph ONCE in its own memory and every query answers as a
  live read (2026-07-29; the §7 ledger entry of the same name carries
  the record). Hβ.session.resident-verbs' first rung, satisfying all
  three swap-crossing constraints BY CONSTRUCTION (no swap exists —
  the image IS the session's memory; per-request strings share the
  process; no mid-chain restore). The derivation is resolution-
  conditional; the analysis instances live for the server's life; a
  propose still judges in its OWN nested infer_context (fresh
  innermost instances shadow the session's — the world law — and its
  mints die with the request region while the session's spine lives
  below every mark); a session READ never resets its region (its small
  mints are durable session growth — the fork-spine class held off at
  the serve loop). The `query` tool joins `propose` on the surface.
  Measured live: five messages (initialize, tools/list, two queries,
  a propose) in 0.7s wall — one resident line, both schemes answered
  with Reasons, PROVEN after the reads — vs 3.8s for ONE cold query.
  Named residue: query Reasons render weave spans on this channel
  (Hβ.diag.file-local-span-render's session face); the at/audit/why
  tools and the oracle-as-search are the arc's next rungs.
- boot/mentl.wasm  sha256 659342774e5962142f4adfcab42dbb41e40c48d6fbfd500daade55e3c4e52bd5
  (CLEAN m2 == m3)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

- source: THE SWAP-CROSSING LAW CONVICTS THE WARM VERBS — a measured
  refutation banked whole, and the fmt string-atomicity defect found by
  the one gate that could see it (2026-07-29; the §7 ledger entry of
  the same name carries the record). The warm-image restore was wired
  into the projection verbs (at/query/tighten/edit/check), measured
  correct at the exact-tree face (cold == warm BYTE-IDENTICAL at the
  at-address projection; the key sidecar's splice-pin fix en route),
  then REFUTED by the frontier at the session faces: a mid-verb
  image_resume kills every pre-swap heap value — the caller's own argv
  strings (target rendering EMPTY in its own message), the chain's own
  handler records — and survives only where deterministic allocation
  makes two processes' worlds coincide byte-for-byte (forensic law 5:
  never canonize the accident). The machinery DELETED whole per
  drift-9; the verbs derive fresh; the yield is the resident session's
  MEASURED design constraints (Hβ.session.resident-verbs): no mid-chain
  swap ever, per-invocation strings cannot cross, the image must be the
  session's OWN memory. RIDER: the fmt pass had reflowed space_respond's
  header string ACROSS RAW NEWLINES — the serve answered a bare status
  line, invisible to the march (the wheel never calls it on itself),
  caught by the frontier's serve leg; the literal restored one-line,
  the renderer defect named (Hβ.format.string-literal-atomic-layout:
  a string literal is content; the layout engine never reflows inside
  the quotes). Board at the pin: frontier 303/0, proof-exactness 9/9,
  crown 5/5, census 0.
- boot/mentl.wasm  sha256 6eb1b61ca57796e45ad256dce18a01a3393843ab079e1f0911daa11915491642
  (CLEAN m2 == m3)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

- source: THE MASKS RIDE THE CHASE — the wheel passes its own root-row
  gate (2026-07-29; the §7 ledger entry of the same name carries the
  record). Five roots in one continuous dig, each measured before
  fixed: (1) chase_node's NRowBound arm DISCARDED every level's absent
  set at the destructure — the `~>` pending subtraction dying at the
  ONE mechanism every row read routes through (merge_chased_row /
  merge_row now fold under the reading law: presents filter through
  the masks, absents accumulate; the EtAll special case dissolved into
  the uniform arm); (2) the occurs family went STATE-PASSED (the
  mechanism layer beside chase_node) — the write guards performed
  GraphRead ops from their own arms, chasing whatever instance the
  outer chain held and charging the ops into graph_handler's residual
  (occurs_in_live is the one op-based face for bracketed pre-checks,
  seeded once); (3) the FRAGX collision census spoke show_reason from
  the bind arm — GraphRead + Intern in the residual past every
  bracket; it speaks SPANS now (pure projections); (4) the render
  moved to the report boundary (report renders diag_line in the
  reporter's own world; every arm receives the line made); (5) the
  freeze re-derives its quantifier (branch_replay_one folded the type
  and kept pre-fold qvars — the shared-terminal union pool measured as
  infer_context's two-era, duplicated qvars). With the row truth
  restored, the root-row gate SHARPENED: the stateless-singleton
  demand drop and the installed-anywhere conjunct DELETED (fat-row
  scaffolding) — the escaped-install thunk and the perform-before-
  install shape refuse at compile now; persist_to_disk's arm writes
  the wire directly (an arm re-performing its own op resolves OUTER —
  the residual honestly refused every fixture whose only Persist
  handler was that install). ef_make keeps coexisting present/absent
  (the reading law; the authored `with A + !A` meet moved to
  build_declared_row). The gate settled at THREE TIERS, each fixture-
  pinned: evidence-floor demands STRICT (a dead-chain perform walks
  garbage evidence, no belt — the one sharpening that stands);
  stateful singletons clear on an install (the SingletonUninstalled
  guard the loud belt — the preinstall micro holds 134); stateless
  singletons ground by the value-sound licence (the arm ignores
  state), pinned by the escaped-install tripwire (exit 7) beside
  residual-absence — the modal install-identity frontier owns both
  eventual splits. Board at the pin: micros whole (the backtrack acid
  pair healed — one bare-Option fixture arity trued en route),
  proof-exactness 9/9, crown 5/5, census 0, fmt-canonical touched set.
- boot/mentl.wasm  sha256 0ab5d9033aa4cf6fa758d6f717f9b0f313b9eedf7db972bcc8c2c7ee9ea912b1
  (CLEAN m2 == m3; census 0; via TRANSITION m3 == m4 at the occurs/chase
  crossing, then CLEAN pins as the persist policy, the fmt pass, and
  the three-tier gate landed)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

- source: THE ARGUMENT EDGE RUNS SUBSUMPTION — and the persist barrier
  lands on the op whose contract is re-execution (2026-07-28; the §7
  ledger entry of the same name carries the record). The banked next
  single executed: unify_row_canonical's two Closed~EtAll arms judge by
  row_subsumes — pass, no bind, the negation row a GATE — where the old
  equality arm falsely refused every closed-row argument (the b3 probe's
  RED healed: the quiet thunk admits, exactly the noisy edge reports).
  On it, the resume barrier: persist_branch's param row is
  `() -> a with !WASI + !Filesystem + !Network` — a crashed branch
  RE-RUNS its thunk (§4④), so the row severs what a replay cannot
  un-send; ordinary row checking at the argument edge is the whole gate.
  The audit redirected the barrier from persist(Int, String) — a DATA
  root (the warm compile's asts) replays nothing — to the op that
  replays. Three frontier legs, the RED matrix measured on the prior
  boot: persist-branch-clean 1 false mismatch → 0 + runs 42 through the
  whole checkpoint+run+join loop; hof-row-gate 2 → exactly 1; external
  names the severed triple at the lambda's own span. Named residue:
  the meet is direction-blind (both orientations subsume —
  Hβ.effects.directional-fn-row-edge) and the fanout lowering's
  synthesized persist_branch dispatch is post-inference
  (Hβ.lower.persist-schedule-branch-row-gate).
- boot/mentl.wasm  sha256 7c91063c37c3fde892fc925184744a185d38c2ba46aae6f9ac61432e6a1b4e33
  (CLEAN m2 == m3; census 0)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

- source: THE FN TYPE SPEAKS ITS ROW — the barrier vocabulary parses,
  and the signed-clause fold finds one home (2026-07-28; the §7 ledger
  entry of the same name carries the record). `() -> Int with !WASI`
  now parses in TYPE position (TFun's third field was render-only —
  authored fn types could not state rows anywhere), the row built by
  build_declared_row, which MOVED to effects.mn with its kin
  (is_pure_eff_name, apply_connective, has_pure_declared — pure row
  algebra over parsed triples, one home serving infer's declared rows
  and the parser's type rows; the parser's silent mk_ef_open dependence
  on effects becomes a declared import). Measured live: a param typed
  `() -> Int with !WASI` REFUSES a WASI-performing thunk argument
  (E_EffectMismatch at the call — the resume barrier's whole mechanism,
  ordinary row checking at the argument edge). The named next single:
  the arg check runs through unify's EQUALITY, so a rigid Pure arg
  falsely refuses against the neg param (row_subsumes itself reads
  Pure ⊆ !WASI correctly — the call's arg-to-param edge must run
  SUBSUMPTION for fn-typed params: Hβ.effects.hof-row-subsumption-at-
  call, the b3 probe its banked RED).
- boot/mentl.wasm  sha256 8471a255a2a27500fdd6887fd730226eb6ab18497500ec0463ab1425ab03713a
  (CLEAN m2 == m3; census 0)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

- source: THE INSTANCE JUDGES ITS ARGS — the signature check lands, and
  the prior pin's parse claim is corrected (2026-07-28; the §7 ledger
  entry of the same name carries the record). RETRACTION first (the ⟲
  law): the prior entry's "the decl head parses" was RECOVERY, not
  parse — parse_config_params took bare names only (the 2026-07-24
  measured handler-config lathe-lag), so `rate: Int` left six P_ tokens
  and the params never reached the TParam list; the crucibles passed
  because the negation law needs no params. The head TRULY parses now:
  parse_config_params gains parse_one_param's exact `: Ty` annotation
  arm (one fix, both surfaces — the annotated handler config
  `scaler(f: Int)` heals with it), zero P_ tokens on every instance
  fixture. And the registered signature gets its judge:
  check_instance_args runs at the declared-row site — an authored
  instance's arity disagreement reports EConstructorArity, a
  scalar-literal arg whose ground type disagrees reports ETypeMismatch,
  type- and node-shaped args pass to the instance-unification flow (the
  modal frontier). Sample("hi") against rate: Int reports exactly one
  mismatch; Sample(48000, 2) exactly one arity; the correct instance
  stays clean and runs 42.
- boot/mentl.wasm  sha256 2eb7cee570f29de7b51b4c16afbf9c89b5d55838db0d4cab96c7a723a3f7516f
  (CLEAN m2 == m3; census 0)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

- source: THE NEGATION HOLDS ITS INSTANCE — Arc 3's first landing
  (2026-07-28; the §7 ledger entry of the same name carries the
  record). The parameterized effect DECL head parses (`effect
  Sample(rate: Int)` — SYNTAX's canonical form, ten P_ tokens on the
  prior boot), riding the same [TParam] product fn params and handler
  config ride, and registers its params' types as the effect's env
  scheme (TTuple — the instance signature; the formerly-empty tuple was
  a placeholder). The negation's instance law lands as ONE predicate
  (eff_forbids): a bare !E severs every instance; !E(args) blocks the
  bare name and any instance not PROVABLY distinct — scalar literals
  with unequal values the only proof. Both crown faces read it:
  subsumption's forbidden-membership, and ef_make's absent-minus-
  present through its own pair law (absent_contradicted_by — a bare
  present beside an instance absent is a REFINEMENT, "any Sample except
  44100", the absent surviving where the old by-name dedup silently
  deleted the declared severance at row construction). Three crucibles:
  the provably-distinct sibling admitted and running 42; the same
  instance severed (E_EffectMismatch); the bare perform blocked
  conservatively.
- boot/mentl.wasm  sha256 9f4ebef2db2fbb28dede826b9a4ae72e0b7518c9bc1afe2c5d5ad29525604d3e
  (CLEAN m2 == m3; census 0)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

- source: THE ENCLOSING DECL DESCENDS THE TREE — containment becomes a
  structural read (2026-07-28; the §7 ledger entry of the same name
  carries the record). Spans cannot resolve containment (decl and body
  parse spans are head-anchored — a three-line body's span measured two
  columns), so the vocabulary's nontermination guard was dead for every
  multi-line body: a hole inside banner proposed banner() and main().
  The child-handle projection is TOTAL now — expr_child_handles gains
  the match-arm bodies it dropped, stmt_child_handles is its new Stmt
  sibling (let values, fn bodies, expr stmts, handler state inits and
  arm bodies), body_child_handles the ONE dispatch over NodeBody — and
  enclosing_fn_decl_at descends the tree (node_contains_handle), with
  nesting resolved structurally: a candidate whose own stmt node sits
  inside the standing best's body is the inner fn and wins. Measured on
  the ranker fixture: 7 survivors → 5, banner() and main() excluded,
  width() still first. The frontier leg pins the exclusion (RED on the
  span-blind boot).
- boot/mentl.wasm  sha256 93be5c5273ed45418878788c73769942ea4933e4fff0079865b300d1780701ae
  (CLEAN m2 == m3; census 0; show-floor markers 0)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

- source: THE SHOW FLOOR CLOSES TO ZERO — the 44-marker census swept
  whole (2026-07-28; the §7 ledger entry of the same name carries the
  record). The liveness triage split the census mechanically (floor
  calls vs twin calls vs self-recursion in the emitted artifact), and
  every splice-only param in the wheel now carries its true type as an
  Intent Boundary — seventeen decl pins across nine files, two
  element-shape pins, and three named-mint hoists where the datum-last
  lambda judgment leaves the binder free past any list pin
  (render_gradient_line's proven pattern: audit_report_lines,
  consume_twice_msg, parallel_collision_msg). The arm-binder gap the
  last marker exposed is named (Hβ.infer.arm-binder-op-param-type: a
  handler arm's binder judges free despite the op scheme's declared
  param type). The class-killer stays banked on Hβ.emit.show-free-floor
  as flow-directed demand (Lutze–Schuster–Brachthäuser, OOPSLA 2025:
  instantiation flow tracked through type variables, cyclic flow the
  refusal) — the union-find already holds the flow; the demand analysis
  learns to read its closure.
- boot/mentl.wasm  sha256 09bfedc2a8225a7fc399612ff31e732c228b00d94920dfd8a15fd5d00b952acf
  (CLEAN m2 == m3; census 0; show-floor markers 44 → 0)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

- source: THE SPLICE TELLS THE TRUTH — four roots under one law, and the
  canonical page lands (2026-07-28; the §7 ledger entry of the same name
  carries the record). The whole-wheel fmt sweep's red legs reduced to
  four Carried-Truth violations at four layers: the formatter's
  predicate render borrowed the voice (authored refinements destroyed as
  `len(...)` on the canonical page — render_predicate_tokens is the
  parse-inverse projection now, and the voice's shadowing `(...)` arm
  deleted); the string scanner's every return DROPPED the live line
  (spans after a multi-line splice stale by its height — the whole-weave
  address collapse, the register mute, and warm-inc's cone
  misattribution were one missing tuple field; the line rides every scan
  return); unify_string_fragments bound every splice fragment to String
  (deleted — a splice keeps its OWN type and renders through the
  structural LShow, closing Hβ.emit.int-splice-empty: "I{7}J" was empty
  in every user program ever, while the wheel dodged with int_to_str
  discipline); and the deletion's unmasking — splice-only params
  generalize, so the word floor showed String operands as pointer
  numerals (spec_mangle/arm_fn_name pinned String; the redrive liveness
  probe routed through the one arm_fn_name home after m3 emitted redrive
  calls without the def — the march's BROKEN verdict caught it, 332 → 18
  → 0 diff lines). The show floor now writes its census marker
  (`;; show on unresolved operand`, 52 on the wheel;
  Hβ.emit.show-free-floor). The intermediate pin's own gates then
  convicted the floor LIVE in the driver: driver_module_path's floor'd
  splices minted pointer-numeral paths, fs_exists refused every module,
  and the warm manifest hashed all zeros — the incremental compile
  blind to every edit ("image current" on a patched tree; the WINCPROBE
  eprint census read cur=0 old=0 for every module). The measured liars
  pinned String (driver_module_path, contains_module,
  ownership_suggestion, show_one_rejection's pair, render_gradients'
  element with its line mint hoisted to the pinned render_gradient_line
  — the map lambda's splice judged datum-last while the element was
  still free, so the pin had to anchor at a named call — Intent
  Boundaries, each comment-anchored), and at_run's diag_errors()
  refusal DELETED: branch-judged errors never reached the root count
  before the join carried them truthfully, so the guard had never fired
  — armed, it refused exactly the error-carrying files the address
  projection exists to serve (the README's own voice.mn:9). The
  canonical page: 51/51 rendered canonical, prose conserved,
  tree-idempotent at two consecutive sweeps.
- boot/mentl.wasm  sha256 a971601e47e738b1b3bff981c9f11717cb7c3f5cbe77ecfea4e01a42d918f67c
  (TRANSITION m3 == m4 at 331,182 lines through the interp crossing,
  then CLEAN m2 == m3 at 330,323 lines with the repair set; census 0;
  show-floor markers 52 → 44 as the pins landed; the mn-int-splice
  micro RED 15 on the prior boot, 42 here; the mn-splice-line-carry leg
  RED placeholder on the prior boot)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

- source: THE RANKER READS LOCAL INTENT — cost gets its first reader
  (2026-07-28; the §7 ledger entry of the same name carries the
  record). Every candidate carried `cost: Float // gradient-rank score`
  with fourteen hardcoded literal writers and ZERO readers — the
  resume_kinds pattern at the fan (a score claiming gradient-ness that
  read nothing; survivors surfaced in enumeration order). candidate_rank
  replaces the vocabulary literal with a SUM of graph reads against the
  hole's span, now carried on Context as its fourth field: the decl's
  nearness (the env entry's own Located reason) plus every existing USE
  of the name weighted by its nearness (refs_of_name × the one
  scope_distance_decay — cursor's decay table, one home). rank_sort
  (stable insertion, highest first) orders survivors before render; a
  constraint-tie still teaches — rank is a prior, never a guess.
  Measured on the discriminating fixture: the pre-ranker boot surfaced
  kerning() first (enumeration order); this pin surfaces width() first
  (one use edge in the enclosing body outranking both decl orders). The
  felt walk also convicted the nontermination guard as DEAD for
  multi-line bodies (spans cannot resolve containment — decl spans
  head-only AND body node spans head-anchored, measured 5:32-5:33 for a
  three-line body); the probe graduated into the scan's comment and the
  tree-descent fix is the named next single.
- boot/mentl.wasm  sha256 3b69fb7dd30a39205c00215902765db9429bce4ff30db6049a83337fcca3cf62
  (CLEAN m2 == m3 at 324,035 lines; census 0; comment-refs 0;
  frontier 286/0 — the ranker leg RED on the prior boot by order;
  proof-exactness 9/9; crown 5/5)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

- source: THE GATE SERVES AGENTS — `mentl mcp`, the Synth-gate on MCP
  stdio (2026-07-28; the §7 ledger entry of the same name carries the
  record). Any MCP client connects and PROPOSES source through one tool
  (propose); the judge is the canonical converged stdin judgment with
  the gate law batch-shaped (gate_reads — extracted as the one home for
  executable_gate's refuse+exit, battery_compile's count-and-continue,
  and the server's verdict-as-data; the server outlives every refusal
  where the CLI compile proc_exits). Emission streams through
  wat_to_file to .build/mcp/last.wat — only proven bytes ever land;
  stdout carries protocol lines only. The diagnostic line render
  (diag_line) is likewise one home now (the root arm, the branch bank,
  and the mcp collector all read it), and the collector's diag_absorb
  arm carries branch-judged diagnostics into the verdict. Rider: the
  UNeg emit reads the node's repr live (emit_unaryop_for — f64.neg for
  floats; the word-floor dance fabricated i32 negation on the wheel's
  first negative float literal and the assembler refused it; the fix
  crosses this generation, so the mcp error codes ride as the Ints
  they are with float_of_int at the JNum boundary).
- boot/mentl.wasm  sha256 347b0a415a4a207cefa9c3112ea6e6a3cebb296f35e32c287e44bc59c55d30a3
  (CLEAN m2 == m3 at 323,725 lines; census 0; comment-refs 0;
  frontier 285/0 — the four mcp legs seen RED against the pre-verb
  boot, exit 2 with zero jsonrpc lines; proof-exactness 9/9; crown 5/5)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

- source: THE PROPOSER SPEAKS THE REGISTER — a synthesized candidate's
  Reason is genuinely unlocated and speaks the medium's voice
  (2026-07-28; the §7 ledger entry of the same name carries the
  record). All thirteen candidate mints wrapped their Reasons in
  Located(span_zero(), …) — fabricating a source site for a node the
  medium synthesized, exactly the ghost the refs facet already filters
  ("a ghost is not a source site"), and reason_span_or_zero's own doc
  says unlocated falls back to zero — so the wrapper contradicted the
  projection it fed and rendered "at 0:0-0:0:" into every survivor
  line. The wrappers are DELETED (the span-neutral proof is the
  fallback itself), and the eleven "synth_proposer: …" strings — an
  internal fn name in user-facing text — re-register as the medium's
  vocabulary ("the type's integer inhabitants", "the unit type's one
  inhabitant", "a nested ?? — the shape narrows, the fill recurses").
  The two gate assertions pinning the old strings updated in the same
  change. The fan's survivor line is now
  "0  — inferred from the type's integer inhabitants".
- boot/mentl.wasm  sha256 d6826c2e3a6b97b371bfb21e5ccc72a62470accd4fd3e8bf474f241c252ef5f3
  (CLEAN m2 == m3 at 321,181 lines — 130 smaller, the wrappers gone;
  census 0; comment-refs 0; frontier 281/0; proof-exactness 9/9;
  crown 5/5)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

- source: THE RENDER REGISTER — narration scopes to the file the user
  asked about; errors always render (2026-07-28; the §7 ledger entry of
  the same name carries the record). The user path flooded ~173 lines of
  the substrate's own lint before the six-line answer (the banked felt
  residue): every parse warning printed TWICE — the DAG discovery walk
  parses each module for its imports and reports in file-local
  coordinates no range can place, then the weave parse re-reports with
  weave spans — and a solo weave's T_OverDeclared/comment-ref verdicts
  on wheel fns are weave-RELATIVE (the wheel census holds them clean),
  so no source sweep could ever zero them. DiagScope (ScopeAll |
  ScopeNone | ScopeAt) lands as the diagnostics handler's register
  state via the single-op DiagRegister effect (armed on the root only —
  the BranchDiag precedent); the driver owns both performs (ScopeNone
  before discovery, ScopeAt(entry range) the moment the fold completes,
  before any reporting parse); the branch bank carries (line,
  span-line, is-error) so the join re-applies the same register; the
  census paths never perform the op and stay ScopeAll byte-for-byte.
  range_of_module moved to driver.mn beside the fold that mints the
  map. 173 → 0 with the fan projection intact; the user's own warning
  renders exactly once; refusals unharmed.
- boot/mentl.wasm  sha256 3f889ff511e24f546e6c932a859230f659dcc243ed880b193039fad8f3969da5
  (CLEAN m2 == m3 at 321,311 lines; census 0; comment-refs 0; frontier
  281/0 — the two register legs RED on the prior boot; proof-exactness
  9/9; crown 5/5)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

- source: THE LATTICE COMPLETES — the teaching write is a JOIN, the
  scheme boundary is a VALUE boundary at every face, and K=8 becomes
  the default judge, byte-equal to sequential (2026-07-28; the §7
  ledger entry of the same name carries the record). Row cells are
  join-semilattice LVars (graph_bind_row's bound arm joins via
  row_join, fragments coexisting by GRAPH identity — addr() identity
  was convicted by a 13-line m3 ≠ m4; the decl-exit REPLACE rides
  graph_finalize_row); the frozen-read instantiate closed census-86
  at the scheme boundary; and the terminal gate's six-battery caught
  the last mid-flight read — a published scheme carrying a live
  EtVar row pointer through chase_row_deep's untouched tail, folded
  by concurrent callers at schedule-dependent moments — fixed by the
  bound-tail fold at the chase face (subst_row_build's law). The
  window-1 judge's bytes equal the K=8 judge's bytes; six identical
  shas on the battery; the sharper judge's 65 honest under-declared
  rows widened in one round to census 0.
- boot/mentl.wasm  sha256 28c39633164ebae30ba00d565e16d4ecbbd248606a2941030ef858e225e374e1
  (TRANSITION m3 == m4 at 320,102 lines, then CLEAN m2 == m3 through
  the blessed pin; census 0; comment-refs 0; frontier 279/0;
  proof-exactness 9/9; crown 5/5; K=1 == K=8 with six identical
  shas; judge_window = 8 default)
- generated by: TRANSITION; re-pinned from m3 per march.sh (prior pin follows)

- source: THE BOUND TAIL FOLDS BY RECURSION — the trio law completes at
  the subst face, and the rebind chain gets its name (2026-07-27; the
  §7 ledger entry of the same name carries the record). subst_row_build's
  merge arm checked only ONE level of a bound row chain and SHARED the
  whole chain on the inner-tail miss, handing every caller of a
  recursive fn's scheme the owner's band cell at depth two-plus — the
  window-8 first-binder race's carrier (cell 1873202 bound at four
  handle_to_smt sites across two branches, the reason census). The arm
  now RECURSES into the binding — nested frees freshen at any depth,
  both former branches subsumed by the recursion's own arms, the inner
  fork deleted (the wheel 178 lines smaller). The born-reason census
  then named the surviving flip mechanism whole: one shared row cell
  per mutual-recursion family is SERIALLY REBOUND by its same-layer
  caller branches (a born-to-why chain five deep on emit_expr's cell) —
  graph_bind_row on an already-bound root, the union-find law violated
  regardless of concurrency; the rebind instrument and kill plan are
  banked at judge_window's decl. The window holds at ONE.
- boot/mentl.wasm  sha256 2ef0964e52703e87978d762feeca0f52e645d656e79bb9c9d15da1b2199e3c22
  (CLEAN m2 == m3 at 323,700 lines; census 0; comment-refs 0; frontier
  279/0; proof-exactness 9/9; crown 5/5; micros-through-m2 116/0)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

- source: THE KILL LEDGER REACHES THE WRITERS — the value boundary
  closes at generalize and compression leaves the branches (2026-07-27;
  the §7 ledger entry of the same name carries the record). The K>1
  dig ran six probe batteries — the per-branch bind census with band
  bounds, the can_yield tail census, the reason census whose every
  prior-generation row bind confessed "row path compression" — and
  SIX labels died to the artifact: the prepass registrations were
  INNOCENT (op schemes fully quantified all along), the NFree/NRowFree
  publish fallbacks were REAL shared cells (generalize now QUANTIFIES
  its unresolved arms — callers freshen instead of teaching the decl
  through the publish; Law-7 byte-identical on the wheel), and row
  path compression was the largest shared-write class (1,463 foreign
  row binds per compile) — now its own op, graph_compress_row, whose
  BRANCH arm skips (the root's sequential walks re-compress; an
  optimization write never again makes sibling chases
  schedule-dependent). The K>1 flip itself SURVIVES both fixes,
  confined to the float-format trio's k2 yield-floor wraps (two
  self-stable attractors, 4-2 at window 8, census 0 in both); the
  window holds at ONE with the surviving writer class and the next
  probe banked at judge_window's decl.
- boot/mentl.wasm  sha256 714431ceeaaaf3504ae4d08c73a0ee79488681ccc01aa0aa1da23526e660bec8
  (CLEAN m2 == m3 at 323,878 lines, ~22s/leg; census 0; comment-refs
  0; frontier 279/0; proof-exactness 9/9; crown 5/5; micros-through-m2
  116/0)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

- source: THE DIG NAMES THE RACE'S CELLS AND THE PUBLISH FREEZES
  (2026-07-26; the §7 ledger entry of the same name carries the
  record). Six-run window-8 batteries turned the K>1 flip from a
  mystery into a mechanism: per-run single-callee k2 yield-floor
  flips (the float-format family 2-in-6, then driver_incremental),
  each a can_yield chase finding one row var bound in some runs and
  free in others — and the callee's scheme is DECLARED-CLOSED, so the
  raced cell is upstream. The publish freeze landed (every
  join-crossing scheme chase_deep'd to a VALUE in branch_replay_one —
  the value-boundary law at every publish kind) and the flip MOVED,
  which convicts the remaining shared-live-var carrier: the PREPASS
  registrations (register_effect_ops / pre_register_handler_sig)
  hold live sig vars every branch's installs and op edges unify
  against — same-block concurrent binds on one prepass cell are the
  race, the handler-sig barrier class confirmed real. K>1's landing
  design is banked at the window's decl: the value-boundary law
  reaches the prepass (quantified value schemes, per-branch
  instantiation, join-side instance algebra). Also in this pin: the
  crc walk bounded to its own pass's parse range (comments attach at
  parse; the old 0-to-graph_next walk judged every stale generation's
  copy — the 19% profile share), and the window at ONE, proven
  deterministic across three identical runs. The freeze's 8-line
  m2/m3 crossing is Reason renders sharpening through the frozen
  values.
- boot/mentl.wasm  sha256 2df771e286b456fcfdd9a338efc129536607f6ba313cff4cca34858487220ee7
  (TRANSITION m3 == m4; census 0 at every generation; comment-refs 0;
  frontier 279/0; proof-exactness 9/9; crown 5/5; micros-through-m2
  116/0; self-compile ~20s/leg)
- generated by: TRANSITION; re-pinned from m3 per march.sh (prior pin follows)

- source: THE WEAVE FLATTENS AND THE FAN MEASURES ITS FIRST RESIDUE
  (2026-07-26; the §7 ledger entry of the same name carries the
  record). Morgan's catch — "since when does Mentl take so long?" —
  exposed a ~60× wall-time regression the convergence landings had
  shipped unmeasured (5.3s → 5:42); host perf named it in one line:
  98% of the whole self-compile inside attach_comment_weave's bsearch
  probes, each runs[mid] a list_index over a push-built SNOC spine,
  multiplied by every re-frontend the rounds run. One flatten (the
  iterate-flattens-once law) recovered 15×: 5:42 → ~23s. The fan's
  block window then ran K=8 and ITS OWN instrument convicted a
  residue: one lambda's k2 yield-floor wrap flipped in one of four
  otherwise byte-identical self-compiles — a rare schedule-dependent
  race in a post-seal staging read. The window holds at ONE (every
  branch still a real spawned instance; deterministic, CLEAN); the
  K>1 dig owns the flip with the repro cadence banked. crc_walk
  (19% of the healed profile, list_index beneath it) is the named
  next flatten-class strike.
- boot/mentl.wasm  sha256 c7f08fdf63678fd94b0bdb3f2359a386eebda2cb460e59287472ad239c6a9f12
  (CLEAN m2 == m3 at ~26s/leg; census 0 at every generation;
  comment-refs 0; frontier 279/0; proof-exactness 9/9; crown 5/5;
  micros-through-m2 116/0)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

- source: THE JUDGMENT SPAWNS — every stmt's judgment runs as a REAL
  host thread over the shared image, and the spawned judge's bytes
  EQUAL the sequential judge's (2026-07-26; the §7 ledger entry of the
  same name carries the record). layer_judge_walk spawns each layer
  branch as a task (spawn_task/join_task — the substrate ops direct;
  spawn-join immediate, so concurrency is the in-flight count, not a
  semantics change), the task body = the eleven-handler self-contained
  bracket wrapped in the root's own outer shape (an empty dispatch
  graph instance — an arm's performs resolve OUTER to its install, so
  diag_branch's renders chase outside the bracket; sequentially they
  landed on the dispatch chain's empty instance whose density guard
  answers virgin t{h}@e0, a discovered latent wrong-instance read now
  named Hβ.diag.render-chases-wrong-instance; the wrapper reproduces
  those answers byte-for-byte — the spawned trap was the same chase
  meeting an EMPTY world, measured by the 6,444-line repro). The
  intern rides as intern_view (read-only over the root's table via
  intern_seed; a miss traps loudly — a view-minted handle would fork
  identity). The wheel is a SPAWNING MODULE now: shared-image import,
  CAS-bump allocation, per-instance init — the memory flip the
  substrate landed for. THE VERDICT IS THE STRONGEST FORM: CLEAN
  m2 == m3 — the spawned judgment reproduces the SEQUENTIAL judgment's
  bytes exactly (the banded partition's execution-order-free numbering
  cashing out), and the whole board holds through the spawning
  compiler.
- boot/mentl.wasm  sha256 5cb9503920649b8dbb7c6f3ce40840db13436f267737c51b865cac54bfc9b340
  (CLEAN m2 == m3; census 0 at every generation; comment-refs 0;
  frontier 279/0; proof-exactness 9/9; crown 5/5; micros-through-m2
  116/0 — every gate through the spawning wheel)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

- source: THE PARTITION GOES EXECUTION-ORDER-FREE — banded overflow
  completes the deterministic-handle-partition keystone (2026-07-26;
  the §7 ledger entry of the same name carries the record). The seal
  takes the walk's true frontier (the threaded form's own latent
  collision: branch overflow mints lived above a seal that parked
  `next` below them). Measured (OVERFLOWPROBE, one build): 612
  overflow handles across 324 stmts, every delta 1 or 2 — the
  fingerprint-blindness residue on the convergence's carried counts —
  so each stmt gets a PRIVATE 64-handle overflow band above the plan
  (32× margin): the mint arm's jump takes the banded ceiling
  (high_limit, config slot 7; 0 = the root's unlimited) and CONSUMES
  the target, so crossing a band trips the existing unseeded guard
  loudly instead of invading a neighbor. The high threading DELETED
  (graph_mint_high op and all; the region end is static). Numbering —
  jumps included — is now a pure function of (source, plan) with no
  execution-order input: the fan reproduces these bytes at any K.
- boot/mentl.wasm  sha256 f7c96d1b81480384be5e03bb1105dc7f1c3333d9286f862d5f25a99547ea2d48
  (TRANSITION m3 == m4; census 0 at every generation; comment-refs 0;
  frontier 279/0; proof-exactness 9/9; crown 5/5; micros-through-m2
  116/0)
- generated by: TRANSITION; re-pinned from m3 per march.sh (prior pin follows)

- source: THE BRANCH CURSOR IS WHOLE — every handler branch-local, the
  spawn now pure scheduling (2026-07-26; the §7 ledger entry of the
  same name carries the record). Four byte-gated rungs completed the
  bracket's self-containment: the barrier shape (judge-then-join at
  the layer), the three ledgers (affine seeded + log-replayed via the
  Consume triple; verify banked + re-performed, exact by the
  operands-in-own-range stability argument; region zero-replay), the
  inner trio (infer_ctx/fresh stmt-local-fresh; summaries re-armed
  over the threaded classify value), and the graph instance — the
  C1c-2a config cashed out with span_index0 + high0 (six slots), the
  graph_branch_seed export (post-plan spine + open count + the
  parse-complete span index), graph_mint_at dissolved into per-branch
  config, and the open-space frontier THREADED branch to branch via
  graph_mint_high (the first branch-instance run faulted at the 4GB
  boundary because overflow is a DESIGNED path — an over-measure
  stmt jumps above the plan — and a zero high minted at handle 0;
  the threading restores the root's sequential semantics exactly,
  with the spawn era's abort-and-requeue as the banked tightening).
- boot/mentl.wasm  sha256 4a1363ab99274adeeb74d7c9d70084fcd1559f070c22156a73bbc9bd22b94b83
  (TRANSITION m3 == m4 — the branch-cursor judge reproduces itself;
  census 0 at every generation; comment-refs 0; frontier 279/0;
  proof-exactness 9/9; crown 5/5; micros-through-m2 116/0)
- generated by: TRANSITION; re-pinned from m3 per march.sh (prior pin follows)

- source: THE SCHEME BOUNDARY BECOMES A VALUE BOUNDARY — census 0 with
  the judge CONVERGING, the incremental cone, five measured roots
  (2026-07-26; the §7 ledger entry of the same name carries the full
  record). The census-3 trio's banked diagnosis was refuted by
  discriminating measurement (old judge on new source = 0; the deepest
  root predates the rounds — convergence only exposed it); the true
  conjunction: subst_row dropping the pending mask's absent set at
  instantiation, the closure creation edge charging creators (deleted —
  abstraction is pure; the call edge charges appliers under
  world-as-value dispatch), loose-scheme raw row handles shared into
  caller frames (the share-guard: top-level FnScheme callees never
  share; the rounds iterate forward-ref truth), the round prepass
  starving cone-masked handler entries (cone-gated), and the T-handle
  row-var class — sort-blind row_var_is_free left both of
  infer_context's scheme row tails unquantified while subst_row's
  catch-all SHARED them: every caller chained one live row, the first
  binder's thunk row became the E_EffectMismatch quartet (the probe
  caught the stored scheme mutating between its callers' reads). The
  three-walk tolerance (free/changes/build agree on NFree in row
  position) closed it: ZERO row mismatches in the whole self-compile,
  the rounds CONVERGE, and round K+1 re-judges only the moved cone.
  Micros mn-absorb-poly (+ -fwd) bank both faces RED-first
  (E_PurityViolated through the prior pin; 0 errors / 42 here);
  movers_line is the bound-hit's permanent projection;
  report_effect_mismatch carries its Reason to the report site
  (Hβ.diag.effect-mismatch-reason names the DiagKind widening).
- boot/mentl.wasm  sha256 6d89491cb576c32041ba6e41d3cf23fea03b4e48ef463757f6f6f54ecb57bd6f
  (TRANSITION m3 == m4 — the value-bounded judgment reproduces itself;
  census 0 at every generation, the raised census lane closed
  (census_errors_max back to 0); comment-refs 0; frontier 279/0;
  proof-exactness 9/9; crown 5/5; micros-through-m2 116/0 — the two
  absorb faces included)
- generated by: TRANSITION; re-pinned from m3 per march.sh (prior pin follows)

- source: THE JUDGMENT CONVERGES TO A FIXPOINT (retitled 2026-07-26 —
  "honest fixpoint" was cushioning language around a carried
  regression, Morgan's catch; the next pin's entry carries the trio's
  measured root) — the converged
  rounds, the branch cursor, and four measured roots land as one arc
  (2026-07-26; the §7 ledger entry of the same name carries the full
  record). infer_program_converged iterates quiet rounds until
  alpha-normalized scheme fingerprints stabilize, then the reporting
  final judges bracketed — every stmt under its own env-over-frozen-base
  and render-at-collect diag cursor, replayed per stmt. The four roots,
  each measured before fixed: the affine branch stack's wrong-end push
  (nested alternatives corrupted the enclosing frame); the
  edge-evolution join (value-replay lost the op→handler ambiguity —
  graph_mutated collapsed to a lsp_adapter singleton and the
  SingletonUninstalled floor fired at every compile's first mint);
  parse-truth layers (the range-reason scan read zero edges because
  VarLookup reasons live on parse nodes outside every inference-mint
  range — stmt_layers_ast reads each stmt's own free names and the
  range machinery deleted); and the piggyback-effect de-parameterization
  law. The sharper judge's harvest rode in: the thirteen-verb exit-code
  contract, the verify facet's whole triples, the diverge-chain render,
  the record/lambda synth carriers, four row widens. payloadfn runs its
  true 2 through the whole machinery; the fmt write-time hook guards
  every edit. The arc's tail closed two more measured classes: the
  field projection's junk handles were THE FLOAT SCORE'S HIGH WORD
  (0x3FE68F5C read as a handle — the wide-tuple let-destructure
  compiled at the word floor, offset 4 in the pinned wat; field_ranked
  now pins its return [(Float, Int)], the representation Intent
  Boundary at the generic sort/reverse laundering, and both holes
  project true spans and fans again), and the census ratchet rose
  0 → 3 by its own documented lane for the understood absorption trio
  (the rounds losing infer_context's effect-polymorphic subtraction —
  bracket exonerated by the discriminator; the fix-spec banked in
  PLAN §11, the line ratchets back with it).
- boot/mentl.wasm  sha256 512a4c85d9814e25ce61171c51ada05fea6623cd4f6fa756e7b114fce3aebb52
  (CLEAN m2 == m3; census 3 — the understood absorption trio, ratchet
  raised with its reason; comment-refs 0; frontier 279/0;
  proof-exactness 9/9; crown 5/5; micros 114/114 both tiers)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

- source: MENTL MARCH — the wheel judges its own generation in-process,
  and the dig that landed it healed three latent breaks (2026-07-26).
  The verb: `mentl march` walks the wheel's file set (fd_readdir's
  self/parent links skipped — the unfiltered walk descended src/./.
  forever, +2 bytes a level to the alloc ceiling), compiles it through
  the canonical converged stdin chain IN-PROCESS, STREAMS the emission
  through the new wat_to_file(fd) sink into .build/march/m2.wat, reads
  the census inside the same install, and verdicts against the last
  generation — its first CLEAN was against the bash march's own m2, so
  the in-process and exec invocation routes are proven byte-identical.
  wat_to_file is the third sink (write-through to an fd; fs_create_impl
  the streaming open): region-immune because bytes reach the host at
  each arm, which is the scope law now written at wat_to_string's decl
  — a whole-compile install of the COLLECTING sink banks segment
  pointers the emit phase's per-fn region resets zero, and that is
  exactly how the in-process battery (`mentl test <dir>`) had been
  SILENTLY BROKEN since the emit-arena landed (its own gate never
  re-ran; the board's micros ran through the bash loop). The battery
  streams per-micro wat through the same sink now: 114/114 in-process.
  The explicit-stack concat drain (flat_fill_concat — call depth =
  representation alternation, never concat depth; proven on a
  5000-deep rope) and the correctly-rounded float family (callee-first
  order + Float pins on parse_float/parse_float_body/parse_mantissa_f64
  — the DAG path's single-pass judgment floored the f64 results of
  loose forward schemes to words, an assembly refusal in mentl run)
  landed through the same arc. Named en route with its 9-line RED
  fixture: Hβ.lower.install-config-capture-read — an install-config
  arg that references a CLOSURE CAPTURE reads 0 silently (a let-local
  reads true; march_emit binds sink_fd locally until the init path
  rides the capture ladder).
- boot/mentl.wasm  sha256 0d153e0ce531751071b2717713f5f0104ca6da978a4ab962236935f94f6504b4
  (CLEAN m2 == m3; census 0; comment-refs 0; frontier 279/0;
  proof-exactness 9/9; crown 5/5; micros-through-m2 114/114;
  in-process battery 114/114; mentl march CLEAN vs the bash m2 and
  idempotent on re-run)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

- source: THE REFS FACET SPEAKS ITS SPANS (2026-07-25→26; the
  self-exemplification research pass's opening move #1, executed the
  hour its report landed). `mentl query <path> "refs of NAME"` walked
  the live weave, collected every use-edge span, and the render threw
  them away as a bare count — the propose-facet seam one facet over.
  QRRefs now renders one located line per inbound use-edge
  ("at 3:50-3:56" per call site); en route the collector's non-tail
  recursion (stack depth = the whole handle space) fell to the first
  DAG-scale query and went tail-with-accumulator (the buffer-counter
  law), zero-span synthetic mints filter from the answer (a
  pre-registration ghost is not a source site), and the query verb's
  print gained its final newline (println — every query answer had
  always ended flush against the next prompt). Development grep for
  "who calls X" now has a graph read to die into. The research pass's
  full report is banked (three opening moves: this render; fixtures
  stating their own contracts — the frontier bash's dissolution into
  `mentl test`; wiring interrogate_at into the address surface — the
  eight interrogations running as code; plus `mentl march` measured
  as UNBLOCKED now that B-i's warm image landed).
- boot/mentl.wasm  sha256 758f65f2036ddc6c409ce08712fa858c12770656b7ee997fa74aef338e9e2740
  (CLEAN m2 == m3; census 0; comment-refs 0; frontier 279/0;
  proof-exactness 9/9; crown 5/5; micros-through-m2 114/114;
  idempotent-all-50; prose conservation 0 lost)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

- source: ZERO PROSE LOST — the weave conserves the whole wheel
  (2026-07-25; the comment arc's terminus: 2,803 → 0, verified by the
  verb's own lexical gate on all 50 files with one-invocation
  idempotence). Two closures finished it. (1) The else-if
  DESTRUCTURES its child node and renders the pieces, so the child's
  uniform emission never fired — render_else_block now emits the
  nested if's attached prose itself, uncuddling the chain at exactly
  the prose-carrying link (the lexer's two-char table comments were
  the measured class). (2) desugar_block returns the MINTED NODE:
  the head-destructure match now mints AT THE LET'S SPAN, so the
  weave pass's span-tie rule (latest mint wins) resolves a body-head
  comment to the LIVE match instead of the dissolved let — the last
  silent prose-drop class, and the callers' nexpr wraps delete with
  the contract change. The fmt summit's PROSE gate is fully open:
  every authored comment line in the wheel survives the canonical
  render, measured by the medium itself. The remaining swap-gates
  are sugar re-preservation (the __dp lambda-destructure render) and
  the drift-audit hook's leading-marker accommodation.
- boot/mentl.wasm  sha256 6cd7a75e532b6ecf9a22039b43d4ef09f034eb98be74d5c02c8882d3789b49e3
  (CLEAN m2 == m3; census 0; comment-refs 0; frontier 279/0;
  proof-exactness 9/9; crown 5/5; micros-through-m2 114/114;
  idempotent-all-50; fmt prose conservation 0 lost of 16,786)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

- source: THE WEAVE INVERTS — one attachment pass, one emission point
  (2026-07-25; Morgan's "isn't there a better way" answered by
  deleting the whole per-site family). The nine hand-closed comment
  families were one law fought site by site; the inversion replaces
  them: attach_comment_weave runs ONCE after parse_program (every
  route), resolving each comment run by SPAN against the node weave —
  trailing runs to the widest node starting on their line, leading
  runs to the widest node at the minimal start past the run, span
  ties to the latest mint (which also retires the desugar transfer:
  the replacing match out-mints the dead let), and a blank-separated
  earlier block INHERITS the next run's target (each node competes
  only for its nearest run). One O(N log C) scan of the span index
  serves every run. EMISSION went uniform too: render_tokens_for —
  the one entry every node's render flows through — prefixes the
  node's attached prose; the per-construct emission calls delete
  (rows keep theirs: op/variant cnodes never flow through the entry),
  and prose-forced breaks now CASCADE from fits_inline seeing the
  emitted newline instead of per-construct special cases. The verb
  writes its own FIXPOINT: fmt renders, re-parses, re-renders, and
  writes the second render, so a mid-line attach moves to its
  line-start home internally and one invocation is idempotent by
  construction. ~50 parse/render sites deleted (the collects, the
  probe-law contortions, attach_leading_prose/attach_trailing_comment/
  collect_leading_comments/same_line_back whole); the parser's skips
  are comment-transparent again. TWO artifact-taught corrections en
  route: the run record went NOMINAL (CwRun — the generic bsearch
  walkers compile once, so an open element floored their field reads;
  the brand + param Intent Boundaries prove the offsets), and the
  first form lost blank-separated heads until the inherit rule
  landed. MEASURED: conservation 1,969 → 741 → 106 lost lines across
  the whole wheel (vs the per-site machinery's 35 with ~50 more
  sites), IDEMPOTENT-ALL-50, and the whole board green.
- boot/mentl.wasm  sha256 ffc3fd58f9a654f6b0d3441779cd0d45ca9dbcc46cf1354a0fa039547ce3002d
  (CLEAN m2 == m3; census 0; comment-refs 0; frontier 279/0;
  proof-exactness 9/9; crown 5/5; micros-through-m2 114/114;
  idempotent-all-50)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

- source: THE PROBE LAW COMPLETES — resume updates and the chain
  entry stop consuming, and the weave conserves 99.8% (2026-07-25;
  the comment arc's convergence: 2,803 → 35). Two more probe-consume
  sites fell to the verb's own lost-line reports: the resume-with
  UPDATE list's comma probe returned past the trailing run on a miss
  — the drift-audit suppression markers live exactly there — and
  binop_loop's ENTRY skip consumed the run while probing for an
  operator, so prose before a chain continuation (`// why` above
  `~> affine_ledger`) vanished; the entry now scans without
  consuming, attaching a pre-operator run to the FOLLOWING stage
  when an operator follows and returning the pre-run position when
  none does. infer.mn went 28 → 3 and main.mn to full conservation.
  THE TRUE REMAINDER, verb-measured on a fresh tree: 35 lines across
  8 files, one structural leaf — trailing comments on record-TYPE
  fields, where a comment carrier cannot live (TRecord is a TYPE;
  a node in its field tuple would make prose part of type identity).
  Named Hβ.parser.record-field-comment-attach with that reason; the
  bridge to the canonical-source swap is truing those 35 authored
  sites to carriable positions, plus the drift-audit hook learning
  the leading-position marker (the render moves trailing markers
  above their line; the hook reads per-line today).
- boot/mentl.wasm  sha256 4e7d5013a769db908e1503f0ed8b40e5f0e7aca9930286bf444ba849d5d7fea1
  (CLEAN m2 == m3; census 0; comment-refs 0; frontier 279/0;
  proof-exactness 9/9; crown 5/5; micros-through-m2 114/114;
  idempotent-all-50)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

- source: THE CHAIN BREAKS AT PROSE + THE GATE NAMES ITS LOSSES
  (2026-07-25; the operator-interior family and the report that
  retired the external diff for good). Prose after a binary operator
  belongs to the RIGHT operand: binop_loop — the ONE path pipes and
  binops share — collects the run after the operator and attaches it
  to the operand's node, and the renders break the chain there: a
  prose-carrying operand puts the operator at line end, the comment
  above the operand (the || ladder shape), and the chain renders
  (converge/tee) emit stage prose above the verb line at stage
  indent (render_stage_comment). The conservation gate now NAMES its
  losses: on a shortfall, fmt prints each source comment line whose
  text appears nowhere in the render's lex ("  lost: // …", up to
  eight) — the verb replaced the last hand diff, and its first
  report immediately identified the remaining ~70 lines as trailing
  drift-audit markers (load-bearing: the audit hook reads them
  per-line), argument-interior trailing series, and one block shape.
  En route the medium re-judged this landing's own edits: the two
  chain renders' declared rows widened to their bodies' new weave
  reads (the census ratchet caught them at 2 and the widen closed
  it) — and the fmt run's W_CommentRefUnresolved on a backticked
  `unreachable` in infer prose surfaced through the same channel.
- boot/mentl.wasm  sha256 45153159067e063e5c63bad179223eff47712977ba42feeaaa28d1e71b15b940
  (CLEAN m2 == m3; census 0; comment-refs 0; frontier 279/0;
  proof-exactness 9/9; crown 5/5; micros-through-m2 114/114;
  idempotent-all-50)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

- source: DEPTH IS COMPOSITION + FITS-OR-BREAKS — the layout engine's
  two primitives land (2026-07-25; the width regression falls from
  2× to near-parity). REINDENT-ON-COMPOSE: every interior renders at
  depth ZERO and each enclosing block indents its whole interior
  exactly once at composition (indent_block) — depth is never
  threaded and never wrong, because nesting depth IS composition
  depth (the old fixed two-space literals rendered a nested match's
  arms at column 2 at any depth; the probe now shows depth-correct
  nesting to level four). The six wrappers recompose: block, match,
  effect decl, handler decl, vertical variants, prose-else.
  FITS-OR-BREAKS: an inline render survives when it is one line
  within the 100-column budget, else the construct takes its block
  layout — fn and let break after `=` with the body indented; an
  overflowing if takes the cuddled-block vertical (`} else if c {`,
  SYNTAX's own chase_node shape) through
  render_branch_block/render_else_block; an overflowing call breaks
  one argument per line; a long prose-free variant set takes the
  vertical layout. The budget measures at depth zero (a render never
  knows its depth under reindent-on-compose) — the honest
  approximation, stated at the fits_inline site. MEASURED: formatted
  non-comment lines over 100 columns fell 1,335 → 628 against an
  authored baseline of 568, with IDEMPOTENT-ALL-50 held at every
  rung and the march CLEAN through all three pins of the arc. The
  remaining swap-gate is the ~65 operator-interior prose lines
  (is_seq_op's || ladder and kin) — the chain-break-at-prose form.
- boot/mentl.wasm  sha256 e4d6897fbc9b1e7a885e6ee69b9a1c95ac4f138a674d342aea91c43b59671320
  (CLEAN m2 == m3; census 0; comment-refs 0; frontier 279/0;
  proof-exactness 9/9; crown 5/5; micros-through-m2 114/114;
  idempotent-all-50)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

- source: THE ARM'S ARROW STOPS EATING + THE GATE GOES LEXICAL
  (2026-07-25; the ninth comment family, and the conservation gate
  trued to the line). Prose between an arm's `=>` and its body — the
  block above a multi-line arm body, the single biggest remaining
  class (lower.mn alone held 116 lines) — collects at both arm loops'
  arrows and attaches to the same body handle the leading run uses;
  the weave merges, the arm render emits it above the arm, and the
  reparse re-attaches it there. lower.mn's loss went to ZERO and
  infer.mn's 84 → 31. THE GATE'S OWN BLIND SPOT, found by
  cross-checking its verdict against the independent line census: a
  weave-to-weave compare sees only what the parse ATTACHES — a line
  the parse still consumes as layout is invisible on BOTH sides,
  conserved-by-blindness (and block-counting also misread merges as
  drops). The metric went LEXICAL: the lexer emits one TComment per
  authored prose line, so fmt lexes source and render and compares
  token counts — the difference IS the lost lines, including
  never-attached ones; the weave-walk fns deleted with the count
  metric. The verb's verdict now matches the external census
  line-for-line ("31 of 2079 prose lines would not survive") and the
  python census retires for real. THE REMAINING ~65 LINES ARE ONE
  CLASS: prose interleaved in OPERATOR chains (is_seq_op's `||`
  ladder, `~>` stage gaps, before-closer tails) — attachable today
  but only renderable under multi-line operator layout, so the
  residue merges into the width-aware layout engine build (the
  summit's other dep; one build now, not two).
- boot/mentl.wasm  sha256 409fc675f686f730441e34c53c84b9e85bedad474c1ec52d52c0c09933d3392a
  (CLEAN m2 == m3; census 0; comment-refs 0; frontier 279/0;
  proof-exactness 9/9; crown 5/5; micros-through-m2 114/114;
  idempotent-all-50; fmt prose-conservation green on 41 of 50 files,
  the nine holdouts all operator-interior)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

- source: THE VERB CARRIES ITS OWN CENSUS — fmt gains the prose
  conservation gate, and the else/file-tail families land
  (2026-07-25; Morgan's cut — "I wish we didn't have to run python
  just to run Mentl stuff" — absorbed as the ⟳ law prescribes: the
  hand census was the confession, the projection is the verb).
  fmt_run now re-parses its OWN render inside the same context (two
  single-file frontend runs bracketed by graph_next watermarks, so
  the DAG's other modules never skew the count), counts the weave's
  attached blocks on both sides, and REPORTS the verdict itself:
  "prose conserved (25 blocks)" or "8 of 435 prose blocks would not
  survive a reparse" — the medium naming its own residue per file;
  the python comment-diff retires. WITH IT, families seven and
  eight: BEFORE-ELSE (150 — the else probe scans without consuming,
  the prose attaches to the else-branch node, and render_else breaks
  the line to emit it above its own `else`); FILE TAILS (155 — the
  end-of-file block attaches to the synthetic unit carrier the
  TComment arm already minted, the carrier renders prose-and-no-
  tokens, and the POrphanDocstring raise retires: the orphan has a
  home, exactly as SYNTAX's "never dropped" always claimed). The
  content-matched census: 513 → 299 unsurvived lines; the remainder
  is match-heads in expression position, argument-list interiors,
  and variant-inline residue — each now VISIBLE through the verb's
  own report instead of a hand tool.
- boot/mentl.wasm  sha256 7b1b8b61300436bf836fcea47583abfb4760bf2133b072c5a8a802ebd0b3833a
  (CLEAN m2 == m3; census 0; comment-refs 0; frontier 279/0;
  proof-exactness 9/9; crown 5/5; micros-through-m2 114/114;
  idempotent-all-50)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

- source: THE ATTACH TARGET IS THE LIST'S NODE — the mid-block
  statement family lands (2026-07-25; the sixth comment family, and
  the content-matched census that trued the metric). A mid-block
  expression statement wraps in a fresh ExprStmt node AFTER the leg's
  leading attach ran — the prose landed on the inner expr's handle
  while the block render reads the wrapper's, so every mid-block
  statement comment dropped while let/fn statements carried theirs
  (measured: "before the unit if" vanished in one pass; the fix
  attaches per-branch to the node the statement list will hold).
  IDEMPOTENT-ALL-50 holds; the line-count metric SATURATED (formatted
  now carries MORE comment lines than authored — trailing prose
  becoming its own line inflates lines while preserving content), so
  the honest measure moved to the content-matched census: 513
  authored comment lines whose text does not survive, bucketed —
  file-TAIL blocks (155; no following decl — the Module-handle
  attach + end-of-render emission is the fix), before else/else-if
  (150; the else probe's skip consumes the run — the else-branch
  node is the target), before match heads in expression positions
  (45), variants' inline-render residue (19), and comments inside
  multi-line call argument lists (~80 across the other buckets —
  the genuine expression-interior tail).
  Hβ.parser.expr-interior-comment-attach carries exactly those
  named shapes, each with the recipe this arc proved six times:
  collect at the position the old skip consumed, attach to the node
  the render reads, render where the parse re-collects.
- boot/mentl.wasm  sha256 ae600b3b3348b3654436e2c3c8f5495020da8b1f2dec4d4f409a1a613a81daa2
  (CLEAN m2 == m3; census 0; comment-refs 0; frontier 279/0;
  proof-exactness 9/9; crown 5/5; micros-through-m2 114/114)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

- source: THE PROSE JOINS THE WEAVE — five comment families attach,
  ops and variants become addressable, and the probe law lands
  (2026-07-25; the summit's 2,803 dropped comment lines fall to 469 —
  83% recovered — with IDEMPOTENT-ALL-50 held throughout). The
  families, each RED-measured by the sweep's census before its fix:
  (1) MATCH ARMS (1,353 — the biggest class): an arm's leading prose
  attaches to the arm's BODY node (the arm's one handle; patterns are
  bare values), trailing same-line backward to the same handle; the
  caller's skip_ws was eating the run before the loop's collect ever
  ran. (2) HANDLER ARMS: the same recipe closes
  Hβ.parser.handler-arm-doc-attachment — a seed-era "for now" skip
  that named its own peer. (3) BRACELESS-CHAIN CONTINUATIONS (344):
  the expression-position let now runs the block loop's full
  discipline (trailing backward to the let node, leading forward to
  the continuation's first statement), and desugar_block TRANSFERS a
  dissolving destructure-let's prose to the arm body (the render puts
  it above the pattern that WAS the let — movement, never loss).
  (4+5) EFFECT OPS (677) and TYPE VARIANTS (312) become ADDRESSABLE:
  each row widens with its own minted NTypeAnn node (the op's return
  type / the variant's payload product — the row's genuine type fact,
  its handle in the weave); the census named all thirteen consumer
  destructures and the sweep converged in four compiles. THE
  NON-CONSUMING PROBE LAW, paid for by the alternating-drop the
  memory.mn idempotence diff measured (every arrowless op swallowed
  its successor's prose): a lookahead probe (the effect-op arrow, both
  arm loops' comma) returns the PRE-probe position on a miss — a scan
  is a read, never a consume. The vertical variant layout renders
  prose-carrying ADTs one variant per line (`=`/`|` leading, the
  connective owned by the variant render — the double-`=` was the
  first sweep's wreckage), and parse_type_decl routes the
  name-to-`=` run to the first variant's node. THE RATCHET'S OWN
  PROOF: the moment op/arm/variant prose entered the weave, the
  comment-refs gate JUDGED it — 16 phantoms fired on prose that had
  never been audited (backticked names in trailing op comments), each
  trued or unbackticked to 0. The named remainder (469): before
  if/else (286) and before match head (126) — expression-position
  runs needing render sites at the if/match projections — plus the
  before-closer and eof tails; Hβ.parser.expr-interior-comment-attach
  narrows to exactly that residue.
- boot/mentl.wasm  sha256 694716c1b45856c6e09879264504e240afb67411df966d934a41f37999fd00f8
  (CLEAN m2 == m3 at 299,248 lines; census 0; comment-refs 0;
  frontier 279/0; proof-exactness 9/9; crown 5/5;
  micros-through-m2 114/114)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

- source: IDEMPOTENT-ALL-50 — the fmt fixpoint reached, the summit's
  swap gated by its own comment law (2026-07-25; four render roots +
  one parse canonicalization, each convicted by the sweep). The whole
  wheel now formats to a byte-fixpoint in ONE pass. The roots, in
  landing order: (1) THE LET-CHAIN FLATTENS AT PARSE — a braceless
  let-chain parsed to nested per-let BlockExprs while the braced
  spelling parsed flat (two graphs for one meaning; the formatter
  faithfully rendered the redundancy as a brace pyramid); the
  continuation now merges into ONE statement sequence
  (parse_let_expr), Law-7 byte-identical — lower dissolves block
  nesting, so only the graph's spelling changed. (2) THE SURFACE-TYPE
  PROJECTION — the formatter borrowed show_type (the VOICE: List(T)
  spellings, tN@eN free-var handles, every row rendered), which leaked
  inferred internals into rendered source: an effect op's fn-typed
  param rendered `with t45323@e23311` and the second pass misparsed it
  into THREE bogus op declarations (silent semantic corruption through
  a formatter). render_type_tokens is the parse's inverse — [T] never
  List(T), fn-type params are TYPES (no invented `_:` names), a row
  renders only its AUTHORED present/absent content (a parse-minted
  tail var IS authored absence) — swapped in at all eight fmt type
  sites; PLAN §5.U's voice/format boundary note enforced at last.
  (3) A LAMBDA OPERAND ALWAYS RE-WRAPS (prec 0 in expr_prec_of + the
  render_chain_pos head guard at every chain renderer + render_operand
  on the generic pipe arm): the recurrence `((prev) => …) <~ delay(1)`
  rendered paren-free and pass 2 read `delay(1)(prev)` as an
  application. (4) A BODY-LEAD `{` IS THE BLOCK — a record literal or
  update as a fn/lambda/arm body re-wraps in the parens the parser
  consumed (render_grouped_body at the five body-lead surfaces);
  `= ({cmp: [], …})` had rendered bare and reparsed as a block of
  garbage holes. (5) INT_MIN renders its WRAPPING positive spelling
  (int_to_str's own minus re-parsed as unary minus and accreted one
  `-` per pass). THE ORACLES: idempotence ALL 50 wheel files
  (pass2 == pass1 byte-identical); the formatted tree compiles
  census-0 and its wat is value-identical to the pristine wheel's
  modulo three CLASSIFIED diffs — handle renumbering (names +
  provenance constants), the FNV -2128831035 const-fold firing in one
  tree and not the other (value-identical code shapes; an order-
  sensitive e-graph fold, banked with band G's typed-rulecyclic), and
  yield_from's open-record demand-order offsets (the named
  trecordopen class, internally consistent per compile). THE GATE
  THAT HOLDS THE SUMMIT: the formatted tree DROPS 2,803 comment lines
  (16.7% of authored prose — expression-interior comments consumed as
  layout, the named Hβ.parser.expr-interior-comment-attach) and
  doubles >100-char lines (572 → 1208; the width-aware layout
  engine); canonizing prose destruction is forbidden by the weave's
  own law, so the SWAP gates on those two builds — the gate doing its
  job, not deferral. The voicey carry leg re-banked to the
  SURFACE-canonical annotation spelling (the authored byte-identical
  `{kind: String, level: Int}`; the old expectation was show_type's
  voice spacing).
- boot/mentl.wasm  sha256 672a924bb0b1529638ba77a176b5565ae6dcac30e574227ffbae6fa93e50a76d
  (CLEAN m2 == m3 at 298,159 lines; census 0; comment-refs 0;
  frontier 279/0; proof-exactness 9/9; crown 5/5;
  micros-through-m2 114/114)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

- source: THE TWO READERS AGREE — parse_float goes correctly rounded,
  the shortest render becomes the one float projection, and the
  list_set flat contract goes loud (2026-07-25; the fmt summit's
  idempotence sweep forced all three roots into the open). The summit's
  19 non-idempotent files reduced to ONE mechanism with THREE liars,
  each convicted by probe: (1) parse_float paid one rounding PER
  fractional digit — rebuilt as ONE f64-integer mantissa accumulation
  (exact through 2^53, every authored literal) + ONE scale by the exact
  power of ten (float_scale10, splitting past 10^22), proven EQUAL to
  IEEE division at the probe (parse("0.3") == 3.0/10.0); this closes
  Hβ.runtime.parse-float-correctly-rounded over the exact-mantissa
  range. (2) float_render_positive normalized by DIVIDING by
  float_pow10(exp<0) — an unrepresentable power (1/10 rounds) skewing
  scaled a whole ulp (0.3 normalized to 2.9999999999999996, not 3.0);
  a negative exponent now MULTIPLIES by the exact positive power, one
  correctly-rounded step, with a decade re-clamp. (3) THE ROOT UNDER
  THE ROOT: the shortest ascent's rounded-last bump list_set into a
  PUSH-BUILT digits list — and list_set computed FLAT addresses for
  every tag, so the bump wrote a digit over the snoc node's PARENT
  POINTER and never took effect (the ascent's round-last arm was dead
  since birth; measured: list_set(push(make_list(0),2),0,3)[0] == 2,
  while index 1 on a two-element snoc "worked" by layout coincidence —
  the payload slot sits where flat elem[1] would). The documented
  flat-only contract is now ENFORCED: list_set's base refuses a
  structured tag loudly (the census instrument for any other violator —
  the board ran clean, so the class had one instance); the extraction
  buffer is preallocated flat (make_list(nd), the buffer-counter
  idiom). With parse correct the shortest render's oracle
  (parse_float(cand) == f) gives the SAME verdict as the assembler on
  every short candidate — so float_to_str_bits is DELETED whole and
  the emit boundary reverts to float_to_str: the bits render existed
  only to compensate for the broken parse, and its own digit
  extraction was a ulp off in the tail anyway (true nearest-0.3
  rendered "...96", outside the rounding radius — the assembler landed
  one ulp LOW, which is why the wat-born 0.3 differed from the parsed
  one). Probe battery on the fixed wheel: parse("0.3")/lexed
  0.3/parse("0.995")/6.02/2.5/1.0/100.5/0.001 all render canonical
  short forms; 0.1+0.2 renders "0.30000000000000004"; READERS-AGREE.
  Named residue: the unverified nd=17 tail's float-stepped extraction
  (Hβ.runtime.float-render-17-digit-exact — never reached by a lexed
  literal, whose own spelling is a short witness). The march ruled
  TRANSITION (76 diff lines = the corrected f64 constants crossing one
  generation) and m3 == m4 held — the wheel's floats are now TRUE to
  their authored spellings for the first time.
- boot/mentl.wasm  sha256 ea643e6c57c83810386788e48f94a815847b6441e66f493602e96fc40d29bebf
  (TRANSITION m3 == m4 at 296,456 lines; census 0; comment-refs 0;
  frontier 279/0; proof-exactness 9/9; crown 5/5;
  micros-through-m2 114/114)
- generated by: TRANSITION; re-pinned from m3 per march.sh (prior pin follows)

- source: FMT RUNG 6 — THE SHORTEST FAITHFUL FLOAT + THE TWO-READER
  DISCOVERY (2026-07-25; the ladder's last rung, and the probe that
  found a real pre-existing divergence). float_to_str renders the
  FEWEST digits whose parse-back equals the value (1.0 → "1.0",
  3.14 → "3.14"; the ascent tries truncated then last-digit-rounded,
  seventeen digits terminate it) — the oracle is parse_float ITSELF,
  the lexer's own conversion, so fmt round-trips under the medium's
  real reader by construction. En route the probe convicted TWO
  pre-existing truths: digit_at's doc claimed a zero-pad its raw
  list_index never did (trued at the one reader — the first short
  digit list trapped the armed bounds check), and THE TWO READERS
  DISAGREE: parse_float("0.3") != the wat-born 0.3 (parse_float's
  naive digit/scale summation is not correctly rounded, while the
  assembler's conversion is) — named
  Hβ.runtime.parse-float-correctly-rounded. Until it closes, the
  EMIT boundary keeps the bits-faithful seventeen-digit projection
  (float_to_str_bits — an emitted constant is re-read by the
  assembler's correctly-rounded reader; a shortest form tuned to the
  naive reader could change the constant's value through emission),
  and values whose naive parse disagrees with their bits render
  longer-but-always-faithful at the human registers. The generic-show
  leg's banked "2.5000000000000000" was the inflation era's text —
  re-derived (2.5 is exact) and re-banked at "val: 2.5".
- boot/mentl.wasm  sha256 997e42f5e8b46cafdecf320138b8b6d10faf1480b57116a7841d9e54fe4b9633
  (CLEAN m2 == m3 at 296,429 lines; census 0; comment-refs 0;
  frontier 279/0; proof-exactness 9/9; crown 5/5;
  micros-through-m2 114/114)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

Prior pin:

- source: FMT RUNG 5 — THE RENDER GOES TOTAL OVER THE WEAVE (the
  interior-prose loss closes to the parser's own named residue). The
  final expr's attached prose renders exactly as a statement's does
  (render_stmt_comment — one interior-prose projection, two callers:
  the block-stmt walk and the block final), and attached prose FORCES
  the block layout at an if branch (the single-line form would drop
  the final's comment — measured as the loss's biggest residue after
  the first strike). MEASURED on parser.mn, the census's worst file:
  809 → 623 comment lines kept (77%, from the census era's 68% — and
  the file TRAPPED under fmt then; it formats clean now). The
  remaining drops classify to expression-interior positions the
  parser consumes as LAYOUT and never attaches (a match-arm header
  comment sampled decisively) — Hβ.parser.expr-interior-comment-attach,
  the named parser refinement, not a render debt: the render now
  emits everything the weave holds at every altitude it writes.
- boot/mentl.wasm  sha256 5ad4aef6f39effbc7f8b732dc7796ca90451796c0db062d35eaa28c729a9ef40
  (CLEAN m2 == m3 at 296,102 lines era; census 0; comment-refs 0;
  frontier 279/0; proof-exactness 9/9; crown 5/5;
  micros-through-m2 114/114)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

Prior pin:

- source: FMT RUNG 3 — RENDER∘PARSE IDENTITY AT THE IF BRANCH (the
  census's one SEMANTIC-FLIP class dies). The parser wraps a braced
  if-branch in a BlockExpr, and the old render added literal braces
  ON TOP — a wrap accreted per pass (+423 sites over seven files)
  until the inner braces re-parsed as the record pun and a float
  became a record. The render now supplies braces EXACTLY ONCE
  (render_if_branch): a statement-carrying block takes the multi-line
  block render, a single-expression block the single-line braced
  form, an else-if chain renders bare, and the parser's implicit-unit
  else renders nothing (the re-parse re-inserts the same node — the
  identity). The voicey leg gains the no-accretion assertion.
- boot/mentl.wasm  sha256 a0172f04909168ed4b37ea63d660efec372007c137ee018bc883a4572b3ed8b0
  (CLEAN m2 == m3 at 296,102 lines; census 0; comment-refs 0;
  frontier 279/0; proof-exactness 9/9; crown 5/5;
  micros-through-m2 114/114)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

Prior pin:

- source: FMT RUNGS 1·2·4 — THE VOICE'S RENDER REGISTER STOPS LYING
  (2026-07-25; the census's two universal trap classes + the retty
  drop, in one strike). The with-clause renders the parser's SIGNED
  TRIPLES verbatim — connector per entry (+/-/&), per-entry !, the
  instance args through show_eff_arg (the ONE arg renderer: the
  diagnostics voice and the formatter speak the same projection) —
  replacing the closed-row rebuild that read tuples as bare EffNames
  and destroyed 41 declared rows as «invalid-effect». The handler
  renders read through the TYPED closed projections
  (state_field_name/init, handler_arm_op_name/args/body — the cure
  that existed and was bypassed; every handler decl trapped before).
  An authored -> RetTy carries back through render_retty (91 heads
  were silently dropped). Three RED-first legs on the voicey fixture:
  behavioral 42 before/after, the three carries verbatim, byte-exact
  idempotence.
- boot/mentl.wasm  sha256 7215e8c22e82e3b06e57ced936adc6e7b9310a123d7a8c9ddc9927c600c925d2
  (CLEAN m2 == m3 at 295,930 lines; census 0; comment-refs 0;
  frontier 279/0; proof-exactness 9/9; crown 5/5;
  micros-through-m2 114/114)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

Prior pin:

- source: HANDLER-CONFIG DEFAULTS — THE PRODUCT LAW AT THE HANDLER
  DECL (2026-07-25; the fleet's builder, cherry-picked three-way onto
  live main under the new base-freshness contract and re-derived).
  A handler's config list IS a parameter product, so its fields carry
  defaults: the carrier goes [String] → [TParam] — the fn-param
  carrier itself, ONE default representation (the TParam's fifth
  slot), ONE fill (resolve_call_args), zero second mechanisms.
  parse_config_params mirrors parse_one_param's scalar face
  (name [= default]; annotations/destructures stay the named
  lathe-lag, refused loudly as today); register_handler types each
  default in the handler's own scope under a DefaultReason edge (a
  bare ~> h has no call node to splice through); lower's
  extract_handler_config_args resolves the install product through
  the same seam infer's CallExpr path reads; the formatter renders
  name = default back. Four-face fixture: bare install fills (8),
  explicit fills (16), parens omitted fills all (6), labeled skips
  over (12) = 42. The stale-base tax showed once at re-derivation
  (render_config_params' row predated the intern era — one widened
  row) and the numbering TRANSITION crossed in the same march.
- boot/mentl.wasm  sha256 9de2ecc4fe6fd50a98f9406c942a44dac5f7eb4c5073d2847ea0108c2f517379
  (CLEAN m2 == m3 at 295,514 lines after the crossing; census 0;
  comment-refs 0; frontier 276/0; proof-exactness 9/9; crown 5/5;
  micros-through-m2 114/114)
- generated by: TRANSITION then CLEAN; re-pinned per march.sh (prior pin follows)

Prior pin:

- source: THE TYPE NAMESPACE REFUSES (2026-07-25; the fleet's
  type-registry builder, transplanted onto main and re-derived — the
  last named silent-MERGE class closes). Two `type X` decls in one
  namespace silently MERGED: each decl's ctors tag from 0, so a
  cross-tag match returned the wrong arm with zero diagnostics
  (measured RED through the prior pin: exit 13 where the honest answer
  was 99 — Halt, tag 0 of a second Signal decl, matched the first
  decl's Go arm; 3,443 bytes of WAT, silent). E_DuplicateTypeName
  joins DiagKind ARMED AT BIRTH (the decl-site-fact licence, the
  E_DuplicateFnName precedent; wheel-blob census 0 at arming) —
  refuse_duplicate_type_decls claims every type head (alias, refined
  alias, ADT/nominal record) in a WALK-LOCAL seen-set, never an env
  probe (a multi-variant decl registers only ctor names; legitimate
  re-registration across walks is the two-pass judge's own shape);
  the second decl's span carries the report; registration proceeds
  (productive under error) and the armed class refuses the
  executable. Both registration walks run it — the trial's copy is
  absorbed by diag_quiet, the final's reports for real. Frontier leg
  refuse-dup-type PASS (E_DuplicateTypeName=1, exit 1, zero WAT).
- boot/mentl.wasm  sha256 3f4fba83df4bb54dfbb17f542f2941aa9010c84ff33660962adbd6e2348d5b6b
  (CLEAN m2 == m3 at 294,823 lines; census 0; comment-refs 0;
  frontier 273/0; proof-exactness 9/9; crown 5/5;
  micros-through-m2 114/114)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

Prior pin:

- source: THE UNSEEDED-OVERFLOW GUARD (2026-07-25; the bracket
  refutation's F1 hardened on the live arms). The planned-mint
  overflow jump is ROOT-ONLY semantics — only the root's plan seeds
  mint_high — so an overflow firing with mint_high still 0 is an
  unseeded instance (a future branch cursor past its range), and the
  jump would mint handle 0 over the shared Module root's cell. Both
  fresh arms now trap that case loudly (the deliberate cross-2^32
  load, the world-find-miss precedent) instead of minting; the branch
  protocol stays abort-and-requeue by design. Unreachable on every
  root path (the plan always seeds before limits exist) — the guard
  is the belt the refuter demanded before any branch instance is
  built.
- boot/mentl.wasm  sha256 ed5dc82bcd688600c771199892059e056be341600873685d77914dccd3e42985
  (CLEAN m2 == m3 at 294,435 lines; census 0; comment-refs 0)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

Prior pin:

- source: THE OVERLAY FAMILY DIES WHOLE (2026-07-25; the fleet's
  recon-overlay census executed — the C1b dividend turned out to
  undershoot). The per-module overlay index was DEGENERATE: graph_fork
  had ZERO perform sites for the structure's entire life (re-derived
  by the orchestrator before acting), so overlay_count was forever 1
  and every mint registered into one "global" row feeding a filter
  nobody called (query_module_queue: zero performs). DELETED: the five
  overlay state fields, the per-mint overlay_register_at write (two
  index reads + extend + three sets + a tuple allocation off the
  HOTTEST write path, ×~200k mints per self-compile), the graph_fork
  op and arm, the five overlay fns, the eager overlays_to_pairs build
  on every snapshot (twenty sites, nineteen of which ignored it), and
  the Graph snapshot's middle field — Graph(next, span_index). The
  checkpoint record shrinks 12 → 7 fields; the rollback restore
  10 → 5. The ONE real reader (QueueItem.module_path, filled at
  construction — a SNAPSHOT of a live fact) rewires to the live read:
  filter_by_module keys on module_path_of_span(parse_span_of(pos)) —
  NModule span containment, the fact the graph already carries;
  module_path_of_span moves to graph.mn (DAG-homed beside
  parse_span_of; oracle and cursor both import graph). Resolves
  Hβ.graph.fork-dead-code as deletion. Wheel 621 lines smaller.
- boot/mentl.wasm  sha256 6913e09db1f4095f3eac3a52178096340c51699a9b51996b53726c1b304232d9
  (CLEAN m2 == m3 at 294,395 lines; census 0; comment-refs 0;
  frontier 272/0; proof-exactness 9/9; crown 5/5;
  micros-through-m2 114/114)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

Prior pin:

- source: RESOLVED OR LOUD (2026-07-25; the field-access cluster's
  emit half — the trecordopen-wrong-field silent-wrong class CLOSED).
  resolve_field_offset's open-record arm now resolves the FULL field
  set through the row residual the graph already carries behind the
  row var (open_record_full_fields — NRecordRowBound, chained open
  rows, or a closed bind — canonically sorted), and floors -1 when
  any tail is genuinely free: never again a prefix-sum over the
  partial demanded set. The 31-line m2/m3 crossing showed the fix in
  the bytes both ways: offset=0 loads became offset=4 (LIVE
  wrong-slot reads in the wheel itself, healed) and unprovable floors
  became correct reads. The nine red micros were the fix working: the
  whole payload ladder (2026-07-01) had banked the WRONG-SLOT values
  (alpha read as "beta"); payloadfn's own comment carried "Expected
  value when fixed: 2" since birth — re-banked to 2 alongside
  hoflambda and mapfield; the six genuinely-free-tail arms re-banked
  at the honest 134 floor, with arm specialization (the spec-twin
  machinery at handler arms, closing the op-payload residual) the
  named successor.
- boot/mentl.wasm  sha256 723220b30aef087a8472cda089be8b75eac4eff8a770dd4f8f8bd88c347bd821
  (TRANSITION m3 == m4 at 295,451 lines; census 0; comment-refs 0;
  frontier 272/0; proof-exactness 9/9; crown 5/5;
  micros-through-m2 114/114)
- generated by: TRANSITION; re-pinned from m3 per march.sh (prior pin follows)

Prior pin:

- source: THE NOMINAL SATISFIES THE ROW (2026-07-25; the field-access
  cluster's judgment half — a canonical SYNTAX form stopped lying).
  unify's TName arm gains the TRecordOpen case: a nominal record
  satisfies a structural field demand by its own declaration, read
  through the live env edge (nominal_record_fields — the THIRD
  reader of RecordSchemeKind beside the literal check and the ctor
  arrow view). The brand never erases — the TName side stays bound;
  only the demand's field vars and row residual bind
  (unify_record_open_against_closed, the existing machinery, zero
  new unifiers). Measured RED first: `p.age` on a let-bound Person
  raised a false E_TypeMismatch (Person vs {age: t | r}) on the
  canonical form while emit resolved the offset correctly, and a
  row-polymorphic {age: Int, ...} parameter refused a Person
  outright; both heal to 42 through the fixed wheel. A CLOSED
  structural demand still refuses — exact-shape identity is the
  brand's point. Two frontier legs registered (nominal-field-access,
  rowpoly-accepts-nominal), RED on the prior pin.
- boot/mentl.wasm  sha256 f0ab3177dc5513a108a13595ba46f3faabf87ad6eb817695579e744554c342bc
  (CLEAN m2 == m3 at 295,191 lines; census 0; comment-refs 0;
  frontier 272/0; proof-exactness 9/9; crown 5/5;
  micros-through-m2 114/114)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

Prior pin:

- source: THE PAGE PRE-OPEN (2026-07-25; Phase C rung 1 step C1c-2c's
  opening move, Law-7-inert). graph_mint_plan now pre-opens every
  band the plan will touch, sequentially, before any planned walk —
  a branch cursor then only writes cells, never opens a band, so the
  fan's page opens cannot race by construction (two cursors whose
  ranges share a band boundary would otherwise both allocate the
  page and one would leak with writes lost). Allocation order shifts
  earlier; addresses never reach the wat.
- boot/mentl.wasm  sha256 3a53f7754b567bebd3e58952b69818d540851ac991bbb29f23a6d8d13634007a
  (CLEAN m2 == m3 at 295,016 lines; census 0; comment-refs 0;
  frontier 266/0; proof-exactness 9/9; crown 5/5;
  micros-through-m2 114/114)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

Prior pin:

- source: THE ENV VIEW (2026-07-25; Phase C rung 1 step C1c-2b —
  the branch cursor's second leg, Law-7-inert). env_handler gains the
  base triple (base_buf, base_count, base_index): a branch installs
  over the root's shared entries as a read-only BASE — its own
  extends land in a fresh private buffer that dies at the join, with
  publishes re-applied through the root in stmt order — while the
  root installs pass the empty base, so every root resolve is one
  level exactly as before. The four lookup arms try private then
  base by composing the existing env_resolve family (Option fallback,
  zero new resolve machinery); a branch snapshot is the base view
  with the private prefix appended.
- boot/mentl.wasm  sha256 d8c42e3a3ac1080a9d04827c7f5eada69a63c256c1dc1f885a7c89e6f941305c
  (CLEAN m2 == m3 at 294,988 lines; census 0; comment-refs 0;
  frontier 266/0; proof-exactness 9/9; crown 5/5;
  micros-through-m2 114/114)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

Prior pin:

- source: THE CURSOR CONFIG (2026-07-25; Phase C rung 1 step C1c-2a —
  the branch-cursor substrate, Law-7-inert). graph_handler gains four
  config params (spine0, spine_open0, next0, limit0): a parallel
  branch cursor installs over the SHARED spine table with a private
  planned range, while the nine root installs pass the empty graph
  explicitly. Handler state records live in shared heap; the install
  chain stays per-instance, correctly private — the spawn substrate's
  own shape. Handler-config DEFAULTS were probed and the lathe lags
  (the parameter-product law implies them; parse_arg_names takes bare
  names) — the explicit quadruple is the honest form until that lathe
  turn.
- boot/mentl.wasm  sha256 3f4262bca93ee65cb2f4cc7e9766227a65d5741cca3570b3b574c451d62baefc
  (CLEAN m2 == m3 at 294,873 lines; census 0; comment-refs 0;
  frontier 266/0; proof-exactness 9/9; crown 5/5;
  micros-through-m2 114/114)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

Prior pin:

- source: THE LAYER-ORDERED FINAL (2026-07-25; Phase C rung 1 step
  C1c-1 — the fan's exact execution shape, run sequentially first).
  The trial's own reasons ARE the stmt DAG: every reference it
  resolved drew a VarLookup Reason, so a stmt's edges are the
  VarLookup names over its trial range — read from the graph through
  the new graph_reason_at (the unchased reason cell), never re-walked
  from the AST. Only BACKWARD edges order the walk (a forward fn
  reference resolves to the trial's published final scheme; the
  genuinely order-coupled stmts — top-level lets — precede their
  dependents in source), so depths compute in one ascending pass and
  the layer graph is acyclic by construction; a missing edge leaves
  equal depths whose within-layer source order preserves source
  behavior. The final pass now walks LAYER BY LAYER with
  source-order pre-assigned bases — the numbering stays
  plan-determined while the judgment order becomes the one the
  parallel fan will reproduce. The march arbitrated the reorder
  TRANSITION-grade: the layer-order judgment differs from the
  source-order judgment by 1,228 emit lines and reproduces ITSELF
  exactly (m3 == m4) — walk-order convergence proven by the
  fixpoint, which is precisely the gate C1c-2's concurrency will be
  held to.
- boot/mentl.wasm  sha256 406e4d2a6cbf492c0576cf8a0e2a79f2c0f8a70171bb63d069cf4fc8268119f3
  (TRANSITION m3 == m4 at 294,828 lines; census 0; comment-refs 0;
  frontier 266/0; proof-exactness 9/9; crown 5/5;
  micros-through-m2 114/114)
- generated by: TRANSITION; re-pinned from m3 per march.sh (prior pin follows)

Prior pin:

- source: THE PLANNED MINT (2026-07-25; Phase C rung 1 step C1b — the
  deterministic handle partition goes live, sequential-planned). The
  trial IS the sizing oracle: each stmt's mint count is the graph_next
  delta around its trial inference (a fact the graph already carries,
  read at the boundary — zero instrumentation), and the final pass
  mints each stmt into a pre-assigned dense range [base, base+count),
  bases prefix-summed from the final's own post-prepass frontier. The
  numbering is a pure function of the source through the trial's
  determinism — the exact numbering the parallel fan (C1c) must
  reproduce, which is its byte-equality gate. Three graph ops carry
  the plan (mint_plan seeds the overflow target above every base;
  mint_at enters a range and raises the high-water past what it
  leaves; mint_seal resumes sequential at the high-water); a mint at
  a range's ceiling jumps to the open space and clears the limit
  (the decl finishes deterministically — decl order is); a stmt
  minting under its measure leaves virgin gap cells, absorbed by the
  word-face guard now on graph_node_at (the C1a Cast read). Handle
  space stays DENSE — no sparse encoding, no iterator migration, no
  storage change: the partition is a NUMBERING plan over the C1a
  pages, fully decoupled from physical paging. Self-compile 5.57s
  median / 515MB — flat.
- boot/mentl.wasm  sha256 c53dd17b93740775ab9ae6a5d2a2cf7f4a51bf1a332358e77147d30793da06ac
  (TRANSITION m3 == m4 at 292,882 lines — the 6,586-line m2/m3 diff
  is the planned numbering crossing one generation; census 0;
  comment-refs 0; frontier 266/0; proof-exactness 9/9; crown 5/5;
  micros-through-m2 114/114)
- generated by: TRANSITION; re-pinned from m3 per march.sh (prior pin follows)

Prior pin:

- source: THE PAGED SPINE (2026-07-25; Phase C rung 1 step C1a — the
  deterministic handle partition's storage substrate, Law-7-inert).
  The graph's six handle-weaves (nodes, program, comments, canon,
  narrowing, boundaries) dissolve into ONE structural page record per
  band — six 16,384-slot columns allocated whole at band-open, written
  in place, NEVER MOVED — so the doubling copy (graph_extend_to + the
  NFree gap-fill) is deleted, the seventeen-field checkpoint snapshot
  collapses to twelve fields of which the spine contributes two words
  (the band table + the open count), and rollback is the count restore
  plus a plain in-place backward trail walk: the fork-spine class (the
  boundaries[23] stale-spine trap) is unconstructible because a page
  that never relocates cannot dangle. The open-band COUNT is the whole
  guard — the trail's own logical-length discipline one level up; the
  nodes column is mint-dense so its read guards by `next` alone. The
  Graph snapshot sheds its zero-reader nodes and epoch fields (every
  destructure ignored them); current_overlay (a write-only cache of
  names[idx]) is deleted. One trap en route, pinned against the
  binary: the page read's inferred open-record receiver computed field
  offsets over the partial demanded set (`.nodes` read offset 0 — the
  boundaries column — the named trecordopen-wrong-field class); the
  fix closes the record at the one projection, structural because the
  judge refuses nominal-vs-open-row (12 census errors named every
  site). Self-compile 5.49s median / 511MB RSS — neutral time, flat
  memory (+5MB = 13 bands × 384KB), churn deleted.
- boot/mentl.wasm  sha256 0e3af09c7a966a1ba50adf34ecaa57f685a2c288b7d0581adb32177934d95834
  (CLEAN m2 == m3 at 291,710 lines; census 0; comment-refs 0;
  frontier 266/0; proof-exactness 9/9; crown 5/5;
  micros-through-m2 114/114)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

Prior pin:

- source: THE FORK TRIPLE (2026-07-25; Phase C rung 0 — R6, the world
  arc's final rung, and the two-channel interleave's first live
  witness). Every candidate fork now restores THREE legs — graph
  checkpoint, heap region, WORLD — at all three fork sites (the synth
  fan's two loops and try_each_annotation, which had held half a pair
  since its birth). world_restore lands as a substrate op (the fork
  boundary's world reset, NOT a general setter — the one sound use is
  a value world_top() returned at the fork point, which predates every
  push the forked extent made), crossed the recognition boundary in
  the two-generation dance the crossing constraint demands (the gate
  refused the single-step form exactly as the world_top precedent
  says). The world leg had held by extent-balance alone — an
  accident-invariant named a contract before the parallel fan needs
  it. THE INTERLEAVE WITNESS: landing the heap leg at
  try_each_annotation trapped the field leg in intern_probe — a
  candidate's intern_str("Alloc") FIRST-inserted its canonical row IN
  the region, the reset zeroed it, and the next candidate's probe
  walked garbage. The intern is durable state; a fork extent must
  never first-intern. The pre-warm (the three severed-effect names
  interned once below the fork marks) makes every in-fork intern a
  pure hit — the contract stated at the site, and the two-channel
  design's first measured case banked for the §5.O decl regions.
- boot/mentl.wasm  sha256 09b95e50d1f410011a31221325e4aeb013752f539ce83753171fc332dff56223
  (CLEAN m2 == m3; census 0; frontier 266/0; proof-exactness 9/9;
  crown 5/5; micros 114/114)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

Prior pin:

- source: THE ORDER-INDEPENDENT JUDGE GOES LIVE (2026-07-25; phase B-ii
  step 5 — the two-pass walk re-wires into compile_stdin, un-gated by
  the day's own arena strikes and made VIABLE by four cuts its landing
  forced). The banked recipe re-landed (diag_quiet trial → final pass
  resolving trial finals; fn pre-registration skipped; the seen-set dup
  walk already the incremental cursor's form) and self-compiled with NO
  OOM on the first try — then the convergence loop cut it from 24.5s
  to 5.3s: (1) TypeVariants — the type's own env entry carries its
  variant specs, minted at registration where the list is in hand; the
  whole-env backward scans (variant_specs_filter_from, 46% of the
  two-pass compile once the env doubled) deleted whole; registered
  BEFORE the ctors so a same-named single-variant ctor shadows it in
  value position (the 585-error refusal that taught the order), read
  through the kind-filtered walk. (2) The env walk family unified: the
  three predicated bucket walks (type/ctor/variants) are ONE
  env_bucket_pos_where over top-level predicate fns passed as static
  closure pointers — zero allocation; the plain hottest-path resolve
  keeps its own predicate-free shape. (3) The trial is a WORLD of its
  own: fresh affine/region/verify ledgers die with its bracket. (4) THE
  CLASS EXPOSES ITS MOST-REFINED MEMBER — the ret-annotation pin
  rebinds the ret class to the resolved refined annotation, so
  generalize publishes refined finals and every caller inherits the
  decl's own proof; without it the sharper judge raised four
  undischargeable copies of already-carried proofs (the capability
  leg's differential caught it; the probe named all four as `self == 7`
  at the call-bodied decls). The order-independent judgment now costs
  ~12% over single-pass (5.3s vs 4.7s; RSS 506MB) and the wheel it
  emits is 40k lines smaller than the single-pass wheel — the finals
  prune what looseness padded.
- boot/mentl.wasm  sha256 5b693139445b0183f7cf9ed713f95e96d861cdca27a230a74d248258735e031b
  (CLEAN m2 == m3 at 290,409 lines; census 0; frontier 266/0;
  proof-exactness 9/9; crown 5/5; micros 114/114)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

Prior pin:

- source: THE WORTHINESS CLOSURE (2026-07-25; the arena arc's second
  strike — the demand analysis' 274MB monster dies, and its fix's own
  first form was caught under-approximating by the gate). The
  measurement chain: spec_demands_of owned 362MB; spec_resolve went
  check-then-build (A.3's exact shape — the eager walk re-cloned every
  fully-concrete site type; ~29MB); the sub-seam probe then pinned
  spec_worthy_fix at +274MB — every ROUND re-scanned every candidate's
  whole body twice and re-minted every interior site's mangled name,
  though BOTH facts are round-invariant. The fused form: ONE body scan
  per candidate (inside the transitive-closure fix) yields the inner
  candidates, the sensitivity witness, and the interior twin-name
  edges; the worthiness fixpoint is then a pure name-set closure
  (closure_fix's species — zero scans, zero mints per round). THE
  CATCH, counted: the first fused form ran the sensitivity witness
  UNDER the instantiation ctx — but the witness's own law reads "at
  the floor, no substitution: the var-ness is exactly what the floor
  emission sees" — ctx masked every var, twins under-emitted by 17k
  lines, and the address-comparison miscompile class re-opened; the
  float frontier leg convicted it (exit 1) through the unblessed
  intermediate pin, and the witness moved ctx-clear. MEASURED, the
  corrected build: self-compile 6.78s → 4.71s (−31%), RSS 728MB →
  407MB (−44%), every twin kept (frontier 266/0, the float legs
  green).
- boot/mentl.wasm  sha256 a9d0fb4515ec765769fd2f797645234ff2267b30318a91b8c67093b296ebfbd3
  (TRANSITION m3 == m4 at 332,604 lines — the analysis change crossing
  one generation; census 0; frontier 266/0; proof-exactness 9/9; crown
  5/5; micros 114/114)
- generated by: TRANSITION; re-pinned from m3 per march.sh (prior pin follows)

Prior pin:

- source: THE EMIT PHASE JOINS THE ARENA (2026-07-25; phase B-ii steps
  1+3 open the arc — measure first, then the region). The watermark
  probe named the image's true shape (post-read 11.8MB / post-parse
  66.4 / post-infer 176.0 / post-saturate 178.0 / post-lower 277.5 /
  post-reach 307.8 / post-emit 753.8MB): EMIT owned 59%, and the
  sub-seam probe split it exactly — spec_demands_of alone 362MB (its
  own "zero new storage" comment refuted by measurement), fn-record
  collection ~15MB, per-fn emission churn ~54MB. The per-fn cut landed:
  emit_functions built EVERY fn's full text before writing one byte
  (map-then-each — the whole module materialized); now each record
  builds → streams → resets under its own heap region, and the twin
  loop, wide wrappers, and fold leaves ride the same bracket. Measured:
  emission's allocation share fell to ~0 (pre-fns 699.0MB, post-fns
  699.03MB); Law 7 held byte-identical (allocation addresses never
  reach the wat). The bracket's standing contract, stated at the site:
  per-fn metadata handlers overwrite before any read, so a dangling
  region pointer is never read. The 362MB spec-demand churn is the
  arc's named next strike, number in hand.
- boot/mentl.wasm  sha256 8ea44a737ad856efda1338111709f2249c521d02f9b423c4fc811dcb87ea0367
  (CLEAN m2 == m3; census 0; frontier 266/0; proof-exactness 9/9;
  crown 5/5; micros 114/114)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

Prior pin:

- source: THE INCREMENTAL CURSOR (2026-07-25; phase B-i landing 3 —
  §2's cached-cursor mode made cross-run real, and the eight
  interrogations cut it twice before it landed). Patch one module of a
  DAG and the next compile restores the image, names the exact cone
  ("warm: re-deriving b main" — a stays cached), re-judges ONLY the
  cone into the restored world (re-registration shadows; the graph
  grows monotonically; the entry is always in a nonempty cone), and
  re-persists — the image tracks the tree edit by edit, and the
  emission is BYTE-IDENTICAL to a cold compile of the patched tree.
  The image carries its own manifest, entry name, and per-module
  statement buckets as typed roots, so the post-swap path takes ZERO
  pre-swap arguments. The interrogation cuts: warm_root DIED (a stored
  flatten of warm_asts — the derived value persisted beside its
  source; warm_program() is the read); the pairs/deps/order walks
  fused into ONE tree scan (three content passes over the same files
  became one, all three consumers projections of it). Two classes paid
  en route: the pointer-eq class twice (module_in and manifest_hash_of
  compared erased list elements at the word floor — every module
  "changed" and none joined the cone; the `: String` Intent-Boundary
  pins carry the proof until name-is-handle retires the class), and
  the duplicate-fn refusal DECOUPLED from the env into the
  registration walk's own seen-set (an env-based check misread a prior
  judgment's entry as a duplicate the moment the cone re-registered —
  the exact flaw the two-pass build had named; its banked design is
  now the landed form).
- boot/mentl.wasm  sha256 dfe19175dd3434460062e9553b176eb085d47d251dc0344b54423f68f89321b3
  (CLEAN m2 == m3 at 330,626 lines; census 0; frontier 266/0 — twelve
  persist/warm/resume/incremental legs; proof-exactness 9/9; crown
  5/5; micros 114/114)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

Prior pin:

- source: THE RESUME VERB + THE FINGERPRINT DISSOLUTION (2026-07-25;
  phase B-i landing 4 — the image IS the process, and the interrogation
  deleted a gate). `mentl resume <image>` re-enters a persisted compile
  image and emits with the SOURCE ABSENT — the projection rode the
  image; the frontier leg deletes main.mn and the emission stays
  byte-identical. The restructure that made it sound: rehydrate's
  effect arm DISSOLVED into image_resume, a direct-substrate restore —
  an arm-form restore put a dispatch bracket's pre-swap world write on
  the trust path, which is precisely what the world-fingerprint gate
  existed to protect; with the restore as a bare substrate call, every
  remaining pre-swap-saved world write fires in the benign tail
  (extent-ends after the last perform) and every post-swap dispatch
  walks the RESTORED $world_g — image-resident end to end, resumable
  from ANY process regardless of its own install or allocation prefix.
  The fingerprint, its walk, and its wire field are DELETED (a gate
  that guards nothing is not carried); the wire is
  [key][k][size][gcount][globals][image] and the layout belt
  (corrupt-gcount → the structural trap) plus the build-key refusal
  (both keys named) are the two corruption legs, each seen RED.
  compile_remainder is the ONE home for the projection half — cold,
  warm, and resume all run it, so the three routes cannot drift.
- boot/mentl.wasm  sha256 99ecf00d639ebffb89c51b8acb09f731fbb807b57169eeadf931c98541c5b2df
  (CLEAN m2 == m3 at 330,542 lines; census 0; frontier 263/0 — nine
  persist/warm/resume legs green, the source-absent resume among them;
  proof-exactness 9/9; crown 5/5; micros 114/114)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

Prior pin:

- source: THE WARM START (2026-07-25; phase B-i landing 2 — the compile
  restores its own analyzed image). driver_compile_entry keys the weave
  (every DAG module's text, hashed in canonical order, mixed with the
  build key) and, on a clean analysis, persists the whole rooted image
  into the project's .build; the next compile of the same weave probes
  BOTH wire gates cold-side (a stale or foreign cache falls back to the
  cold path instead of refusing), restores, and lowers the live graph —
  frontend and inference skipped whole. The typed re-entry is warm_root,
  a top-level cell the globals record restores. TWO laws paid for en
  route: (1) VIRGINITY IS THE RESTORE'S CONTRACT — $image_restore now
  zeroes [image-extent, old-bump): the restored heap line rewinds, so
  the resuming process's dead pre-swap heap must read as never
  allocated, or post-restore allocations serve stale bytes as unwritten
  slots (measured: the warm saturate walk read a dead spine and chased
  a negative handle into list_index's bounds trap; the two-process
  fixture had survived only by allocating almost nothing post-swap —
  an accident named a contract, forensic law 5). (2) A PREAMBLE FIX
  CROSSES A GENERATION — the executing $image_restore is the EMITTING
  compiler's, so every probe of the fix through the boot-emitted m2 ran
  the old form; the fix first executes in m3's own body (five probe
  rounds ran before this was seen — the crossing constraint at the
  preamble altitude, now in the ledger so it is never re-paid).
- boot/mentl.wasm  sha256 28eb3444492523779828046824f14d28be47b71f26e71e5973b03765ca754e6c
  (TRANSITION m3 == m4 at 331,274 lines — the 2-line m2/m3 diff is the
  restore preamble crossing one generation; census 0; frontier 260/0
  with the two warm-start legs green — cold persists, warm restores
  and emits BYTE-IDENTICAL WAT off the restored image; proof-exactness
  9/9; crown 5/5; micros 114/114)
- generated by: TRANSITION; re-pinned from m3 per march.sh (prior pin follows)

Prior pin:

- source: THE ORDER-INDEPENDENT JUDGE'S HARVEST (2026-07-25; phase B-ii
  step 0 closes — the two-pass walk rebuilt, its verdicts converged to
  ZERO, its OOM re-measured as REAL, and its fifty findings landed as
  fixes while the machinery itself unwired). The two-pass build (trial
  pass quiet, final pass re-judging fresh nodes against the trial's
  final schemes) convicted the wheel of ~20 latent order-masked wrongs,
  each verified and fixed: abs pinned Int by its own literals and was
  an IDENTITY on negative floats at every dsp call site (now Float);
  infer_unaryop string-matched "Neg"/"Not" against the parser's UNeg/
  UNot ADT — no arm ever matched and a catch-all fabricated `!x` as its
  operand's type (now the exhaustive ADT match); the formatter's chain
  arms matched Expr constructors against ast_kind_of's NodeBody — every
  chain fell to the non-chain render; autodiff declared its matrix
  [Float] at the op AND the tape entry; the record-literal field
  carrier is the parser's (name, value) tuple, not the resume-update
  record — oracle's three literal arms read the wrong shape; the
  str-raw satellites (count_lines, nth_line_offset, line_end_from, the
  lsp_frame header family) pinned String args as raw Ints — now read
  through the typed byte faces, with read_headers_until_blank gaining
  its str_of_buf construction boundary; list_eq's loop crossed
  altitudes (typed list_index inside a raw-word family — now the
  unchecked read its f64 siblings use); driver_check_module's if arms
  disagreed () vs [Node]; with_run exited unit on a named verb; and
  ~35 declared rows widened to their bodies' truth across the show
  family, the Intern render family, and the Pure-declared helpers
  (the widen loop, 13 → 22 → 18 → 6 → 2 → 0 under boot). The two-pass
  machinery itself is UNWIRED: the judge-0 wheel's m3-leg self-compile
  exhausts the 4GB bump image at emit_wide_wrappers (alloc's
  wraparound guard, ~28s wall, 1.1GB RSS with a 4GB bump extent) —
  the arena DEP is measured real, and the working build recipe is
  banked on Hβ.infer.order-independent-verdicts.
- boot/mentl.wasm  sha256 6965d5bb842fafcb58765f02b7a4104db3607ec81d9a29e9626e0fd31dad3c04
  (TRANSITION m3 == m4 at 330,255 lines — the 14-line m2/m3 diff is
  the unary-op inference correction crossing one generation; census 0;
  frontier 258/0; proof-exactness 9/9; crown 5/5; micros 114/114)
- generated by: TRANSITION; re-pinned from m3 per march.sh (prior pin follows)

Prior pin:

- source: THE ROOTED-IMAGE PERSIST (2026-07-25; phase B-i landing 1 —
  persist = memcpy made real, two processes proving it). The emitter
  projects three facts of a module over ITSELF, only when the reachable
  tree performs an image op (Law 7 elsewhere): $build_key (a hash of
  the fn table names, the interned data, and the mutable-global census
  — exactly the same-build-only word classes a restored image's baked
  words depend on), the $globals_save/$globals_restore pair (the
  mutable-global record, slot 0 the heap line), and $image_restore
  (globals from the wire first, then ONE memory.copy of the image over
  [0, size) — memmove semantics make the overlapping self-copy exact,
  so no staging buffer exists; wasm call frames live engine-side and
  survive their own image swap). persist.mn sheds the record-copy
  prototype whole: the wire is [key][k][size][fingerprint][gcount]
  [globals][image], rehydrate refuses through two gates — the build
  key (same-build identity) and the world fingerprint, RE-PURPOSED:
  under whole-image restore it is the install-prefix correspondence
  contract that makes every unwinding dispatch bracket's saved world
  address valid in the restored image. The declared-unwired
  E_ResumeWorldMismatchWorld deleted (wire-or-delete resolved: the
  refusal rides fail, and the cross-world-resume class dissolves when
  the world rides the image as data). The typed re-entry channel is a
  restored top-level root — no up-cast exists anywhere in the path.
  Gates seen at all four corners: leg A persists mid-computation (40),
  leg B — a FRESH PROCESS of the same wasm — re-enters the image and
  runs A's thunk against A's heap pointees (42); a corrupted key
  refuses naming both keys; a corrupted fingerprint refuses naming
  both worlds. RED on the pre-image boot: the ops are unrecognized
  substrate, the executable refuses at compile.
- boot/mentl.wasm  sha256 88c1b888b8416e422a688717b30e7e90a61396b5346433770c3f5f7e9340f77b
  (CLEAN m2 == m3 at 330,102 lines; census 0; frontier 258/0;
  proof-exactness 9/9; crown 5/5; micros 114/114)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

Prior pin:

- source: THE SEQ-OP ROW UN-REVERTS (2026-07-24; phase B-ii step 0 —
  re-measurement, not the arena, un-gated it). infer_seq_op reads the
  callee's OWN declared row live (graph_chase(fh), the read its own
  comment prescribed), so the seq-op family's honest Memory + Alloc
  attributes at every call site; the Memory floor survives only for an
  unresolved callee. The 2026-07-17 OOM ruling was one representation
  era stale (559k-line wheel then; ~694MB peak against the 4GB ceiling
  now). The honest-attribution compiler re-judged its own source at the
  m3 leg and named nineteen under-declared rows; the widen loop closed
  them 19 → 3 → 2 → 0, and the honest wheel lands 3,600 lines SMALLER.
- boot/mentl.wasm  sha256 7aa2d7a091ee564343047b28734abb8a578db3a4a1ec81ff4e4f1cd773589709
  (CLEAN m2 == m3 at 329,046 lines; census 0 at every generation;
  frontier 254/0; proof-exactness 9/9; crown 5/5; micros 114/114)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

Prior pin:

- source: THE TSTRING DISSOLUTION LANDS WHOLE (2026-07-24; A.4 steps
  4+5 in one arc — PHASE A COMPLETE). The nullary TString constructor
  is DELETED from the Ty ADT; "String" IS one canonical
  TAlias("String", TList(TByte)) node, minted once in THE CANON
  (src/canon.mn — the new one-home for shared canonical nodes, sorted
  early so every consumer resolves under the current boot; the
  order-independent-lets capability is the named dissolver of the
  placement constraint). Every mint flipped: parse_type_atom, the four
  infer binds, the eighteen face-table rows. The unify/same_ground
  string arms, both already_string probes, and every H6 scalar row
  (repr_of, fold_strip, fold_sig, show_type, query_flow_label,
  occurs_in, extract_row, ty_handle_of, the chase/subst walks) deleted
  whole — the compile-guided sweep the recon prescribed, with the
  census naming each site. BConcat strips first (one list arm, loud
  floor). Synth's byte-element dispatch keeps String holes proposing
  string literals; enumerate_typed's row widened by seq_elem_face's
  LookupTy. The sentinel era's own comment carried its retirement
  reasons: instantiate now shares (A.3), and one shared node cannot
  partially migrate.
- boot/mentl.wasm  sha256 5673c47ca37a6cc99d80d8afa731b267f7c709714a4b0b3ca041cbcf85ad65aa
  (TRANSITION m3 == m4 at 332,555 lines — the 6,336-line m2/m3 diff is
  the type-representation change crossing one generation; census 0 at
  every generation; frontier 254/0 with every string-oracle and Cast
  leg green through the TString-free compiler; proof-exactness 9/9;
  crown 5/5; micros 114/114 — every : String annotation in every
  micro minting the alias)
- generated by: TRANSITION; re-pinned from m3 per march.sh (prior pin follows)

Prior pin:


- source: ONE TYPE, ONE HASH (2026-07-24; A.4 step 3). hash_node_of's
  list arm dispatches on the element FACE: a byte face hashes as text
  (str_hash) — the same projection a String field always took — closing
  the route split the recon surfaced (top-level hash(s) ran the
  length-seeded list mix; a field ran the text mix). The reach seed
  mirrors the route exactly (the visitor-walk projection law). BConcat
  already emitted one concat for both spellings; its strip-first wrap
  rides the mint flip where it becomes load-bearing.
- boot/mentl.wasm  sha256 540eb9502e4da077be153a58ec9a0f21b763d074b37049399cf424386b799c16
  (CLEAN m2 == m3 at 333,019 lines; census 0; frontier 254/0 — the
  scalar-faces leg green through the new route; crown 5/5; micros
  114/114)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

Prior pin:


- source: THE DEAD-BUT-AGREEING ARMS DELETE (2026-07-24; A.4 step 2 of
  the TString dissolution). Thirteen TString match arms across the
  emit's eq/cmp/show/hash dispatches, show_node_of/hash_node_of, the
  literal stride class, and lower's reach seeds — each verified by the
  recon to sit behind fold_strip (a string arrives at every one of
  these matches already as TList(TByte), so the byte-route sibling arm
  IS the live path) — delete whole. The census confirmed no match lost
  exhaustiveness; the fixpoint held CLEAN with the wheel 280 lines
  smaller; and the three A.4 oracle legs (scalar faces, aggregates,
  annotation boundary) plus the full string battery ran green through
  the arm-less wheel — the behavioral proof that the byte faces carry
  every route the deleted arms claimed.
- boot/mentl.wasm  sha256 e213ce1b912fb04cb2a3d8b9c7cd3afe2b3e22617e26d1b72ca7687d9783d5c0
  (CLEAN m2 == m3 at 332,942 lines; census 0; frontier 254/0;
  proof-exactness 9/9; crown 5/5; micros 114/114)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

Prior pin:

- source: THE VOICE SPEAKS ITS PAYLOADS (2026-07-24; two render fixes in
  the diagnostics family — #9 residue singles). show_eff_arg's EAType
  arm renders the instance's actual type through show_type (the old
  opaque marker hid the exact instance the diagnostic teaches — the
  cast-refused crucible now reads `!Cast vs Cast(t…)` with the live
  payload); show_pred_operand gains the call-shaped arm, so the
  canonical refinement vocabulary (len(self), abs(x)) renders as the
  call instead of an opaque marker. callee_name's sentinel is
  load-bearing (a non-matchable name), untouched.
- boot/mentl.wasm  sha256 10b79aa8cd25379ac9beffcf17dc96b8f5f4413070a0a8653a35e74a9589abf5
  (CLEAN m2 == m3 at 333,222 lines; census 0; frontier 245/0;
  proof-exactness 9/9; crown 5/5; micros 114/114)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

Prior pin:

- source: IDENTITY IS THE FIRST STRUCTURAL FACT (2026-07-24; the eq
  short-circuit strike). The generated $eq_<sig> helpers — product
  conjunction and sum tag-dispatch alike — open with one word compare:
  the same record equals itself, before any field walk or tag load.
  list_eq and list_eq_f64 say the same fact in-source (their raw bodies
  read operands as words; identity is the value-faithful reading even
  for a NaN-holding sequence compared to itself). Sound unconditionally;
  effective wherever canonical instances flow, which the intern made
  the common case for every name. str_eq's typed body cannot say it —
  the String == identity path is the named emit-wrapper strike.
- boot/mentl.wasm  sha256 3d862357c99d58650e05d557d49db5ab3a4abffde237b7a51e99e23b9df3cb4d
  (TRANSITION m3 == m4 at 333,071 lines — the 20-line m2/m3 diff is
  the two generated preambles crossing one generation; census 0;
  frontier 245/0; proof-exactness 9/9; crown 5/5; micros 114/114;
  self-compile ~6.4s)
- generated by: TRANSITION; re-pinned from m3 per march.sh (prior pin follows)

Prior pin:

- source: THE CAST VOCABULARY (2026-07-24; A.5 in its refuted-to-correct
  scope — zero-reader vocabulary, the RI8 precedent; the body sweep the
  panel killed stays with the arena-gated signature-driven landing).
  effect Cast { addr(a) -> Int } declares the structure-forgetting
  DOWN-cast beside effect Memory (the substrate home both worlds see);
  lower ERASES the perform to its operand — identity at the word level,
  zero emission, so no census demand reaches the executable gate and
  the row carries the whole meaning. No from_addr up-cast exists, ever.
  Crucibles: cast-addr proves word-face facts through the erase (42;
  RED on the pre-Cast boot, where the op lowered as a handler-less
  demand and the gate refused) and cast-refused proves the severance
  REPORTS (E_EffectMismatch naming !Cast vs Cast at the declaration —
  the A.6 bare-vs-instance handle match doing the work; the class is
  not yet an armed refusal, and arming it is the refusal-law's own
  licence-gated landing, named, never smuggled).
- boot/mentl.wasm  sha256 394917cf5cf4dacbade194aec51bea4c54100a948cdbd8ee3fedc6b6ee4b3a9a
  (CLEAN m2 == m3 at 333,002 lines; census 0; frontier 245/0 with the
  two Cast legs; proof-exactness 9/9; crown 5/5; micros 114/114)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

Prior pin:

- source: INSTANTIATE SHARES, NEVER CLONES (2026-07-24; A.3 — the panel's
  allocation fix, landed in its second form after the artifact refuted
  the first). subst_ty and chase_deep_at are CHECK-THEN-BUILD: a Bool
  change-walk (word returns, zero allocation) gates the original eager
  arms, and the build recurses through the sharing face so unchanged
  sub-subtrees re-share at every level; find_mapping's per-leaf filter
  materialization is a plain walk now. THE COUNTED KILL: the first form
  flag-threaded (Ty, changed) tuples per arm, and tuples are heap
  records here — the scalar arms minted garbage that canceled the
  sharing, a MEASURED +6% (medians 6.42s → 6.82s over three runs each);
  the check form measures neutral (6.51s median, inside the reference
  band) with zero records on unchanged paths. Max RSS flat (~717MB —
  the self-compile peak lives in lower/emit, not the instantiate
  channel); the sharing's real purchase is headroom for the TString
  alias dissolution, whose type-node bloat multiplies through exactly
  this channel.
- boot/mentl.wasm  sha256 6d6ac5ab98f81bad9d4bfb79c74973b9efc6fa34a0af8d452886f2a941be8f96
  (CLEAN m2 == m3 at 332,964 lines; census 0; comment-refs 0;
  frontier, proof-exactness, crown, and the micro tier green at the pin)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

Prior pin (A.3's first form — superseded same session by the pin above;
kept in the chain for the counted kill):

- boot/mentl.wasm  sha256 0bb49387b11f0211a3d2c504e589b33a5e64f377558ebe6a233c75d3503fa093
  (CLEAN m2 == m3 at 332,833 lines; the flag-tuple sharing form —
  measured +6% and rebuilt as check-then-build the same hour)

Prior pin:

- source: EFFNAME IS A HANDLE — identity becomes a contract (2026-07-24;
  A.6, the crown cash-out). ENamed/EParameterized carry the intern
  HANDLE; EPure carries the reserved absent 0. TWO projections replace
  the one by-name deref: eff_name_handle (Pure — the comparison KEY;
  the whole by-name family's six str_eq leaves are word i32.eq now, and
  a bare name matches its parameterized instance by the same compare)
  and eff_name_str (the RENDER projection, reading the intern table via
  intern_name_of — diagnostics, show, and lower's ename extraction).
  The intern gained intern_str + intern_name_of + the entries reverse
  read (the reader arriving with its writer), and intern_table moved
  OUTERMOST in the analysis core: the report arm renders its message
  live, and an arm's performs resolve outer to its install — an inner
  intern would leave diagnostics' row rendering with no handler. The
  census-guided sweep converged 12 → 2 → 1 → 0, every typed site named
  by the medium. THE SITE NO CENSUS COULD SEE, the fixpoint caught:
  eff_names_of pushed the raw handle word into lower's erased
  bare-string list — no type ever meets an erased list — and the m3
  leg died in the escaping fixpoint where str_lt walked a tiny address
  into a garbage allocation size. The string BOUNDARY now renders
  canonicals at that one projection; the two-oracle law (armed census
  above, self-application beneath) is what makes the blind spot
  survivable. The crown's positive-path residual (~146 byte-equal-but-
  pointer-distinct false mismatches) is closed BY CONSTRUCTION — the
  pair is untypeable under Int handles, and a missed mint is a loud
  E_TypeMismatch, never a silent miss.
- boot/mentl.wasm  sha256 91e35f1eac91f3e68f0ff8da6d297af124c241c16df32d0f2f3b81c380a6e424
  (CLEAN m2 == m3 at 330,661 lines — handle assignment is a pure
  function of the source, so the new compiler reproduces boot's exact
  emission; census 0; crown 5/5, frontier 241/0, proof-exactness 9/9,
  micros 114/114; self-compile 6.9s)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

Prior pin:

- source: THE INTERN SUBSTRATE — a name is born once (2026-07-24; A.2,
  the phase's spine). Every identifier resolves through intern_table
  (the analysis core's innermost handler, all 14 chains): one row per
  unique spelling — (canonical String, handle, keyword kind) — hashed
  over the SOURCE RANGE (str_hash_loop; no slice minted to hash),
  compared by str_eq_at, classified by keyword_kind ONCE at first
  sight. A repeat occurrence returns the same String pointer with its
  banked kind: zero allocation, zero keyword compares, where the old
  path minted a slice and walked up to 18 str-compares per identifier.
  Handle 0 reserved = absent; handles are per-install and never cross
  a bracket. The march ruled TRANSITION (592 lines) and the divergence
  was ARBITRATED to a benign heal, not a leak: the capture dedup
  (set_contains at lower.mn's capture walk) compares names without a
  String proof — pointer identity — so boot's per-occurrence slices
  double-captured a twice-mentioned free name (index_of's search
  captured sublen TWICE, verified in the m2 WAT); canonical instances
  make the dedup byte-accurate and the records shrink. The class
  itself (identity-sensitive name compares) is now MASKED by
  canonicity within a lex session, not fixed — the accident-invariant
  law applies, and A.6's Int handle is where identity becomes a
  contract. MEASURED: self-compile 6.42s → ~6.0s; str_eq share
  5.85% → 4.0%; the intern machinery itself below the profile floor.
- boot/mentl.wasm  sha256 66e097e9898ee92f7d7c2417ce4d8af21101435b60571388fae0d9c982b13f1f
  (TRANSITION m3 == m4 at 330,123 lines; re-pinned from m3; census 0
  at every generation; frontier, proof-exactness, crown, and the
  micro tier green at the pin)
- generated by: TRANSITION; re-pinned from m3 per march.sh (prior pin follows)

Prior pin:

- source: THE STALE-BUCKET FALLBACK WAS THE SCAN (2026-07-24; phase A's
  fourth strike — env_find_flat DELETED whole). env_resolve validated
  only the FIRST bucket hit and bailed to the O(n) linear scan on
  staleness, and the profile showed staleness is HOT (5.85% — every
  scope re-entry reuses positions), not the "rare case" its comment
  claimed. env_bucket_pos now validates against the buffer INSIDE the
  walk (the type/ctor siblings' exact shape) and continues past stale
  pairs; the walk is complete by construction (every env_extend adds
  its bucket pair, pairs are never removed), so the fallback is
  unreachable and §5.O's documented villain dies. MEASURED:
  self-compile 6.42s (15.56s → 6.42s across the day's three strikes;
  70.58s → 6.42s, 11×, across the campaign's perf arc).
- boot/mentl.wasm  sha256 3d2b029cc5ff52873591214ed41266306a3ebe888538acaa2b8bb0e971bb5de4
  (CLEAN m2 == m3 at 330,057 lines — 50 lines smaller; census 0)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

Prior pin:

- source: THE PATTERN PATH FOLLOWS THE ENV'S OWN EDGE (2026-07-24; phase
  A's third strike). ctor_payload_tys_of resolved a constructor by
  scanning the whole env SNAPSHOT backward per pattern bind — 6.86% of
  the self-compile, env_find_flat's sibling at the emit layer, with a
  pinned str_eq the untyped snapshot tuple forced. The env's bucket
  index already draws the edge: env_lookup_ctor (the third kind-filtered
  read beside env_lookup_type, same validated-walk shape) resolves the
  newest live ConstructorScheme entry O(1); the snapshot scan and its
  pointer-eq essay are DELETED whole. MEASURED: ctor_payload_tys_find
  gone from the profile; self-compile ~8.5s → ~7.2s.
- boot/mentl.wasm  sha256 8ba823f5515888d1ec64e5f15b287cc5378b6c111ac7c5a39135053665d21001
  (CLEAN m2 == m3 at 330,107 lines; census 0; battery 114/114 through
  the pre-commit gate)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

Prior pin:

- source: THE CEREMONY FUSE (2026-07-24; phase A's measure-first second
  strike — the post-crc profile put the sequence header ceremony at the
  top: seq_stride 9.4% + seq_tag 4.5% + decode_stride 4.2% +
  load/store_strided 5.8% raw, with list_index_unchecked 18.3% and len
  8.9% paying it per element; the documented instantiate cluster
  sampled ~0%). The fix reads the tag WORD once per entry and derives
  both projections locally: list_index_unchecked's fused arms (word
  fast path one load; concat/slice recursion derives no stride),
  list_set's tw==0 direct store (byte range-trap and wide copy-protocol
  preserved through store_strided), push's one-read header, and the
  flat_fill family collapsed — flat-to-flat ranges are ONE native
  mem_copy (flat_fill_flat_src, copy_range, flat_fill_slice_range's
  flat-base fast path), snoc fills derive the stride once per node.
  MEASURED: self-compile 15.56s → 8.46s (1.84×; 70.58s → 8.46s across
  the two perf landings). Bytes-exact by construction per arm; the
  battery, census 0, and the fixpoint arbitrated behavior.
- boot/mentl.wasm  sha256 cec0f2dfdda224f10b513d438784bfad95332f4f848594db228f7d89fd0269e4
  (CLEAN m2 == m3 at 329,959 lines — a lib-source change, both
  generations compile the same source; census 0; frontier,
  proof-exactness, crown, and the micro tier green at the pin)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

Prior pin:

- source: THE PROSE GATE COST FIVE COMPILERS (2026-07-24; the campaign's
  measure-first opening — host perf showed 83% of the entire self-compile
  inside comment_refs_check's scope resolution: a snoc-spined scopes list
  paid its depth on every list_index, O(scopes²) spine steps per commented
  handle, while every documented perf target measured under 1%). The fix
  is one flatten (crc_fn_scopes' result through list_to_flat before the
  per-comment scans — the iterate-flattens-once precedent). MEASURED on
  the same wheel source: boot (pre-fix) 70.58s → fixed wheel 15.56s, 4.5×;
  the whole build loop, march legs and battery included, inherits it.
- boot/mentl.wasm  sha256 d15411f7eb08f8cbf38dd77057581c1026b663e31238756ffa32d94ee1732189
  (CLEAN m2 == m3 at 329,776 lines — a compiler-internal perf change,
  byte-identical emission; census 0; the board whole at the pin)
- generated by: CLEAN; re-pinned from m2 per march.sh (prior pin follows)

Prior pin:

- source: THE FANOUT SPAWNS FOR REAL (2026-07-24; the task-record
  landing — `>< ~> parallel_compose` runs branches on host threads over
  ONE shared image, and the whole phantom glue dies). The task record
  ([task closure@0][completion@4][result@8]) is allocated per spawn
  through the ONE allocator: $spawn_task_impl builds it, hands its
  address to the thread-spawn import, refuses loudly on a host spawn
  failure; the rewritten $wasi_thread_start stamps the per-instance
  identity ($tid_g), runs $__init_lets when lets exist, invokes the task
  through the closure protocol, stores the result, releases the
  completion word; $join_task_impl atomic-waits and reads it. The
  module's MEMORY OWNERSHIP follows its own proof (spawn_task in the
  fifth projection): a spawning module IMPORTS the shared image
  (re-exported for the p1 ABI) and allocates through the shared CELL at
  address 64 (compare-exchange bump — per-instance globals cannot serve
  two instances of one image); a thread-free module keeps its defined
  memory, plain bump, and ships NO thread-spawn import at all.
  heap_mark/heap_reset go strategy-invariant at the call site
  ($heap_mark_impl beside $alloc). threading.mn rewrote whole: the
  ffi/sentinel/intrinsic block, the write-only threads ledger, num_cores
  (no truth source under preview1), and the bypassed wasi_threads
  handler are DELETED; WaitResult's decl order now matches the wait32
  ABI (was silently swapped); atomic_rmw gains its real tag dispatch
  (its emit was a dangling call) and two-operand cmpxchg becomes its own
  op. The wheel's dispatch chain drops its dead schedule installs — a
  schedule installs lexically at the fanout it schedules. Gates seen
  RED on the prior pin (exit 134, unaligned atomic in the join):
  frontier real-spawn / real-spawn-float / real-spawn-identity — 60
  through BOTH engines (wasmtime CLI + mentl-runner), the float leg
  proving a spawned instance's carrier readable from the joiner, the
  identity leg proving $tid_g. Sequential twins unchanged at 60.
- boot/mentl.wasm  sha256 8891428f2b2caff4f498332eb8c9dfdffec1be691dc50e0c545c4101ab42a162
  (TRANSITION m3 == m4 at 329,774 lines — the 36-line m2/m3 diff is the
  emit change crossing one generation; census 0 at every generation;
  after the re-pin the board is whole: CLEAN m2 == m3, frontier 241/0,
  proof-exactness 9/9, crown 5/5, battery green)
- generated by: TRANSITION; re-pinned from m3 per march.sh (prior pin follows)

Prior pin:

- source: THE COMMIT'S RECORD RIDES THE __k RAILS (2026-07-24; the
  arm-closure-captures-record landing — the last bracket-maintained cache
  dies). LStateSlotStore's fourth field is the resolved record READ,
  minted at lower through the __hrec ladder: the arm frame binds `__hrec`
  as an LLet alias of its own $__state (the install record the driver
  passed), a lambda captures it (lower_seed_hrec — lower_seed_k's exact
  walk-free shape, transitive by induction), and a resume-bound fn takes
  it as the second trailing param after __k (both call-site forks append
  the pair). With every commit path through the one ladder, the
  $<hname>_state_g singleton globals, the install bracket's
  save/set/restore triple, the _prev locals, and the whole
  singleton_hnames walk family are DELETED — emitted artifacts carry
  ZERO _state_g. The OneShot cousin the pre-build probe surfaced dies
  with it: a OneShot resume-with-state inside a thunk lost its commit
  into the thunk's closure record (mn-oneshot-lambda-commit: 20 on the
  prior pin, 25 here — the RED-first gate).
- boot/mentl.wasm  sha256 bb4b870e0057200a4e981f638d167a6808913685940353b1c6e0c68bae0b28fc
  (TRANSITION m3 == m4 at 329,794 lines — the 2,018-line m2/m3 diff is
  the emit change crossing one generation, and the new wheel is SMALLER
  than its old-emit form; census 0 at every generation; the four
  commit-class micros 25/46/46/51 through the new logic)
- generated by: TRANSITION; re-pinned from m3 per march.sh (prior pin follows)


- source: THE SINGLETON TIER READS THE WORLD + A4 UN-FLOORS (2026-07-24;
  the world arc's R4 completed at the singleton tier, the A4 crossing
  composed on top). The singleton perform's record source is the LIVE
  world chain: LWorldResolve(handle, hname) → $world_find walks
  [key][entry][parent][iw][hkey] nodes by the interned HANDLER name at
  hkey@16 (the op→handler edge is per-OP — an ename key mis-resolved the
  split-effect voice pair, caught by the LSP serve leg's postmortem), and
  the $<hname>_state_g cache is no longer the perform's home (it survives
  solely as the resume-commit store's closure home; the redrive driver
  uses its own __state — both callers always passed the record). A k
  remainder's singleton perform therefore resolves through the FROZEN
  world LResumeK rebinds — the crossed install's record lives in the
  chain after its bracket exits, where the bracket-restored global read
  the null page as config (mn-world-resume-frozen: 134 → 30 → 42).
  handler_stateful counts config params (the arm reads the record for
  config OR state — the silent 30 rode the config-only guard skip), which
  tightens the compile gate's refusal conjunct in lockstep. k2_floor_guard:
  installs are TRANSPARENT to the terminus flag; body_has_foreign_yield
  deleted (the frozen world carries what the foreign clause re-derived).
- boot/mentl.wasm  sha256 8458415b9db09befc315c9493cfa10ab55f3d981dfe4a0dc221fb9ec1115dafc
  (TRANSITION m3 == m4 at 331,648 lines — the m2/m3 diff was the chain
  read + node widening crossing one generation; census 0; frontier 232/0
  incl. the graduated mn-world-resume-frozen leg and the healed lsp
  serve leg; proof-exactness 9/9; crown 5/5)
- generated by: TRANSITION; re-pinned from m3 per march.sh (prior pin follows)


- source: THE KEYED SCAN'S LAST CORPSE LEAVES THE SOURCE (2026-07-24;
  the foundation cut's second write-side cleanup, the ev_perform_entry
  precedent completed). ev_lookup/ev_scan — kept one generation only for
  the prior boot's emitted LEvRef sites — are DELETED from
  lib/runtime/memory.mn exactly as their own comment scheduled (the
  emitted artifact carries zero references; reach had pruned them), and
  pipeline.mn's grounding comment is trued from the dead call-boundary
  evidence-threading mechanism to the live one (the stateful-default
  zero-state read the SingletonUninstalled guard refuses).
- boot/mentl.wasm  sha256 9bfcf506bc2c1733a4b1cce6c65e0c484d0675050cf543faec7cad2be298cb95
  (CLEAN m2 == m3; census 0, comment-refs 0 — the deletion shifts spans,
  so the bytes move while the fixpoint holds)
- generated by: CLEAN fixpoint; self-confirms below (prior pin follows)


- source: THE EVIDENCE REGION DIES WHOLE + THE FORK KEEPS ITS SPINES
  (2026-07-24; the world arc's foundation cut, rung two, shipped with
  the latent fork-pair root its one red surfaced). The keyed-evidence
  machinery is deleted end to end: LSuspend collapses into bare calls,
  LFnRef/LEvRef/LEvEntry/LUnresolvedEvidence die with every walker arm,
  closures are [fn_ptr][nc][captures], the handler record loses
  captured_evs, and the k record freezes the LIVE world —
  [fn_ptr][nc][captures][state_idx][ret_slot][WORLD], the last word
  $world_g at reify, read by $__k_world; LResumeK brackets every resume
  call in the record's frozen world (R4 real). The __resume channel is
  an ARGUMENT (trailing __k param on resume-bound fns). persist's wire
  carries a name-level chain fingerprint read from the record's own
  world slot; rehydrate refuses divergence. The cut's one red
  (capability-hole edit accept, deterministic 134) dug to a root as old
  as the fork pair: graph_push_checkpoint recorded only trail_len, so
  a state spine grown during a candidate fork had no inverse —
  rollback restored slots into the region-doomed spine, heap_reset
  zeroed it, and the next check's finalize read the interned ": " as a
  PendingContinuationBoundary. The checkpoint is now the fork's STATE
  VALUE (seventeen fields snapshotted at push, pre-mark); rollback
  walks the trail backward into the snapshot spines (undo_set_within —
  bounded, never extends) and restores wholesale; the pop is drop_last
  (the snoc-parent read — a slice node minted pre-reset died with the
  region). emit_match_arms' exhaustive fallthrough now carries the
  in-scratch postmortem (scrutinee → word 0, match handle → word 4),
  and the pinning probe called the wheel's OWN
  lookup_ty/show_type/show_reason from the crash branch — the medium
  narrating its own death.
- boot/mentl.wasm  sha256 612b65892dfe7032c1bf1403a14d51a60319abccd0866ec7277e6b33417f7f2b
  (CLEAN m2 == m3 at 334,028 lines — 40% smaller than the pre-cut
  559k; census 0, comment-refs 0, frontier 229/0, battery 113/113,
  proof-exactness 9/9, crown 5/5; the intermediate TRANSITION pin
  4900d2c7 carried the postmortem emit across its one generation)
- generated by: CLEAN fixpoint; self-confirms below (prior pin follows)


- source: THE PERFORM SCAN'S CORPSE LEAVES THE SOURCE (2026-07-23; the
  world arc's write-side cleanup, rung one). ev_perform_entry — the
  prior generation's perform-side frame-region scan, kept in source
  only while the pre-R2 pin's emitted sites named it — is DELETED with
  its reach seed, and every comment naming it is trued to the live
  mechanism (ev_perform_node, the world-chain walk) or reworded to
  name the class without the dead symbol. Nine prose sites across
  memory.mn / lower.mn / wasm.mn; the emit_runtime_contract seed list
  shrinks by one. The march ran CLEAN — both generations emit the
  same bytes because reach had already pruned the fn from every
  module; the deletion removes exactly the source the ledger's own
  comment promised to remove.
- boot/mentl.wasm  sha256 9448692bba6bffce83454c14da978d1c14bb675c27e88815d44c6ee9711da513
  (CLEAN m2 == m3 at 577,727 lines, census 0, comment-refs 0,
  frontier 229/0, battery 113/113)
- generated by: CLEAN fixpoint; self-confirms below (prior pin follows)


- source: THE FORMATTER SPEAKS — render totality + `mentl fmt`
  (2026-07-23; Hβ.format.render-totality-before-fmt's first landing).
  The 18 missing render arms land (Expr 25/25, Stmt 9/9, Pat 8/8 — the
  three surrender-fallbacks DELETED into exhaustive enumeration), and
  first light on the dormant module surfaced four latent bugs, each
  fixed: every list joiner rendered REVERSED (last/drop_last walks over
  source-ordered lists — zero callers had ever seen it); `{{` is not an
  escape (the file's own idiom emitted doubled braces; SYNTAX's \{ is
  the escape — 22 sites swept, the pre-existing IfExpr arm included);
  string content never re-escaped (format∘parse = identity was false
  for any string carrying a quote or newline); and
  `graph_node_at(handle)` typechecked against an op binding while the
  direct-call tier emitted the two-arg raw fn — the op/fn name
  collision class (Hβ.emit.op-fn-name-collision-direct-call, named),
  surfaced the moment format_default gained its first installer.
  PRECEDENCE-INVERSE parens: an operand looser than its parent (or
  equal on the right of left-assoc) re-wraps in the parens the parse
  consumed — (a+b)*c and a+b*c both typecheck, so the gate is
  BEHAVIORAL: the fixture compiles and runs to 42 before AND after the
  render. The render carries the comment weave (decl + interior; a
  trailing comment re-renders leading — movement not loss, stable from
  pass two) and authored param annotations (fields re-sorted to the
  parse's own alphabetical canon). assemble_render gained String
  intent boundaries (the fan's tuple elements reach it free — the §9
  pointer-eq law's sibling). Named refinements: sugar preservation
  (a destructuring let re-renders as its desugared match — the graph's
  truth; byte-fidelity needs the sugar recorded), the width-aware
  layout engine, and the summit — the wheel formatting ITSELF into
  canonical source. En route the fixture walked into the standing
  trecordopen-wrong-field class in its lambda shape (a lambda param's
  field read on a plain record returns garbage — the peer's repro set
  grows one shape; annotated-closed dodges it).
- boot/mentl.wasm  sha256 013e26bd76331622763a4ea9099360c7ab6f0dbb78a34cbb5db7b96c14c6cfbf
  (CLEAN m2 == m3 at 577,765 lines, census 0, comment-refs 0,
  frontier 229/0, battery 113/113)
- generated by: CLEAN fixpoint; self-confirms below (prior pin follows)


- source: THE RATCHET RUNS — 77 tightenings authored by the medium, 19
  corrected by its own canonical judge (2026-07-23). tighten went BATCH
  (every authorable candidate per run; per file bottom-up so offsets
  hold; monotone-safe, each proposal computed against wider
  declarations): iteration 1 authored 75, iteration 2 authored 2, then
  the plateau — 15 multi-line decl heads the single-line patcher
  refuses to mangle (named residue). THE GATES THEN DID THEIR JOB: the
  blob's canonical judgment refuted 19 of the DAG-computed rows
  (census 19 — the order-conditional class, Hβ.infer.order-independent-
  verdicts, measured live at scale), and the correction was not a
  revert but the TRUER tightening: each site trued to the canonical
  judge's own found-row, converging in two passes (19 → 13 → 0). The
  emitted wheel held BYTE-IDENTICAL through census 19, 13, and 0 — the
  whole correction arc was inference truth, zero emit drift. Residue
  banked: the 15 multi-line heads, and the DAG-vs-blob verdict gap as
  the tighten loop's sharpest measured consumer of order-independent
  inference.
- boot/mentl.wasm  sha256 b0204323b9e1b1ea7a9e69770d45bf07cfcaac839ba4cec00626ffc86d276972
  (CLEAN m2 == m3 at 563,042 lines, census 0, comment-refs 0,
  frontier 226/0, battery 113/113)
- generated by: CLEAN fixpoint; self-confirms below (prior pin follows)


- source: THE MEDIUM AUTHORS ITS FIRST CHANGE (2026-07-23 — the
  self-build audit's step 1 executed whole). `mentl tighten` turns the
  first authorable T_OverDeclared into the patch: the collecting
  forwarder over Diag (every diagnostic re-performs outward from the
  arm — sound by the world arc's deep-handler law; all three Diag ops
  armed, since a partial-effect handler leaves zero fn slots) gathers
  (declared, used, name, span); the driver rewrites the decl's
  with-clause to the PROVEN body row and reports old → new. Run on the
  wheel's own DAG it authored: spec_is_agg (src/backends/wasm.mn:223)
  `with Memory` → `with Pure` — the wheel read its own diagnostic
  about its own source and rewrote its own declaration; the human's
  role reduced to running the gates and committing. Three frontier
  legs (author + fresh-check + fixpoint, the fixture copied out since
  tighten mutates its target) seen RED on the pre-verb boot; the
  verify-proof landing (fadbbee7, one pin back) is this landing's
  soundness floor. Tightening is the monotone-safe direction: a
  narrower declaration can only shrink what callers accumulate.
- boot/mentl.wasm  sha256 ab8daa07f54e0145d191cc076f9f65479754d561320dadd35c0f8708a57eacd8
  (CLEAN m2 == m3 at 561,214 lines, census 0, comment-refs 0,
  frontier 226/0, battery 113/113)
- generated by: CLEAN fixpoint; self-confirms below (prior pin follows)


- source: THE ANNOTATION VERIFIER PROVES (2026-07-23;
  Hβ.mentl.verify-after-apply-boundness-only RESOLVED — the self-build
  audit's gap 2, standing directly on the self-authorship path). The
  tentative-apply returns the PROOF: for a row claim,
  row_subsumes(resolve_row(base_ty's row), narrowing) — the fn-finalize
  gate's own engine reading the body truth the graph already carries;
  boundness (verify_after_apply) demotes to the structural belt,
  conjoined at try_each_annotation. Seen RED live: an allocating main
  (++ carries its callee's row) was offered "!Alloc ... proven zero
  allocation"; under the fix it loses the false !Alloc and gains the
  TRUE !IO severance instead, and the pure control keeps !Alloc — the
  proof engine choosing honestly. Two frontier legs judge main's own
  teach line (teach projects every fn in the linked blob, so the
  runtime's non-allocating fns legitimately earn !Alloc lines).
- boot/mentl.wasm  sha256 fadbbee75996c565921578c099611013aa7e1ae45f5faeb770ce6ab733658951
  (CLEAN m2 == m3 at 559,311 lines, census 0, comment-refs 0,
  frontier 223/0, battery 113/113)
- generated by: CLEAN fixpoint; self-confirms below (prior pin follows)


- source: THE BRACKET LANDS — R5, the world arc's first wheel-internal
  consumer (2026-07-23; Hβ.cli.infer-context-bracket RESOLVED). The
  analysis core installs as ONE fn: infer_context (pipeline.mn) takes
  the body as a thunk and brackets it in the eight-handler core in the
  settled order — the exact form REFUTED twenty-four hours earlier by
  the mint-time evidence snapshot, now sound because R2 made every
  evidence-tier perform resolve in the world where it is CALLED. All
  14 inference-reaching chains route through it (compile /
  compile_stdin / compile_source / pipeline_check / check_source /
  query / analyze_fns / battery_compile / edit / at / serve / doc /
  teach / repl); the per-chain parallel_compose + wasi_threads
  re-installs die (dispatch's boundary installs them once, as its own
  comment always demanded); the wheel shrinks 2,168 lines. The
  refuting smoke re-run ALL GREEN: at projects (exit 0), compile -
  emits, check refuses broken input, doc/teach/query/repl hold. A
  future verb structurally cannot mis-order or forget the core.
- boot/mentl.wasm  sha256 2644dab5757478d1dc14b05665e2d62e3706d1c0a33acf202eb9b55e8e4a2792
  (CLEAN m2 == m3 at 558,997 lines, census 0, comment-refs 0,
  frontier 221/0, battery 113/113)
- generated by: CLEAN fixpoint; self-confirms below (prior pin follows)

- source: THE PERFORM READS THE WORLD — R2+R3 of the world-as-value arc
  (2026-07-23). Every evidence-tier perform resolves through the LIVE
  install chain: the LEvPerform arm calls ev_perform_node ONCE (the
  world top passed inline as (global.get $world_g) — the wheel source
  performs no world_top op, the crossing constraint that lets the prior
  pin's gate ground it), the node
  [key@0][entry@4][parent@8][install_world@12] resolves into a per-site
  local (the old form re-scanned the frame region four times per
  perform), and the ARM CALL alone runs bracketed in the node's
  install_world — deep-handler semantics delivered dynamically: an
  arm's own performs resolve outer, never self; args evaluate at the
  perform-site world; a tail-resumptive resume-as-return continues the
  remainder under the perform-site world by construction. The world
  node carries the install's existing [record][base] entry (one view
  object, never copied). ev_perform_entry (the frame-region scan)
  stays in source ONLY for the prior pin's emitted call sites; it has
  zero callers in this generation and leaves with the write-side
  cleanup rung. world_top joined is_substrate_mem_op + the emit table;
  ev_perform_node/world_find_from joined the emit runtime contract.
  THE THREE GATES FLIPPED: thunk-world 134 → 42 (a thunk minted
  outside an install performs correctly where CALLED — the exact class
  that refuted the queue-5 bracket), arm-config-ev 2 → 40 (band N's
  config-fn-evidence-in-arm silent-wrong class DEAD — the arm-internal
  install shadows), arm-shadow control 40 → 40. All three graduated to
  frontier legs (RTLIBS-linked). En route the medium refused its own
  builder twice: world_top first declared in Alloc floored an Alloc
  demand at the root, and the prior pin's gate refused the performed
  world_top op until the argument-passing form removed the perform —
  E_EffectUnhandled catching the arc's own hand, both times before any
  binary ran wrong.
- boot/mentl.wasm  sha256 e8bcfb14abdbd06abe7e954151fb937612737fc4a7e4acaf31555d724880e69c
  (TRANSITION m3 == m4 at 561,165 lines — the 32,564-line m2/m3 diff is
  the world dispatch crossing one generation; census 0, comment-refs 0,
  frontier 221/0, battery 113/113)
- generated by: TRANSITION re-pin from m3; self-confirms below (prior pin follows)

- source: THE WORLD CHAIN — R1 of the world-as-value arc (2026-07-23;
  the converged design lives at PLAN's Hβ.cli.infer-context-bracket
  entry). Every emitted install now draws its runtime edge: $world_g
  (the live world, top of the install chain; 0 = empty) plus
  $world_push cons one [key@0][record@4][parent@8] node per absorbed
  effect group, save/restored around the install extent by the
  LHandleWith emit (the same save/restore shape as the scoped-singleton
  global, world outermost; the save local declares for anonymous
  installs too — the bracket is the invariant, not an optimization).
  Install enames intern through the one visit_string channel so every
  key resolves to a stable nonzero offset. WRITERS ONLY — the perform
  swap (R2) is the reader rung; the three repro-world fixtures hold
  their measured values through the new emit (thunk-world 134,
  arm-config-ev 2, arm-shadow control 40), proving the chain inert,
  and the m4 leg ran the wheel's whole self-compile under live world
  brackets.
- boot/mentl.wasm  sha256 8156ee0c22938eed9bcac207bb35753c107e58c4201a8f538b788bab210b1ebe
  (TRANSITION m3 == m4 at 561,069 lines — the 26,623-line m2/m3 diff is
  the world bracket crossing one generation; census 0, comment-refs 0,
  frontier 212/0, battery 113/113)
- generated by: TRANSITION re-pin from m3; self-confirms below (prior pin follows)

- source: THE ANALYSIS-CORE ORDER LAW + doc's env completion (2026-07-23).
  The infer-context-bracket audit's yield, landed as what the artifact
  admits: the core's install order is SETTLED and written at ONE home
  (pipeline.mn's spine block — ledgers innermost, lookup_ty before env
  before graph before mutate_sink, diagnostics outer to graph because
  graph_bind's occurs-check reports FROM the arm), and doc_run gains the
  env_handler its core lacked (its env ops resolved through the dispatch
  boundary's instance while its graph was chain-local — the
  accident-invariant class, now a contract). The consolidation itself —
  a bracket fn over a body thunk, all 14 chains rewritten, wheel 1,568
  lines smaller, census 0 — was REFUTED by the artifact before commit:
  a closure's evidence snapshot predates the bracket's installs, so
  evidence-dispatched core ops (executable_gate's verify_debt; the at
  verb's ambiguous cursor ops) fault the ev-scan into the sentinel while
  singleton-tier ops pass. Reverted whole; the measurement, the DEP
  (evidence crossing fn frames vs the M3 lexical fence), and the build
  order are banked on Hβ.cli.infer-context-bracket (named-residue index).
- boot/mentl.wasm  sha256 4b10e457ff1ce7ad25a1f36348fdc9c4a05629e100025fe871afeef9c48f027b
  (CLEAN m2 == m3 at 559,460 lines, census 0, comment-refs 0,
  frontier 212/0, battery 113/113)
- generated by: CLEAN fixpoint; self-confirms below (prior pin follows)

- source: THE WHEEL SERVES ITS OWN IDE — `mentl space` (2026-07-23).
  ide/serve.mn absorbed whole as src/main.mn's space arms: the mime
  projection, the one-read request (bytes_buf + str_of_buf), the
  request-line path parse, the route ("/" and directory paths to
  index.html; ".." refuses), the isolation-pair respond (COOP/COEP —
  the shared-memory compiler's requirement, on every response), and
  the tail accept loop (constant stack for the server's life).
  serve.mn + serve.sh + serve.wasm are DELETED — the scaffold's own
  written dissolution ("dissolves at the verb") executed. The
  listener is the install shim's seam: WASI p1 has no bind/listen,
  so the host resource rides the same shim boundary as run's exec
  seam (--dir "$MENTL_HOME::." maps the repo at guest "." — the verb
  serves from any directory; -S tcplisten=127.0.0.1:7378, port
  override MENTL_SPACE_PORT); without a listener the verb refuses
  and TEACHES the seam. Two frontier legs, both seen RED on the
  pre-verb boot (unrecognized command): the no-listener
  refusal-teach, and the live serve asserting 200 + the
  cross-origin-isolation pair on ide/index.html.
- boot/mentl.wasm  sha256 61c10ab8986cdba63e7713c94b5b4fd856a3b7eccfe2c96843dc6ab3f2e30d7d
  (CLEAN m2 == m3 at 559,432 lines, census 0, comment-refs 0,
  frontier 212/0, battery 113/113)
- generated by: CLEAN fixpoint; self-confirms below (prior pin follows)

- source: GRADIENT_QUEUE DELETED WHOLE (2026-07-23). The
  built-exposed-zero-callers block (the GradientQueue effect, the
  gradient_queue_default handler, top_n_by_score / sort_by_score_desc /
  insert_descending / reason_of_located — 107 lines) is gone; the
  named-residue peer Hβ.cursor.gradient-queue-activate-or-delete
  resolves as DELETE. The CONCEPT (idle cores ask the cursor "what
  next?"; the argmax IS the priority queue) stays band E's
  work-stealing-via-gradient design — the dead larval body taxed the
  read without serving it.
- boot/mentl.wasm  sha256 56f01996e24cfe7486818b968866eb4d07a6ad573107b1fe7cb55f6e01823c3d
  (CLEAN m2 == m3 at 558,531 lines, census 0, comment-refs 0,
  frontier 210/0, battery 113/113)
- generated by: CLEAN fixpoint; self-confirms below (prior pin follows)

- source: THE COMMENT WEAVE AT THREE ALTITUDES (2026-07-23). SYNTAX's
  "never dropped" is now true for the measured gap: a block-INTERIOR
  comment attaches to the finest FOLLOWING node (the next statement,
  the final expr, or the block's own unit node — attachment by HANDLE,
  so the comment-before-final-expr case never wraps the final as an
  ExprStmt), and a TRAILING same-line comment attaches BACKWARD to the
  node whose line it shares (same-line decided by token geometry — no
  TNewline between item and comment; span comparison failed because a
  braceless let-chain leaves the position past consumed newlines, the
  str_contains doc attaching to str_compare until the token test).
  The address surface renders the attached prose's first line as the
  Lede facet (the one comment_first_line projection — a duplicate lede
  helper died to E_DuplicateFnName's own refusal). Interior comments
  thereby joined the backtick gate, and the referent set grew its
  SCOPE face: each top-level decl contributes (extent, comment_locals)
  — extents from consecutive stmt starts, since decl spans are
  head-only — so an interior comment's backticks resolve against the
  enclosing fn's params and binders; the eight residual phantoms were
  the narration class (other-scope and historical names) and
  unbackticked per the contract. The affine ledger's
  destructure-arm/sibling-move pairing forced the block loop
  destructure-free (helpers own the collects — the pattern is banked
  in the loop's own comment).
- boot/mentl.wasm  sha256 aa6338e90b9ffc585ebf8dd84fb2b50061fcb9a3b8e98d3c0cc131a479436815
  (CLEAN m2 == m3 at 558,532 lines, census 0, comment-refs 0,
  frontier 210/0, battery 113/113)
- generated by: CLEAN fixpoint; self-confirms below (prior pin follows)

- source: THE BLOB DECLARES CALLEE-FIRST + THE SUBSTRATE FACE TABLE +
  THE N-CURSOR FIELD (2026-07-23). The canonical wheel input is now
  lib-before-src (wt_wheel lib src; march.sh matches): lib declares the
  vocabulary src consumes, so every src->lib reference reads the FINAL
  scheme instead of the loose pre-registered snapshot — measured, the
  src-first blob left 492 wheel fns with fully-bare published schemes
  (every param and the result a raw var; the order-conditional class at
  its true size), and the flip halves it to 256 (the residual is
  intra-src forward references, the named order peer). The sharper
  order re-judged the wheel: 57 diagnostics trued in one sweep — 33
  declared rows widened to their bodies' truth (mostly + Alloc), the
  escaping family's two op declarations now speak the string name-sets
  the family always carried, expr_child_handles reads {name, init}
  records by their init edge (MakeRecordExpr AND ResumeExpr passed the
  record itself to node_handle), render_propose_arm's guarded unwrap
  became the match it always was (Pure holds), and the zero-caller
  cursor_session_batch + CursorSessionResult deleted whole. WITH the
  flip: seq_op_sig — ONE face table both substrate paths derive from
  (infer_seq_op forces each position against it; a HOLED/under-applied
  seq-op call builds its product-with-holes from it, never from the raw
  body's env scheme whose `l + 8` types l Int — the 14 wheel
  `|> list_set(??, …)` pipes rewrote saturated and the class is gated),
  and the N-cursor FIELD lands: `mentl <file>:0` projects the whole
  absence field — holes ranked first (each with its Propose fan, ties
  teaching), the annotation gradient after, every position through the
  same cursor_at_handle + render_at path the single address uses.
- boot/mentl.wasm  sha256 6807a214cb300d88f7d9416ff751821e112c9f0a850955c0cba8c6387b08e944
  (TRANSITION m3 == m4 at 557,233 lines, census 0 at every generation,
  comment-refs 0, frontier 209/0, battery 113/113)
- generated by: the prior pin's wheel compiling this tree (m3 == m4
  self-confirmed by tools/march.sh; prior pin follows)

- source: THE INSTANCE CROSSES THE FN BOUNDARY (2026-07-23). The
  fold-family dig's root, landed whole: subst_row's EtVar arm returned
  the EMPTY row when the var was SOLVED to a closed row — the bound
  row's whole present set (every effect instance a callee's scheme
  proved) discarded at instantiation, so no Iterate/user-effect
  instance ever crossed a fn boundary and map's element died at the
  caller. The merge arm reads the binding live (names freshened
  through the same mapping the params use, under the solved tail) —
  and the TRIO LAW completes it: free_in_row and occurs_in_row now
  descend a BOUND tail exactly as subst_row traverses it (collection
  without descent left the inner payload vars unquantified → shared
  raw across every call site → map's install bound the shared answer
  var to [b] and each later HOF decl collided against it, the
  cross-decl Bool-vs-List wave the install probe pinned). With them:
  publish_with_instances (a declared-bare name keeps the body's
  parameterized instance), the install-frame gate (fragments of ONE
  instance join by position only inside a tee body's frame — a
  caller's independent callee instances stay independent), the
  effects.mn bare→parameterized upgrade at the name-set seams,
  prelude's callee-first iterate order, the compare-leaf f64 stash
  pair (the bare (f64.lt) select never assembled), and the spec walk
  chasing body-bound vars. The two-pass walk built en route was
  REVERTED by measurement (its second pass tips the 4GB image at the
  m3 emit — the seq-op-row precedent; order-independent verdicts stay
  the named peer). The fix exposed one canonized silent-wrong:
  mn-mapelem banked exit 1 — offset-0 alpha where .beta=2 was meant —
  and one under-declared fixture row (stats fsum + Alloc); both trued.
  Gates: mn-forward-wide-instantiation (RED on the prior pin) +
  bisect-r 42 + the fixed micro.
- boot/mentl.wasm  sha256 cad3ca5372e9962b65b12f8becc108bfbbf10bf3ce134d3e8b72dc82f64b6631
  (TRANSITION m3 == m4 at 553,900 lines, census 0 at every generation,
  comment-refs 0, frontier 205/0, battery 113/113)
- generated by: the prior pin's wheel compiling this tree (m3 == m4
  self-confirmed by tools/march.sh; prior pin follows)

- source: THE TUPLE PATTERN READS THE BRACKET (2026-07-23). The last
  lower-time layout bake in the pattern path dies: LPTuple carries
  (elem_ty, sub_pat) — the LIVE element vars — and (offset, width)
  project at EMIT through the bracket-aware channel (pat_elem_repr
  reads lookup_ty, the same channel the binop dispatch and the
  construction stores read; pat_tuple_off is the identical align_for +
  repr_width prefix-sum). A var-typed tuple destructure is itself a
  worthiness witness (spec_oph_wide_pair's TTuple arm — a var element
  landing WIDE at the key; pointer instantiations keep word slots, no
  twin minted). Seen RED twice on the prior pin: the worthy twin's WAT
  did not assemble ($x.f64 undefined local — the in-flight N-cursor
  code's (Float, Handle) ranked tuples were the first trigger), and
  the non-worthy floor read an f64's high word at baked offset 4 as
  the next element — an invalid exit status, zero diagnostics
  (probe-second). Gate: mn-generic-wide-tuple-pattern (twin assembly +
  floor-face value + mixed-order (Int, Float, Int) alignment, exit 42).
- boot/mentl.wasm  sha256 808524325f7ab799c479331bde7a404ea29f3c9a3078d4ce5086d75220d4e412
  (m2 == m3 at 550,965 lines, census 0, comment-refs 0, frontier 202/0,
  battery 113/113)
- generated by: CLEAN fixpoint; self-confirms below (prior pin follows)

- source: THE FAN PROJECTS (2026-07-23, the /loop's queue-0 landing). A
  tie at a `??` now LISTS the proven survivor space — every candidate
  rendered with its Reason, then the collapsing move — never a bare
  count (propose-fan-demo: `mentl bit.mn:8:30` projects both Bit
  survivors 0 and 1 with their seed Reasons; seen RED as the count-only
  line on the prior pin). Both candidate-verify loops gain the FORK
  PAIR: graph checkpoint + heap region (mark/reset with the virginity
  contract) per candidate — memory AND graph restored at each fork
  boundary, O(1) each (one trail mark + one bump word); the medium
  itself demanded verify_each_enriched's row widen to the body's Alloc
  truth (census 0 -> 1 -> 0). enumerate_inhabitants' multi-shot
  resume-per-proven arm carries the same pair; its first wheel-internal
  consumer is the N-cursor pass (the queue's next landing).
- boot/mentl.wasm  sha256 08640f17d5d8a1030681a3863a5085c97137df0bfc656565d27497ea03b4ba72
  (m2 == m3 at 550,078 lines, census 0, comment-refs 0, frontier 199/0,
  battery 113/113)
- generated by: CLEAN fixpoint; self-confirms below (prior pin follows)

- source: THE MANIFEST TELLS THE TRUTH (2026-07-23). Every src module's
  import list resolves its whole vocabulary — the medium's own
  E_MissingVariable diagnostics were the worklist (probe each module at
  `mentl src/<m>.mn:1:1`, fix, re-probe to zero): nine primary fn moves
  to DAG-honest homes (emit_runtime_contract, ls_find_str_local(+loop),
  main_param_count, seq_elem_face, handler_stateful, variant_specs_of
  (+filter/+from), ctor_payload_tys, ctor_result_is, param_tys_of →
  lower; node_handle, parse_span_of → graph; why_expand → query;
  query_flow_label (+predicate/+row siblings) → types), plain imports
  across fourteen modules (lower += infer alone cleared seven names;
  the voice family's odd misses were oracle effect ops), and
  main_param_count rewritten on lower's own decl walk — whose first
  flat form MISSED the LLet-closure named-fn shape and dropped _start's
  argv arg; the march's transition arbitration caught the unassemblable
  m3 before any pin moved, and the fix matches exactly the shape set
  reach_decl_name enumerates. The comment-truth sweep rode along
  (T2–T7: the deleted-seed mirror claims, the six-vs-eight field count,
  dead item-11.B pointers healed to the live peer handle, the stale
  voice.mn:9 example, era-name narration dropped for mechanism prose)
  plus SYNTAX's two corrected claims (the retired third projection; the
  comment-attachment gap measured and named with its build).
- boot/mentl.wasm  sha256 cab557e806c2794f9cce818bb5519ec82fc190b26a268959bc04b7a7f70a2431
  (m2 == m3 at 549,887 lines, census 0, comment-refs 0, frontier 198/0,
  battery 113/113)
- generated by: CLEAN fixpoint; self-confirms below (prior pin follows)

- source: THE DIRECT-CALL CASH-OUT (2026-07-23). A call whose callee
  inference proved a top-level FnScheme emits `call $name` /
  `return_call $name` with NATIVE argument widths — no spill cells, no
  table hop, no wf$ wrapper, true tails including wide results. The root
  it cures, dug from the signal-crucible's stack exhaustion: the
  word-face wrapper broke tail calls (a body's return_call lands in the
  wrapper, whose body-call was plain — one leaked frame per iteration;
  4096 samples deep tipped the stack, the atan ladder innocent on top).
  Arg widths follow the callee's DECLARED signature (word floor for
  generics, native f64 for declared Floats); a twin's widths read the
  SAME spec_site_pairs/spec_subst_pairs projection that minted the
  redirect. The wheel SHRANK ~20k lines / −5.3% wasm bytes — every named
  call dropped its indirect ceremony. Word-result wrappers tail-collapse
  as the belt. signal-crucible transplanted and GREEN (exit 42,
  oracle-cross-validated); frontier 198/0; battery 113/113; census 0.
- boot/mentl.wasm  sha256 f8abad90edb6f26ffdf14cf745e83db283d5b88005a4b008f04b91b6e44565d8
  (m2 == m3 at 549,924 lines, census 0, comment-refs 0)
- generated by: TRANSITION then CLEAN (the node's emit crossed one
  generation at the 37,350-line diff; the twin-width fix confirmed the
  fixed point); self-confirms below (prior pin follows)

- source: THE SOCKET SPEAKS AT THE ADDRESS SURFACE (2026-07-23). render_at
  surfaces the CursorView's Propose facet (`mentl hole.mn:9:37` → `Query:
  ?? : Positive · Propose: 1` — the one proven survivor through the same
  projection the edit accept path applies; ties teach, never first-wins),
  carried by the resolver's column law: file:L:C picks the TIGHTEST
  containing node (the code caught up to its own comment), identical spans
  pick the LATEST mint (the more derived node — the NHole over its id
  cell). The sharper wheel caught the session's own first ty_of_kind
  NHole arm as ill-sorted (NodeKind vs NodeBody, census 0 → 1) — deleted;
  the census-as-ratchet keeping the builder honest within the hour.
  Frontier 193/0 (+ cursor-address-propose, seen RED pre-arm).
- boot/mentl.wasm  sha256 17d1c3be1ef9bb21793549cb706f764969397f594310d98c02ceb8356a82f4b4
  (m2 == m3 at 567,046 lines, census 0, comment-refs 0)
- generated by: CLEAN fixpoint; self-confirms below (prior pin follows)

- source: THE NULL-SINGLETON CLASS CLOSES AT EVERY ALTITUDE (2026-07-23).
  The cross-compile trap's root — effect_census_collector running as a
  null-state singleton in emit's walk extent, its accumulator at absolute
  address 12 — is closed three ways: project_emit_state installs every
  visitor family its walk fires; singleton_perform_block guards STATEFUL
  singleton op calls with the SingletonUninstalled refusal (the compile
  gate's conjunction at the runtime altitude — stateless stays unguarded
  by the same licence, byte-identical to the pre-guard form); and the
  battery ships REGION-BRACKETED (heap_mark/heap_reset per micro, 113/113,
  ~192MB peak — the arena's first real workload). The guard was seen RED
  live: its first firing exposed the test verb's own missing handler
  bracket (battery_run's fs ops as null-state singletons), fixed in the
  same landing; its second firing exposed the edit chain's missing
  lookup_ty_graph (null page read as the graph in `??` authoring
  sessions) and triggered the seven-chain census — five chains gained
  their missing analysis installs (edit, pipeline_check, check_source,
  at, repl, doc). Fixture mn-singleton-preinstall-call (expect 134)
  holds the class. Five ladder runs, one landing: TRANSITION (guard
  bytes) → CLEAN (test-verb bracket) → TRANSITION (stateful-only
  refinement, wheel 95 lines smaller) → CLEAN (edit-chain install) →
  CLEAN (the census sweep).
- boot/mentl.wasm  sha256 cadb105b68b64a00e496e1611363c94f3ed239794bf724f5a517817c5ffd302c
  (m2 == m3 at 566,812 lines, census 0, comment-refs 0)
- generated by: CLEAN fixpoint (the census sweep crossed in one
  generation from the prior transition pins); self-confirms below
  (prior pin follows)

- source: THE RESET RESTORES VIRGINITY (2026-07-23). heap_reset zeroes the
  region before the rewind (the dedicated $heap_reset_impl preamble fn);
  zero-reads stay true under reuse instead of holding by monotonicity's
  accident. The twelve-kill forensic corpus and the loaded address-pair
  probe live in the PLAN named-residue index; CLAUDE.md gains the
  alive-law section and the forensic laws. Board whole, frontier 192/0.
- boot/mentl.wasm  sha256 229fda2f71a5972f6614ce8b1dde5da529126bc294d25674c05cde3816d0ea20
  (re-assembled from the SAME byte-identical fixpoint wat by the canonical
  wt_asm — the hand ladder had dropped --debug-names; the march's container
  carries the name section, readable backtraces; the wat is unchanged)
- generated by: TRANSITION (the 2-line preamble/call-form crossed one
  generation from the singleton pin fc2a9520…); re-pin from m3 per the
  transition rule; self-confirms below (prior pin follows)

Prior pin's provenance:

- source: THE SINGLETON TIER TELLS THE TRUTH (2026-07-22). LDirectPerform —
  the singleton perform as its own node, every reader delegating through
  singleton_perform_block (byte drift impossible; the ladder confirmed
  CLEAN) — and the gate's stateful-and-never-installed refusal closes the
  zero-state-global silent-wrong. Frontier 192/0, census 0.
- boot/mentl.wasm  sha256 fc2a952039580e390f02f5ecc0735828025e64f7f3574424a918920d77e0db3e
- generated by: the singleton-arc wheel compiled by the crucible pin
  (fe68767f…) held CLEAN m2 == m3 == m4; re-pin via cp of that m2;
  self-confirms below (prior pin follows)

Prior pin's provenance:

- source: THE CRUCIBLE TIER (2026-07-22). DSP / ML / adaptive-LMS
  real-workload gates with python cross-oracles, RED-by-perturbation; the
  build surfaced and fixed the float `<~` miscompile (feedback slots
  hardcoded i32 — repr now read live). Frontier 191/0, census 0.
- boot/mentl.wasm  sha256 fe68767f39431eed1deff292945aba5cbe946f3bc28d70bba11709d06bbc0b49
- generated by: the merged tree held CLEAN m2 == m3 == m4 under the
  staging-root pin (ccd9381d…); re-pin via cp of that m2; self-confirms
  below (prior pin follows)

Prior pin's provenance:

- source: THE STAGING CLOBBER'S ROOT (2026-07-22). The interpolation fold
  stamped one handle on every interior concat; emit's per-handle staging
  local folded all segments into one cell (nine re-sets in the diagnostics
  arm, reads against stale values). Interior concats now mint their own
  handles; the outermost keeps the node's. TRANSITION m3 == m4; board whole.
- boot/mentl.wasm  sha256 ccd9381d5564b33f0aaea7fdc19ea7b0a532e986c6fad85467d6edd5e722c4a8
- generated by: the fixed wheel's m3 (the emit change crossed one
  generation from the gate pin d56abbb6…); re-pin from m3 per the
  transition rule; self-confirms below (prior pin follows)

Prior pin's provenance:

- source: THE EVIDENCE HOLE REFUSES AND TEACHES (2026-07-22, the gate arc).
  E_EffectUnhandled born armed: main-row effect with a floor demand and no
  install refuses with the graph-derived proposal (HandlerKind arms through
  EffectOpScheme). The criterion reads the ARTIFACT via the emit's own
  single-walk seventh projection (EmitEffectCensus) over the post-reach
  tree — the lower-side ledger it replaced was the Carried-Truth violation
  Morgan called. Two inference roots felled (the var-tail mask into the
  graph at diff_row; multi-effect installs subtract the full arm set); the
  Show/Hash effect shells dissolved into seq-op table faces; the
  effectful-arg staging clobber witnessed thrice and sequenced around; the
  pure row is the wheel's own first top-level let; fn env entries carry
  Located decl spans. Frontier 181/0, census 0, crefs 0.
- boot/mentl.wasm  sha256 d56abbb6b6f38971b5d45b219554170ec36af6ae3cd8d68a8ad469ced13723dc
- generated by: the gate wheel compiled by the pure-row pin (3cecf9e9…)
  held CLEAN m2 == m3 == m4; re-pin via cp of that m2; self-confirms below
  (prior pin follows)

Prior pin's provenance:

- source: THE BACKTICK CONTRACT AT ZERO (2026-07-22, same day as the triple).
  All 52 unresolved comment references swept — dead names repointed to live
  successors, narration unbackticked per the contract (a backtick is a
  reference into the one namespace); the checker's leftover CLH per-comment
  eprint deleted (1,536 stderr lines per compile). comment_refs_max: 0 —
  the prose boundary is now a hard gate.
- boot/mentl.wasm  sha256 10639d69c22fe792e42e4942b36842468a7103b2c98a9926f3abee906e8f2c00
- generated by: the swept wheel compiled by the triple pin (09380a33…) held
  the clean m2 == m3 == m4 ladder; re-pin via cp of that m2; self-confirms
  below (prior pin follows)

Prior pin's provenance:

- source: THE ROW IS A TRIPLE — the six-form EffRow tree dissolves into ONE
  canonical record (2026-07-22, the representation-law update). EfRow(present,
  absent, tail), EffTail = EtClosed | EtVar | EtAll; reading = present ∪
  (tail ∖ absent); canonical at birth (ef_make), zero read-time
  normalization. Kills the fat-row root (the old normalize_inter open×neg
  arm dropped the negation from an unresolved tail, so installs typed before
  their body's row grounded never subtracted the handled effect — dead
  evidence entries, the undiagnosed 0x100000000 class). One-writer rulings:
  subsumption reads via resolve_row (no binds); path compression is
  unify_row's alone. The free-tail law re-derived on the triple (a
  still-free tail is vacuous at the gate — the 646-error wave of m2's first
  self-judgment, cleared). The first top-level let the medium ever compiled
  exposed emit_init_lets as the one emit_expr caller without the local
  `~> emit_memory_bump` bracket (fixed to emit_one_fn_to_string's shape;
  frontier mn-top-level-let RED exit-134 on the prior pin, 42 here).
  Hygiene: 12 six-form remnants + 23 zero-reference fns deleted; zero
  six-form mentions remain in code or prose. Net −372 lines. Board: census
  0 at every generation; feedback-iir 30; handled-Ping 42; top-level-let 42.
- boot/mentl.wasm  sha256 09380a336df9a1f36bd219cec331d21ae7bd1afec089ccc35a423cd2f5390651
- generated by: the triple wheel compiled by the prior pin (658f3988…) held
  the clean m2 == m3 == m4 ladder (size-guarded, byte-identical); re-pin via
  cp of that m2; the re-pinned boot self-confirms below (prior pin follows)

Prior pin's provenance:

- source: THE TEST VERB + THE REGION SUBSTRATE — CLI absorption stage 2
  (2026-07-22). `mentl test <dir>`: a DIRECTORY target IS the battery
  (fs_list_dir_impl — the fd_readdir transport joins io.mn; a file target
  stays the single check). Each micro declares its own oracle in its first
  line (`// expect: N` — the expectation is graph content ON the artifact,
  never a side-table row); each compiles IN-PROCESS under a fresh handler
  chain (verdict = holes + refusals, the check verb's own licence), its WAT
  written under .build/test/ beside a streamed manifest line
  (MICRO stem path want). Execution stays the shim's seam — WASI owns no
  process-spawn (the process_exec precedent). One process now does what a
  wasmtime boot per micro did. The REGION substrate landed with it: Alloc
  gains heap_mark()/heap_reset (emit = the bump global read/written — the
  §5.O arena's first grain), gated RED-first (mn-heap-region: a reclaimed
  region's next alloc returns the SAME address; exit 134 on the pre-arm
  pin f60110f4, 42 here). The battery's own region is the honest residue:
  bracketing each compile with mark/reset traps compile #2 in its own
  infer while EVERY input is probe-verified intact (libs len+head+tail,
  the names, the reset address), and WITHOUT the reset 100 compiles run
  straight to the 4GB ceiling — 12 short of the battery. The no-reset form
  ships; verify.sh keeps the per-process loop as the gate; the peer
  Hβ.runtime.cross-compile-durable-state carries the probe corpus (PLAN
  §7 named-residue index). Board: census 0, frontier 168/0 (+heap-region),
  proof-exactness 9/9, crown 5/5, micros green.
- boot/mentl.wasm  sha256 658f39883a03ac31cfbdd74c81ebe8a991a0e2377628e402744c8ad32d0d3b64
- generated by: an intermediate transition mid-arc (the substrate-arm emit
  change crossed one generation; re-pin from its m3), then the battery
  close-form held the clean m2 == m3 under it; re-pin via cp of that m2;
  the re-pinned boot self-confirms the clean m2 == m3 (prior pin follows)

Prior pin's provenance:

- source: THE LSHOW/LHASH BUILD — show/hash dispatch moves to emit; four
  latent breaks close (2026-07-22). to_string/hash arrive at emit as
  STRUCTURAL nodes (LShow/LHash) whose type dispatch runs under the active
  specialization bracket — the LAST pair of type-directed lower-time bakes
  (the D3 map's inventory), so a worthy twin's to_string renders the VALUE
  (the name bake rendered the ADDRESS) and its hash agrees with its eq; the
  show/hash operand is a worthiness witness, so a render-only generic still
  twins. Every emit-side walk delegates through show_node_of/hash_node_of
  (the one dispatch home, synthesizing the exact call shapes lower baked —
  concrete scalar sites byte-identical); the reach projections mirror the
  same dispatch (show_reach_names/hash_reach_names) so the prune keeps what
  emit mints. FOUR pre-existing breaks closed, each probe-pinned: (1) the
  aggregate call is a DIRECT (__state, arg) call to $show_<sig>/$hash_<sig>
  (§5.U "called directly like $str_eq") — the old closure-convention call
  referenced a global no module ever emitted (undefined-global assembly
  failure in every minimal module); (2) the leaf's interior renderers
  survive reach (int_to_str was pruned — undefined-fn assembly failure);
  (3) the decor literals actually register — the call-site fold_closures
  .show field read resolved a WRONG EMPTY SLOT (the trecordopen-wrong-field
  class LIVE: register_show_literals received n=0 while generation read n=1
  from the same record; fixed by passing the whole closed-annotated record);
  (4) hash(x) gains its DECLARATION (the exact to_string mirror — the name
  never resolved before, E_MissingVariable at every user call; the entire
  hash surface was unreachable). Twin fold collection runs per-demand under
  its bracket, so a twin's substituted aggregate sigs generate beside the
  floor's. Gates RED-first on the prior pin: mn-generic-show (run 1, the
  address render), mn-aggregate-show (assembly failure), mn-aggregate-hash
  (E_MissingVariable). Board: census 0, frontier 165/0, proof-exactness
  9/9, crown 5/5, micros green.
- boot/mentl.wasm  sha256 f60110f4e6e303e126be92df9166988d95265afbca1ba4506235a5d66b7c8b58
- generated by: boot -> m2 -> m3 (the emit change crossing one generation,
  8352-line diff; the new wheel is SMALLER — the name-bake machinery
  deleted) -> m4 (m3 == m4, TRANSITION); re-pin via cp of the m3 artifact;
  the re-pinned boot self-confirms the clean m2 == m3 (prior pin follows)

Prior pin's provenance:


- source: THE EXACT-SUBSTITUTION SWAP — monomorphization residue face 3 closes
  (2026-07-22). The specialization bracket now carries the site's EXACT
  instantiation pairs (root handle -> concrete type) instead of one wide type:
  a free leaf answers its OWN root's binding, a free var whose root is not in
  the pairs stays the honest floor TVar, and the one-wide-type demand guard —
  the free-leaf rule's requirement — is DELETED. Sound because the subscript
  fracture fix (prior pin) made a generic body's element classes one
  union-find root with the scheme's quantified vars; the interim free-leaf
  rule and its guard were scaffolding around the fracture and retired with
  it. Mixed instantiations twin correctly: sqboth(a, b) = (a*a, b*b) at
  (Float, Int) emits $sqboth$sp20 squaring the f64 natively and the i32
  natively in one body (gate mn-generic-multitype, RED run 1 — address
  arithmetic — on the prior pin, 42 here). Board: census 0, frontier 156/0,
  clean m2 == m3.
- boot/mentl.wasm  sha256 4a114123c8816b066940ce7cd5921521c658b0c407d33d8c832d9466f282b863
- generated by: boot -> m2 == m3 (clean fixed point — the wheel demands no
  twins, so the swap is wheel-neutral yet tool-changing; re-pin from the
  m2cache artifact, self-confirming) (prior pin follows)

Prior pin's provenance:


- source: THE SUBSCRIPT FRACTURE FIX — Force = UNIFY, never overwrite, at the
  xs[i] sugar arm (2026-07-22, infer.mn IndexExpr). The arm bound the receiver
  with graph_bind — a cell OVERWRITE that DISCARDED the receiver's proven
  TList(elem) and orphaned a fresh element class per subscript — while its own
  sibling seq_force states the law in its comment ("graph_bind would DISCARD
  the arg's proven type"). Probe-pinned by an adversarial worktree agent:
  merge's condition subscripts rooted at 13777 while its param element rooted
  at 13793 — the union-find FRACTURE that forced the monomorphization
  free-leaf rule; with the fix the same probe shows ONE class, so the exact
  root-keyed substitution (and the multi-type-generic unlock) becomes
  possible. Both forces now unify (receiver against TList(elem), index against
  TInt), with the §4① String seam preserved: a proven-TString receiver is
  SATISFIED (a String IS a sequence) and s[i] binds TByte — sharper than the
  old clobber-to-TList. The refuted hypothesis is part of the record:
  instantiate-shares-never-clones was NOT the fracture root (instantiate heals
  through argument unification, polymorphic recursion included) — the label
  died to the artifact, as the pipeline demands. Board: census 0, frontier
  153/0, proof-exactness 9/9, crown 5/5, micros green.
- boot/mentl.wasm  sha256 da45bcdded23d097c7bef96dc733eff328c80f4089cff5567fc83669989ade23
- generated by: boot -> m2 (the fix in m2's code) -> m3 (the sharper classes
  applied to the wheel's own subscripts) -> m4 (m3 == m4, TRANSITION); re-pin
  via cp of the m3 artifact; the re-pinned boot self-confirms the clean
  m2 == m3 fixed point (prior pin follows)

Prior pin's provenance:


- source: NESTED-LAMBDA TWINS — the monomorphization residue's first face
  closes (2026-07-22). A lambda born inside a generic body inherits the
  parent's vars, so it twins under the same instantiation: a worthy demand
  contributes every record nested in its body (collect_fn_emit_records over
  the parent, mangled with the parent's enc), and the closure-mint arm
  redirects the record's table index to the nested twin through the bracket's
  active enc (spec_closure_name — the LGlobal redirect's closure-mint face,
  gated on the registry; captures, self-binding, and local naming keep the
  original name, so the record local and self-recursion are untouched). The
  bracket state widened from spec_wty to (spec_wty, spec_enc) — spec_ctx_set/
  clear/spec_enc_active on the ONE LookupTy handler. Landing it surfaced and
  fixed a width-consistency break: a param used ONLY inside a nested lambda
  had no param-decl width source (find_local_handle_expr stopped at the
  closure boundary) while the capture-store read the live handle — the twin
  declared (param $k i32) and read $k.f64, an assembly failure. Captures and
  evs are PARENT-frame expressions; the walk now reads them (bodies stay
  scoped out). Gate: tests/frontier/mn-generic-nested-lambda.mn, RED (run 1)
  on the prior pin, 42 here. Board: census 0, frontier 153/0, m2 == m3.
- boot/mentl.wasm  sha256 a919906dd9d7acb76f9f4666ca57772b6c98ba6a07ae897ea8bcaa506e36e5d2
- generated by: boot -> m2 == m3 (clean fixed point; the wheel itself demands
  no twins, so the emit change is wheel-neutral yet tool-changing — re-pin
  from the m2cache artifact; the re-pinned boot self-confirms) (prior pin follows)

Prior pin's provenance:


- source: NAMED-GENERIC MONOMORPHIZATION — the §5.U scalar half lands
  (2026-07-22). A named generic fn used to compile ONCE at the RI32 word floor;
  reached at a Float instantiation its body compared/added the word-protocol
  REFERENCES of the f64 values (bump addresses ascend, so prelude sort returned
  its input in allocation order and reduce(xs, min) picked by address — values
  intact, order garbage, zero diagnostics). Now emit runs a DEMAND ANALYSIS:
  every reference site's instantiation is a PROJECTION off the live union-find
  (the scheme's type walked against the site's resolved type — zero new
  storage, zero infer changes), a site whose quantified vars land on ONE
  distinct wide type becomes a candidate, and a candidate is WORTHY when its
  body performs arith/compare/eq on a free-floored operand (the address-
  comparison witness) or an interior site redirects to a worthy twin (the
  transitivity fixpoint — sort twins because merge is worthy, no recursion
  special-case). A worthy twin is the SAME fn record emitted once more under
  the SPECIALIZATION STATE: lookup_ty_graph (the one LookupTy handler) carries
  spec_wty, set/cleared around the twin's emission, and while set a free var
  answers the wide instantiation — the free-leaf rule, forced by the artifact
  (a generic body's unresolved classes FRACTURE: merge's param element and its
  comparison operands root at different handles, so no root-keyed mapping can
  be complete; one-wide-type demands make every free unambiguous). STATE-swap,
  never a second handler: a second LookupTy handler demoted every lookup_ty
  perform out of the singleton direct-dispatch tier — measured as 16 frontier
  reds (ev-scan faults at infer sites that never threaded evidence). Twins ride
  every name surface appended (table via their $wf$ word faces when wide, idx
  globals, static closure records); the LGlobal/LFnRef arms redirect through
  spec_target_name gated on the seeded spec_registry (SpecTwins, the
  StringTable pattern), with a spec_twins_exist fast path keeping twin-free
  modules at one op per reference. Two more roots fixed en route, each
  probe-pinned: the concat floor at wide_all (an open element through a
  filter-with-handler chain — the declared [String] Intent Boundary), and the
  order-conditional row class (wasm.mn sorts before types.mn/lower.mn, so
  inferred rows disagreed between Tier-2 callees and bare callers — every spec
  fn now DECLARES its row, the build_reach_index precedent). Gates RED-first:
  tests/frontier/mn-generic-float-{accumulator,comparator}.mn run 1 (the
  silent-wrong) on the pre-spec boot, 42 here; Int instantiations stay at the
  byte-identical floor. Board whole: census 0, frontier 150/0, proof-exactness
  9/9, crown 5/5, micros green, m2 == m3.
- boot/mentl.wasm  sha256 92fceff07558b20136fab3134366a78d19c3b7212ff2d5b97bdda7016bde94a4
- generated by: the handler-swap candidate pinned mid-arc went through
  boot -> m2 -> m3 == m4 (TRANSITION), then the state-swap correction reached
  the CLEAN m2 == m3 fixed point; re-pin via cp of the m2cache artifact; the
  re-pinned boot self-confirms m2 == m3 (prior pin follows)

Prior pin's provenance:


- source: EFFECT-POLYMORPHIC STORED FUNCTIONS + the mentl verb table (2026-07-21).
  A first-class CLOSURE carrying its own effect row can now be stored in an ADT
  field and called — the medium carries functions-with-their-rows through data,
  the same capability handlers rest on. The root was a hole in the inferencer:
  quantify_ctor_ty passed a function-typed ctor field's effect ROW through
  unquantified, so its open tail was a single free var that finalize closed to
  Pure — every effectful stored closure then "violated Pure." The fix
  (quantify_ctor_row) re-mints the field's open tail as a genuine row variable
  (parse_type_ty had minted it as a TYPE handle, which free_in_row never
  collects and instantiate cannot freshen) and quantifies it into the ctor
  scheme; result_ty filters row handles out by node-kind so the type stays
  non-parametric, and instantiate's is_row_handle -> mint_row freshens the row
  per use, so a table of heterogeneous-row closures unifies by the row algebra.
  This UNBLOCKED the mentl CLI overhaul: the verb set had three drifting homes
  (the hand-written verb_catalog help, the `if mode == "..."` parse chain, the
  dispatch match); now ONE VerbSpec table — each row carrying the verb's name,
  arg hint, BUILDER CLOSURE, and description — is projected by both the parser
  (find_verb -> the closure builds the typed Invocation, or a per-verb
  ParseError that names the missing argument) and the help catalog, so they
  cannot drift. Witnessed RED->GREEN on the wheel's own census: the verb table
  drove it from 7 E_PurityViolated (unfixed boot -> m2) to 0 (fixed m2 -> m3),
  then m3 == m4 byte-identical. Board whole: census 0, frontier 131/0
  (+stored-fn-effect-poly capability test), proof-exactness 9/9, crown 5/5,
  micros green, phantom 286. The re-pinned boot self-confirms the CLEAN m2 == m3
  fixed point.
- boot/mentl.wasm  sha256 1167ddfecdcd2ab763e9a586a86888fc0e8df5bdbb1a8afd40f5e96f39328f36
- generated by: boot -> m2 (7 verb-table diagnostics, functional core + the fix
  in m2's code) -> m3 (clean, the fix applied) -> m4 (m3 == m4); re-pin via cp
  of the m3 artifact; the re-pinned boot self-confirms the CLEAN m2 == m3 fixed
  point (prior pin follows)

Prior pin's provenance:

- source: THE WIDE-ELEMENT [Float] CASH-OUT — §5.U's stride carrier reaches its
  first wide element (2026-07-21). A [Float] is now a first-class packed
  sequence, end to end: the literal is born stride-8 (make_list_sc, the
  literal allocator's second face paired with make_list), the generic layer
  crosses every wide value BY REFERENCE (the WORD PROTOCOL — "a handle IS a
  word": emit_wide_ref spills an f64 to a fresh cell and leaves its address,
  emit_wide_deref cashes it back, and every call_indirect speaks the WORD-ARITY
  face), and the concrete sites cash the width. This KILLS the float-evidence-ft
  class: a wide-signatured fn — named (`scale`) or a capturing lambda — reaches
  the fn table through a `$wf$<name>` word-face wrapper (deref args, direct-call
  the native body at full speed, spill the wide result), so a polymorphic
  call site never needs to know the callee's emission. structural == compares
  VALUES through list_eq_f64 (never the references); the reach walk contributes
  the f64 leaf family (list_eq_f64/list_compare_f64/list_hash_f64/float_to_str)
  at the exact ==/ordering/show/hash sites emit mints them — the visitor-walk
  projection the runtime-contract names, so a float-free module drags in
  nothing. One real emit bug fixed en route: emit_wide_ref used $state_tmp as
  scratch, clobbering the closure record a capturing lambda holds live there —
  a dedicated $wide_cell local closes it. TRANSITION m3 == m4 (the 6056-line
  m2/m3 diff is the emit change — the $wf$ wrappers, the $wide_cell local, the
  reachable f64 family — crossing one generation), then the re-pinned boot
  holds the CLEAN fixed point m2 == m3. Board whole: census 0, frontier 128/0
  (+4 wide-element fixtures: list/map/hof/show, RED-first under the
  pre-wide-element boot where f64.const into an i32 slot did not even
  ASSEMBLE), proof-exactness 9/9, crown 5/5, micros green, phantom 286.
- boot/mentl.wasm  sha256 683d66cbe44992046abdcac0c01d902b07adafe53173d6308d1d8912d7ca056c
- generated by: boot -> m2 -> m3 (TRANSITION m3 == m4), re-pin via cp of the m3
  artifact; the re-pinned boot then self-confirms the CLEAN m2 == m3 fixed
  point (prior pin follows)

Prior pin's provenance:

- source: THE STRING=[BYTE] MERGE — §4① lands whole (2026-07-21). String IS a
  stride-1 byte list: TString unifies with [byte] and binds a generic element
  to TByte (the merge lives in unify_types; TString stays nullary, no clone),
  strings carry the unified [count][tag_word][bytes@+8] record (literals via
  the emit's 8-byte header; every builder via bytes_buf; io/net/json/lsp/
  persist/driver/main off the +4 layout), byte_at IS list_index, ++ IS the
  O(1) concat node, str_slice IS the slice node, and the sign-bit view shape
  is deleted (view_base -> str_payload, the stride-normalizing
  materialization boundary). TByte~TInt is check-level value-equality
  (same_ground, never a binding, forced by datum-last inference order); the
  slot is guarded by the carrier on reads, store_strided's 0..255 range trap
  on writes, and construction-follows-result-type. list_to_flat split to two
  altitudes (typed [a]->[a] table entry over flat_raw) — healing the entire
  13-entry libs-isolation shadow at its one root. Landed via the two-stage
  DISPOSABLE ladder: boot compiled W1 (old layout + new compiler) -> m2; m2
  compiled the real tree -> m3 == m4 == m5 byte-identical (m3 == m4 directly:
  the migration is behavior-preserving for the compiler's own computation).
  Census 0; frontier 116/0 including the NINE-fixture behavioral battery the
  fixpoint oracle cannot see (map/fold/filter/eq/concat/slice/interp/parse
  over real strings + the range trap at exit 134), RED-first under the
  pre-merge boot (15 refusals). W1 discarded — zero permanent compat.
- boot/mentl.wasm  sha256 dbcaca3e4be07108adc4ad31f38569b57946d1eb24a6f9efb771a0a787bfdbeb
- generated by: the two-stage ladder (boot -> m2[W1] -> m3 -> m4 -> m5, fixpoint
  m4 == m5) + re-pin via cp of the m4 artifact (prior pin follows)

Prior pin's provenance:

- source: STEP 0 of the String=[byte] value-ontology unification (2026-07-20,
  Hβ.value.ontology-derivation-complete). Adds TByte — the packed sequence
  ELEMENT-slot leaf (a byte VALUE is a word, repr RI32, arithmetic is Int; its
  SLOT stride in a packed sequence is 1) — as the 15th Ty constructor, with a
  new stride_of projection (1 for TByte, repr_width otherwise) and a distinct
  fold-sig "b" so [byte] never shares a generated helper with [Int]. H6 forced
  every arm: 12 exhaustive Ty matches gained a TByte case (occurs_in,
  same_ground, subst_ty, free_in_ty, chase_probe_tag, show_type, ty_handle_of,
  extract_row, query_flow_label, the synth enumerator, the to_string dispatch).
  Dead vocabulary — nothing produces TByte yet — so the emit is unchanged for
  every real program: CLEAN FIXED POINT m2 == m3 (byte-exact), census 0, micros
  72/72. The RI64/RF32 gradient-arm precedent (a trigger, not a new scalar). The
  value/slot split is the novel move the eventual String = TList(TByte) merge
  rides — Rust's u8 conflates them; Mentl keeps byte-value = Int-word, byte-slot
  = stride-1. Next in the arc: the element-stride-dispatched flat leaf, then the
  per-decl arena (§5.O, the OOM keystone), then the merge.
- source: the §4① string-layer typing + the expect_same chase-first root fix
  (2026-07-20, Hβ.infer.expect-same-chases-bound-var). A Float POSITIONAL
  constructor field filled from an unannotated param left the param an
  unresolved var — the scalar clobbered the arg reference's NBound(TVar(binder))
  binding in expect_same (the lone unify arm that bound without chasing),
  orphaning the binder — so `g` floored to i32 and the f64 call site trapped
  `indirect call type mismatch` (the ctor-arg face of Hβ.emit.float-evidence-ft;
  it also blocked the LSP serve on json's parse_number Float). The fix is one
  line — expect_same chases the var live like every other arm. It unmasked the
  runtime's pervasive handle-word pun (§4① — a handle IS a word), so the
  foundational fix TYPED the string layer: byte_len/byte_at/str_slice/str_concat/
  the float builders are seq-ops (typed calls over self-consistent raw bodies,
  the list_index pattern), str_of_buf is the one construction boundary (a raw
  [len][bytes] buffer IS a String), raw byte access in the parse/comment
  functions became byte_at/byte_len, and state slots dedup by handle identity
  (i32.eq) not str_eq. Full-wheel census 0; the LSP json float blocker cleared
  (serve reaches the LSP layer, hover-response the next rung). TRANSITION
  m3 == m4 (594-line m2/m3 diff = the emit change crossing one generation);
  re-pinned from m3. Board whole at pin: census 0, frontier 89/0,
  proof-exactness 9/9, crown 5/5, micros 72/72, instrument reads clean-passes.
- source: the mount-independent weave rank (2026-07-19). The instrument
  gate went RED on the ultracode pin: `check <clean>` from an installed
  context exited 1. driver_module_order_key ranked src-before-lib by a
  position-0 prefix test (`str_slice(p, 0, 4) == "src/"`), so under the
  /mentl-home mount every resolved path (`/mentl-home/src/...`) ranked as
  lib — a mount-DEPENDENT weave order that re-fired the order-conditional
  verdict class (two E_TypeMismatch at prelude consumers) the canon
  exists to sidestep. The rank now reads the src/ SEGMENT
  (`str_contains`), invariant under any mount prefix. Blob path
  untouched; clean fixed point m2 == m3; boot re-pinned because the fix
  is wheel-neutral yet tool-changing (the check/compile verbs run from
  boot). Board whole at pin: census 0, frontier 86/0, proof-exactness
  9/9, crown 5/5, instrument reads clean-passes.
- boot/mentl.wasm  sha256 628b509f54cee3516e865b6b43ea60bf8545414fc0942d23777f64eb50aabfdc
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/march/m3.wat boot/mentl.wasm` (prior pin follows)

Prior pin's provenance:

- source: THE ULTRACODE BATCH — COLUMN 2 CLOSES (2026-07-19). Four
  isolated worktree builders (a 4-reader recon fleet first, then 4
  opus builders), each transplanted onto main by hand and marched
  serially, every merge gated whole. (1) R3 — the decidable arithmetic
  Verify fragment: node_const_at folds +,-,*,/,% over ground Int/Float
  operands with zero minting; nested `self` resolves through the
  PWithSelf BINDER threaded into every fold (subst_self is the
  one-line wrapper; the shallow operand swap deleted); div/mod-by-zero
  refuses to fold. `self + 1 <= 10` discharges with zero debt, SYNTAX's
  own Even discharges, and a proven-false construction REFUSES under
  the armed class (frontier 84/0 at that pin). (2) The generative
  self-test loop: the medium proposes through the edit transport,
  compiles, runs, classifies, reduces a failing case to minimal form
  (842B -> 131B), banks crucibles — four real bugs on run one,
  including the sharpest demonstration in the repo: `0`, the only
  inhabitant of `Int where self == 0`, is a PROVEN fill that still
  divides by zero — proof is a monotone filter over admissible values,
  not a guarantee about the program around them, now executable. Its
  regression detector caught s03's case GRADUATING against current
  boot (the bucket sweep had fixed it). (3) The LSP transport runs the
  frontend: didOpen drives driver_check into the session-level graph,
  serve_run carries the analysis handlers, and `query "type double"`
  projects a real function type off the live graph; the end-to-end
  serve session documents its pinned pre-existing blocker (json
  parse_number — the FOURTH independent hit on
  Hβ.emit.float-evidence-ft this session). (4) The CFC pipeline's
  first real pass: cos/sqrt/atan2 into math.mn, the comodulogram in
  lib/dsp/cfc.mn, and the synthetic PAC gate finds the planted (6,40)
  coupling at exit 42 — MVL peak/median 5.93 matching a numpy oracle
  (5.92); read_recording is the real-file transport. Frontier 86/0 at
  this pin; census 0 throughout; every march held m2 == m3.
- boot/mentl.wasm  sha256 162875bb87c227587457dd11da48b73349821ba06ac681062d95ee52613fcd89
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/march/m3.wat boot/mentl.wasm` (prior pin follows)

Prior pin's provenance:

- source: THE OCCURS CHECK SEES BOUND STRUCTURE — THE EIGHTH ARMING
  (2026-07-18). `fn selfapply(f) = f(f)` TRAPPED the compiler: chase_deep
  and subst_ty spun to stack exhaustion with ZERO occurs reports, because
  the occurs leaf compared handles while the cycle lived in BOUND
  structure — bind a := TFun([b], r) first (b free, passes every
  comparison), close b := TVar(a) after; no handle compare sees it, only
  descending a's binding does. The leaf now answers the real question —
  "does handle occur in the type GRAPH reachable from here": raw handle,
  then root (union-find), then the root's BINDING recursed (NBound and
  NRecordRowBound; TRecordOpen's rest symmetric). Termination is the
  guarded-write invariant itself: every bind runs the check, the graph
  stays acyclic, the recursion bottoms out — and chase_deep carries a
  depth-200 tripwire as the belt (a firing names the shape by
  constructor tag; show_type on a cycle would spin, measured). The
  probe arc: the fixture's tag probe named the cycle (TVar(3)/TVar(19))
  and PROBE-OCCURS proved the guards never fired — the diagnosis was
  mechanical. Wheel compile time unchanged (~11s). E_OccursCheck ARMS
  (the eighth): the fixture that found the trap now gates it — reports
  the infinite type and refuses (exit 1, zero WAT; frontier 73/0).
  m2 == m3 clean fixed point; census 0; micros 72/0.
- boot/mentl.wasm  sha256 220203939deaf611468b4fe442699ec617d0bfd9b5f4353ba74e0275aeeb456f
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/march/m3.wat boot/mentl.wasm` (prior pin follows)

Prior pin's provenance:

- source: THE SEVENTH ARMING (2026-07-18, the column-2 closeout's first
  half). E_MissingVariable ARMS: wheel census 0, and the user-path
  licence MEASURED — a no-import stdlib program (map / fold / len / any)
  resolves via the DAG's prelude seed, compiles exit 0, runs 20; the
  stdin contract is self-contained input, where a miss is a real break.
  Pre-arming, the refusal fixture compiled a missing-name executable to
  2,795 bytes at exit 0 (the diagnostic on stderr) — the silent-wrong
  the arming deletes; post-arming: exit 1, zero WAT (frontier 72/0,
  run_refusal). E_OccursCheck does NOT arm with it: its fixture found a
  live trap — `fn selfapply(f) = f(f)` spins chase_deep to stack
  exhaustion BEFORE any report (the occurs guards at graph_bind and
  unify never see the cycle this shape builds), banked as
  Hβ.infer.selfapply-cyclic-ty-spin with the fixture bannered. The
  three occurs var-leaves now ROOT-CHASE (TVar / TRecordOpen's rest /
  EfOpen's tail — unify IS union-find; a raw handle compare passed the
  indirect cycle a ~ (b -> r) with b's root already a), sound hygiene
  the march holds. And Hβ.cli.process-exec-wire is CLOSED BY
  MEASUREMENT: run_run already routes process_exec ~> process_no_exec,
  which refuses exit 1 and teaches the shim — PLAN's entry was stale.
  m2 == m3 clean fixed point; census 0.
- boot/mentl.wasm  sha256 b19a768fe708952d18c66f81f13b83089d29470e98b39fedd471d8d7e842657c
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/march/m3.wat boot/mentl.wasm` (prior pin follows)

Prior pin's provenance:

- source: THE UNIFIED EACH (2026-07-18, the eight interrogations over the
  fold's traversals). FIVE walks of the lowered tree were ONE walk five
  times: four byte-identical ~60-arm collectors (eq / compare / hash /
  show — each re-walking the whole tree for one leaf's type closure, the
  eq/cmp copies silently DROPPING their trigger's right subtree) + four
  byte-identical 13-line dedup walkers + register_show_literals
  re-collecting the show closure for the data section. Now: ONE collector
  carries the four closures as one record (a trigger arm contributes to
  its slot; recursion is uniform over every child, the dropped-subtree
  class closed by construction); ONE dedup walk emits each distinct
  signature's helper, the leaf selected by the FoldOp ADT; emit_module
  computes the record ONCE and both readers read it.
  cmp_agg_operand_contrib deleted as a byte-copy of eq_operand_contrib.
  25 fns deleted; net −318 lines in the emit. The landing's own first
  compile hit Hβ.lower.trecordopen-wrong-field live (an unannotated acc
  inferred a partial OPEN record and the field offsets floored at
  runtime) — the closed-record Intent-Boundary pins on every acc param
  are load-bearing and the comment names why. TRANSITION m3 == m4;
  census 0 (warnings 738 -> 732); frontier 71/0, proof-exactness 9/9,
  crown 5/5, micros-through-m2 72/0.
- boot/mentl.wasm  sha256 7853c3f295ebdcfa50bf4ae646c747505cded8f108312cfa1e0c3c63d4404186
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/march/m3.wat boot/mentl.wasm` (prior pin follows)

Prior pin's provenance:

- source: THE MEDIUM BUILDS ITSELF WITH ITS OWN MODULE RESOLUTION
  (2026-07-18, the manifest arc — Hβ.driver.wheel-imports-are-the-
  manifest CLOSED). `mentl compile main` emits the whole wheel (497,934
  WAT lines, ZERO diagnostics), it assembles, and the DAG-built wheel
  compiles and runs programs. The roots, each found by interrogation:
  (1) `import own` lexed TImport TOwn and ident_at's fabrication floor
  walked an empty module path — import segments are TRANSPORT names, so
  the keyword-spelled words {own, ref} are legal segments
  (import_segment_at, defined ABOVE its caller: the forward-ref
  quantified-return floor turned the first placement into a loud ++
  trap). (2) The prelude is PRE-IMPORTED — the DAG seeds it where it
  resolves; a bare scratch context compiles bare programs. (3) The weave
  order is CANONICAL, never the discovery order: leaves-first exposed
  three real latent mismatches alphabetical masked (prelude sum/chunk/
  trim meeting TIGHT runtime schemes where the canon met loose
  pre-registered ones — Hβ.infer.order-independent-verdicts names the
  class); the DAG discovers the SET, the sort fixes the WEAVE. (4) The
  edit session reads ONE weave (driver_entry_with_ranges — the address
  transport's route) and OPENS AT THE ENTRY (a Located caret at the
  entry's weave line; the argmax's module-aware decay then ranks the
  entry's own `??` above the closure's recovery holes). (5) NModule had
  readers and ZERO writers — the driver's fold now mints the module
  structure into the graph (same_module's decay was flattening every
  span to the "" module). (6) check rides the same weave (the
  per-module walk's missing-vocabulary noise retires until the
  per-module env overlay lands). Named live bug DODGED not fixed:
  patch_set_target's state write clobbers the tail of the last-allocated
  list (the ranges read is hoisted above it; peer
  Hβ.patch.set-target-state-clobber, two-line repro in the session).
  Frontier 71/0, census 0, fixed point, proof-exactness 9/9, crown 5/5.
- boot/mentl.wasm  sha256 95a1b3ddb4aa87b9924f45ffc755f408fc8e0cd6c89f529eb559574277dd40ed
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/march/m3.wat boot/mentl.wasm` (prior pin follows)

Prior pin's provenance:

- source: ▶▶▶ CENSUS ZERO (2026-07-18). The medium's verdict on its own
  source: NO ERRORS — 2,266 three days ago, 73 this morning, 0 at this
  pin. The last two representation changes: (1) NRecordRowBound — the
  record-row residual (a `...R` rest's remaining fields) gets its OWN
  node kind with a type-side occurs guard (occurs_in_record_fields);
  NRowBound's payload is EffRow ONLY, occurs_in_row is total over the
  six row forms with no cross-sort catch-all, mk_record_row_residual's
  Ty-wrap DELETED, ten mirror arms across the NodeKind matches. (2)
  slot_present — the Option-of-handle niche's READ half: the arg-slot
  presence test goes through a substrate-table-typed `a -> Bool` whose
  raw body compares the word, so the test never pins a slot's element
  type onto Int (next_free_slot + collect_slot_nodes read it; the full
  niche repr — construction and match projection — stays named as
  Hβ.emit.option-niche-repr). m2 == m3 clean fixed point. Frontier
  71/0, proof-exactness 9/9, crown 5/5, micros-through-m2 72/0, the
  census ratchet holds at ZERO — a rising census is now a refusal to
  merge, and every future class arming has its wheel-side precondition
  met (the user-path condition still gates name-dependent classes on
  the manifest arc, per the panel's licence correction).
- boot/mentl.wasm  sha256 bf00e685b1d122dbe3fa4c88a8c1540e461d3b24dcdcb1e6add6c35e5d036991
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/march/m3.wat boot/mentl.wasm` (prior pin follows)

Prior pin's provenance:

- source: EVERY APPLICATION STAGES ITS BOUNDARY (2026-07-18). Census
  6 -> 2; E_UnresolvedType EXTINCT. The seq-op fast path typed its call
  and returned — skipping the stage_continuation_boundary every other
  application performs ("EVERY application result is a possible
  suspension boundary", the fn's own law). Lower's k2 crossing machinery
  then found NoBoundary at exactly four wheel sites (a seq-op call on
  the k2 spine: alt_same_name_set, rows_equal, splice_at, k2_is_park) —
  identified by a one-build probe hijacking the reports' epoch field as
  a site marker (all four fired at continuation_boundary_at's NoBoundary
  arm). The fix is the law applied: the fast path stages the same
  boundary. TRANSITION m3 == m4 (the staged boundaries shift emit one
  generation); the new wheel reproduces itself. Remaining census: the
  two named representation changes (Hβ.graph.rowbound-ty-residual-tagged,
  Hβ.emit.option-niche-repr's arg-slot instance).
- boot/mentl.wasm  sha256 4fdeba773fd8ac8adbafaf2754f6ce7dd5fc0a853628ffe8e7d851bf320c1e4b
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/march/m3.wat boot/mentl.wasm` (prior pin follows)

Prior pin's provenance:

- source: THE DIAGNOSTIC LEARNS ITS ADDRESS (2026-07-18). Census 7 -> 6.
  The escaping family speaks canonical NAME-SETS end to end — the
  `-> Option(EffRow)` op declaration was the family's one row-speaker
  (compute_escaping_rows builds [(String, [EffName])]; ev derivation
  consumes names), and its instantiation at escaping_row's match was the
  List-vs-EffRow error. And E_UnresolvedType stops reporting at 0:0:
  all eight lower report sites read the node's own weave span
  (parse_span_of) instead of span_zero — the four census entries became
  LOCATABLE and immediately named their root: every one is a generic
  fn's && short-circuit whose boundary-weave thunk row never resolves
  (alt_same_name_set, rows_equal, splice_at, k2_is_park — one root,
  named Hβ.lower.boundary-weave-generic-thunk-row). m2 == m3 clean
  fixed point.
- boot/mentl.wasm  sha256 f96428611ff80817a8f908b6023a989a32d00f7398f44473673a43bd384121dc
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/march/m3.wat boot/mentl.wasm` (prior pin follows)

Prior pin's provenance:

- source: THE BOUNDARY MADE NAMEABLE (2026-07-18). Census 30 -> 7 in one
  landing, three families. (1) The representation layer's two altitudes
  got two NAMES: alloc_list / slice_raw are the RAW constructors
  (word-level, callable from lists.mn's own walks); make_list / slice
  stay the table-typed consumer names — a raw body calling a table-typed
  name forced [a] onto its word locals (the whole 10-error lists
  cluster; flat_fill_slice_range's list_index became the unchecked read
  its construction proves). int_to_str joined the table's String face
  (a [len][bytes] builder whose consumers read String). (2) The lathe
  caught up to SYNTAX's `-> !`: parse_type_atom gained the TBang arm and
  quantify_ctor_ty mints a fresh bottom var per occurrence — abort()
  finally types as never-returns, and the voice tree_list stub + search
  backtrack mismatches dissolved. (3) Decl-vs-artifact lies fixed where
  the artifact was right: verify_candidate takes Candidate;
  drain_string_literals returns pairs; HandlerDeclStmt / LHandleWith
  carry their real element records; CursorSessionResult constructs its
  record; the LPLit emit arm matches LowValue's LString (the
  tag-coincident LitValue spelling was the duplicate-ADT merge class
  live); seq_force admits TString under a TList force (§4①: len on a
  String is canonical). TRANSITION m3 == m4; shadow re-blessed 14 -> 3.
- boot/mentl.wasm  sha256 1502fb0acb175e3eed669b912e5b8a8174b224c80dfe4fdb89a74a786f6915bf
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/march/m3.wat boot/mentl.wasm` (prior pin follows)

Prior pin's provenance:

- source: A BUCKET IS A LIST THAT STARTS EMPTY (2026-07-18). The four
  str_hash/handle indexes (env, imap's smap, own's region index, wasm's
  string-offset index) zero-filled their slot buffers and pushed every
  reader through `if x == 0 { [] } else { x }` — a word test no type
  admits (eleven List-vs-Int census errors, the D root's bucket half).
  The absence was never Option-of-list; an empty bucket IS an empty
  bucket. list_filled(n, []) mints each index with one shared empty
  (buckets are only ever REPLACED, so the sharing is sound); the guards
  and env_bucket_at DELETE into direct list_index reads. And lower's
  eff_names_of null guard (`name == 0`, three Int-vs-EffName) came out
  with its class: the dissolved sorted insert was the null's home, the
  buffer-counter set builds slice to exact count, and the battery +
  self-compile arbitrated the removal green. Census 44 -> 30.
  m2 == m3 clean fixed point; runtime-shadow fingerprint re-blessed
  (lists.mn spans shifted under list_filled).
- boot/mentl.wasm  sha256 213a89ebdf63ac86f9666bd6dd79ad4a54aace66a56daac634fb72ed6c77869e
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/march/m3.wat boot/mentl.wasm` (prior pin 7455cd9d… follows)

Prior pin's provenance:

- source: THE KIND IS THE NAMESPACE (2026-07-18). `effect Sample` and
  `type Sample = Float where …` are both canonical (SYNTAX'S own §4③ and
  §refinement examples share the name) — but the env is one entry stack
  and the effect decl registers in phase 2, always newer, so every
  annotation naming the type resolved to the EFFECT's entry and baked a
  bare nominal TName that never unified with its base (10 Sample-vs-Float
  + 5 kin in lib/dsp). The read now carries its expectation:
  env_lookup_type walks the same str_hash bucket for the newest LIVE
  type-bearing entry (alias-schemed Forall or RecordSchemeKind) —
  quantify_ctor_ty, every annotation surface's one resolver, reads it.
  Effect-position readers keep the plain lookup. Census 59 -> 44.
  m2 == m3 clean fixed point; boot re-pinned (wheel-neutral yet
  tool-changing — the filtered read must live in the compiling wheel).
- boot/mentl.wasm  sha256 7455cd9da117bf4734b4b3d0124f1ccaafee4c6396f71179b13d1c1713790180
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/march/m3.wat boot/mentl.wasm` (prior pin b4d8011f… follows)

Prior pin's provenance:

- source: THE SEQ-OP HOLE GUARD + THE RECURRENCE PRIOR (2026-07-18).
  Two inference roots. (1) `list_set(??, i, v)` and kin took the seq-op
  fast path SATURATED — the hole counted as an argument, so the partial
  application the Stage Law promises never minted (E_TypeMismatch at
  every piped substrate stage). The fast path now guards on
  hole-freedom + full arity; a holed call falls through to the
  partial-application machinery like any other callee. (2) The
  `(prev) => body <~ spec` recurrence form typed the lambda ITSELF as
  the feedback result — prev never unified with the prior output, and
  the RHS was never checked as a state element. PFeedback now applies
  the single-param lambda to its own result (the prior IS the prior
  output, read live) and unifies the RHS against FeedbackSpec. Census
  73 -> 59. TRANSITION m3 == m4; the new wheel reproduces itself.
- boot/mentl.wasm  sha256 b4d8011f92d5faf294a0b13a57a7e8eb47164eb59628fabb1ba27dbe17ec0d09
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/march/m3.wat boot/mentl.wasm` (prior pin 8e248607… follows)

Prior pin's provenance:

- source: THE RECORD-CTOR ARROW VIEW (2026-07-18). `X({...})` — the
  single-variant nominal record's ctor in the one application syntax —
  met the env's RESULT binding at the call's chase and mismatched at 29
  sites (the tail's biggest root). The arrow is a VIEW minted at the
  read (record_ctor_arrow_view — the identical instantiation infer_pat's
  RecordSchemeKind arm performs; one view, two readers) and the ordinary
  saturated machinery does the rest. Census 101 -> 73. TRANSITION
  m3 == m4; the new wheel reproduces itself.
- boot/mentl.wasm  sha256 8e248607ed693e415f406ecf549d1b765d57344affa102cd4690c3b51501a90c
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/march/m3.wat boot/mentl.wasm` (prior pin bfc576f2… follows)

Prior pin's provenance:

- source: FIVE MORE CLASSES EXTINCT (2026-07-18). E_EffectMismatch and
  E_PurityViolated to ZERO (the widen loop's fixpoint — nine
  rebuild-and-re-judge iterations; negation-carrying declarations
  matched on positive parts, ! entries preserved). E_ResumeOutsideArm:
  the synth candidate fan moved INTO its arm (backtrack's try_each
  shape — the census was right that resume is arm-bound).
  E_ConstructorArity: QRFlowLabel's second field bound.
  E_PatternInexhaustive: the counter was PAlt-BLIND — both walks
  (collect_arm_tags, arms_have_wildcard) now flatten alternation
  branches, and voice's two H6-exhaustive matches were exhaustive all
  along. register_one_variant's refutable let-destructure became the
  match it always was; unlock_capability gained AWrapHandler ->
  CSandbox; dead lib/runtime/buffer.mn DELETED (zero consumers).
  Census 185 -> 101: E_TypeMismatch 97 + E_UnresolvedType 4 are ALL
  that remain. m2 == m3 clean fixed points throughout.
- boot/mentl.wasm  sha256 bfc576f2c997d98458dc6b45c473073b54ed2fd059a1a96e5b55cabb9f599ac2
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/march/m3.wat boot/mentl.wasm` (prior pin 6e7c10b2… follows)

Prior pin's provenance:

- source: THE ADDRESS IS THE SURFACE (2026-07-18). `mentl voice.mn:9`
  answers — the README §9 promise made a working transport. VAt parses
  at the argv boundary; driver_entry_with_ranges carries the module-
  range map out of the concatenation fold it always computed; the
  three-case line rule (widest-starting-on-line, tightest-covering,
  nearest) resolves over the span index every node writes at birth;
  cursor_at_handle projects the same eight-arm CursorView mentl edit
  reads; the terse facet-silent render reads the source slice, env
  schemes for decls, literal bodies for free cells, and the Why walk
  through the range map. propose_at is the ABSENCE facet structurally
  (authored_hole — the same read the transport gates patches on).
  The session's exact-reason-span resolver SURVIVES, documented as the
  live-generation crutch (measured: find_tightest over mint spans
  resolved a stale generation's hole and 13 edit fixtures went red) —
  it dissolves with Hβ.cursor.session-weave-epoch-scope. Named peers:
  Hβ.emit.float-evidence-ft (an f64-arg candidate ctor through an
  all-i32 $ft — first fired by the first float-position enumeration
  ever taken), why-flow-naming + refinement-provenance (the README
  Why lines' remaining substrate). Census 213 -> 185 (the widen
  cascade's second round + the transport's own rows). Frontier 71/0.
- boot/mentl.wasm  sha256 6e7c10b252e04757062516fbc04a28bd417c51f745d994a5c2b5fb7e9dcb633f
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/march/m3.wat boot/mentl.wasm` (prior pin 01ccaa3a… follows)

Prior pin's provenance:

- source: THE EFFECT-TRUTH SWEEP, ROUND 1 (2026-07-18). The census-spec
  fleet (29 per-file readers) produced exact edit specs for 192 sites;
  148 applied: declared rows widened to the body's truth, aspirational
  `with Pure` dropped for the honest Memory/Alloc the bodies perform,
  missing match arms written with correct payload arities, the
  LStateSlotStore patterns bound their fourth field. Census 301 -> 213
  (EffectMismatch 95 -> 70, PurityViolated 56 -> 24, PatternInexhaustive
  25 -> 7, ConstructorArity 4 -> 1). m2 == m3 clean fixed point.
- boot/mentl.wasm  sha256 01ccaa3af891863b55cbbc480ddc00dc4a8f08368188ac99aa0d23ead5addcce
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/march/m3.wat boot/mentl.wasm` (prior pin 57a2113e… follows)

Prior pin's provenance:

- source: TWO CHECKER ROOTS (2026-07-18). The medium's own diagnostics
  were the bugs. E_IfMissingElse EXTINCT (31 -> 0): the unit test matched
  TName("Unit"), a spelling the inferencer never produces — real unit is
  TUnit — so every genuinely-unit then was judged non-unit (probe:
  then_ty=() at all 25 sites), plus 6 unresolved op-results the unify
  below resolves. E_FeedbackNoContext EXTINCT (11 -> 0): the check read
  the COMPILER'S runtime handler stack while inferring the COMPILED
  program's structure — a category error firing on every real `<~`,
  SYNTAX's canonical lowpass included; the compile-time class question is
  the named peer Hβ.effects.iterate-class-declaration. Census 343 -> 301.
  m2 == m3 clean fixed point (report-only changes).
- boot/mentl.wasm  sha256 57a2113e9ce840cf54d61e2e750130b66862b672ed381480960f859b31f1cd72
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/march/m3.wat boot/mentl.wasm` (prior pin 3112cec5… follows)

Prior pin's provenance:

- source: THE INDEX LAW IS REAL (2026-07-18). SYNTAX §Indexing's "traps
  on out-of-range" was prose — list_index tag-0 raw-loaded. The checked
  entry landed with its structural prerequisite: `&&`/`||` SHORT-CIRCUIT
  (lower's BKBool arm — boolean verbs are control, not arithmetic). The
  eager i32.and had run every guarded read: set_insert, cache_filter_loop,
  register_one_variant (nullary ctors probed index 0 of empty lists on
  every `type X = A | B`) — three latent OOBs surfaced in the trap's
  first hour. Then the trap swept the DRIVERLESS-CHAIN class: check,
  check_source, edit, and repl chains all ran infer's region/consume ops
  with no analysis handlers installed — zero state records reading the
  zero-filled sentinel page as empty ledgers, "working" on garbage since
  the chains were born. All four now install affine_ledger +
  region_tracker. The dead narrowing-elision write deleted (never fired;
  discharge-gated write is the named peer). micro oob-traps=134 is the
  law's executable form. Census 348 -> 343. TRANSITION m3 == m4 for the
  lowering, then clean fixed points through the chain fixes.
- boot/mentl.wasm  sha256 3112cec50a6a779c9e8f67c8707b6d8d2eb50d52de5ab42c10529af95d95b814
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/march/m3.wat boot/mentl.wasm` (prior pin 01d77f31… follows)

Prior pin's provenance:

- source: THE VALUE CLASSES ARM (2026-07-18, §11 col 2). diag_refuses
  gains ERefinementRejected (a decidable-FALSE claim emitted 2,513 bytes
  at exit 0 while §11 said the class was locked) and EOwnershipViolation
  (its one false channel — the unresolved-callee move default — died with
  graph_bind_hole; the detector survived adversarial falsification).
  node_const folds negated literals, so the canonical Sample range
  (`-1.0 <= self`) DECIDES — and the sharper compiler immediately caught
  the wheel fabricating tag -1 into TagId's 0..255 refinement
  (lower_pat's unresolved-ctor arm). That fabrication is DELETED into
  LPUnresolvedCon: match test honestly false, sub-binders still declared,
  the dead arm assembles. mn-refine-reject (dormant, asserted the
  pre-arm exit-0) superseded by frontier refusal fixtures; the
  handler-forward-ref regression 2522f0dc lacked is added. Census 348;
  frontier 69/0; m2 == m3 clean fixed point.
- boot/mentl.wasm  sha256 01d77f31cd05f3071fbe34e16e157c9a5a8ee7d4a197f84b42df118176e3639b
- its .wat (m2 == m3 asserted) sha256 ee42b8dd531004ec9991ad2321dcc8ec2ce2565ba8739bbb3fd89bf99ca214e9
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/march/m3.wat boot/mentl.wasm` (m2 == m3 byte-identical;
  prior pin d6dd8ed9… follows)

Prior pin's provenance:

- source: ONE MISS, ONE DIAGNOSTIC + THE HANDLER-NAMESPACE REFUSALS
  (2026-07-18, §11 col 2). The env-miss path bound TVar(self), tripping
  graph_bind's own occurs check (a spurious 0:0 E_OccursCheck per missing
  name) and reading downstream as an unconstrained var (the ownership
  move-default cascade). It now binds NErrorHole via graph_bind_hole; an
  unresolved VarRef callee borrows its direct args. Two new classes born
  ARMED (decl-site, no resolution dependency): E_HandlerStateShadowsOp —
  a state field naming an op of its handled effect compiled clean and
  returned the WRONG value; the medium's own m3 leg caught the second
  wheel violation (`caret`) the same hour the check landed — and
  E_DuplicateFnName. Voice state fields renamed turns/caret_now; the
  dead duplicate FeedbackSpec deleted. Census 352 -> 348; the last
  forward-ref roots felled. E_MissingVariable/E_OccursCheck at wheel-zero
  but NOT armed (the panel's licence correction: user paths still fire
  them on unresolved stdlib vocabulary until the manifest lands).
  m2 == m3 clean fixed point; re-pinned so the refusals + the hole-bind
  are live in the CLI. Frontier gains run_refusal + two fixtures.
- boot/mentl.wasm  sha256 d6dd8ed968e66bfd48cc34aa1a43866353bc068a993afe654f8a6300071d33b4
- its .wat (m2 == m3 asserted) sha256 c81ebde12c7fcb5168651bb53cec56159f3fc8e8c2ffa69260b54c33958053fd
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/march/m3.wat boot/mentl.wasm` (m2 == m3 byte-identical;
  prior pin 45244e15… follows)

Prior pin's provenance:

- source: THE SMT HANDLER TELLS THE TRUTH (2026-07-18, §11 col 2 + ARC 0).
  verify_smt declared only `witnesses` while its arms update and read
  `debt` — the unknown-debt ledger never existed as state, so both arm
  references were unbound names (2 E_MissingVariable + 2 paired
  E_OccursCheck). `debt = []` added. Same commit: the solver's polarity
  fixed to validity-as-UNSAT-of-the-negation (SAT of an open predicate
  admitted any satisfiable predicate as discharged; SmtSat now returns a
  COUNTEREXAMPLE alongside the rejection — predicate_decide already
  short-circuits ground predicates, so the solver only ever sees open
  ones); node_to_predicate gained its missing BNe arm (`self != 0` fell
  to the opaque PBoolNode and accrued debt instead of deciding); and
  show_pred_operand renders UnaryOp/BinOp operands recursively (the
  `<expr>` fabrication hid the exact bound the diagnostic teaches).
  Census 356 -> 352. m2 == m3 CLEAN FIXED POINT (content change, not
  compiler-behavior change); re-pinned so the renderer/predicate/debt
  fixes are live in the CLI. THE BOARD IS WHOLE.
- boot/mentl.wasm  sha256 45244e15579736fcdc6d90bbbb2cd92040765bffbb9113c47d2032cc777e012f
- its .wat (m2 == m3 asserted) sha256 e0e1ba1664fe7131ec66bcc82e577345c41b607307a2bae8f867aa2b01e72cf6
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/march/m3.wat boot/mentl.wasm` (m2 == m3 byte-identical;
  prior pin 8bf740a4… follows)

Prior pin's provenance:

- source: A HANDLER'S IDENTITY IS A PARSED FACT (2026-07-18,
  pre_register_handler_sig, §11 col 2). `pre_register_stmt` registered
  FnStmt, TypeDefStmt, EffectDeclStmt — but not HandlerDeclStmt. Handlers
  entered the env only at the main walk's `register_handler`, so every
  `~> handler_name` before the handler's declaration in source order
  (115 sites across the concatenated wheel) floored E_MissingVariable.
  The handler's identity — its effect (derivable from the arms' op names),
  its instance type, its config, its residual row handle, and its
  HandlerKind — reads only parsed structure and the effect env (already
  registered by EffectDeclStmt). Moving the identity registration to
  pre_register makes handler names resolve order-independently.
  The main walk's register_handler reads the pre-registered r_handle from
  the env so forward references share the same residual row handle.
  Census 578 -> 356 (-222): E_MissingVariable 115 -> 4, E_OccursCheck
  115 -> 4. Clean m3 == m4 TRANSITION. THE BOARD IS WHOLE.
- boot/mentl.wasm  sha256 8bf740a442c8bf00e83f1bfae3c8c06d4afcc61e0a61711b222a101c04ed13b7
- its .wat (m3 == m4 asserted) sha256 7a532ca3ad0f5d3f894e6dac126e808e90cd39b54ac1fdb5dd504585f5701369
- generated by: `bash tools/march.sh` + re-pin via
  `cp .build/march/m3.wasm boot/mentl.wasm` (m3 == m4 byte-identical;
  prior pin 361ed16c… follows)

Prior pin's provenance:

- source: A BORROWING PARAMETER BORROWS (2026-07-17, move-vs-borrow part 3,
  §11 col 2). Call inference inferred every argument before reading the
  callee's parameter product, so an owned value passed to `observe(ref value)`
  was consumed exactly like one passed to `take(own value)`. The call now reads
  the canonical TParam product after labeled/default argument resolution: a
  direct variable passed to authored or inferred Ref enters the existing borrow
  scope; Own parameters and nested computations keep normal move inference.
  Runtime `len` and `list_index` declare their actual read-only access, and
  update_file_text_loop carries list_set's returned owner. Census 582 -> 578;
  E_OwnershipViolation 4 -> 0. Soundness control: `take(value) + take(value)`
  remains one ownership violation. RED-first gates:
  mn-own-call-arg-borrow, mn-own-forward-ref-seq, and mn-own-call-arg-move.
  Clean m2 == m3 fixed point. THE BOARD IS WHOLE.
- boot/mentl.wasm  sha256 361ed16cdc4d10fcefcbfe89d385028560ee7a37700c46c171b9756e0dfb71db
- its .wat (m2 == m3 asserted) sha256 6756ca73d9c132e30e46862ad478564ea0e7071ed3e648dda8e3531d0845dfc9
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/m2cache/m2.wat boot/mentl.wasm` (m2 == m3 byte-identical;
  prior pin 1e06cdaa… follows)

Prior pin's provenance:

- source: A FIELD READ IS A BORROW (2026-07-17, move-vs-borrow part 2, §11
  col 2). Reading `.field` borrows the receiver — it never moves the record —
  but infer_field_access_record consumed the receiver at every access, so
  `Buffer{data: push(buf.data, x), count: buf.count + 1}` floored
  E_OwnershipViolation on `buf`. The receiver is now bracketed in a borrow
  scope when it is a value chain (a VarRef or nested field access); a call
  receiver (`f(x).field`) keeps its normal inference so `x`'s move into `f`
  still consumes. buffer.mn's buf.data/buf.count cleared, census 583 -> 582,
  soundness intact, a clean m2 == m3 TRANSITION. Residue (4): the
  call-arg-borrow (`len(nodes)`, `list_set(overlay_bufs)`) — an `own` value
  passed to a BORROWING parameter — the last move-vs-borrow piece, reading the
  callee's param ownership at each argument. THE BOARD IS WHOLE.
- boot/mentl.wasm  sha256 1e06cdaa47e0d861a5826c931566b699df0c9bb7aa603cc8ed007f74cf188898
- its .wat (m2 == m3 asserted) sha256 97844f832c9abd2577abaa15690a205a8cd6d0133e6e1290362659bae04e8385
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/m2cache/m2.wat boot/mentl.wasm` (m2 == m3 byte-identical;
  prior pin fbcf14f2… follows)

Prior pin's provenance:

- source: A READ IS A BORROW (2026-07-17, move-vs-borrow part 1, §11 col 2).
  An `if` condition and a `match` scrutinee are READ to decide control and
  never moved, so an `own` value used inside them (`if set_contains(buf, x)`)
  is a borrow, not a consume — but the ledger consumed at every use. A
  borrow_depth counter on affine_ledger, bracketed by borrow_enter /
  borrow_exit around the condition and scrutinee, drops consumes while inside.
  Cleared 32 of the 37 remaining "consumed twice" (census 615 -> 583) with
  soundness intact: a real double-move `(take(buf), take(buf))` to two `own`
  params is still caught. Carried-Truth: the graph knows a condition is a read;
  the ledger treated it as a move. Emit grew (the counter + two ops), a clean
  m2 == m3 TRANSITION. Guarded by the extended
  tests/frontier/mn-own-alternative-branches.mn (a condition-borrow case, 4
  false consumes on the old boot, clean run to 33 on this one). Residue (5):
  field-read (`buf.data`) + sequential call-arg-borrow — the move-vs-borrow
  tail needing a field-receiver borrow and callee-param ownership at call args.
  THE BOARD IS WHOLE.
- boot/mentl.wasm  sha256 fbcf14f28fb5a09c24b57793e3e43b484ff0f6a8a38012e3c1bd4bd95f1c1357
- its .wat (m2 == m3 asserted) sha256 92138a14e9285d2e871601953dc77224a952422569b74d47874d263de6bed3ec
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/m2cache/m2.wat boot/mentl.wasm` (m2 == m3 byte-identical;
  prior pin 6d693a87… follows)

Prior pin's provenance:

- source: OWNERSHIP STOPS FIGHTING SAFE CODE (2026-07-17, the affine model,
  §11 col 2). 115 of the 152 `E_OwnershipViolation` "consumed twice" errors
  were false positives — the medium was stricter than Rust on provably-safe
  code, the opposite of §4⑤'s Hylo-quiet thesis. Two masked bugs: (1) `if` /
  `match` arms were never bracketed as branches, so an `own` value read in
  both arms of `if i<0 {slice(buf)} else {list_set(buf)}` counted as two
  consumes — but the arms are ALTERNATIVES (exactly one runs). A `BranchMode`
  ADT (BParallel | BAlternative) now rides the branch frame; branch_exit
  reports a cross-arm collision only for BParallel (`><` / `<|`, all arms run),
  unions without collision for BAlternative. (2) affine_ledger is installed
  ONCE at the pipeline, not per body, so a name consumed in one function stayed
  in `used` and collided in the next naming the same `own` param — `buf` / `acc`
  are everywhere, the dominant shape. consume_enter_fn / consume_exit_fn now
  bracket each infer_fn body (a fresh consume scope, restored on exit,
  re-entrant for nested fn decls). Carried-Truth: the graph already knows a
  branch is an alternative and a body is a scope; the ledger re-derived neither.
  Census 727 -> 615; the `><` / `<|` collision path is unchanged (crown + the
  micro battery green). Emit grew (the ADT + two ops), a clean m2 == m3
  TRANSITION. RED-first gate: tests/frontier/mn-own-alternative-branches.mn
  (2 false "consumed twice" on the old boot, clean run to 30 on this one).
  Residue named (37 left): the move-vs-borrow-by-callee case — an `own` value
  read in an `if` condition or a field is a BORROW, not a move. THE BOARD IS WHOLE.
- boot/mentl.wasm  sha256 6d693a8739f6d8c7bb0ad29b52ab862982b7b33d62769041d2953a55515e4363
- its .wat (m2 == m3 asserted) sha256 769c995f7cc19e79c586c101e965b5e6ee163896e0001e5a2bc557dcd0186a49
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/m2cache/m2.wat boot/mentl.wasm` (m2 == m3 byte-identical;
  prior pin 5e9ec2d6… follows)

Prior pin's provenance:

- source: THE ALIAS EDGE IS READ, NOT RE-DERIVED (2026-07-17,
  `Hβ.infer.alias-preserving-unify`, §11 col 2). 362 census errors —
  `Span vs ValidSpan` / `Int vs ValidOffset`, both directions — were ONE
  root, the census's single biggest. `pre_register_decls` ran a single
  source-order pass, so a fn signature quantified before its refined alias
  was declared (parser.mn's `span: ValidSpan` precedes types.mn, which sorts
  last in the concatenated wheel) called `quantify_ctor_ty` against an env
  that did not yet hold the alias edge — and baked a bare nominal
  `TName("ValidSpan")` into both the param handle and the fn scheme. The main
  walk then tried to refine that bare name to its live
  `TAlias(ValidSpan, TRefined(Span, _))` and `unify(bare-TName, Span)` floored
  at the leaf. Fix: register the alias edges in a phase BEFORE any fn
  signature (`pre_register_alias`), so the resolver reads the LIVE edge.
  Carried-Truth exactly — the graph drew the alias; the resolver re-derived a
  name. The banked "a two-pass is a no-op" (2026-07-17, one session prior) was
  measuring BOOT's census (old inference) rather than the fixed compiler's;
  the fixed compiler reports 0 for the class, 1233 -> 874 total. Emit is
  byte-identical (an error hole and a resolved TAlias both lower at word
  width, same codegen path), so m2 == m3 held as a clean fixed point and boot
  was re-pinned only because the compiler binary changed (wheel-neutral yet
  tool-changing). Guarded RED-first:
  tests/frontier/mn-refined-alias-forward-ref.mn (a refined-alias param used
  before its type is declared) fails `Pos vs Int` on the old boot and runs to
  42 on this one. THE BOARD IS WHOLE.
- boot/mentl.wasm  sha256 5e9ec2d66327bf475130557e7976999602b30d333d2ca1a54072adf04533f62f
- its .wat (m2 == m3 asserted) sha256 e74354e7a3398a557e40a1446ba471fb8423624b00a91be406dc3e82a16d6b68
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/m2cache/m2.wat boot/mentl.wasm` (m2 == m3 byte-identical;
  prior pin ab34a853… follows)

Prior pin's provenance:

- source: THE SURFACE PARSES ITS OWN CANONICAL FORM (2026-07-17,
  `Hβ.parser.refined-alias-nonatomic-base`, §11 col 2). SYNTAX §«Type
  aliases»' own example `type NonEmpty = [a] where len(self) > 0` did not
  parse: parse_type_decl probed only `p2 + 1` for `where`, so a multi-token
  base (`[Int]` is three tokens) put `where` out of reach — P_UnexpectedToken
  on `where` at the `]`, then E_MissingVariable on the orphaned len/self. The
  fix parses the whole base type FIRST, then branches on what follows
  (`where` / `|`+`-`& / a ctor payload / nothing) — a `[base]` / `Option(k)`
  base now refines. The wheel uses no such form, so m2 == m3 stayed
  byte-identical and the march read a clean fixpoint; the parser fn's own
  bytes changed, though, so boot was stale until this re-pin (the case where
  a change is wheel-neutral yet tool-changing). Tooling improved WITH the
  medium in the same change: tests/frontier/mn-refined-alias-nonatomic.mn +
  its run_program registration guard the form (frontier 47 → 50). Verified in
  a pty: old boot failed the form, this boot exits 0. THE BOARD IS WHOLE.
- boot/mentl.wasm  sha256 ab34a85301fdfe4ec02c4c41b76ca67aa86dd1975248ab26c82bf6c806000afb
- its .wat (m2 == m3 asserted) sha256 377dc38e7c18ce16a24b298da99b73e874d29b598068dd92c9975f1bbf99d976
- generated by: `bash tools/march.sh` + re-pin via
  `wt_asm .build/m2cache/m2.wat boot/mentl.wasm` (m2 == m3 byte-identical;
  prior pin e2babb24… follows)

Prior pin's provenance:

- source: BARE MENTL PROJECTS WHERE YOU ARE (2026-07-16, the welcome
  arc; two rungs). Bare `mentl` in a terminal hung on a stdin read; the
  fork lands on fd 0's filetype (fd_fdstat_get: character device = a
  human → welcome_run; pipe/file = the compile-stdin contract, the
  fixpoint path untouched). The welcome is a PROJECTION, not a manpage:
  fs_list_dir_names (fd_readdir on the cwd preopen, the dirent wire
  parse) names the directory's .mn sources and offers each one's next
  keystroke; the verb catalog is ONE string with two surfaces (welcome →
  stdout exit 0; the error path → stderr). `mentl help`/-h/--help reach
  the same projection. Rung 1 landed the ops with the fork deferred
  (reachability pruned the caller-less read — clean fixed point; boot
  cannot lower an import it predates, and stdin_is_tty executes ON the
  march path); rung 2 restored the fork through the new boot — fixed
  point again. Measured in a real pty: the directory projection saw
  song.mn and spoke first. THE BOARD IS WHOLE at the bless.
- boot/mentl.wasm  sha256 e2babb24b3912c0843ec5681f762d8ef34fd7c528803db39eefbb29f9f6b5c90
- its .wat (m2 == m3 asserted) sha256 caa80b0c3d619080da2a1887de285a321d334a233ed8c4005751872577f89cd6
- generated by: `bash tools/march.sh` (the ratchet loop; prior pin
  26bfe90a… — the anywhere-CLI wheel, whose entry follows; rung 1's
  intermediate pin is in git history)

Prior pin's provenance:

- source: MENTL RUNS FROM ANYWHERE — the install story is a pointer, the
  imports are the manifest (2026-07-16, ninth pin of the day; §11 column 1
  executed). Four landings in one arc: (1) the resolver chain
  (driver_module_path): direct existing-path → src/ → lib/ →
  /mentl-home/{src,lib} — the well-known guest path the install shim maps
  the repo to (an address, not an env var); (2) fs_at — the preopen table
  IS the mount table: absolute paths resolve by LONGEST guest-name prefix
  (the wasi-libc rule; fd_prestat_dir_name joins the import set), so the
  mapping is reachable — the old fs_dir_fd opened everything against the
  FIRST preopen; (3) prelude.mn declares its runtime imports (memory /
  strings / lists / threading) — the imports ARE the manifest, so a user
  program's one `import prelude` pulls the whole runtime through the dep
  walk; (4) driver_compile_entry — the compile verb reads the entry as the
  one-namespace CONCATENATION of its dep DAG (deps first, post-order — the
  wheel's own proven build form), so every dep's decls lower and emit
  (driver_check_entry lowered only the entry: $len undefined, the gap the
  first temp-dir run exposed). tools/install.sh writes ~/.local/bin/mentl,
  a POINTER to this boot — every re-pin is instantly the global CLI (the
  pin IS the release). MEASURED from a fresh temp dir: `mentl check` /
  `mentl compile` / `mentl < file` work; `mentl run t.mn` (import prelude,
  len([1,2,3])+39) → exit 42 through the shim's compile→assemble→execute
  half (WASI has no exec — the wheel's run verb names that seam);
  `mentl run hole.mn` REFUSES (exit 1, zero bytes, E_UnresolvedHole) —
  the refusal law survives the whole pipeline. Method notes re-paid: the
  stale-m3.wasm cp trap (assemble from the wat, NEVER cp on a clean fixed
  point) and one eprint-Heisenberg null. Shadow fingerprint re-pinned
  (6273b71e… — prelude's new imports shift the shadow); IDE shims gain the
  fd_prestat_dir_name stub. THE BOARD IS WHOLE at the bless.
- boot/mentl.wasm  sha256 26bfe90af945aab6274bc64d735f5c52d9a9988c31617cad8772f8bc216ac300
- its .wat (m2 == m3 asserted) sha256 49631004b8905571e352e44fa4a0b9ef8ccbf36698a78b8d31816452d66e0bf1
- generated by: `bash tools/march.sh` (the ratchet loop; prior pin
  94e449dc… — the live-regions wheel, first line of the ledger below;
  two intermediate transition pins of this arc — f0572443…, the
  fd_prestat_dir_name import wheel, and 84868a0b…, the /mentl-home
  resolver wheel — are in git history)

Prior pins, one line each, newest first (full mechanics in git history and
the PLAN §7 landing ledger; each was self-confirmed by the same ratchet at
its pin time; a line without a pin landed byte-identical under the pin
above it):

- regions are live: fn body = region, return = transfer (Hylo-quiet) · pin 94e449dc…
- the region tag speaks truth — the board is whole · pin 4b7f998f…
- the executable gate + the refinement-ledger truth · pin 701c7024…
- partial application as a value + the NFree-report deletion · pin cf479f9d…
- the capability `??` workflow + the let-statement bind · pin c323a40f…
- the positive `??` workflow green end to end · pin 7b188a31…
- the felt route lives — mentl edit reaches its projection window · pin 9c8b23ba…
- inference-owned executable boundaries + the pattern-constraint law · pin 7bd9e3e7…
- carry `<|` branch application identity from inference through emit · pin 289b2886…
- the fanout application-edge wheel · pin f711573d…
- Cluster A — the smap indexed-map consolidation · pin 2dc47d2f…
- value-proof (R2) — refinements discharge against the live value + the O(1) interner · pin 06847e5c…
- heal the crown dedup/membership inconsistency · pin 358faa34…
- the crown positive-gate root (by-name membership, 598→146) · pin 8a5d8ff7…
- unify the analysis verbs · pin 45ee71af…
- connect the analysis verbs · pin c4bdba19…
- the argv/CLI-dispatch wheel · pin bea3692b…
- the O(1) reachability index (perf iteration 6) · pin 349a3302…
- M4 the Abandon discipline · pin 67e44c9c…
- M3 the cut (the classifier flip) · pin ac204467…
- M1.1 the world-tag homomorphism · pin 70d184a0…
- the image-layout pin · pin d24af35a…
- FIRST LIGHT · pin b3314001… (commit 87c0152, tag `first-light`)

The hand-WAT seed (`bootstrap/`) was DELETED (7401c4b "Fly, my pretty <3",
2026-07-10, the day after first light); `--from-seed` is gone with it. The
cold-bootstrap recipe — and the diverse-second-seed ingredient for
trusting-trust (PLAN band J, `Hβ.closure.diverse-double-compilation`) — lives
at tag `first-light`, git archaeology out of the run path (PLAN §6).
