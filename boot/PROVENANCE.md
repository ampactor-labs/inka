# boot/mentl.wasm — the pinned fixpoint compiler

This binary IS Mentl: the wheel compiled by itself to the byte-identical
fixed point (first light, 2026-07-10, commit 87c0152, tag `first-light`).
It replaces the hand-WAT seed as the bootstrap: the default build loop is

    boot/mentl.wasm  compiles  src/**+lib/**  →  m2   (wheel-emitted)
    m2               compiles  src/**+lib/**  →  m3
    assert m2 == m3                                    (the fixpoint ratchet)

Because boot is itself wheel-emitted, m2 == m3 IS the fixed point — the
seed-era rule "NOT m2 == m3" (PLAN §6) compared a SEED-emitted m2 against a
wheel-emitted m3 and retired with the seed.

Provenance, self-confirmed at pin time:

- source: the multi-shot wheel — the k1 producer (LYield, the reified
  continuation record, the install driver, resume = k-call; 1746a87) plus
  M1.1 of the general arc: the i32_or/i32_and/i32_shl substrate siblings and
  the union-homomorphic world_tag (the OR-of-bits fold). Pinned from m3 (an
  EMIT change crosses one generation — boot's emit predated the new
  substrate projections while the wheel's world_tag calls them: m2 != m3 by
  design, m3 == m4 self-confirmed; battery 52/52 + the three k gates
  30/36/40 verified THROUGH m3 before the bless)
- boot/mentl.wasm  sha256 70d184a04a0e02ad4160532d18ac55e36ebba620453e8f54718eb915ae603bf7
- its .wat (m3 == m4 asserted) sha256 ab0e46828c49f2a825f8b84e71cc9b22a18ef28b13dc50eb50d54f5ebbc04edb
- generated by: `bash tools/march.sh` (the ratchet loop; prior pin
  d24af35a… — the image-layout wheel — itself from the first-light boot,
  sha b3314001…, commit 87c0152, tag `first-light`)

Re-pinning (a deliberate act, like updating a lockfile): after a wheel change
reaches its new fixpoint, copy the SELF-REPRODUCING generation here — on a
steady-state run (m2 == m3) either binary is that generation; on a TRANSITION
(m2 != m3, m3 == m4) it is m3.wasm, NEVER the fresh m2 (m2 is the OLD emit's
output — blessing it is the trusting-trust mistake march.sh:123-132 warns
about and its printed `cp .build/march/m3.wasm boot/mentl.wasm` avoids).
Update the hashes above and commit with the march output in the message.

The hand-WAT seed (`bootstrap/`) stays as the cold-bootstrap recipe and the
diverse-second-seed ingredient (trusting-trust, PLAN band J:
`Hβ.closure.diverse-double-compilation`). It no longer runs in the loop;
`--from-seed` on verify/march-gate/march walks the full ladder from it.
