# boot/mentl.wasm — the pinned fixpoint compiler

This binary IS Mentl: the wheel compiled by itself to the byte-identical
fixed point (first light, 2026-07-10, commit 87c0152, tag `first-light`).
It replaces the hand-WAT seed as the bootstrap: the default build loop is

    boot/mentl.wasm  compiles  src/**+lib/**  →  m2   (wheel-emitted)
    m2               compiles  src/**+lib/**  →  m3
    assert m2 == m3                                    (the fixpoint ratchet)

Because boot is itself wheel-emitted, m2 == m3 IS the fixed point — the
seed-era rule "NOT m2 == m3" (PLAN §6) compared a SEED-emitted m2 against a
wheel-emitted m3 and retired with the seed.

Provenance, self-confirmed at pin time:

- source: the felt CLI connect (2026-07-14) — argv is READ, the verbs
  dispatch and load. `emit_start` now emits the WASI argv wire (main taking
  params gets `(call $wasi_args ...)` when io.mn is linked, else zeroed
  words — the micro harness links no io.mn), `args_sizes_get`/`args_get` join
  `is_wasi_import_op` + `wasi_import_reprs` (the two-word preview1 ABI), and
  io.mn grows `wasi_args`/`cstr_to_string`. So `mentl compile lib/foo.mn`
  reads argv → dispatches the verb → resolves the module → fs-loads it →
  emits — the whole chain live, where bare `mentl` was the only reachable
  form before. Two latent bugs the connect EXPOSED (never hit while argv was
  dead): `path_to_module` stripped `lib/` as 5 bytes (it is 4 — "tutorial"
  became "utorial"), and `QWhy` returned the raw reason where the deep walk
  belongs (now `why_expand`, the Fresh-chain chased to root). The emit change
  is the argv wire only; it crosses one generation (m2 != m3), m3 == m4
  self-confirmed 469475 lines, battery 8/8 rungs + 66/66 micros + 5/5 crown
  verified THROUGH m3, and `compile`/`check`/`run` load-and-emit end-to-end
  before the bless.
- boot/mentl.wasm  sha256 bea3692b5e438286f747d4bd79532de6c231d5a53ff073f237b16b6e8ce2c5a3
- its .wat (m3 == m4 asserted) sha256 e5bc7099d1dda98779e72973af2fd8ecf23f76837d957125681cf1e9d1d5d55a
- generated by: `bash tools/march.sh` (the ratchet loop; prior pin
  349a3302… — the O(1) reachability index wheel — itself from
  67e44c9c… — the M4 ABANDON-discipline wheel — from THE CUT ac204467…,
  the M1.1 world-tag pin 70d184a0…, the image-layout pin d24af35a…, the
  first-light boot b3314001…, commit 87c0152, tag `first-light`)

The hand-WAT seed (`bootstrap/`) was DELETED (7401c4b "Fly, my pretty <3",
2026-07-10, the day after first light); `--from-seed` is gone with it. The
cold-bootstrap recipe — and the diverse-second-seed ingredient for
trusting-trust (PLAN band J, `Hβ.closure.diverse-double-compilation`) — lives
at tag `first-light`, git archaeology out of the run path (PLAN §6).
