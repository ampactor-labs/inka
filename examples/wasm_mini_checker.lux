// Mini checker: fold inside handler arm, NO captures in lambda

effect Inner {
  get_list() -> Int
}

let answer = handle {
  get_list()
} with items = [10, 20, 30] {
  get_list() => {
    let total = fold(items, 0, |acc, x| acc + x)
    resume(total)
  },
}

println(answer)
