// CROWN control — `run` applied to a pure lambda: no E performed, `!E` holds.
// Must be ACCEPTED (no mismatch) — the gate must not over-reject.
effect E { op() -> Int }
fn run(f) = f()
fn good() with !E = run(() => 42)
fn main() = good()
