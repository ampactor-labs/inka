// The stride-1 store RANGE trap: an Int outside 0..255 entering a byte slot
// traps loudly (store_strided's guard — the bounds-trap precedent), never a
// silent wrap to 44. When the compile-time ground narrowing gate lands
// (Hβ.infer.byte-narrowing-ground-discharge) this becomes a refusal fixture.
fn main() = {
  let s2 = push("ab", 300)
  len(s2)
}
