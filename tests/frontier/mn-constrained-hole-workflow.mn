// One candidate can satisfy this hole. Integer synthesis seeds 0, 1, and -1;
// the Positive refinement rejects 0 and -1, leaving 1. The frontier gate
// requires the editor projection to expose that survivor and its Reason, apply
// `1` as a patch, report zero proof debt, and execute with exit 1.

type Positive = Int where 0 < self

fn choose() -> Positive with Pure = ??

fn main() = choose()
