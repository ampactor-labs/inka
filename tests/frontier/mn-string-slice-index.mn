// str_slice is the list slice node (tag 4, zero copy); xs[i] on a String is
// the one checked list_index reading stride 1.
fn main() = {
  let mid = str_slice("hello", 1, 3)
  if mid == "el" && "hello"[1] == 101 && len(mid) == 2 { 42 } else { 1 }
}
