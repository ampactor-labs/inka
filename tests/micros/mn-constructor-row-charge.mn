// expect: refuse E_PurityViolated
//
// The constructor-row charge's contract (graduated 2026-08-01 with the
// landing, pin 4a822b299c — banked RED against the pre-charge boot,
// which compiled all three decls clean). The class: every constructor
// form allocates, so a declared `with Pure` over a tuple/list/record
// literal is a FALSE PROOF and `!Alloc` (real-time) falsely admits
// every literal. The judgment charges Memory + Alloc at the
// construction judgments (presents, no edges; nullary tags and the
// tuple-const READ stay chargeless — they allocate nothing); this file
// refuses at all three decls.

fn pure_tuple(a, b) with Pure = (a, b)

fn pure_list() with Pure = [1, 2]

fn pure_record(x) with Pure = ({alpha: x, beta: 2})

fn main() = match pure_tuple(1, 2) {
  (p, q) => p + q + len(pure_list()) + pure_record(3).alpha,
}
