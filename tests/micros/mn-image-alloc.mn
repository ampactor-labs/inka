// expect: 42
// The ImageAlloc vocabulary (arena step 1, Hβ.perf.per-decl-arena): an
// image-space allocation types under its own effect — the row is the
// classification layer the six image families declare — and runs. The
// step-1..2 emission rides the one bump ($alloc; indistinguishable until
// a reset exists); step 3 forks the pointer when the image-map draws the
// image/scratch boundary. Pre-landing this refused (ialloc unbound, the
// armed E_MissingVariable).
fn main(q) = {
  let p = ialloc(16)
  store_i32(p, 42)
  load_i32(p)
}
