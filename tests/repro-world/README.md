# The world-as-value RED gates (minted 2026-07-23)

Three fixtures measuring the evidence-model boundary the world-threading
arc closes (PLAN named-residue: Hβ.cli.infer-context-bracket carries the
converged design and the build rungs). Run each by concatenating the
runtime set (memory strings lists threading prelude) and piping through
the pinned boot, as tools/run-micro.sh does.

- thunk-world.mn — a thunk minted outside an install, called under one;
  the op is evidence-dispatched (two handlers carry it). Today: trap 134
  (the mint-time evidence snapshot misses; ev-scan walks the sentinel).
  Under worlds: 42 (the call-site world resolves it).
- arm-config-ev.mn — band N's config-fn-evidence-in-arm +
  arm-internal-perform-scope in one shape: a config-param thunk performed
  under an arm-internal install. Today: exit 2, SILENT WRONG VALUE (the
  performs re-enter the outer handler). Under worlds: 40.
- arm-shadow.mn — the plain-block control: the same shadowing shape
  outside any arm. Today: 40, correct. Stays 40 (the no-regression leg).

These graduate to frontier legs when the arc lands; until then they are
the arc's diagnosis, banked executable.
