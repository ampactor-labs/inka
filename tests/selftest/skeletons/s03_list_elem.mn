// selftest-shape: list-elem
// selftest-expect: refuse-unfilled — GRADUATED 2026-07-19: the original edit-trap (region_bucket_scan -> list_index bounds trap under the cursor projection) died with the bucket sweep (a bucket is a list that starts empty); propose now declines the hole and the executable gate refuses it honestly.
// An Int hole in a list-literal element position. Projecting the cursor over a
// list-bearing program traps the medium during inference:
// `unreachable` reached through region_bucket_scan -> list_index (the region
// tracker OOB-indexes its own bucket list while inferring the list expression).
// The trap fires with or without the hole, so this crucible marks the edit
// transport as unable to handle list programs, not a synthesis fault.

fn main() = {
  let xs = [4, 8, ??]
  xs[2]
}
